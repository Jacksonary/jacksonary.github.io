<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Jacksonary&#39;s blog</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://jacksonary.github.io/"/>
  <updated>2021-06-04T12:39:58.650Z</updated>
  <id>https://jacksonary.github.io/</id>
  
  <author>
    <name>Weiguo Liu</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>MySQL实用经验</title>
    <link href="https://jacksonary.github.io/posts/36236.html"/>
    <id>https://jacksonary.github.io/posts/36236.html</id>
    <published>2021-05-23T06:55:49.000Z</published>
    <updated>2021-06-04T12:39:58.650Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;在实际开发中，会有一些值得注意的点，下面简单记录一下。</p><h3 id="1-关于更新时间"><a href="#1-关于更新时间" class="headerlink" title="1. 关于更新时间"></a>1. 关于更新时间</h3><p>&emsp;在设计表时，通常会将创建时间和更新时间带上（即<code>create_time</code>和<code>update_time</code>），在更新记录时往往在代码层面不断的去手动设置<code>new Date()</code>，而且由于这个字段的存在感可能不是很强，所以有时候常常会忘了在更新时同时去更新这个字段，一旦后面需要用到的时候发现这个字段从来没有更新过，这就很尴尬，<a id="more"></a>其实mysql可能帮我们做这个事，不需要我们自己去做，只要在设计表时指定<code>update_time datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP</code>即可,下面是一个完整的表结构：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> school (</span><br><span class="line"><span class="keyword">id</span> <span class="built_in">INT</span> ( <span class="number">16</span> ) <span class="keyword">NOT</span> <span class="literal">NULL</span> Auto_increment <span class="keyword">COMMENT</span> <span class="string">"id"</span>,</span><br><span class="line"><span class="keyword">name</span> <span class="built_in">VARCHAR</span> ( <span class="number">32</span> ) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">COMMENT</span> <span class="string">"学校名"</span>,</span><br><span class="line"><span class="comment">-- 创建时间直接默认给当前时间</span></span><br><span class="line">create_time datetime <span class="keyword">DEFAULT</span> <span class="keyword">CURRENT_TIMESTAMP</span> <span class="keyword">COMMENT</span> <span class="string">"创建时间"</span>,</span><br><span class="line"><span class="comment">-- 更新时间默认给当前时间，且在更新记录时自动更新为当前时间</span></span><br><span class="line">update_time datetime <span class="keyword">DEFAULT</span> <span class="keyword">CURRENT_TIMESTAMP</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="keyword">CURRENT_TIMESTAMP</span> <span class="keyword">COMMENT</span> <span class="string">"更新时间"</span>,</span><br><span class="line">PRIMARY <span class="keyword">KEY</span> ( <span class="keyword">id</span> ) </span><br><span class="line">) <span class="keyword">ENGINE</span> = <span class="keyword">INNODB</span> <span class="keyword">DEFAULT</span> <span class="keyword">charset</span> = utf8;</span><br></pre></td></tr></table></figure><h3 id="2-关于IP"><a href="#2-关于IP" class="headerlink" title="2. 关于IP"></a>2. 关于IP</h3><p>&emsp;mysql中有专门处理ip的函数，不要直接用字符型存放IP地址，一是无法占用空间较大（满位是<code>255.255.255.255</code>占用16字节），而将其转成<code>int</code>型只需要4字节；二是无法对IP进行筛选；三对于字符类型检索也很慢。若将IP地址格式定义为<code>A.B.C.D</code>，那将其转成<code>INT</code>数值类型就为<code>A*2^24+B*2^16+C*2^8+D</code>，最大值为4294967295，所以int类型4位32字节足够了，主要涉及2个函数<code>INET_ATON([ipStr])</code>（将字符串IP转成整数型）和<code>INET_NTOA(ipInt)</code>（将整型数值转成字符型IP地址）。</p><h3 id="3-联合查询增加条件"><a href="#3-联合查询增加条件" class="headerlink" title="3. 联合查询增加条件"></a>3. 联合查询增加条件</h3><p>&emsp;联合查询条件是指在<code>join X on condition1</code>后面通过<code>and</code>关键字继续追加条件（如<code>and a.name like &quot;test%&quot;</code>或者<code>and b.category_id=20</code>等等），会造成什么结果，这个问题其实对于大部分的开发极少用到，一般而言就是<code>on A.b_id=b.id</code>，就拿<code>left join</code>为例，过程如下：将所有的结果都取出来，主表全显、次表只显示满足联合联合条件的，总体而言联合条件不会影响记录的总条数，只是影响次表是否展示。这里区别一下和<code>where</code>条件后面使用<code>and</code>追加条件就好了，<code>where</code>条件称之为全局条件，它是会影响最终的结果数目的，示例如下：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 语句1，left join 在on后面又跟了条件 r.name like '%门%'</span></span><br><span class="line"><span class="keyword">SELECT</span> sc.id,sc.name,sc.shop_id,r.id r_id, r.name <span class="keyword">from</span> shop_component sc</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> rfa r <span class="keyword">ON</span> r.id = sc.component_id <span class="keyword">and</span> r.name <span class="keyword">like</span> <span class="string">'%门%'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 语句2，直接使用 where 进行过滤</span></span><br><span class="line"><span class="keyword">SELECT</span> sc.id,sc.name,sc.shop_id,r.id r_id, r.name <span class="keyword">from</span> shop_component sc</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> rfa r <span class="keyword">ON</span> r.id = sc.component_id </span><br><span class="line"><span class="keyword">WHERE</span> r.name <span class="keyword">like</span> <span class="string">'%门%'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 语句3，left join 在on后面又跟了条件 sc.name like '%门%'（主表）</span></span><br><span class="line"><span class="keyword">SELECT</span> sc.id,sc.name,sc.shop_id,r.id r_id, r.name <span class="keyword">from</span> shop_component sc</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> rfa r <span class="keyword">ON</span> r.id = sc.component_id <span class="keyword">and</span> sc.name <span class="keyword">like</span> <span class="string">'%门%'</span></span><br></pre></td></tr></table></figure><p>对照上面的结论：<br>语句1：在普通join的结果上加了限制，所以 shop_component 主表全显，关联表 rfa 只显示满足 <code>name like &#39;%门%&#39;</code> 条件的记录，其他记录的关联表都为空；<br>语句2：直接在最后加了<code>where</code>条件，它是对整个结果集进行了过滤，只在中间结果集中取满足 <code>name like &#39;%门%&#39;</code> 条件的记录，其他记录不限制，所以最终的结果记录要比语句1更少；<br>语句3：只是 <code>left join x and</code> 后面的条件有关联表rfa的<code>name like &#39;%门%&#39;</code>改成了主表 shop_compoment 的<code>name like &#39;%门%&#39;</code>，结果是类似于语句1的，这时主表也是全显，关联表只显示满足<code>sc.name like &#39;%门%&#39;</code>条件的记录；</p><p>综上所述，<code>left join on x and y</code>这种语句的on语句并不会影响结果记录数，主表是全显的，关联表只会显示满足<code>and</code>之后条件的记录，其他为null。</p><h3 id="4-小表驱动大表"><a href="#4-小表驱动大表" class="headerlink" title="4. 小表驱动大表"></a>4. 小表驱动大表</h3><p>&emsp;这算是sql优化的一种方式吧，它的使用场景主要有两种：存在于和join操作。</p><p>&emsp;存在于判断主要是<code>in</code>和<code>exists</code>操作：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 如果B表小、A表大，应优先使用 in 关键字，先循环的是  select id from B，这样利用这个小表来驱动A这个大表</span></span><br><span class="line"><span class="keyword">select</span> A.* <span class="keyword">from</span> A <span class="keyword">where</span> A.b_id <span class="keyword">in</span> (<span class="keyword">select</span> <span class="keyword">id</span> <span class="keyword">from</span> B)</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 如果A表小、B表大，应优先使用 exists 关键字，这样先循环的就是 select A.* from A（小表），然后在小表内再去验证是否满足 select 1 from B where B.id=A.b_id（大表）</span></span><br><span class="line"><span class="keyword">select</span> A.* <span class="keyword">from</span> A <span class="keyword">where</span> <span class="keyword">exists</span> (<span class="keyword">select</span> <span class="number">1</span> <span class="keyword">from</span> B <span class="keyword">where</span> B.id=A.b_id)</span><br></pre></td></tr></table></figure><p>其实 <code>in</code> 和 <code>exists</code> 主要是看场景、看他们的驱动表，MySQL 表关联的算法是 Nest Loop Join，是通过驱动表的结果集作为循环基础数据，然后一条一条地通过该结果集中的数据作为过滤条件到下一个表中查询数据，然后合并结果。</p><p>&emsp;第二种就是联合查询了，左连接就尽量让左表尽量小，右连接就尽量让右表尽量小。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 如左连接查询应该尽量保证左表尽可能的小</span></span><br><span class="line"><span class="keyword">select</span> A.* <span class="keyword">from</span> A <span class="keyword">left</span> <span class="keyword">join</span> B <span class="keyword">on</span> A.b_id = B.id</span><br></pre></td></tr></table></figure><h3 id="5-mysql中的锁"><a href="#5-mysql中的锁" class="headerlink" title="5. mysql中的锁"></a>5. mysql中的锁</h3><p>&emsp;现有主流mysql开发基本都是使用的INNODB引擎，INNODB引擎支持表锁和行锁（myISAM仅支持表锁），细分为 record lock、gap lock 和 next-key lock（即record lock + gap lock）。比如在同时修改同一条记录时就会出现其中一个线程（会话）在一直等待，或者<code>select * from A for update</code>（直接加写锁，仅允许一个线程加写锁）和<code>select * from A lock in share mode</code>（读锁，多个线程可以同时加读锁，和写锁互斥，一旦加了读锁就无法再加写锁），像<code>alter table</code>、<code>insert into A select * from B</code>（锁表A）这类操作表的行为会锁表。若需要验证上述的内容，需要在2个会话里面进行操作，并且将自动提交事务关闭（或者使用<code>begin ... commit</code>）。若出现慢SQL的话，排查的时候可能需要考虑到这种情况，主要可以这样排查：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 检查mysql的进程情况</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">processlist</span></span><br><span class="line"><span class="comment">-- 查看正在被锁定的表</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">OPEN</span> <span class="keyword">TABLES</span> <span class="keyword">where</span> In_use &gt; <span class="number">0</span>;</span><br><span class="line"><span class="comment">-- 查看事务锁的情况</span></span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> INFORMATION_SCHEMA.INNODB_TRX;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 杀掉指定的进程，pid就是 processlist 中的id</span></span><br><span class="line"><span class="keyword">kill</span> [pid]</span><br></pre></td></tr></table></figure><p>&emsp;有些时候我们在mysql中会来做乐观锁和悲观锁的方法，具体方案如下：</p><ul><li>乐观锁：我们直接在表中新增一个<code>version</code>字段，每次更新前我们都比较一下当前记录的<code>version</code>，如果<code>version</code>字段和我们手中的<code>version</code>字段一致，那就进行更新同时将<code>version</code>字段进行更新（可以是自增或者时间戳），否则不更新；</li><li>悲观锁：在查询后添加<code>for update</code>即可人工加锁，即所谓的悲观锁的实现方案；</li></ul><h3 id="6-关于分布式锁"><a href="#6-关于分布式锁" class="headerlink" title="6. 关于分布式锁"></a>6. 关于分布式锁</h3><p>&emsp;现在分布式锁的主流实现方式是ZK（最小文件节点）和redis（setNX），其实mysql本身也可以用来做分布式锁的解决方案，mysql实现分布式锁主要是靠唯一键的约束，和redis的实现方式比较类似，直接以约定好的key（可以是指定好的字符串，可以是日期+方法名）插入DB中对应的列，该列做唯一约束，那么当多个线程同时操作时只可能有一个线程可以插入成功，其他的插入失败，其中插入成功的那个线程就表示成功获取了锁，其他插入失败的就表示没有抢到锁。</p><h3 id="7-数据库黑名单"><a href="#7-数据库黑名单" class="headerlink" title="7. 数据库黑名单"></a>7. 数据库黑名单</h3><p>&emsp;即某台主机或者ECS被mysql拉入黑名单，无法连接，通常发生的场景多发生在网络较差的情况，服务端和DB Server多次短时间的连接、断开，反复多次会出现这样的情况，在java服务端抛出的异常大概如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Caused by: java.sql.SQLException: null, message from server: &quot;Host &apos;129.60.199&apos; is blocked because of many connection errors; unblock with &apos;mysqladmin flush-hosts&apos;&quot;</span><br></pre></td></tr></table></figure><p>同一个ip在短时间内产生太多（超过mysql数据库max_connection_errors的最大值）中断的数据库连接而导致的阻塞；</p><p>解决方式：<code>mysqladmin flush-hosts</code>，可以直接在客户端运行<code>flush-hosts</code>。</p><h3 id="8-代码层面实现mysql的手动事务提交"><a href="#8-代码层面实现mysql的手动事务提交" class="headerlink" title="8. 代码层面实现mysql的手动事务提交"></a>8. 代码层面实现mysql的手动事务提交</h3><p>&emsp;在一些数据库双写的场景下可能需要手动提交事务，单独使用mybatis可以这样关闭事务的自动提交<br><code>SqlSession sqlSession = sqlSessionFactory.openSession(false)</code>，在与SpringBoot集成后，可以作如下实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> DataSourceTransactionManager transactionManager;</span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">TransactionDefinition transactionDefinition;</span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> UserMapper userMappper;</span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> UserSearchClient userSearchClient;</span><br><span class="line"></span><br><span class="line">TransactionStatus transaction = transactionManager.getTransaction(transactionDefinition);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">delete</span><span class="params">(Long id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// mysql的行为</span></span><br><span class="line">    userMapper.delete(id);</span><br><span class="line">    <span class="comment">// es的行为</span></span><br><span class="line">    userSearchClient.delete(id);</span><br><span class="line">    transactionManager.commit(transaction);</span><br><span class="line">  &#125; <span class="keyword">catch</span>(Exception e) &#123;</span><br><span class="line">    logger.error(<span class="string">"user delete occur error."</span>, e);</span><br><span class="line">    transactionManager.rollback(transaction);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="【附】"><a href="#【附】" class="headerlink" title="【附】"></a>【附】</h3><p>&emsp;实际使用中注重性能的存在一些优化点(摘自PPT):</p><ul><li>查询频繁的列需要建立索引；</li><li>少用类似<code>select * from t where xx is null</code>;</li><li>尽量避免在<code>where</code>子句中使用<code>!=</code>或<code>&lt;&gt;</code>操作符;</li><li>尽量避免在<code>where</code>子句中使用<code>or</code>来连接条件；</li><li>尽量避免使用<code>in</code>和<code>not in</code>，否则会导致全表扫描；</li><li>连续的数值，能用<code>between</code>就不要用<code>in</code>；</li><li>尽量用<code>Exits</code>代替<code>In</code>，（<code>Exits</code>会使用索引，<code>In</code>不会使用索引）；</li><li><code>like ‘%abc%’</code>不使用索引；</li><li>尽量避免在<code>where</code>子句中对字段进行表达式操作，如<code>select id from t where num/2 = 100</code>、<code>select id from t where num = 100*2</code>；</li><li>避免在<code>where</code>子句中对字段进行函数操作，这将导致引擎放弃使用索引而进行全表扫描，比如<code>select id from t where datediff(day,createdate,’2005-11-30′) = 0</code>、<code>select id from t where name like &#39;abc%&#39; select id from t where createdate &gt;= &#39;2005-11-30&#39; and createdate &lt; &#39;2005-12-1‘</code>；</li><li>对于多张大数据量（这里几百条就算大了）的表<code>JOIN</code>，要先分页再<code>JOIN</code>，否则逻辑读会很高，性能很差；</li><li>尽量用<code>Count(1)</code>代替<code>Count(*)</code> 两个都是返回所有的；</li><li>索引列最好不要超过6个；</li><li>尽量使用数字类型代替字符类型（数字类型只比较一次，字符类型逐个字符对比）；</li><li>尽可能的使用<code>varchar/nvarchar</code> 代替 <code>char/nchar</code>，因为首先变长字段存储空间小，可以节省存储空间，其次对于查询来说，在一个相对较小的字段内搜索效率显然要高些；</li><li>任何地方都不要使用<code>select * from t</code>，用具体的字段列表代替<code>*</code>，不要返回用不到的任何字段；</li><li>尽量避免使用游标，因为游标的效率较差，如果游标操作的数据超过1万行，那么就应该考虑改写；</li><li>尽量避免大事务操作，提高系统并发能力；</li><li>尽量避免向客户端返回大数据量，若数据量过大，应该考虑相应需求是否合理。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;在实际开发中，会有一些值得注意的点，下面简单记录一下。&lt;/p&gt;
&lt;h3 id=&quot;1-关于更新时间&quot;&gt;&lt;a href=&quot;#1-关于更新时间&quot; class=&quot;headerlink&quot; title=&quot;1. 关于更新时间&quot;&gt;&lt;/a&gt;1. 关于更新时间&lt;/h3&gt;&lt;p&gt;&amp;emsp;在设计表时，通常会将创建时间和更新时间带上（即&lt;code&gt;create_time&lt;/code&gt;和&lt;code&gt;update_time&lt;/code&gt;），在更新记录时往往在代码层面不断的去手动设置&lt;code&gt;new Date()&lt;/code&gt;，而且由于这个字段的存在感可能不是很强，所以有时候常常会忘了在更新时同时去更新这个字段，一旦后面需要用到的时候发现这个字段从来没有更新过，这就很尴尬，
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="MySQL" scheme="https://jacksonary.github.io/tags/MySQL/"/>
    
  </entry>
  
  <entry>
    <title>InnoDB存储引擎</title>
    <link href="https://jacksonary.github.io/posts/64693.html"/>
    <id>https://jacksonary.github.io/posts/64693.html</id>
    <published>2021-04-23T06:41:00.000Z</published>
    <updated>2021-06-04T12:37:27.766Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-MySQL-逻辑架构"><a href="#1-MySQL-逻辑架构" class="headerlink" title="1. MySQL 逻辑架构"></a>1. MySQL 逻辑架构</h2><p>&emsp;整体的逻辑架构分为 Server 层和引擎层两部分。其中 Server 层又细分为连接器和核心层，其中连接器那一层并非MySQL独有，大部分的C/S工具都有类似的架构（比如连接处理、授权认证、安全等），Server 层的第二层是 MySQL 独有的，MySQL 很多核心功能都在这一层，包括查询解析、分析、优化、缓存以及内置函数，还有所有跨存储引擎的功能也在这一层实现（包含存储过程、视图等等）。<a id="more"></a>服务器和存储引擎通过 API 进行通信，存储引擎不会去解析 SQL，它只负责数据的存储和读取。具体逻辑架构如下：</p><p><img src="https://img-blog.csdnimg.cn/20210423205944554.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="MySQL的逻辑架构"></p><h2 id="2-物理存储"><a href="#2-物理存储" class="headerlink" title="2. 物理存储"></a>2. 物理存储</h2><p>&emsp;在 MySQL 的文件系统中，MySQL 的每个数据库（也称为schema）都会以<code>data</code>目录下的一个子目录的形式存在（库名即文件夹名），然后各个数据库里面的表定义会以<code>[table_name].frm</code>的文件形式存储在所属的数据库目录中（但现在的8.0+版本中已经取消了<code>*.frm</code>文件，只有表空间文件<code>.ibd</code>）。另外 schema 的定义和数据库的定义差不多，在 MySQL 中，对 <code>DATABASE</code> 的操作完全可以使用 <code>SCHEMA</code> 来替换操作（如<code>create database test</code>和<code>create schema test</code>效果一样，都是创建一个test库）。</p><h2 id="3-InnoDB-的结构"><a href="#3-InnoDB-的结构" class="headerlink" title="3. InnoDB 的结构"></a>3. InnoDB 的结构</h2><p>&emsp;Innodb是目前MySQL最常用也是最推荐的一种存储引擎，它的架构（8.0版本）如下：</p><p><img src="https://img-blog.csdnimg.cn/img_convert/cf7d017565dee783807f5d7447b0fc9e.png" alt="Innodb的架构图"></p><p>&emsp;InnoDB 目前主要优势有：</p><ul><li>它的DML（data manipulation language，即数据操作，主要是增删改查，注意和DDL的区别；DDL-data definition language，即数据定义语言，主要是创库、建表）操作遵ACID模式，且支持事务提交、回滚以及crash恢复；</li><li>行级锁和一致性读的特性提高了其并发能力；</li><li>它将数据存储到磁盘上且基于主键来优化查询，每个 InnoDB 表都有一个叫做聚簇索引的主键索引，这个索引用来组织数据以便减少根据主键进行搜索的 I/O；</li><li>为了维持数据完整性，InnoDB 支持外键约束。外键的存在可以保证在增、删、改时不会导致相关表的不一致；</li></ul><h3 id="3-1-InnoDB的内存结构"><a href="#3-1-InnoDB的内存结构" class="headerlink" title="3.1 InnoDB的内存结构"></a>3.1 InnoDB的内存结构</h3><p>&emsp;Innodb的内存结构主要有 Buffer Pool、Change Buffer、Adaptive Hash Index 以及 Log Buffer 几部分组成。</p><h4 id="3-1-1-Buffer-Pool"><a href="#3-1-1-Buffer-Pool" class="headerlink" title="3.1.1 Buffer Pool"></a>3.1.1 Buffer Pool</h4><p>&emsp;Buffer Pool 是 InnoDB 缓表和索引数据的主要内存区域，它允许直接从内存中频繁操作数据（这样的操作很快），在专用服务器上通常会将高达80%的物理内存会分配给 Buffer Pool。为了大容量的读取行为的性能，Buffer Pool 被分割为一个个的页（Page，页可以承载多个 row），为了提高缓存管理的效率，页的实现是基于 linked list，使用 LRU（least recently used） 算法的变体将很少使用的数据从缓存中移除。很多时候 MySQL 的调整往往都是基于如何更大程度的使用 Buffer Pool 来操作数据考虑的。</p><p>&emsp;当需要空间去增加 page 到 buffer pool 中时，最近最少使用的 page 将会被驱逐（MySQL中将其称为老化，这些页也就是老年页），新的 page 将会被增加到列表的中间，这种中点插入法主要将列表分为2个子列表：</p><ul><li>在 head 处，子列表是最近最经常使用的，这里的子列表称之为 new sublist；</li><li>在 tail 处，子列表是最近最不经常使用的，这里的子列表称之为 old sublist；</li></ul><p><img src="https://img-blog.csdnimg.cn/img_convert/0e6028fb9f09b261555198f159e4f6e3.png" alt="Buffer Pool List"></p><p>&emsp;LRU 算法使得最近最常使用的保持在 new sublist 中，而 old sublist 中则是一些不被经常使用的 page，也是可能在不久后被驱逐那部分 page。如上图所示，默认 3/8 的 buffer pool 被划分为 old sublist，中点被定义在 new sublist 的尾部和 old sublist 的头部的相遇边界点，当 Innodb 读取 page 到 buffer pool 时，这些新 page 默认插入到被插入到中点处。</p><p>&emsp;默认情况下，查询所需要的 page 将立刻被放到 new sublist 中，这部分的page也会活得更久（短时间内不会被驱逐）。扫表行为（如 mysqldump 操作或无 where 条件的 select）会带入大量数据到 buffer pool 中，将原来 old list 中大量的老 page 驱逐出 Buffer Pool，即使这些新 page 后面不被使用。相似的，由预读后台（read-ahead）线程加载且仅访问一次的 page 将移至 new sublist 的开头，这些场景中可以将频繁使用的 page 推到 old sublist 中，最终被驱逐，实际来讲，这些场景都需要进行优化，我们最终的目的是将频繁使用的 page 对象保留在 Buffer Pool 中，而且尽可能保留在 new sublist 中，因为这样查询会更快。</p><p>&emsp;为了让Innodb更好、更快的工作，在配置 Buffer Pool 时可以做下面的一些优化：</p><ol><li>理想状态下，可以将 buffer pool 设置为实际的内存大小，保留足够的内存处理无需过多的page，buffer pool 设置的越大，Innodb 就越表现的像基于内存的数据库，从磁盘读取一次数据，然后在后续读取期间从内存访问、操作数据。buffer pool 的配置是基于一块一块的（即chunks），一个块（chunk）默认是128M，buffer pool 的大小必须是 <code>buffer_pool_chunk_six</code><em><code>buffer_pool_instances</code>的整数倍，<code>buffer_pool_chunk_six</code>默认是128M，如<code>mysqld --innodb-buffer-pool-size=8G --innodb-buffer-pool-instances=16</code>就是将 Buffer pool 配置为8G（128M </em> 16 * 4 = 8G），当配置的值不满足上述的整数倍时，Innodb会自动调整为向上取整，如<code>mysqld --innodb-buffer-pool-size=9G --innodb-buffer-pool-instances=16</code>，9G不是整数倍，会被自动调整为10G。</li><li>对于足够内存的64位系统，可以将 buffer pool 分割为多个部分，以最大程度地减少并发操作之间的内存结构争用。</li></ol><h4 id="3-1-2-Change-Buffer"><a href="#3-1-2-Change-Buffer" class="headerlink" title="3.1.2 Change Buffer"></a>3.1.2 Change Buffer</h4><p>&emsp;Change Buffer 是一种特殊的数据结构，它缓存了那些不在 buffer pool 中的第二索引（即辅助索引）的 page 对象的改动，当一些insert、update、delete操作让缓存变化后，当这些 page 被其他的读行为加载到 buffer pool 中时，可能导致上述的改变最后合并到一起，这就是 change buffer（和它的名字一样，记录变化的缓存），结构如下：</p><p><img src="https://img-blog.csdnimg.cn/img_convert/3bf0c7c861f168eeccba673e96f5802f.png" alt="innodb-change-buffer"></p><p>&emsp;和聚簇索引不一样，第二索引通常不是唯一的，且插入是相对随机的顺序，delete、update 可能会影响索引树种那些不相邻的第二索引的page。稍后，当其他操作将受影响的 page 读入 buffer pool 时，合并缓存的更改可以避免从磁盘将辅助索引页读入缓冲池所需的大量随机访问I/O。在系统基本处于空闲状态或在缓慢关机期间运行的清除操作会定期将更新的索引页写入磁盘，和立即写入磁盘相比，清除操作可以更高效的将一系列的索引值写入磁盘块。当有很多受影响的行和需要更新的辅助索引值时，change buffer 合并更改的行为可能会持续几个小时，并且在此期间，磁盘I/O会增加，与磁盘绑定的查询会变得十分缓慢。Change buffer 合并的行为可能会在事务提交后、甚至服务器重启后持续进行。</p><p>&emsp;在内存方面，其实 change buffer 占据了 buffer pool 的内存的一部分。在磁盘方面，change buffer 是系统 tableSpace 的一部分，当数据库服务器关闭时，索引的更改将会被缓存。在 change buffer 中的缓存数据类型主要是由变量<code>innodb change buffering</code>维护管理。注意的是：Change buffer 不支持包含降序索引的列或者主键中包含降序的列。</p><h4 id="3-1-3-adaptive-hash-index"><a href="#3-1-3-adaptive-hash-index" class="headerlink" title="3.1.3 adaptive hash index"></a>3.1.3 adaptive hash index</h4><p>&emsp;自适应hash索引可以让 Innodb 表现得更像是一个基于内存的数据库，前提将buffer pool 的工作负载和 buffer pool 具有合适充足内存合理结合，并且不丧失事务特性和可靠性。自适应 hash 索引可以通过变量<code>innodb adaptive hash index</code>开启或者通过启动参数<code>--skip-innodb-adaptive-hash-index</code>关闭。</p><p>&emsp;基于观察到的搜索模式，使用索引的前缀构建hash索引，前缀长度无限制，但可能只有 B 树的部分值会出现在 hash 索引中，它是由那些使用频繁的 page 对象按需进行构建的。如果一个表几乎完全在主存中，hash 索引可以将索引值转换指针，它允许直接搜索任何元素，这样可以加速查询速度。Innodb 有一种监控索引搜索的机制，如果Innodb发现某些查询可以受益于构建的hash 索引，那它就会自动这么做。</p><p>&emsp;在某些场景下，hash 索引的加速要大大胜过监控搜索和维护 hash 索引结构的额外工作。在繁重的负载下（比如多并发连接），hash 索引可能成为争抢的资源。像<code>like</code>和<code>%</code>匹配符往往不能受益于 hash 索引，对于不能受益于自适应 hash 索引的负载，可以将其关闭以避免不必要的性能开销，因为很难预先预测自适应哈希索引特性是否适合于特定的系统和工作负载，所以考虑在启用和禁用它的情况下运行基准测试。MySQL5.6中的体系结构更改使禁用自适应哈希索引功能比在早期版本中更合适。</p><p>&emsp;自适应hash索引是被切割开的，每个hash索引绑定了某个特定的区域，每个区域都有独立的锁保护，具体的划分行为是由<code>innodb_adaptive_hash_index_parts</code>变量控制，这个值默认是8，最大可设置为512。</p><p>【注】innodb 引擎是具备自适应hash索引特性的，默认自动开启，可以通过<code>SHOW VARIABLES LIKE &#39;%adaptive_hash_index%&#39;</code>查看，当InnoDB 注意到某些索引值被使用的非常频繁时，它会在内存中基于 B-Tree 索引上再建立一个 hash 索引，这样就可以让B-Tree 也具备 hash 索引的特性（如快速的hash查找）。这个建立自适应 hash 索引是自动的、内部行为，用户几乎无法控制，只能进行上述基础的开关和区域锁的控制。hash 索引可以创建在一些特别长的字段上（如url），这样可以使用直接使用 hash 值进行快速查找。</p><h4 id="3-1-4-log-buffer"><a href="#3-1-4-log-buffer" class="headerlink" title="3.1.4 log buffer"></a>3.1.4 log buffer</h4><p>&emsp;log buffer 是主要内存区域，它保存着待写入磁盘日志文件的数据。log buffer 的大小由变量 <code>innodb_log_buffer_size</code> 设定，默认为16MB，log buffer 中的内容会定期刷入磁盘中，一个大的 log buffer 可以使得一个大事务在提交前不需要往磁盘写 redo log，因此如果有更新、插入、删除多条记录的大事务，增大 log buffer 的大小可以节省磁盘I/O。</p><h3 id="3-2-InnoDB-的磁盘结构"><a href="#3-2-InnoDB-的磁盘结构" class="headerlink" title="3.2 InnoDB 的磁盘结构"></a>3.2 InnoDB 的磁盘结构</h3><p>&emsp;前面的内容基本都是InnoDB的内存结构，InnoDB的磁盘组成结构组要包含下面的几个部分：</p><ul><li>表 Table；</li><li>索引 Index；</li><li>表空间 Tablespace；</li><li>双写缓冲 Doublewrite Buffer：双写缓冲是一块 InnoDB 将数据从 Buffer pool 中刷入磁盘前的存储区，只有数据在刷入 DW Buffer 后，InnoDB 才会将将数据正式写入磁盘的数据文件中。如果由于某种原因 MySQL 线程在写 Page 退出，那它可以在故障恢复时使用 DW Buffer 中副本数据进行数据恢复。虽然这种机制数据需要写2份，但它不会消耗2倍I/O资源，而是以一份完整的数据块（可能很大）调用操作系统的<code>fsync()</code>写入。默认双写开启，可通过变量<code>innodb_doublewrite</code>控制（0-disable,1-able）。</li><li>Redo Log：重做日志是一种基于磁盘的数据结构，用于在崩溃恢复期间更正由不完整事务写入的数据，正常操作时，redo log 会将那些会更改数据的请求（即写请求）或底层的api调用进行编码。但若更改数据的请求可能由于某些原因没有完成完整的数据文件的修改，那此时在MySQL服务重启初始化时，在正式接受处理外部连接前将会重放 Redo log 日志。Redo log 在磁盘上主要体现在文件<code>ib_logfile0</code>和<code>ib_logfile1</code>上。MySQL 写 redo log 时是以循环写的方式进行，redo log 中的数据按照受影响的记录进行编码；这些数据统称为重做，redo log 的过程是由一个不断增长的 LSN 值表示。和其他ACID的数据库引擎一样，它会在事务提交前刷入 redo log，InnoDB 使用组合提交（group commit）的设计来将多个 flush 请求组合批量提交，这样可以避免每次commit都flush一下请求。使用组提交，InnoDB只需向日志文件发出一次写入操作，即可对大约同时提交的多个用户事务执行提交操作，从而显着提高了吞吐量。</li><li>Undo Log：undo log 中记录了一个独立的读写事务撤销日志的集合，一个 undo log 记录了怎样撤销最近的事务对聚簇索引的修改。若另一个事务需要查看原始数据作为一致性读的一部分操作，那此时未更改的数据将会从 undo log 中取出。Undo log 存在于 undo log segment 中，而 undo log segment 又存在于 rollback segment 中，rollback segment 存在于系统表空间中（system tablespace）和 undo tablespace 中以及 temporary tablespace中。rollback segment 支持的事务数量取决于 rollback segment 中 undo 插槽的数量以及每个事务所需的 undo log 的数量。其中 rollback segment 中undo 插槽的数量主要是由InnoDB page size 的大小决定的，通常就是 pageSize/16（如PageSize为4K，那undo 插槽为4*1024/16）。一个事务最多分配4种类型的 undo logs：用户自定义表的<code>INSERT</code>操作、用户自定义表的<code>UPDATE</code>和<code>DELETE</code>操作、用户自定义临时表的<code>INSERT</code>操作、用户自定义临时表的<code>UPDATE</code>和<code>DELETE</code>操作。</li></ul><h4 id="3-2-1-表Table"><a href="#3-2-1-表Table" class="headerlink" title="3.2.1 表Table"></a>3.2.1 表Table</h4><p>&emsp;InnoDB的表它的索引可以在系统表空间（system tablespace）、表文件（file-per-table，可能译为每个表作为一个文件更好理解一点）空间或者通用表空间（general tablespace）。当开启<code>innodb_file_per_table</code>时（默认开启），InnoDB的表将会隐式的创建每个表文件的独立表空间，相反的，当<code>innodb_file_per_table</code>关闭时，InnoDB的表将会隐式的创建系统表空间。可以在创建表时通过<code>CREATE TABLE ... TABLESPACE [tablespace_name]</code>指定，这时就是创建在通用表空间中。</p><p>&emsp;当在 file-pre-table 表空间中创建表时，MySQL 默认会在数据库的目录下创建一个<code>.ibd</code>（即idbdata）表空间文件；当在 system 表空间中创建表时，将会在已存在的 idbdata 文件中创建，这个文件驻留在 MySQL的data目中；当在通用表空间中创建表时，它将会在已存在的通用表空间中通用表空间中的<code>.idb</code>创建。通用表空间文件可以在MySQL的data目录内部或外部创建。</p><p>&emsp;<code>.frm</code>文件中存储了各个表的数据字典（表定义），InnoDB和其他存储引擎不一样，它仍然会在内部数据目录的系统表空间中编码这些信息，当MySQL drop 表或库时，它将会删除一个或多个<code>.frm</code>文件以及InnoDB中相应的条目，但注意不能简单的通过移动<code>.frm</code>文件来移动InnoDB的表。注：8.0中已经取消了该文件。</p><p>&emsp;InnoDB表的行格式决定了这些行记录怎样存储在物理磁盘上，InnoDB支持4种行格式（每种都有不同的存储特性）：<code>REDUNDANT</code>、<code>COMPACT</code>、<code>DYNAMIC</code>（默认格式）和<code>COMPRESSED</code></p><p>&emsp;内部，InnoDB会将每个表的条目添加到数据字典中，这个条目中包含了表空间的名字。比如在test数据库中创建表t1，那这个表在数据字典中的条目的名字就是’test/t1’，这就表示可以在不同的数据库中创建同名的表，表名不会在数据字典撞上。</p><p>&emsp;外部表可以在data目录外创建，在外部创建表主要原因有：空间管理，I/O优化，或者将表存储在某些特殊性能和能力的特性的设备上。在外部创建表主要有下面几种方式：</p><ol><li><code>CREATE TABLE ... DATA DIRECTORY=[external_directory]</code>，<code>DATA DIRECTORY</code>方式支持每个表一个文件的方式（file-per-table），当指定<code>DATA DIRECTORY</code>时，表的数据文件（即<code>table_name.idb</code>文件）就会在数据库目录下的指定目录创建；</li><li></li></ol><h2 id="关于锁"><a href="#关于锁" class="headerlink" title="关于锁"></a>关于锁</h2><p>&emsp;MySQL中大体主要分为表锁和行锁：</p><ul><li>表锁：会在用户对表进行写操作（即<code>ALTER TABLE</code>操作包括插入、删除、更新等）时触发，先获取写锁，写锁阻塞其他的读、写操作，是目前锁策略中开销最小，也是最简单粗暴的一种方式。</li><li>行锁：可以提高并发度，但也加锁的开销，需要加到行级别，行锁是存储引擎自己实现的。<ul><li>record lock：</li><li>gap lock：</li><li>next-key lock：</li></ul></li></ul><p>其中MyIASM只支持表锁。显式锁可以通过<code>SELECT ... FOR UPDATE</code>或<code>SELECT ... LOCK IN SHARE MODE</code>来锁定，或者使用<code>LOCK TABLE</code>和<code>UNLOCK TABLE</code>来控制表锁，但这个行为和存储引擎无关，是在Server层实现的。</p><h3 id="共享锁和排他锁"><a href="#共享锁和排他锁" class="headerlink" title="共享锁和排他锁"></a>共享锁和排他锁</h3><p>&emsp;InnoDB 实现了2种标准的行级锁：shared lock 和 exclusive lock。共享锁（s锁）：允许持共享锁的事务读取行记录；排他锁（x锁）：允许持有排他锁的事务更新或删除行记录。当事务T1持有记录行r的共享锁，另一个事务T2再来请求行记录r的锁时，若请求的为共享锁那将会即刻获取，若请求的是排他锁则无法即刻获取。当事务T1持有记录行r的排他锁，那其他事务无法即刻获取r上的任意类型锁（共享锁和排他锁），必须等到T1的排他锁释放。</p><h3 id="意向锁"><a href="#意向锁" class="headerlink" title="意向锁"></a>意向锁</h3><p>&emsp;InnoDB 支持多重颗粒度的锁：允许行级锁和表级锁共存。当给某个表加一个排他锁时（如：<code>LOCK TABLES heartbeat WRITE</code>，释放锁是<code>UNLOCK TABLES</code>），为了实现多颗粒度级别的锁，InnoDB 引入了意向锁（intention lock）。首先<font color="red">意向锁是表级锁</font>，它标示着一个事务即将对于表中的某个行记录所要加的锁，同样的 intention lock 也分为意向共享锁（IS）和意向排他锁（IX），意向共享锁标识着某个事务将要对表中的每个行记录加共享锁（<code>SELECT ... FOR SHARE</code>），意向排他锁则标识将要对表中的每个记录加排他锁（<code>SELECT ... FOR UPDATE</code>）。意向锁的协议如下：</p><ul><li>在一个事务获取一个表中某行记录的共享锁之前，它必须先要获取表级别的 intention share lock 或者是更强的锁；</li><li>在一个事务获取一个表中某行记录的排他锁之前，它必须先要获取表级别的 intention exclusive lock 或者是更强的锁；</li></ul><p>S、X、IS、IX之间并不是都可以相互兼容，它们之间的兼容关系如下：</p><table><thead><tr><th style="text-align:center"></th><th style="text-align:center">X</th><th style="text-align:center">IX</th><th style="text-align:center">S</th><th style="text-align:center">IS</th></tr></thead><tbody><tr><td style="text-align:center">X</td><td style="text-align:center">×</td><td style="text-align:center">×</td><td style="text-align:center">×</td><td style="text-align:center">×</td></tr><tr><td style="text-align:center">IX</td><td style="text-align:center">×</td><td style="text-align:center">√</td><td style="text-align:center">×</td><td style="text-align:center">√</td></tr><tr><td style="text-align:center">S</td><td style="text-align:center">×</td><td style="text-align:center">×</td><td style="text-align:center">√</td><td style="text-align:center">√</td></tr><tr><td style="text-align:center">IS</td><td style="text-align:center">×</td><td style="text-align:center">√</td><td style="text-align:center">√</td><td style="text-align:center">√</td></tr></tbody></table><p>当前即将加的锁和已有的锁只有兼容的时候才能加上，否则必须等到已有冲突锁释放，如果没有这个机制很可能导致死锁。</p><p>&emsp;intention lock 不会阻止除完整表请求以外的任何请求（例如<code>LOCK TABLES ... WRITE</code>）。意向锁的主要目的是显示有人正在锁定一行，或者将要锁定表中的一行。</p><h3 id="record-lock"><a href="#record-lock" class="headerlink" title="record lock"></a>record lock</h3><p>&emsp;记录锁是索引记录上的锁，比如<code>SELECT c1 FROM t WHERE c1 = 10 FOR UPDATE</code>，它将阻止其他事务读对 c1=10 的记录进行删、改、插入行为。记录锁始终锁定索引记录，即使定义的表没有索引，因为InnoDB会自动为没有聚簇索引的表创建隐式聚簇索引。</p><h3 id="gap-lock"><a href="#gap-lock" class="headerlink" title="gap lock"></a>gap lock</h3><p>&emsp;间隙锁是一种锁住索引记录之间间隙的锁（如<code>SELECT c1 FROM t WHERE c1 BETWEEN 10 and 20 FOR UPDATE</code>），或者锁住在第一个索引记录之前的锁、亦或者是锁住最后一个索引记录之后锁。间隙锁可以防止其他事务将一些中间值插入到所在区间，无论该列中是否已有这样的值，都无法插入，如上述是锁住了10~20，即使不存在15的记录，15记录仍然无法插入。间隙锁将会作用于0个或1个也或是多个索引值。间隙锁是性能和并发性之间权衡的一部分，并且在某些事务隔离级别中使用，而在其他级别中则不使用。</p><p>&emsp;那些是使用唯一索引检索某个唯一行记录时是不需要间隙锁的（注：但不包含搜索条件中只有那些联合唯一索引），如：<code>SELECT * FROM child WHERE id = 100</code>这个SQL，若 id 上具有唯一索引，那对 id=100 记录加的锁就是 index-record lock；但如果 id 上没有索或者没有唯一索引，那这个SQL就会锁住 id=100 前面的间隙，此时就是间隙锁，<font color="red">这里检索的字段上一定是无索引的，否则就成了index-record lock</font>。示例如下：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 初始数据准备</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="string">`test`</span> (</span><br><span class="line">  <span class="string">`id`</span> <span class="built_in">int</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> AUTO_INCREMENT <span class="keyword">COMMENT</span> <span class="string">'id'</span>,</span><br><span class="line">  <span class="string">`name`</span> <span class="built_in">varchar</span>(<span class="number">32</span>) <span class="keyword">DEFAULT</span> <span class="string">'default'</span> <span class="keyword">COMMENT</span> <span class="string">'名字'</span>,</span><br><span class="line">  <span class="string">`create_time`</span> <span class="built_in">timestamp</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="keyword">CURRENT_TIMESTAMP</span> <span class="keyword">COMMENT</span> <span class="string">'创建时间'</span>,</span><br><span class="line">  <span class="string">`update_time`</span> <span class="built_in">timestamp</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="literal">NULL</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="keyword">CURRENT_TIMESTAMP</span> <span class="keyword">COMMENT</span> <span class="string">'更新时间'</span>,</span><br><span class="line">  <span class="string">`property`</span> <span class="built_in">varchar</span>(<span class="number">32</span>) <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="string">`second_index`</span> <span class="built_in">int</span> <span class="keyword">DEFAULT</span> <span class="literal">NULL</span> <span class="keyword">COMMENT</span> <span class="string">'普通第2索引'</span>,</span><br><span class="line">  <span class="string">`no_index`</span> <span class="built_in">int</span> <span class="keyword">DEFAULT</span> <span class="literal">NULL</span> <span class="keyword">COMMENT</span> <span class="string">'无索引字段'</span>,</span><br><span class="line">  PRIMARY <span class="keyword">KEY</span> (<span class="string">`id`</span>),</span><br><span class="line">  <span class="keyword">KEY</span> <span class="string">`si`</span> (<span class="string">`second_index`</span>)</span><br><span class="line">) <span class="keyword">ENGINE</span>=<span class="keyword">InnoDB</span> AUTO_INCREMENT=<span class="number">15</span> <span class="keyword">DEFAULT</span> <span class="keyword">CHARSET</span>=utf8mb3 <span class="keyword">COMMENT</span>=<span class="string">'测试表'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="keyword">test</span>(property, second_index,no_index) <span class="keyword">VALUES</span>(<span class="string">"prop1"</span>, <span class="number">9</span>,<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 会话1</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> heartbeat <span class="keyword">WHERE</span> server_id=<span class="number">1</span> <span class="keyword">LOCK</span> <span class="keyword">IN</span> <span class="keyword">SHARE</span> <span class="keyword">MODE</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 会话2，发现在等待间隙锁</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="keyword">test</span>(property, second_index,no_index) <span class="keyword">VALUES</span>(<span class="string">"prop1"</span>, <span class="number">6</span>,<span class="number">6</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 杀掉等待的进程</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">PROCESSLIST</span></span><br><span class="line"><span class="keyword">KILL</span> xx</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看未提交的事务</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> <span class="string">`information_schema`</span>.<span class="string">`INNODB_TRX`</span></span><br></pre></td></tr></table></figure><p>&emsp;间隙锁可以通过将事务隔离级别设置为 RC 来主动关闭。</p><h3 id="next-key-lock"><a href="#next-key-lock" class="headerlink" title="next-key lock"></a>next-key lock</h3><p>&emsp;next-key lock 就是 record lock 和 gap lock 的组合，它会给当前索引记录加 record lock，同时在该索引记录之前的间隙上加间隙锁。</p><p>&emsp;InnoDB 行级锁是加锁方式是当它搜索或扫描一个表索引时，它将会在遇到的索引记录上加共享或排他锁（可能表述为扫描到一行就对这行加锁）。因此，MySQL的行级锁实际就是 index-record lock。当对一个索引记录上加上 next-key lock 时，实际也会影响这个索引记录之前的间隙，索引上的 next-key lock 是一个 index-record lock 加上这个索引记录之前间的间隙锁。一旦对某个索引记录加了共享锁或者排他锁，那就不能对这个记录以及它之前的记录进行操作。</p><p>&emsp;默认情况下，InnoDB使用的是 RR 隔离级别，这种情况下，InnoDB 使用 next-key lock 搜索或扫描索引，这样做可以避免幻影读（又学了一个单词 phantom - /ˈfæntəm/）。</p><h3 id="Auto-Inc-lock"><a href="#Auto-Inc-lock" class="headerlink" title="Auto-Inc lock"></a>Auto-Inc lock</h3><p>&emsp;AUTO-INC 锁是一种特殊的表级锁，发生在插入存在自增列（AUTO_INCREMENT）记录的时候。如果一个事务正在向表中插入值，其他事务必须等到这个事务插入完成，以便第一个事务收到那个连续的主键值。8.0 默认是交错模式（即<code>innodb_autoinc_lock_mode</code>为2，5.X的版本默认是连续模式1会加锁，但具体是从哪个版本开始就忘了）。Auto-Inc Lock 可以的模式可以是期望可预测的自增值、还是更期望插入行为的更高并发。在交错模式下是不会加 AUTO-INC 锁，需要设置为连续模式1。</p><p>&emsp;对于自增锁的3种模式有如下的说明（通过变量<code>innodb_autoinc_lock_mode</code>设置，需要在配置文件中设置）：</p><ul><li><p>0：传统锁模式（traditional lock mode），它在变量<code>innodb_autoinc_lock_mode</code>引入之前就存在了，它在传统的锁模式选项用于向后兼容性、性能测试以及解决“混合模式插入”问题，因为语义上可能存在差异。在传统锁模式下，所有的 <code>insert</code> like 语句将获取一个特殊级别的表级别的 AUTO-INC 锁，这个锁将持有到语句的最后（而不是事务的最后），为的是确保自增值可预知的、可重复的顺序按照<code>insert</code>语句的顺序分配出去，并确保自增值是按<code>insert</code>循序连续不断的；在主从架构上的 statement-base 复制（是否就是binlog的格式），当SQL语句被复制到从节点上时，从节点上的自增列将会使用和主节点上一样的值。但若多条<code>insert</code>语句插入的结果是未知的，在 statement-based 级别的传播出去的自增值是不可靠。上面描述的场景可以用下面2个事务的示例来描述：Tx1（插入1000条） - <code>INSERT INTO t1 (c2) SELECT 1000 rows from another table ...</code>，Tx2（插入1条） - <code>INSERT INTO t1 (c2) VALUES (&#39;xxx&#39;)</code>，对于事务1在执行时，Innodb是不能提前获知<code>insert into t1 select ...</code>这个语句会取出多少行记录，当它执行时一次分配一个自增值。它会持有表级锁直到在该语句的末尾，一次只能执行一个引用表t1的<code>INSERT</code>语句，并且不会交错使用不同的语句生成自动递增编号。但是事务1生成的自增值是连续的，事务2中生成的自增值要么比事务1中所有自增值大，要么比它所有自增值要小，这取决哪个语句先执行。若从节点通statement-base格式的binlog重放SQL，只要他们的执行顺序和主节点上一致就没有问题，因此，在语句结束之前保持的表级锁使使用自动增量的<code>INSERT</code>语句可以保证安全地用于基于语句的主从复制，但也因此限制Innodb多事务的并发。若上述行为没有表级锁，事务2中的自增键的值去取决于该语句的执行时机，比如事务2中的行为在事务1插入行为正在进行时执行（既不是在事务1执行之前，也不是在事务1执行之后），分配给2个插入语句的特定自增值是不确定的，因运行时机而定；</p></li><li><p>1：连续锁模式（consecutive lock mode），<font color="red">这是默认的锁模式</font>。在连续锁模式下，”bulk insert” 使用特殊的 AUTO-INC 表级锁，且持有到语句的最后，包含了<code>insert ... select</code>、<code>replace ...select</code>和<code>load data</code>，每次有且只有那一个持有 AUTO-INC 表级锁的语句可以执行，若原始表的批量插入行为和目标表不一样，那目标表上的AUTO-INC锁加上的时机为：在原始表第一行满足条件的记录被选中且加上共享锁时加上。若目标表和原始表的批量插入行为一致，那AUTO-INC锁会在所有行的共享锁被获取时加上。对于简单插入行为（即可以事先知道插入的行数），可以通过在互斥量（一种轻量级锁）的控制下获取所需数量的自增值，这个锁仅仅在分配自增值期间持有，而不持有到语句完成，这样可以避免表级别的 AUTO-INC 锁，除非另一个事务持有AUTO-INC锁，此时简单插入将会等待AUTO-INC锁，就像是批量插入的行为一样。在这种锁模式下，出现不能预知行数的插入语句时(且当在语句执行过程中分配自增值时)，所有的自增值都将会被连续分配，这样可以保证基于语句的主从复制(statement-based)的数据安全；连续锁模式可以可显着提高可伸缩性，同时可安全地用于基于语句的复制，此外，与“传统”锁模式一样，任何给定语句分配的自动增量编号都是连续的。但有种例外情况，对于使用自增的SQL语句，和传统锁模式相比在语义上没有区别。</p></li><li><p>2：交叉锁模式（interleaved lock mode），这种锁模式下，插入语句不会使用AUTO-INC锁，且多条语句可以同时执行，交叉锁模式时最快、可扩展性最高的锁模式，但在基于语句主从复制的情况下，从节点从binlog重放SQL语句时它并不安全。交叉锁模式下，在所有插入语句并发执行时，自增值可以保证唯一且单调增，但由于是多个语句同时生成数值（数字分配在语句之间交错），所以任何给定的插入语句生成的自增值并不是连续的（和它的名字一样，是交错的）。若执行的语句仅仅是简单插入，且他们插入的行数是可预知的，那对于单个语句中生成的数值则是没间隙的，除非是混合模式的插入场景，当执行批量插入时，分配出来的自增值可能会出现间隙。</p></li></ul><p>我们需要更改<a href="https://dev.mysql.com/doc/refman/5.7/en/innodb-auto-increment-handling.html" rel="external nofollow noopener noreferrer" target="_blank"><code>innodb_autoinc_lock_mode</code>的配置</a>（0-标识传统锁模式，1-标识，2-标识），这是一种特殊的锁机制，他不是在一个事务完成后才释放，而是在完成对自增键的增长后就立刻释放，从而提升并发效率。</p><p>&emsp;InnoDB推荐主键顺序插入（即定义为<code>AUTO_INCREMENT</code>），这样可以减少频繁的页分裂和碎片的产生，特别是I/O密集型应用，最好避免随机（不连续且值分布范围很大）的聚簇索引（比如使用UUID来作主键性能会很差且索引占用空间大）。自增主键的情况下，每条记录都存储在上一条记录的后面，在一个页被插满（达到页的最大填充因子，默认15/16，留出部分空间以便后面修改）后继续插入会插入到下一个新页，数据加载是顺序加载，主键页就会近似于被顺序的记录填满，而若主键是随机的，InnoDB无法简单的总是把新行记录插入到索引的最后（因为B-Tree叶子节点是顺序的），这样就需要为新行寻找合适的位置并分配空间，通常是已有数据的中间，这种为新行分配空间的行为会导致频繁的页分裂，页分裂会导致数据的大量移动，页会变得稀疏且填充不规则，最终产生碎片，关于页分裂和页合并可以参考<a href="https://zhuanlan.zhihu.com/p/98818611" rel="external nofollow noopener noreferrer" target="_blank">这篇文章</a>。当然，顺序主键也并非是绝对的友好，对于高并发应用，InnoDB按主键顺序插入可能会造成明显的竞争，所有的插入都发生在主键的上界，并发插入可能导致间隙锁的竞争；另一个是<code>AUTO_INCREMENT</code>锁机制，它是一种描述用于生成自增值的锁机制。</p><p>【注】在描述AUTO-INC LOCK时的一些概念：</p><ul><li>insert-like 语句：插入型语句，指所有可以在表中生成新行的语句，包含<code>insert</code>、<code>insert...select</code>、<code>replace</code>、<code>replace ... select</code>以及<code>load data</code>，也包含下面的简单插入（simple inserts）、批量插入(bulk inserts)和混合模式插入(mixed-mode inserts)。</li><li>simple inserts：可预测的简单插入，这种插入可以提前预测到将要插入的行数，包含了没有嵌套子查询的单个和批量的<code>insert</code>、<code>replace</code>语句，但不包含了<code>insert ... on duplicate key update</code>；</li><li>bulk inserts：不可预测的批量插入，一些需要生成自增键值的批量插入语句，但具体的数量又是不可预测的，这种场景包含了<code>insert ... select</code>、<code>replace ... select</code>和<code>load data</code>语句，不包含可预知数量的<code>insert</code>行为，Innodb会在处理该行数据时为<code>AUTO_INCREMENT</code>的列生成新值；</li><li>mixed-mode inserts：混合插入，就是一些简单的插入语句，在这个语句中指定了一些自增值，但没有为所有新纪录行指定，比如：<code>INSERT INTO t1 (c1,c2) VALUES (1,&#39;a&#39;), (NULL,&#39;b&#39;), (5,&#39;c&#39;), (NULL,&#39;d&#39;);</code>，其中 c1 是自增列。另一种情况就是<code>insert ... on duplicate key update</code>，这个就是最糟的场景（<code>insert</code>后面跟<code>update</code>，因为给自增列分配的值在 update 语句中可能会被使用，但也可能不被使用）。</li></ul><p>【注】<code>insert into t1 select * from t2 where index_field&gt;xx</code>加锁规则：t1是锁表（前提是表必须存在），t2是逐步锁，扫描一个锁一个，随着扫描的行数越来越多锁的行也会越来越多，直到后面t2整表被锁，完成数据复制，这种逐行锁还是有主键或索引作为条件的情况下，若后面跟的条件是非主键，那会导致t2在一开始就直接被整表锁（区别重要）。为了避免t2被锁走全表扫描，最好在后面追加<code>FORCE INDEX (index_filed) where index_filed=xxx</code>，这样就会强制走索引检索，从而避免现用业务表t2被全表锁住，影响业务服务。若数据量巨大，慎用<code>insert into ... select ...</code>，它只是插入数据，且目标表必须事先建好，其次它是DML语句（数据操作语言-database manipulation language，SQL中处理数据等操作统称为数据操纵语言），完成后需要提交才能生效。<code>CREATE TABLE AS SELECT</code> 是DDL语句（数据定义语言-database definition language，用于定义和管理 SQL 数据库中的所有对象的语言 ），执行完直接生效，不提供回滚，效率比较高，执行过程中全程锁表，可以在深更半夜停服的情况下使用。</p><h2 id="关于-MySQL-中指定属性宽度"><a href="#关于-MySQL-中指定属性宽度" class="headerlink" title="关于 MySQL 中指定属性宽度"></a>关于 MySQL 中指定属性宽度</h2><p>&emsp;</p><h2 id="关于索引"><a href="#关于索引" class="headerlink" title="关于索引"></a>关于索引</h2><p>&emsp;索引可以帮助快速定位数据。最常见的索引就是 B-Tree，它的叶子节点是按照顺序存储数据的，所以 MySQL 可以做 <code>ORDER BY</code> 和 <code>GROUP BY</code> 之类的操作，因为数据是有序的，所以B-Tree 会将相关的列值都存储在一起，因为索引中存储了实际的列值，所以某些查询只使用索引就可以完成全部查询。索引的优点：</p><ol><li>索引可以减少服务器需要扫描的数据量；</li><li>索引可以避免排序和临时表；</li><li>索引可以将随机I/O变为顺序I/O；</li></ol><p>&emsp;几个索引的使用建议：</p><ol><li>独立的列：尽量不要在<code>where</code>后面的条件中对列属性作函数操作，如<code>age -1 = 4</code>，这种情况 MySQL 中的 age 索引将会失效，它无法自动解析这种行为，所谓独立的列就是让列单独在一侧，运算的逻辑不要放在列所在一侧，而应该放在另一侧，这样索引才可以正常使用，所以上述的sql应该优化为 <code>age = 4+1</code>；</li><li>索引简短：尽量不要对一些很长的列属性进行索引创建，比如爬虫时持久化的网站url，对这个字段创建索引只会让索引变得巨大且缓慢。有几种方式来避免这种大型字段索引的创建：<ul><li>2.1 使用 hash 索引：如果刚好选用的存储引擎不支持 hash 索引，则手动模拟 hash 索引（如<code>CRC32</code>函数10位），但表中需要额外维护一个该字段的 hash 值并对该字段建立索引，每次查询该属性的条件值时，则可以变相使用那个额外的 hash 值字段来进行替换检索，这是一种非常高效的索引方式，因为索引将变得很小且快，至于维护可以使用触发器来帮助在插入或更新时 hash 值的自动更新。【注】这种方式不要使用<code>SHA1()</code>(40位)或<code>MD5()</code>(32位)，这2个 hash 函数计算结果相对耗时且结果很长，且这个2个 hash 函数目的是为了最大限度的消除冲突，如果数据非常多（大于9W），使用<code>CRC32</code>冲突可能会很多，这时在<code>where</code> 追加对应业务属性字段条件（如<code>where url_hash_value=CRC32(&#39;https://pan.aliyun.com&#39;) and url=&#39;https://pan.aliyun.com&#39;</code>），而不再是<code>where url_hash_value=CRC32(&#39;https://pan.aliyun.com&#39;)</code>（数据量小时可以直接使用），或者可以使用<code>FNV64</code>(它比<code>CRC32()</code>冲突要少得多)、自定义简单的函数来做 hash 函数；</li><li>2.2 前缀索引：只对索引开始的部分字符，这样索引就会变小提升效率，但由于是只对前缀部分的内容做的索引，所以就会降低索引的选择性。如果对<code>BLOB</code>、<code>TEXT</code>类型字段建立索引时必须使用前缀索引，但前缀的选择性也需要足够高，又不能太长，通常是将频繁搜索的值列出来，然后不断增加前缀索引的长度，直到整个前缀索引的选择性接近完整列的选择性即可，在实际比较时可以直接使用<code>count(DISTINCT(LEFT(索引字段), 前缀长度)))/Count(*)</code> SQL 来调整前缀长度观察该长度的索引的选择性。【注】创建前缀索引<code>KEY (索引字段(前缀长度))</code>，前缀索引无使用<code>ORDER BY</code>和<code>GROUP BY</code>操作，也无法做覆盖扫描。</li></ul></li><li>多列索引：即联合索引，对多个字段建立索引，遵循最左匹配原则。常规的2种建立联合索引的法则：将使用越频繁的列放在联合索引的越左侧、将选择性越高的列放在联合索引的越左侧。</li><li>聚簇索引：一个表只允许有一个聚簇索引（覆盖索引可以模拟多个聚簇索引的情况），通常是主键，若未定义主键，InnoDB会隐式定义一个主键来作聚簇索引。聚簇索引的的结构是-叶子节点包含了行的全部数据，但节点只包含索引列，所以通常非聚簇索引（二级索引）的查找往往都涉及到回表的操作，它只保存了主键的值。聚簇索引的有点：<ul><li>4.1 可以将相关数据保存在一起，根据主键聚集数据时，只需要从磁盘读取少量数据；</li><li>4.2 速度快，索引和数据保存在同一个B-Tree中，获取数据快；</li><li>4.3 覆盖索引的扫描可以直接使用叶节点中的主键值；</li></ul></li><li>覆盖索引：当一个索引包含（覆盖）所有所需字段，那此时就不需要二次回表（我们知道，通常除了主键索引外，其他索引的查询一般都是先找到主键，然后在根据主键定位叶节点的数据，需要二次回表），这样的索引就是覆盖索引。Innodb的二级索引在叶节点保存了主键值，若二级索引可以覆盖查询，可以避免对主键索引的二次查询。但并不是所有类型索引都可以成为覆盖索引，覆盖索引必须存储索引列的值，而 hash 索引、空间索引以及全文索引都不会存储索引列的值，所以只能使用 B-Tree 做覆盖索引。索引覆盖查询时，SQL执行计划Extra可以看“Using index”信息（不要和type搞混，type是标识查询访问数据的方式或者是MySQL查找行的方式，即连接方式join type）。其实个人理解下来覆盖索引会出现在2个场景中：一种是主键查询，另一种就是联合索引查询联合列。覆盖索引的优化场景需要合理利用二级索引叶子节点都包含了主键。</li></ol><p>【注】索引的选择性=不重复的索引值（基数）/总记录数=<code>COUNT(DISTINCT(索引字段))/COUNT(*)</code>，选择性越高查询效率就越高，它可以在查找时过滤更多无用行。唯一索引的选择性是1，它也是最好的索引选择，因为每条记录的唯一索引都不一样=N/N，再如<code>gender</code>只会有男、女2个值，那么gender索引的选择性=2/N，随着记录的增加，这个索引的选择性多越来越低，所以通常这种字段就也没有建立索引的必要。</p><p>【注】InnoDB推荐主键顺序插入（即定义为<code>AUTO_INCREMENT</code>），这样可以减少频繁的页分裂和碎片的产生，特别是I/O密集型应用，最好避免随机（不连续且值分布范围很大）的聚簇索引（比如使用UUID来作主键性能会很差且索引占用空间大）。自增主键的情况下，每条记录都存储在上一条记录的后面，在一个页被插满（达到页的最大填充因子，默认15/16，留出部分空间以便后面修改）后继续插入会插入到下一个新页，数据加载是顺序加载，主键页就会近似于被顺序的记录填满，而若主键是随机的，InnoDB无法简单的总是把新行记录插入到索引的最后（因为B-Tree叶子节点是顺序的），这样就需要为新行寻找合适的位置并分配空间，通常是已有数据的中间，这种为新行分配空间的行为会导致频繁的页分裂，页分裂会导致数据的大量移动，页会变得稀疏且填充不规则，最终产生碎片，关于页分裂和页合并可以参考<a href="https://zhuanlan.zhihu.com/p/98818611" rel="external nofollow noopener noreferrer" target="_blank">这篇文章</a>。当然，顺序主键也并非是绝对的友好，对于高并发应用，InnoDB按主键顺序插入可能会造成明显的竞争，所有的插入都发生在主键的上界，并发插入可能导致间隙锁的竞争；另一个是<code>AUTO_INCREMENT</code>锁机制，它是一种描述用于生成自增值的锁机制，可用值有：</p><ul><li><p>0：传统锁模式（traditional lock mode），传统的锁模式选项用于向后兼容性、性能测试以及解决“混合模式插入”问题，因为语义上可能存在差异。在传统锁模式下，所有的 <code>insert</code> like 语句将获取一个特殊级别的表级别的 AUTO-INC 锁，这个锁将持有到语句的最后（而不是事务的最后），为的是确保自增值可以以可预知的、可重复的顺序按照<code>insert</code>语句的顺序分配出去，并确保自增值是按<code>insert</code>循序连续不断的；在主从架构上的 statement-base 复制（是否就是binlog的格式），当SQL语句被复制到从节点上时，从节点上的自增列将会使用和主节点上一样的值。但若多条<code>insert</code>语句插入的结果是未知的，在 statement-based 级别的传播出去的自增值是不可靠。上面描述的场景可以用下面2个事务的示例来描述：Tx1（插入1000条） - <code>INSERT INTO t1 (c2) SELECT 1000 rows from another table ...</code>，Tx2（插入1条） - <code>INSERT INTO t1 (c2) VALUES (&#39;xxx&#39;)</code>，对于事务1在执行时，Innodb是不能提前获知<code>insert into t1 select ...</code>这个语句会取出多少行记录，当它执行时一次分配一个自增值。它会持有表级锁直到在该语句的末尾，一次只能执行一个引用表t1的<code>INSERT</code>语句，并且不会交错使用不同的语句生成自动递增编号。但是事务1生成的自增值是连续的，事务2中生成的自增值要么比事务1中所有自增值大，要么比它所有自增值要小，这取决哪个语句先执行。若从节点通statement-base格式的binlog重放SQL，只要他们的执行顺序和主节点上一致就没有问题，因此，在语句结束之前保持的表级锁使使用自动增量的<code>INSERT</code>语句可以保证安全地用于基于语句的主从复制，但也因此限制Innodb多事务的并发。若上述行为没有表级锁，事务2中的自增键的值去取决于该语句的执行时机，比如事务2中的行为在事务1插入行为正在进行时执行（既不是在事务1执行之前，也不是在事务1执行之后），分配给2个插入语句的特定自增值是不确定的，因运行时机而定；</p></li><li><p>1：连续锁模式（consecutive lock mode），<font color="red">这是默认的锁模式</font>。在连续锁模式下，”bulk insert” 使用特殊的 AUTO-INC 表级锁，且持有到语句的最后，包含了<code>insert ... select</code>、<code>replace ...select</code>和<code>load data</code>，每次有且只有那一个持有 AUTO-INC 表级锁的语句可以执行，若原始表的批量插入行为和目标表不一样，那目标表上的AUTO-INC锁加上的时机为：在原始表第一行满足条件的记录被选中且加上共享锁时加上。若目标表和原始表的批量插入行为一致，那AUTO-INC锁会在所有行的共享锁被获取时加上。对于简单插入行为（即可以事先知道插入的行数），可以通过在互斥量（一种轻量级锁）的控制下获取所需数量的自增值，这个锁仅仅在分配自增值期间持有，而不持有到语句完成，这样可以避免表级别的 AUTO-INC 锁，除非另一个事务持有AUTO-INC锁，此时简单插入将会等待AUTO-INC锁，就像是批量插入的行为一样。在这种锁模式下，出现不能预知行数的插入语句时(且当在语句执行过程中分配自增值时)，所有的自增值都将会被连续分配，这样可以保证基于语句的主从复制(statement-based)的数据安全；连续锁模式可以可显着提高可伸缩性，同时可安全地用于基于语句的复制，此外，与“传统”锁模式一样，任何给定语句分配的自动增量编号都是连续的。但有种例外情况，对于使用自增的SQL语句，和传统锁模式相比在语义上没有区别。</p></li><li><p>2：交叉锁模式（interleaved lock mode），这种锁模式下，插入语句不会使用AUTO-INC锁，且多条语句可以同时执行，交叉锁模式时最快、可扩展性最高的锁模式，但在基于语句主从复制的情况下，从节点从binlog重放SQL语句时它并不安全。交叉锁模式下，在所有插入语句并发执行时，自增值可以保证唯一且单调增，但由于是多个语句同时生成数值（数字分配在语句之间交错），所以任何给定的插入语句生成的自增值并不是连续的（和它的名字一样，是交错的）。若执行的语句仅仅是简单插入，且他们插入的行数是可预知的，那对于单个语句中生成的数值则是没间隙的，除非是混合模式的插入场景，当执行批量插入时，分配出来的自增值可能会出现间隙。</p></li></ul><p>我们需要更改<a href="https://dev.mysql.com/doc/refman/5.7/en/innodb-auto-increment-handling.html" rel="external nofollow noopener noreferrer" target="_blank"><code>innodb_autoinc_lock_mode</code>的配置</a>（0-标识传统锁模式，1-标识，2-标识），这是一种特殊的锁机制，他不是在一个事务完成后才释放，而是在完成对自增键的增长后就立刻释放，从而提升并发效率。</p><p>【注】一些专有概念：</p><ul><li><code>insert</code>-like statements：插入型语句，所有可以在表中生成一个新行的语句，包含<code>insert</code>、<code>insert...select</code>、<code>replace</code>、<code>replace ... select</code>以及<code>load data</code>，也包含简单插入、批量插入和混合模式插入。</li><li>simple inserts：简单插入，这种插入可以提前决定一些待插入的行，包含了没有嵌套子查询的单个和批量的<code>insert</code>、<code>replace</code>语句，但不包含了<code>insert ... on duplicate key update</code>；</li><li>bulk inserts：批量插入，一些需要生成自增键值的批量插入语句，但具体的数量又不知道，这种场景包含了<code>insert ... select</code>、<code>replace ... select</code>和<code>load data</code>语句，不包含清楚数量的<code>insert</code>行为，Innodb会在处理该行数据时为<code>AUTO_INCREMENT</code>的列生成新值；</li><li>mixed-mode inserts：混合插入，就是一些简单的插入语句，在这个语句中指定了一些自增值，但没有为所有新纪录行指定，比如：<code>INSERT INTO t1 (c1,c2) VALUES (1,&#39;a&#39;), (NULL,&#39;b&#39;), (5,&#39;c&#39;), (NULL,&#39;d&#39;);</code>，其中 c1 是自增列。另一种情况就是<code>insert ... on duplicate key update</code>，最糟的场景就是<code>insert</code>后面跟着<code>update</code>，但分配的自增列值不确定是否会被使用。</li></ul><p>【注】<code>insert into t1 select * from t2 where index_field&gt;xx</code>加锁规则：t1是锁表（前提是表必须存在），t2是逐步锁，扫描一个锁一个，随着扫描的行数越来越多锁的行也会越来越多，直到后面t2整表被锁，完成数据复制，这种逐行锁还是有主键或索引作为条件的情况下，若后面跟的条件是非主键，那会导致t2在一开始就直接被整表锁（区别重要）。为了避免t2被锁走全表扫描，最好在后面追加<code>FORCE INDEX (index_filed) where index_filed=xxx</code>，这样就会强制走索引检索，从而避免现用业务表t2被全表锁住，影响业务服务。若数据量巨大，慎用<code>insert into ... select ...</code>，它只是插入数据，且目标表必须事先建好，其次它是DML语句（数据操作语言-database manipulation language，SQL中处理数据等操作统称为数据操纵语言），完成后需要提交才能生效。<code>CREATE TABLE AS SELECT</code> 是DDL语句（数据定义语言-database definition language，用于定义和管理 SQL 数据库中的所有对象的语言 ），执行完直接生效，不提供回滚，效率比较高，执行过程中全程锁表，可以在深更半夜停服的情况下使用。</p><h2 id="3-InnoDB-的排序"><a href="#3-InnoDB-的排序" class="headerlink" title="3. InnoDB 的排序"></a>3. InnoDB 的排序</h2><p>&emsp;使用索引来满足<code>ORDER BY</code>进行排序，可以避免执行<code>filesort</code>排序（文件排序）时涉及的额外排序操作。只要那些没有使用索引的区域和所有<code>ORDER BY</code>列都是在<code>WHERE</code>语句中的常量时，那么甚至可以在非精确匹配索引时使用索引。若索引没有包含查询所需的所有列，那只有在使用索引比其他方式代价更小的场景下才会使用索引。</p><p>&emsp;若有索引建立在k1、k2上，那<code>select * from [table_name] order by k1,k2</code>语句可能会使用上面的索引进行排序，当然只是可能会使用上述索引，优化器如何做，具体是否使用需要取决于读取索引是否比全表扫描更加高效（若不在索引中的列必须读取，那就会进行全表扫）。当然上述的<code>select *</code>表明了获取所有的列（而不是只有索引k1和k2的列），这时如果扫描整个索引，然后再查找那些不在索引列的列，这样干的代价可能相比全表扫然后对结果进行排序更大，因此，优化器在上述语中可能不太会使用索引k1、k2，而直接全表扫。另外，需要注意的是，对于二级索引叶子节点存放的是主键，所以某种意义上，所有的二级索引都是隐含了主键，所以若将上述SQL改为<code>select pk,k1,k2 from [table_name] order by k1,k2</code>，那和<code>select k1,k2 from [table_name] order by k1,k2</code>一样都会使用索引k1、k2来进行排序。</p><p>&emsp;在某些场景下，MySQL是无法使用索引进行<code>ORDER BY</code>操作，即使可以对<code>WHERE</code>中的条件可以命中索引且使用索引查找到记录。有下面几个场景：</p><ul><li>查询对不同的索引使用<code>ORDER BY</code>：如<code>select * from [table_name] Order by k1,k2</code>；</li><li>查询对一个索引的不连续部分使用<code>ORDER BY</code>：如<code>select * from [table_name] where k2=constant order by k1_part1,k1_part2</code>；</li><li>混合使用升序和降序：如<code>select * from [table_name] order by k1_part1 desc, k2_part2 asc</code>；</li><li>查询使用的索引和排序使用的索引不一样：如<code>select * from [table_name] where k2=constant order by k1</code>；</li><li>排序的字段非原始列名，可能做了一些运算：如<code>SELECT * FROM t1 ORDER BY ABS(key);</code>，再如<br><code>SELECT * FROM t1 ORDER BY -key;</code>；</li><li>查询中<code>join</code>多表，<code>order by</code>中的列并不是来自于首个获取数据的固定表（<code>explain</code>输出的第一个非<code>const</code>类型的表）；</li><li>查询中<code>order by</code>和<code>group by</code>的表达式不一样；</li><li><code>order by</code>中的列是建立了索引，但仅仅是前缀索引，这种前缀索引是不能完全用于解决排序问题的，因为前缀索引位置之后的内容是无法区分的，此时是需要进行文件排序的；</li><li>索引没有按序存储行记录，比如 memory 表中的 hash 索引；</li></ul><p>&emsp;索引是否可以用于排序可能会被列的别名所影响，若<code>t1.a</code>已经建立了索引，<code>select a from t1 order by a</code>在这个语句中，查询语句中的列名为<code>a</code>，它指向的是<code>t1.a</code>，在<code>order by</code>中也就指向了a，所以在排序过程中索引<code>t1.a</code>可以被使用；又如<code>select abs(a) as a from t1 order by a</code>，这里面<code>order by</code>后面的<code>a</code>是指的<code>abs(a)</code>，而非原始<code>a</code>列；再如<code>select abs(a) as b from t1 order by a</code>就可以使用<code>t1.a</code>的索引进行排序。</p><p>&emsp;默认情况下，若</p><h2 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h2><p>&emsp;通常一个查询的流程如下：客户端发送查询SQL到服务端 -&gt; 服务端查缓存，若命中，直接返回缓存结果 -&gt; 服务端进行SQL解析、预处理，由优化器生成对应的执行计划 -&gt; 根据执行计划，调用存储引擎的API执行查询 -&gt; 返回结果给客户端。对于一个MySQL连接（线程），某一时刻有且仅有一个状态，可以通过命令 <code>SHOW FULL PROCESSLIST</code> 指令来查看，对应接结果中的Command列，主要有下面几种状态：</p><ul><li><code>Sleep</code>：线程正在等待客户端发送新的请求；</li><li><code>Query</code>：线程正在执行查询或正在将查询的结果发送给客户端；</li><li><code>Locked</code>：MySQL服务层，表示线程正在等待锁，须是引擎级别的锁（如行锁），并不会体现线程状态中；</li><li><code>Analyzing and statistics</code>：线程正在收集存储引擎的统计信息，并生成查询的执行计划；</li><li><code>Copying to tmp table [on disk]</code>：线程正在查询，并将结果复制到一个临时表，这种状态要么再做 GROUP BY 操作，要么在作文件排序或者 UNION 操作，若后面还有 on disk 标记，则表示 MySQL 正在将一个内存临时表放在磁盘上；</li><li><code>Sorting result</code>：线程正在对结果集进行排序；</li><li><code>Sending data</code>：线程可能在多个状态之间传输数据、或者在征程结果集，或者向客户端返回数据；</li><li><code>Daemon</code>：守护线程，通常是事件调度器；</li></ul><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><p>&emsp;MySQL如果不能通过索引进行排序时，那它就需要自己排序，数据量小则在内存进行排序，数据量大就需要借助磁盘，这2种方式统称为文件排序(filesort)，核心在于<font color="red">不能使用索引进行排序</font>。若需要排序的数据量小于“排序缓冲区”，MySQL将会使用内存进行快排；若数据量大于内存，那它将会将数据分块，对每个块进行快排，然后将各个块的快排结果放置磁盘上，最后再合并结果集返回。</p><h2 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h2><p>&emsp;分区表，示例为：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> partiation_test ( create_time datetime <span class="keyword">DEFAULT</span> <span class="keyword">now</span>() ) <span class="keyword">ENGINE</span> = <span class="keyword">INNODB</span> <span class="keyword">PARTITION</span> <span class="keyword">BY</span> <span class="keyword">RANGE</span> (</span><br><span class="line"><span class="keyword">MINUTE</span> ( create_time )) (</span><br><span class="line"><span class="keyword">PARTITION</span> p_1</span><br><span class="line"><span class="keyword">VALUES</span></span><br><span class="line"><span class="keyword">LESS</span> <span class="keyword">THAN</span> ( <span class="number">10</span> ),</span><br><span class="line"><span class="keyword">PARTITION</span> p_2</span><br><span class="line"><span class="keyword">VALUES</span></span><br><span class="line"><span class="keyword">LESS</span> <span class="keyword">THAN</span> ( <span class="number">30</span> ),</span><br><span class="line"><span class="keyword">PARTITION</span> p_other</span><br><span class="line"><span class="keyword">VALUES</span></span><br><span class="line"><span class="keyword">LESS</span> <span class="keyword">THAN</span> ( <span class="number">60</span> ) </span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>最终在对应schema目录下会生成3个物理子表 p_1、p_2、p_3 ，分别对应的 .idb 物理文件的表空间文件为 partiation_test#p#p_1.ibd、partiation_test#p#p_2.ibd 以及 partiation_test#p#p_other.ibd，同时不再生成 partiation_test.ibd 主表空间文件，注：分区字段不能为<code>timestamp</code>类型，故上述将 create_time 定义为<code>datetime</code>类型。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-MySQL-逻辑架构&quot;&gt;&lt;a href=&quot;#1-MySQL-逻辑架构&quot; class=&quot;headerlink&quot; title=&quot;1. MySQL 逻辑架构&quot;&gt;&lt;/a&gt;1. MySQL 逻辑架构&lt;/h2&gt;&lt;p&gt;&amp;emsp;整体的逻辑架构分为 Server 层和引擎层两部分。其中 Server 层又细分为连接器和核心层，其中连接器那一层并非MySQL独有，大部分的C/S工具都有类似的架构（比如连接处理、授权认证、安全等），Server 层的第二层是 MySQL 独有的，MySQL 很多核心功能都在这一层，包括查询解析、分析、优化、缓存以及内置函数，还有所有跨存储引擎的功能也在这一层实现（包含存储过程、视图等等）。
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="MySQL" scheme="https://jacksonary.github.io/tags/MySQL/"/>
    
      <category term="InnoDB" scheme="https://jacksonary.github.io/tags/InnoDB/"/>
    
  </entry>
  
  <entry>
    <title>主备延迟监控</title>
    <link href="https://jacksonary.github.io/posts/52a5a976.html"/>
    <id>https://jacksonary.github.io/posts/52a5a976.html</id>
    <published>2021-04-21T02:51:00.000Z</published>
    <updated>2021-06-04T12:21:34.856Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-基本工具的安装"><a href="#1-基本工具的安装" class="headerlink" title="1. 基本工具的安装"></a>1. 基本工具的安装</h2><h3 id="1-1-安装-percona-toolkit"><a href="#1-1-安装-percona-toolkit" class="headerlink" title="1.1 安装 percona-toolkit"></a>1.1 安装 percona-toolkit</h3><p>&emsp;这玩意儿工具挺全，有时间可以深入了解一下，我的系统是 debian，流程如下：<br><a id="more"></a></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 下载软件包</span></span><br><span class="line">wget https://downloads.percona.com/downloads/percona-toolkit/3.3.0/binary/debian/buster/x86_64/percona-toolkit_3.3.0-1.buster_amd64.deb</span><br><span class="line"><span class="meta">#</span><span class="bash"> 安装，注意路径</span></span><br><span class="line">apt install ./percona-toolkit_3.3.0-1.buster_amd64.deb</span><br><span class="line"><span class="meta">#</span><span class="bash"> 校验，随便一个模块 --version，这里选用 pt-find 模块</span></span><br><span class="line">pt-find --version</span><br></pre></td></tr></table></figure><p>如果你的系统不是，到<a href="https://www.percona.com/downloads/percona-toolkit/LATEST/" rel="external nofollow noopener noreferrer" target="_blank">官网</a>获取对应的资源包再安装。</p><h3 id="1-2-安装-sysbench"><a href="#1-2-安装-sysbench" class="headerlink" title="1.2 安装 sysbench"></a>1.2 安装 sysbench</h3><p>&emsp;这个也很方便就可以安装：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 关联仓库</span></span><br><span class="line">curl -s https://packagecloud.io/install/repositories/akopytov/sysbench/script.deb.sh | bash</span><br><span class="line"><span class="meta">#</span><span class="bash"> 安装</span></span><br><span class="line">apt -y install sysbench</span><br></pre></td></tr></table></figure><p>一些基本参数（来源于网络）说明：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line">--threads=4500  表示发起4500个并发连接</span><br><span class="line">--oltp-read-only=off 表示不要进行只读测试，也就是会采用读写混合模式测试</span><br><span class="line">--report-interval=10 表示每10秒输出一次测试进度报告</span><br><span class="line">--rand-type=uniform 表示随机类型为固定模式，其他几个可选随机模式：uniform,gaussian,special,pareto</span><br><span class="line">--time=120 表示最大执行时长为 120 秒</span><br><span class="line">--max-requests=0 表示总请求数为 0，因为上面已经定义了总执行时长，所以总请求数可以设定为 0；也可以只设定总请求数，不设定最大执行时长</span><br><span class="line">--tables=10  表示10个表</span><br><span class="line">--table-size=100000  单表100000条记录</span><br><span class="line">--percentile=99 表示设定采样比例，默认是 95%，即丢弃1%的长请求，在剩余的99%里取最大值</span><br><span class="line"></span><br><span class="line">SQL statistics:</span><br><span class="line">    queries performed:</span><br><span class="line">        read:                            0         读操作</span><br><span class="line">        write:                           233015     --写总数</span><br><span class="line">        other:                           197026  --其他操作总数(SELECT、INSERT、UPDATE、DELETE之外的操作，例如COMMIT等)</span><br><span class="line">        total:                           430041  --全部总数</span><br><span class="line">    transactions:                        62617  (486.84 per sec.)  --总事务数(每秒事务数)</span><br><span class="line">    queries:                             430041 (3343.55 per sec.) --查询数(每秒查询数)</span><br><span class="line">    ignored errors:                      27087  (210.60 per sec.)  --忽略错误数</span><br><span class="line">    reconnects:                          0      (0.00 per sec.)  --重新连接次数</span><br><span class="line"></span><br><span class="line">General statistics:</span><br><span class="line">    total time:                          128.6147s  --运行总时间</span><br><span class="line">    total number of events:              62617  --事件总数</span><br><span class="line"></span><br><span class="line">Latency (ms):</span><br><span class="line">         min:                                    2.09</span><br><span class="line">         avg:                                 8957.64</span><br><span class="line">         max:                                99069.80</span><br><span class="line">         95th percentile:                    27846.48</span><br><span class="line">         sum:                            560900840.54</span><br><span class="line"></span><br><span class="line">Threads fairness:    #线程平均数</span><br><span class="line">    events (avg/stddev):           13.9149/3.92</span><br><span class="line">    execution time (avg/stddev):   124.6446/2.39</span><br></pre></td></tr></table></figure><p>详细说明看<code>sysbench --help</code>输出即可。</p><p><strong>【注】</strong>sysbench 自带一些测试的 lua 脚本，脚本所在目录 <code>/usr/share/sysbench</code>（后面会用到）。sysbench对数据库进行压力测试的过程分为3个阶段：prepare 阶段，做准备的、比较说建立好测试用的表、并向表中填充数据；run 阶段，去跑压力测试的SQL；cleanup 阶段，清除数据的，即drop掉prepare阶段初始化好的表，下面是一个完整阶段的示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 建表，初始化数据</span></span><br><span class="line">sysbench --mysql-host=localhost --mysql-port=3306 --mysql-user=root --mysql-password=root --mysql-db=ms_rep oltp_insert prepare</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 跑SQL，输出报告</span></span><br><span class="line">sysbench --mysql-host=localhost --mysql-port=3306 --mysql-user=root --mysql-password=root --mysql-db=ms_rep oltp_read_write run</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 清除测试数据</span></span><br><span class="line">sysbench --mysql-host=localhost --mysql-port=3306 --mysql-user=root --mysql-password=root --mysql-db=ms_rep oltp_read_write cleanup</span><br></pre></td></tr></table></figure><p>上述过程如果执行报错：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">sysbench 1.0.20 (using bundled LuaJIT 2.1.0-beta2)</span><br><span class="line"></span><br><span class="line">FATAL: unable to connect to MySQL server on host &apos;127.0.0.1&apos;, port 3306, aborting...</span><br><span class="line">FATAL: error 1045: Plugin caching_sha2_password could not be loaded: /usr/lib/x86_64-linux-gnu/mariadb19/plugin/caching_sha2_password.so: cannot open shared object file: No such file or directory</span><br><span class="line">FATAL: `sysbench.cmdline.call_command&apos; function failed: ./oltp_common.lua:83: connection creation failed</span><br></pre></td></tr></table></figure><p>解决方式，更改加密方式：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 更新用户的密码</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">USER</span> <span class="string">'root'</span>@<span class="string">'localhost'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">WITH</span> mysql_native_password <span class="keyword">BY</span> <span class="string">'root'</span>; </span><br><span class="line"><span class="comment"># 更新用户的密码</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">USER</span> <span class="string">'root'</span>@<span class="string">'%'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">WITH</span> mysql_native_password <span class="keyword">BY</span> <span class="string">'root'</span>; </span><br><span class="line"><span class="comment"># 刷新权限</span></span><br><span class="line"><span class="keyword">FLUSH</span> <span class="keyword">PRIVILEGES</span>;</span><br></pre></td></tr></table></figure><p>上述已经打包好了镜像，可以直接使用<code>docker pull registry.cn-shanghai.aliyuncs.com/hhu/mysql:wgl_8.0.24</code>。</p><h2 id="2-搭建延迟监控"><a href="#2-搭建延迟监控" class="headerlink" title="2. 搭建延迟监控"></a>2. 搭建延迟监控</h2><p>&emsp;先创建好一个测试的数据库 ms_rep，然后</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 主库：创建后台不断更改心跳表的线程</span></span><br><span class="line">pt-heartbeat --user=root --ask-pass --host=127.0.0.1 --create-table -D ms_rep --interval=1 --update --replace --daemonize</span><br><span class="line"><span class="meta">#</span><span class="bash"> 验证后台线程正常在跑，必须验证</span></span><br><span class="line">pgrep -fla pt-heartbeat</span><br><span class="line"><span class="meta">#</span><span class="bash"> 关闭后台线程（结束时执行）</span></span><br><span class="line">pt-heartbeat --stop</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 从库：监控延迟，-h 后指定的是从库的地址，建议在各个从库执行，这样观察更直观</span></span><br><span class="line">pt-heartbeat --user=root --ask-pass --host=127.0.0.1 --table=heartbeat --monitor -D ms_rep</span><br></pre></td></tr></table></figure><p>&emsp;通常刚建立的延迟监控输出都为<code>0.00s [  0.00s,  0.00s, 0.00s ]</code>，这时通过 sysbench 在 master 上生成不同的数据观察延迟即可。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 在 master 上生成数据以便观察 binlog 重放的延迟</span></span><br><span class="line">sysbench --mysql-host=localhost --mysql-port=3306 --mysql-user=root --mysql-password=root --mysql-db=ms_rep --table-size=1000000 oltp_insert prepare</span><br></pre></td></tr></table></figure><p>测试结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">插入的记录数     延迟时间</span><br><span class="line">   1000            0</span><br><span class="line">   2000            0</span><br><span class="line">   2W              0</span><br><span class="line">   3W              0</span><br><span class="line">   4W             0.99</span><br><span class="line">   5W             0.9,1.0</span><br><span class="line">   6W             1</span><br><span class="line">   7W             1, 0.99</span><br><span class="line">   8W             1,1</span><br><span class="line">   9W             0.99, 1, 1</span><br><span class="line">   10W            1,1</span><br><span class="line">   11W            1,1,1</span><br></pre></td></tr></table></figure><p><strong>【注】</strong>在执行完<code>pt-heartbeat --stop</code>后会提示创建文件<code>/tmp/pt-heartbeat-sentinel</code>，下次再建立监控时必须删除此文件，否则会跑一下就挂了。在从节点建立监控后会出现类似于 <code>5s [  0.25s,  0.05s,  0.02s ]</code> 标识延迟时间，默认是1m内、5m内、15m内的时间段，通过参数<code>--frames</code>指定。</p><p>心跳表无须手动创建，指定参数<code>--create-table</code>即可，心跳表的定义如下：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> heartbeat (</span><br><span class="line">  ts                    <span class="built_in">varchar</span>(<span class="number">26</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line">  server_id             <span class="built_in">int</span> <span class="keyword">unsigned</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> PRIMARY <span class="keyword">KEY</span>,</span><br><span class="line">  <span class="keyword">file</span>                  <span class="built_in">varchar</span>(<span class="number">255</span>) <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>,    <span class="comment">-- SHOW MASTER STATUS</span></span><br><span class="line">  <span class="keyword">position</span>              <span class="built_in">bigint</span> <span class="keyword">unsigned</span> <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>, <span class="comment">-- SHOW MASTER STATUS</span></span><br><span class="line">  relay_master_log_file <span class="built_in">varchar</span>(<span class="number">255</span>) <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>,    <span class="comment">-- SHOW SLAVE STATUS</span></span><br><span class="line">  exec_master_log_pos   <span class="built_in">bigint</span> <span class="keyword">unsigned</span> <span class="keyword">DEFAULT</span> <span class="literal">NULL</span>  <span class="comment">-- SHOW SLAVE STATUS</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-基本工具的安装&quot;&gt;&lt;a href=&quot;#1-基本工具的安装&quot; class=&quot;headerlink&quot; title=&quot;1. 基本工具的安装&quot;&gt;&lt;/a&gt;1. 基本工具的安装&lt;/h2&gt;&lt;h3 id=&quot;1-1-安装-percona-toolkit&quot;&gt;&lt;a href=&quot;#1-1-安装-percona-toolkit&quot; class=&quot;headerlink&quot; title=&quot;1.1 安装 percona-toolkit&quot;&gt;&lt;/a&gt;1.1 安装 percona-toolkit&lt;/h3&gt;&lt;p&gt;&amp;emsp;这玩意儿工具挺全，有时间可以深入了解一下，我的系统是 debian，流程如下：&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="docker" scheme="https://jacksonary.github.io/tags/docker/"/>
    
      <category term="mysql" scheme="https://jacksonary.github.io/tags/mysql/"/>
    
      <category term="主备" scheme="https://jacksonary.github.io/tags/%E4%B8%BB%E5%A4%87/"/>
    
  </entry>
  
  <entry>
    <title>docker 部署 MySQL 主备架构</title>
    <link href="https://jacksonary.github.io/posts/ffbed5ba.html"/>
    <id>https://jacksonary.github.io/posts/ffbed5ba.html</id>
    <published>2021-04-20T11:27:00.000Z</published>
    <updated>2021-06-04T12:33:49.136Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>docker 镜像为 <code>8.0.24</code>，MySQL 版本为 8.0.24，若无法拉取可以用我这个 <code>docker pull registry.cn-shanghai.aliyuncs.com/hhu/mysql:wgl_8.0.24</code></p></blockquote><p>&emsp;部署 master 和 slave，可以顺次扩展 slave，流程如下：<a id="more"></a></p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 部署 master</span></span><br><span class="line">docker run --name mysql-master --privileged=<span class="literal">true</span> -v D:\tmp\docker\mysql\master\data:/var/lib/mysql -v D:\tmp\docker\mysql\master\conf\my.cnf:/etc/mysql/my.cnf -v D:\tmp\docker\mysql\master\mysql-files:/var/lib/mysql-files -p 3307:3306 -e MYSQL_ROOT_PASSWORD=root -d mysql</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署 slave</span></span><br><span class="line">docker run --name mysql-slave1 --privileged=<span class="literal">true</span> -v D:\tmp\docker\mysql\slave1\data:/var/lib/mysql -v D:\tmp\docker\mysql\slave1\conf\my.cnf:/etc/mysql/my.cnf -v D:\tmp\docker\mysql\master\mysql-files:/var/lib/mysql-files -p 3308:3306 --link mysql-master:master -e MYSQL_ROOT_PASSWORD=root -d mysql</span><br></pre></td></tr></table></figure><p>上述参数做如下说明：</p><ul><li><code>--name</code>：指定运行之后的容器的名称为mysql-master；</li><li><code>--privileged</code>：指定了当前容器是否真正的具有root权限，所谓的root权限是指具有宿主机的root权限，而不仅仅只是在容器内部有root权限；</li><li><code>-v</code>：指定了容器中指定目录挂载到宿主机上的某个目录，这样做的目的在于防止容器中配置的数据丢失，因为docker容器在重启之后是不会保留前一次在其内部运行的相关数据的；<ul><li><code>-v D:\tmp\docker\mysql\master\data:/var/lib/mysql</code>：挂载mysql的data目录，作外部持久化；</li><li><code>-v D:\tmp\docker\mysql\master\conf\my.cnf:/etc/mysql/my.cnf</code>：挂载mysql的配置文件，当然如果是我们自己打的包就不用了，因为像这种配置文件完全调好后直接打包到镜像中；</li><li><code>-v D:\tmp\docker\mysql\master\mysql-files:/var/lib/mysql-files</code>：正常不需要挂载，在启动出现异常<code>Failed to access directory for --secure-file-priv. Please make sure that directory exists and is accessible by MySQL Server. Supplied value : /var/lib/mysql-files</code>，此时需要额外挂载<code>mysql-files</code>目录；</li></ul></li><li><code>-p</code>：表示宿主机上的某个端口映射到docker容器内的某个端口，这里也就是将宿主机的3307、3308端口映射到容器内部的3306端口；</li><li><code>-e</code>：表示指定当前容器运行的环境变量，该变量一般在容器内部程序的配置文件中使用，而在外部运行容器指定该参数。这里的MYSQL_ROOT_PASSWORD表示容器内部的MySQL的启动密码；</li><li><code>-d</code>：参数指定了当前容器是在后台运行；</li><li><code>--link</code>：用来链接2个容器，使得源容器（被链接的容器）和接收容器（主动去链接的容器）之间可以互相通信，并且接收容器可以获取源容器的一些数据，如源容器的环境变量；</li></ul><p>&emsp;正常启动后，进入 master 和 slave 各个容器，登录 MySQL，发现提示异常：<code>[Warning] World-writable config file &#39;/etc/mysql/my.cnf&#39; is ignored</code>，这是因为我们挂载的配置文件是全局可写的，MySQL Server 不认可，此时我们改一下挂载的配置文件读写权限即可，我这里是win10，直接右击文件<code>D:\tmp\docker\mysql\master\conf\my.cnf</code>修改 属性 -&gt; 属性 -&gt; 只读 即可，重启上述容器<code>docker restart mysql-master</code>、<code>docker restart mysql-slave</code>即可。</p><p>&emsp;配置主备同步：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 登录 mysql</span></span><br><span class="line">mysql -uroot -p</span><br><span class="line"><span class="comment"># 查询master的binlog日志信息</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">master</span> <span class="keyword">status</span>;</span><br><span class="line"><span class="comment"># 在各个slave上配置master的连接信息，master_file和master_Position就是上面插叙的结果</span></span><br><span class="line"><span class="keyword">change</span> <span class="keyword">master</span> <span class="keyword">to</span> master_host=<span class="string">'master'</span>, master_user=<span class="string">'root'</span>, master_password=<span class="string">'root'</span>, master_port=<span class="number">3306</span>, master_log_file=<span class="string">'[master_file]'</span>, master_log_pos=[master_Position], master_connect_retry=<span class="number">30</span>;</span><br><span class="line"><span class="comment"># 在各个备库上开启主备复制</span></span><br><span class="line"><span class="keyword">start</span> <span class="keyword">slave</span>;</span><br><span class="line"><span class="comment"># 查看开启状态，只要看到两个参数Slave_IO_Running和Slave_SQL_Running都为YES，则表示复制是正常进行的</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">slave</span> <span class="keyword">status</span>;</span><br></pre></td></tr></table></figure><p>若<code>start slave</code>执行失败出现<code>ERROR 1872 (HY000): Slave failed to initialize relay log info structure from the repository</code>，重置一下该slave即可<code>reset slave</code>，然后再执行<code>start slave</code>。</p><p><strong>附</strong>：</p><p>主备的配置文件如下（备库自己修改server-id）：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">#设置3306端口</span><br><span class="line">port = 3306</span><br><span class="line"># 设置mysql的安装目录</span><br><span class="line">#basedir=D:\Program Files\MySQL\mysql-8.0.20</span><br><span class="line"># 设置mysql数据库的数据的存放目录</span><br><span class="line">#datadir=D:\Program Files\MySQL\mysql-8.0.20\data</span><br><span class="line"># 允许最大连接数</span><br><span class="line">max_connections=200</span><br><span class="line"># 服务端使用的字符集默认为8比特编码的latin1字符集</span><br><span class="line">character-set-server=utf8</span><br><span class="line"># 创建新表时将使用的默认存储引擎</span><br><span class="line">default-storage-engine=INNODB</span><br><span class="line"># binlog配置</span><br><span class="line"># 开启binlog，指定basefileName</span><br><span class="line"># mysql会在baseName后面追加数值来生成新的binlog日志，在服务启动或重启、flush日志、当前日志文件达到max_binlog_size的值都会生成新的日志文件</span><br><span class="line">log-bin=mysql-binlog</span><br><span class="line"># 和log-bin类似，但可以指定路径，实测未生效</span><br><span class="line">#log_bin_basename=D:\Program Files\MySQL\mysql-8.0.20\data\logs\binlog\</span><br><span class="line"># 默认为 log-bin 后面追加 .index，可以不设</span><br><span class="line">log-bin-index=mysql-binlog.index</span><br><span class="line"># binlog模式设置为row</span><br><span class="line">binlog-format=ROW</span><br><span class="line"># 单个binlog文件的大小，单位字节，默认1073741824（即1G），但不是绝对的，binlog在记录事务时，是不会将一个事务进行分割的，总是以一个整体的形式来记录</span><br><span class="line">max_binlog_size=1073741824</span><br><span class="line"># 8.0以下版本必须在开启binlog时必设，8.0及以上默认为1，但不显式设置，虽可以启动但会出现提示信息</span><br><span class="line">server_id=1</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;docker 镜像为 &lt;code&gt;8.0.24&lt;/code&gt;，MySQL 版本为 8.0.24，若无法拉取可以用我这个 &lt;code&gt;docker pull registry.cn-shanghai.aliyuncs.com/hhu/mysql:wgl_8.0.24&lt;/code&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&amp;emsp;部署 master 和 slave，可以顺次扩展 slave，流程如下：
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="docker" scheme="https://jacksonary.github.io/tags/docker/"/>
    
      <category term="mysql" scheme="https://jacksonary.github.io/tags/mysql/"/>
    
      <category term="主备" scheme="https://jacksonary.github.io/tags/%E4%B8%BB%E5%A4%87/"/>
    
  </entry>
  
  <entry>
    <title>主备复制</title>
    <link href="https://jacksonary.github.io/posts/47049.html"/>
    <id>https://jacksonary.github.io/posts/47049.html</id>
    <published>2021-04-19T07:07:00.000Z</published>
    <updated>2021-06-04T12:33:35.398Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-主备复制的基础-二进制日志"><a href="#1-主备复制的基础-二进制日志" class="headerlink" title="1. 主备复制的基础-二进制日志"></a>1. 主备复制的基础-二进制日志</h2><p>&emsp;用于就描述 MySQL 事件，包含表创建、数据修改以及可能修改数据的语句（比如没有匹配到记录的<code>DELETE</code>操作），除非是使用 row-based 日志形式，二进制日志包含了每条语句更新数据时所花费的时间信息，在 mysql 中，二进制日志存在的主要目的有2个：<a id="more"></a></p><ol><li>主从同步。在master上产生的二进制日志副本，记录着数据的变化，这个二进制日志副本将被读取到各个 slave 上，slave 可以通过这个二进制日志副本来重放在 master 上操作，将自己的数据进行同样的修改；</li><li>数据恢复。某些数据恢复的场景需要使用二进制日志。在备份还原后，将重新执行备份后在二进制日志中记录的事件，这些事件会使数据库从备份时起是最新的。</li></ol><p>&emsp;二进制日志只用于修改行为，不会记录<code>SELECT</code>或<code>SHOW</code>语句。如果要记录所有语句（例如，标识问题查询），直接使用常规查询日志即可。开启二进制日志会使性能稍微变慢，但用较小的性能下降换取主备复制和数据还原的能力还是可以接受的。二进制日志的存在可以很好的弹性处理一些意外情况，它仅记录或读取完整的事件或事务，主要有3种模式。</p><h3 id="1-1-row-based-日志（基于行的日志）"><a href="#1-1-row-based-日志（基于行的日志）" class="headerlink" title="1.1 row-based 日志（基于行的日志）"></a>1.1 row-based 日志（基于行的日志）</h3><p>&emsp;row-based 日志是仅限于某个具体的数据库（物理层面），但对于初始化生成的 default 数据库没有此限制，若在启动时指定 <code>--binlog-do-db=sales</code>，那此时不论在sales库中的哪个表中进行写行为都将会记录到 binlog 中，即使在写行为中之前显式的去指定数据库名<code>use db_a</code>，但只要在写行为语句的表声明时加上了指定记录日志的db名即可<code>sales.table_a</code>。</p><p>&emsp;master 将一些改变各个表中的各行记录（具体到 row 级别）的事件记录到二进制日志种，可以通过 <code>binlog-format=ROW</code> 参数指定， row-based 日志是默认日志形式。当使用 ROW 格式的日志时，<code>binlog-row-event-max-size</code>系统参数（默认8192字节）将会生效（限制 row event 的最大值，如果某个事件不可分割，也有可能超出此限制，和 binlog 文件大小<code>max_binlog_size</code>的限制一样都是软限制），这个系统参数只在服务启动时设置生效，它表明的是 Row 将会以块的形式存储在二进制文件中，这个块的大小不会超过<code>binlog-row-event-max-size</code>参数，该参数必须是256的倍数。</p><p>【<strong>注</strong>】在使用 ROW 格式时，很多写行为的操作都会以 ROW 格式记录到二进制日志中，但有一些行为仍然会以 statement 的格式记录（比如所有的 <code>CREATE TABLE</code>、<code>ALTER TABLE</code>、<code>DROP TABLE</code>）。</p><h3 id="1-2-statement-based-日志（基于语句的日志）"><a href="#1-2-statement-based-日志（基于语句的日志）" class="headerlink" title="1.2 statement-based 日志（基于语句的日志）"></a>1.2 statement-based 日志（基于语句的日志）</h3><p>&emsp;mysql 最初的复制功能是基于 master 上面的 SQL 语句传播到 slave 上实现的，这种方式就是 statement-based log，可以通过 <code>binlog-format=STATEMENT</code> 参数指定。但使用 statement-based 格式日志复制非确定性语句可能存在问题，对于 statement-based 副本使用所给的 statement 是否安全时，mysql 通过是否可以保证可以使用基于语句的日志记录来复制该语句来决定，如果 mysql 不能保证，那就会提示不可靠的提示“statement may not be safe to log in statement format”。对于有临时表的场景，只有 statement 日志才会记录，row 和 mixed 并不会记录。</p><h3 id="1-3-mixed-日志"><a href="#1-3-mixed-日志" class="headerlink" title="1.3 mixed 日志"></a>1.3 mixed 日志</h3><p>&emsp;将 row-based 和 statement-based 2种形式的日志混合使用，可以通过 <code>binlog-format=MIXED</code> 参数指定。混合格式的日志默认使用 statement-based 日志，但在下面的特定场景下会自动切换到 row-based 形式日志（重要）：</p><ol><li>包含了<code>UUID()</code>、<code>FOUND_ROWS()</code>、<code>ROW_COUNT()</code>、<code>USER()</code>、 <code>CURRENT_USER()</code>、 <code>CURRENT_USER</code>、<code>LOAD FILE()</code>；</li><li>当表中包含了<code>AUTO_INCREMENT</code>列被更新，或者包含类似形式的比如触发器；</li><li>当涉及的表中有 mysql 的日志表时；</li><li>当语句中引用了系统参数（当然也有一些特殊的系统参数不会引起切换到 row）；</li></ol><p>日志记录格式还可以通过所使用的存储引擎来设置或限制，这有助于消除在源和副本之间使用不同存储引擎复制某些语句时的问题。</p><p><strong>注：</strong></p><p>&emsp;每个独立的客户端可以控制自己语句的 binlog 格式，可以运行时改变，通过设置 session 级别的参数，如：<code>SET SESSION binlog_format = &#39;STATEMENT&#39;</code>，前后对照一下<code>SHOW VARIABLES LIKE &#39;%binlog%&#39;</code>即可发现格式变了，原先在配置文件 my.ini 中关于 binlog 配置都被改变了。但不是每个 session 都推荐自己去更改 binlog 的格式，只是在某些特殊的场景下，客户端可能去做更改，比如：</p><ul><li>某个 session 中对数据库做了很多细微的写操作，那客户端可能想将日志格式改为 ROW 格式；</li><li>某个 session 中使用 <code>update ... where ...</code>对好多匹配命中的记录做了更改，那这时候可能他会将格式调整为 STATEMENT，因为这样比记录很多行的变化更高效；</li><li>有一些语句执行执行很耗时，但实际结果影响的行记录很少，那这时使用 ROW 格式会更高效；</li></ul><p>此外，有临时表存在时，不要随便更改日志的格式，而且对于有临时表的场景，只有 statement 日志才会记录，row 和 mixed 并不会记录。各种存储引擎对 ROW 和 STATEMENT 日志的支持情况，对于 INNODB 引擎，支持 ROW 格式日志，STATEMENT 格式仅事务隔离级别设置为可重复读或者序列化才支持，其他事务级别不支持 STATEMENT 格式。</p><p><strong>附</strong><br></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看 binlog 文件的内容，若不加 in 'xx' 指定 binlog 文件，默认查看第一个 binlog 文件</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">binlog</span> <span class="keyword">events</span> <span class="keyword">in</span> <span class="string">'mysql-binlog.0000xx'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看当前正在写入 binlog 文件</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">MASTER</span> <span class="keyword">STATUS</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看 binlog 列表</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">MASTER</span> <span class="keyword">LOGS</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 将 binlog 刷入磁盘重新生成一个 binlog 编号开始写入</span></span><br><span class="line"><span class="keyword">FLUSH</span> <span class="keyword">LOGS</span>;</span><br></pre></td></tr></table></figure><h2 id="2-主备同步"><a href="#2-主备同步" class="headerlink" title="2. 主备同步"></a>2. 主备同步</h2><h3 id="2-1-同步过程"><a href="#2-1-同步过程" class="headerlink" title="2.1 同步过程"></a>2.1 同步过程</h3><p>&emsp;MySQL的主从同步主要是通过将 binlog 在备库（从库）重放实现数据异步同步，一般是主库负责写，从库负责读（读写分离），主要架构和过程如下：</p><p><img src="https://img-blog.csdnimg.cn/20210420091614801.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步"></p><ol><li>主库将数据的更改写入 binlog 中：在每次提交事务完成数据更新前，二进制日志的记录行为会按事务的提交顺序而不是每条SQL语句的执行顺序，在记录日志后，主库会通知存储引擎进行事务的提交；</li><li>备库将将主库上的 binlog 复制到自己的中继日志（relay log）上：从库启动一个工作线程（I/O线程），这个线程和主库建立一个客户端连接，然后在主库上启动一个二进制转储线程（即binlog dump thread），这个转储线程会读取主库的 binlog 日志，注意这个线程不会轮询主库的 binlog，如果转储线程读取二进制日志中的行为追上了主库，那转储线程进入睡眠，直到主库通知它有新的事件行为发生才会重新唤醒转储线程，将日志转储（flush）到中继日志中；</li><li>备库读取中继日志中的事件进行重放：从库的SQL线程从中继日志中读取二进制日志在自己上执行。</li></ol><p>这个过程可能出现在某一时刻主从不一致的情况，尤其是一些大语句可能导致几秒甚至几分钟的延迟。从上面的流程不难看出主库的主要开销主要集中在binlog的写和传播，写binlog是写一份，但传播随着从库的增加而增加（即 binlog dump thread 会随着从库的增加而增加），从库的主要创建2个线程：读取主库 binlog 到中继日志的 I/O 线程，从中继日志读取行为事件执行SQL重放的 SQL 线程。在主从同步时，如果主、备的延迟很大，备库的I/O线程可能会写很多relay log，这个过程中如果延迟很高，I/O线程可能会把磁盘撑爆，所以尽量配置合适的relay_log_space_limit，若中继日志大小超过这个值，那I/O线程就会停止等待SQL线程执行完来释放空间。SQL线程在重放完中继日志中的事件后会尽快将其删除（relay_log_purge）。</p><p>&emsp;MySQL的主从同步主要是依托binlog，binlog的格式也会影响同步行为，主要分为基于语句和基于行的复制：</p><ul><li>statement-based：基于语句的复制（即逻辑复制）。主库会记录那些造成数据更新的语句，当备库读取这些事件之后就是简单的将这些SQL再执行一遍。这种方式相对白痴，理论上可以实现主从数据一致。但在某些场景下也会存在问题，比如当语句中存在诸如<code>CURRENT_USER()</code>、<code>NOW()</code>等函数时，另外基于语句的复制必须是串行，这个要求占用更多锁资源；</li><li>row-based：基于行的复制。这种复制方式最大的好处就是可以正确复制每一行数据，但在一些场景下又是非常低效的，比如将一些聚合数据插入另一张表中，通常这里面的聚合行为是很慢的，但产生的数据相对比较有限，这种情况使用基于行的复制方式开销就会很小，因为日志中描述的只是进行行数据的插入，而不会去重放SQL再做一下聚合操作。再如<code>update [table_name] set [column]=x</code>基于语句的复制代价就更小，因为这种SQL是几乎就是批量更改数据，若基于行复制的话，批量更新就会被分解为一个个零散的更新，一方面会使二进制日志变得巨大，也会给日志所在机器增加负载。</li></ul><h3 id="2-2-主备拓扑结构"><a href="#2-2-主备拓扑结构" class="headerlink" title="2.2 主备拓扑结构"></a>2.2 主备拓扑结构</h3><p>&emsp;主、备的拓扑结构（即主、备的组合方式）可能会很多，只要遵守下面的4个基本原则即可：</p><ul><li>主备的对应关系是1:N；</li><li>每个备库必须有一个唯一的服务器ID；</li><li>若开启<code>log_slave_update</code>相关，备库可以将主库的变化传播到其他备库上；</li></ul><p>&emsp;可拓展的拓扑结构有多种方式：</p><h4 id="2-2-1-一主多备"><a href="#2-2-1-一主多备" class="headerlink" title="2.2.1 一主多备"></a>2.2.1 一主多备</h4><p>&emsp;这是最简单的结构，应对写少读多的场景尤为管用。它可以将读分散到多个备库上，可以加到直至备库多到给主库造成压力。结构如下：<br><img src="https://img-blog.csdnimg.cn/20210420092215479.png#pic_center" alt="主从同步-一主多备"></p><p>&emsp;这种拓扑结构有一些常用的场景：</p><ul><li>为不同的角色使用不同的备库（如添加不同的索引和存储引擎）；</li><li>直接将备库作为待用主库，只做复制；</li><li>远程备库，容灾恢复；</li></ul><h4 id="2-2-2-主主复制（双主-双向复制）"><a href="#2-2-2-主主复制（双主-双向复制）" class="headerlink" title="2.2.2 主主复制（双主/双向复制）"></a>2.2.2 主主复制（双主/双向复制）</h4><h3 id="主动双主"><a href="#主动双主" class="headerlink" title="主动双主"></a>主动双主</h3><p>&emsp;2台服务互为对方的主库和备库，任何一方的改动都会复制到另一方中，拓扑结构主要如下：<br><img src="https://img-blog.csdnimg.cn/2021042009180082.png#pic_center" alt="主从同步-双主复制"></p><p>&emsp;双主复制通常用于一些特殊目的，如2个不同地理位置但都需要一份可写的数据拷贝。但这种拓扑结构存在一个很难解决的问题：当同时修改同一条数据时，或同时向表中插入一个包含<code>AUTO_INCREMENT</code>列的记录。在5.0可以设置系统变量<code>auto_increment_increment</code>和<code>auto_increment_offset</code>来改变自增行为中的步长和初始值，这种支持使得双主稍微变得安全一些（因为2个主库之间的冲突减少了），但允许同时对2个主库进行写行为还是很不安全，如 col = 1，SQL <code>update table set col=col+1</code> 和 <code>update table set col=col*2</code> 同时在2个主库上执行，那2台主库上可能出现一个为4、一个为3的情况，并不会出现复制错误，但此时主备数据已经不一致了。</p><h3 id="被动双主"><a href="#被动双主" class="headerlink" title="被动双主"></a>被动双主</h3><p>&emsp;这种拓扑结构主要和主动双主的区别是其中一台服务器只是被动的读取，这样的方式使得反复切换主动和被动服务器非常方便，因为服务器的配置是对称的。当我们执行 <code>ALTER TABLE</code> 时会锁表，阻塞表的读写。这种场景下可以切断主库中 binlog dump 线程，然后在从库中执行 <code>ALTER TABLE</code> 操作，执行完后交换角色，让执行完修改操作的只读主库变成主库，并开启 binlog dump 线程，最后将原先的主库将会读取现在主库的 binlog 执行修改操作，这样就可以在不影响服务的同时去作阻塞式操作。</p><h3 id="带备双主"><a href="#带备双主" class="headerlink" title="带备双主"></a>带备双主</h3><p>&emsp;即在双主的基础上给他们各自加上一个备库，形式如下：<br><img src="https://img-blog.csdnimg.cn/20210420091840496.png#pic_center" alt="主从同步-带备双主"></p><p>这种形式增加了冗余，对于不同地理位置的复制，可以消除单点失效的问题。双主可以一定程度解决单点故障，但也要考虑其复杂性。</p><h4 id="2-2-3-环形复制"><a href="#2-2-3-环形复制" class="headerlink" title="2.2.3 环形复制"></a>2.2.3 环形复制</h4><p>&emsp;环形复制的拓扑结构中，可以有3个及3个已上的主库，结构如下：<br><img src="https://img-blog.csdnimg.cn/20210420091913185.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步-环形复制"></p><p>环形复制没有双主的对称配置和故障转移的有点，它完全依赖于环形上没有可用节点，总的来说环形结构非常脆弱（尽量避免）。虽然在后续衍生出带备环形，可以一定程度上减少环形复制的风险，但仍不能解决在断电或者断网情况下对与各个主节点的强依赖。拓扑结构如下：<br><img src="https://img-blog.csdnimg.cn/20210420091947645.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步-带备环形"></p><h4 id="2-2-4-多主一备"><a href="#2-2-4-多主一备" class="headerlink" title="2.2.4 多主一备"></a>2.2.4 多主一备</h4><p>&emsp;多主复制在 MySQL 5.7 版本中开始支持多主复制，如下：<br><img src="https://img-blog.csdnimg.cn/20210420092017367.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步-多主一备"></p><p>多主一备主要是将用于将数据存储到另一台性能较高的服务器上。</p><h4 id="2-2-5-树形（金字塔型）"><a href="#2-2-5-树形（金字塔型）" class="headerlink" title="2.2.5 树形（金字塔型）"></a>2.2.5 树形（金字塔型）</h4><p>&emsp;这种拓扑结构可以大大减轻主库在传播 binlog 时的负载，但因为不是每个备库都从主库中拉取原始内容，所以一旦中间某些节点出现问题都会影响它的子孙节点，层次越多越容易出现问题：<br><img src="https://img-blog.csdnimg.cn/20210420092045202.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步-树形"></p><h3 id="2-3-主备复制方式"><a href="#2-3-主备复制方式" class="headerlink" title="2.3 主备复制方式"></a>2.3 主备复制方式</h3><p>&emsp;MySQL 传播 binlog 的方式分为异步复制（默认方式）、半同步复制和全同步复制三种方式。详细如下：</p><h4 id="2-3-1-异步复制"><a href="#2-3-1-异步复制" class="headerlink" title="2.3.1 异步复制"></a>2.3.1 异步复制</h4><p>&emsp;异步复制是 MySQL 默认的复制方式，master 自己执行完对应操作并记录 binlog，这个操作就成功了，它不关心 slave 的复制结果（是否转存 relay log 成功，有几个 slave 成功等）；</p><h4 id="2-3-2-半同步复制"><a href="#2-3-2-半同步复制" class="headerlink" title="2.3.2 半同步复制"></a>2.3.2 半同步复制</h4><p>&emsp;异步复制中由于 master 并不关心备库读取 binlog 或重放的情况，所以存在主库执行成功但备库执行失败的情况（如 master 执行成功后，在 slave 读取 binlog 到 relay log 完成之前 master 挂了）。为了解决这个问题，半同步复制可以保证 master 自己执行成功，且至少1个 slave 已经读取 binlog 并 flush 到 relay log 文件中（无需等 SQL 线程重放），这样才算成功。半同步复制需要开启 <code>semisync</code>；</p><h4 id="2-3-3-全同步复制"><a href="#2-3-3-全同步复制" class="headerlink" title="2.3.3 全同步复制"></a>2.3.3 全同步复制</h4><p>&emsp;全同步复制是效率最低的复制方式，因为 master 正常执行成功后，还需要等待所有备库执行成功后才算成功，这么干会严重拖慢并发写。</p><h3 id="2-4-主备架构中存在的问题"><a href="#2-4-主备架构中存在的问题" class="headerlink" title="2.4 主备架构中存在的问题"></a>2.4 主备架构中存在的问题</h3><p>&emsp;主备的场景有很多，诸如容灾备份、读写分离，当然也会存在如复制延迟、过多 slave 在master创建的 binlog 读取线程拖垮 master 等问题。</p><p>若master写并发较高，此时 slave 的数据可能存在几毫秒到几秒的延迟（并发很小，几百并发写，延迟可能就几毫秒；中等并发写，1k~2K可能会出现几十毫秒的延迟；&gt;2K并发可能会出现几秒甚至几十秒的延迟）。</p><h4 id="2-4-1-不能确保-slave-成功复制"><a href="#2-4-1-不能确保-slave-成功复制" class="headerlink" title="2.4.1 不能确保 slave 成功复制"></a>2.4.1 不能确保 slave 成功复制</h4><p>&emsp;开启半同步复制（即 smisync），解决 I/O 线程还未读取 master 到 relay log ，master 就挂数据丢失的问题，半同步机制可以作如下保证：master 操作成功并且至少1个 slave 执行半成功，这里“半成功”的概念是至少1个 slave 的 I/O 线程读取 master binlog 到 relay log 中给 master 反馈，master 才会认为当前操作成功；</p><h4 id="2-4-2-主备延迟"><a href="#2-4-2-主备延迟" class="headerlink" title="2.4.2 主备延迟"></a>2.4.2 主备延迟</h4><p>&emsp;这个问题会在并发写时遇见，主要有下面2种方式来缓解：</p><ol><li>开启并行复制，让 slave 中读取 relay log 执行 SQL 的线程可以并行执行不同库的 SQL，但这种方式解决问题很有限，因为大部分延迟是聚集在某个或某几个热点表，并行复制解决的是异库并行重放 binlog 缓慢的问题；</li><li>主库拆分，对于并发写导致的延迟可以拆分主库的方式来分摊负载，将原来对一个库的并发写分摊为对多个库的并发写，然后再挂几个 slave，可以大大提高并发能力；<br><img src="https://img-blog.csdnimg.cn/20210420092108931.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70#pic_center" alt="主从同步-主备延迟_拆库"></li></ol><h4 id="2-4-3-主备延迟导致更细数据失败"><a href="#2-4-3-主备延迟导致更细数据失败" class="headerlink" title="2.4.3 主备延迟导致更细数据失败"></a>2.4.3 主备延迟导致更细数据失败</h4><p>&emsp;读写分离的场景下，延迟可能导致服务异步逻辑异常，大致的场景如下：服务对主库 insert，然后在备库进行 select，若巨记录存在则进行 update，但因为主备延迟，第二步的 select 没有查到数据，导致流程中断。这个问题知道读写分离架构的前提下，可以在代码处理的逻辑层面来规避问题，即略过对备库进行 select 操作，直接进行 update 操作，此时操作的便是主库，不会出现记录不存在的情况。</p><h2 id="附：主备架构中一些概念"><a href="#附：主备架构中一些概念" class="headerlink" title="附：主备架构中一些概念"></a>附：主备架构中一些概念</h2><p>&emsp;主备架构在进行binlog传播时，是由各个备库在主库上创建一个线程执行 binlog dump 指令读取主库的 binlog 发送到备库的 relay log，各个备库在主库进行这个工作时不会共享 binlog dump 资源，随着备库的增加主库的负载也会不断增大，如果数据不在内存还会导致大量的磁盘检索，这种行为会影响主库的性能。在多备场景下主库有明显负载压力时，就需要使用分发主库了，分发主库实际是一个备库，但分发主库唯一的目的为了提取主库的binlog并对其他备库提供，这样就让主库摆脱了负载，分发主库几乎都是使用 blackhole 引擎（黑洞引擎，用于验证 dump file 语法的正确性、检测 binlog 功能所需的负载，经常用作日志服务器、dummy master、增量备份服务器，没有高级的功能：no transaction,no XA,no save point），使用blackhole引擎可以避免在分发主库上的查询行为（应为这回使得它对binlog的分发性能有所损耗），对于blackhole的使用场景网上有段较为详细的说明：</p><ol><li>充当dummy master。当master后面挂载过多的slave，利用 blackhole 来充当一个 “dummy master” 来减轻master的负载（延迟是需要考虑的）；这里需要提一下的是: 对于 master来说“dummy master” 还是一个slave的角色， 由于binlog 设计中是不记录 <code>engine=xxxx</code> 关键字的，采用默认的存储引擎（除非在 <code>create</code> 语句中 显式声明 <code>engine=innodb</code>）,所以对于dummy master 来说 只要设置 default_storage_engine=blackhole 就能实现主从 使用不一致的存储引擎；</li><li>充当日志服务器。对于blackhole的slave启用 <code>log_slave_updates</code> 这个参数，通过解析相关语句来做审计或者观测服务器负载的情况；需要考虑的问题，如果binlog format 为 row 模式，解析 binlog 查看SQL语句将是一个小问题，<code>mysqlbinlog -vv</code>这个不单纯显示的SQL，还有部分“前后镜像”；</li><li>充当增量备份服务器。免去拷贝binlog的问题，如果机器数据量多，也需要多个 blackhole 的slave实例，这是一个成本问题；其次是 master  binlog，pos 位置和 blackhole 重新产生的pos 是不一致的，在恢复数据的时候需要手动来恢复，不知道有没有工具之类的来做对比；如果 MySQL 版本达到 5.6+ 那启用 GTID 这个选项可以很容易的实现 增量的恢复；</li></ol><p>最终的拓扑结构应该如下：<br><img src="https://img-blog.csdnimg.cn/2021042009213319.png#pic_center" alt="主从同步-分发主库"></p><p>&emsp;主备架构上，若主库接近满负载，根据经验来说，那它的备库应该在10个以内，当然如果引入了分发主库可以适当增加备库来提高读操作的并发性能。</p><p><strong>注</strong>：</p><p>&emsp;master千万不要有blackhole的存储引擎来复制到其他slave，会造成的影响如下：</p><ol><li>如果binlog format 格式为 row或者 mixed 模式，那 delete 和update 是不会记录在binlog里面，并产生warning；</li><li>对于具有<code>auto_incrment</code>属性的column，由于blackhole 是 no op模式，没有任何记录，所以每次插入的记录，主键ID列都是相同的，造成复制失败；</li><li>对于 trigger 由于 for each row关键字，trigger不会被执行，因为没有一条row；</li></ol><p>又翻了下官网，解释到位：blackhole 引擎和名字一样，如同黑洞，吸收数据，但输出的结果集为空。可以如下测试：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 确保支持 blackhole 引擎</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">ENGINES</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建 blackhole 引擎的测试表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> blackhole_test (</span><br><span class="line">    <span class="string">`id`</span> <span class="built_in">INT</span> ( <span class="number">11</span> ) <span class="keyword">NOT</span> <span class="literal">NULL</span> auto_increment <span class="keyword">COMMENT</span> <span class="string">'primary key'</span>,</span><br><span class="line">    <span class="string">`prop`</span> <span class="built_in">VARCHAR</span> ( <span class="number">32</span> ) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="string">'default prop'</span> <span class="keyword">COMMENT</span> <span class="string">'blackholes pop'</span>,</span><br><span class="line">    PRIMARY <span class="keyword">KEY</span> ( <span class="string">`id`</span> ) </span><br><span class="line">) <span class="keyword">ENGINE</span> = blackhole <span class="keyword">charset</span> = utf8 <span class="keyword">COMMENT</span> <span class="string">'黑洞引擎测试'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 插入测试数据</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> blackhole_test <span class="keyword">VALUES</span>(<span class="number">1</span>, <span class="string">'prop'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询数据为空</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> blackhole_test;</span><br></pre></td></tr></table></figure><p>&emsp;BlackHole引擎的表空间是定义在全局数据字典中的，没有其他文件与之关联。blackhole 引擎是支持所有类型的索引，但不支持分区和自增属性（<code>auto_increment</code>）。最大的特点就是它不记录数据但会记录binlog，这个特性天生就是专门为缓解master中的读取binlog线程的压力。当然还有其他的一些场景：</p><ol><li>验证dump文件的语法；</li><li>衡量 binlog 的负载，通过在使用 blackhole 引擎的情况下，开启和禁用 binlog 功能比较两者性能差异即可得出各个slave对master读取 binlog 的造成的负载情况；</li><li>blackhole 引擎本质上是一种无操作（“no-op”）引擎，所以它可以用来排查和存储引擎无关的瓶颈因素，同时“no-op”的原因，blackhole引擎不会保存自增键的状态，所以自增属性<code>auto_increment</code>在blackhole中不生效；</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-主备复制的基础-二进制日志&quot;&gt;&lt;a href=&quot;#1-主备复制的基础-二进制日志&quot; class=&quot;headerlink&quot; title=&quot;1. 主备复制的基础-二进制日志&quot;&gt;&lt;/a&gt;1. 主备复制的基础-二进制日志&lt;/h2&gt;&lt;p&gt;&amp;emsp;用于就描述 MySQL 事件，包含表创建、数据修改以及可能修改数据的语句（比如没有匹配到记录的&lt;code&gt;DELETE&lt;/code&gt;操作），除非是使用 row-based 日志形式，二进制日志包含了每条语句更新数据时所花费的时间信息，在 mysql 中，二进制日志存在的主要目的有2个：
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="MySQL" scheme="https://jacksonary.github.io/tags/MySQL/"/>
    
      <category term="主备" scheme="https://jacksonary.github.io/tags/%E4%B8%BB%E5%A4%87/"/>
    
  </entry>
  
  <entry>
    <title>Spring 中的模块装配</title>
    <link href="https://jacksonary.github.io/posts/42478593.html"/>
    <id>https://jacksonary.github.io/posts/42478593.html</id>
    <published>2021-02-26T00:36:00.000Z</published>
    <updated>2021-06-04T12:41:15.187Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;Spring 中一直有类似于 <code>@EnbaleXxx</code> 这样注解一键开启 xxx 功能的支持，甚至连配置都不需要就可以使用，对于接入方来说实在是太爽了。最近在写内部中间件就接触到模块装配的知识点，因为对写过中间件的同学都明白，<a id="more"></a>中间件有2点很重要：1.对于业务领域的高度抽象；2. 让接入方爽！重点来了，怎么让对方爽？说白一点，你把业务方的公用的、但又不属于中间件抽象出来的领域行为通过SDK封装好，让业务方几乎不用配置啥就可以用你的功能，这样他就爽了。下面就展示一下一般的流程：</p><h3 id="1-自定义装配模块的注解-EnableXxx"><a href="#1-自定义装配模块的注解-EnableXxx" class="headerlink" title="1. 自定义装配模块的注解 @EnableXxx"></a>1. 自定义装配模块的注解 @EnableXxx</h3><p>&emsp;这个自定义注解很简单，唯一要注意的它比一般的注解多一个<code>@Import({ExcelExportSelector.class})</code>，这个很关键，后面会有用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> weiguo.liu</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/11/17</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> excel导出支持的开启配置注解，开启后直接使用 &#123;<span class="doctag">@link</span> ExcelOptTemplate&#125; 提供的方法即可更为简单的操作 推荐使用</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Import</span>(&#123;ExcelExportSelector.class&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> EnableExcelExportSupport &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 导出的场景类型，全局，可多个</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    ExcelExportTaskBizType[] types();</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">`</span><br></pre></td></tr></table></figure><h3 id="2-指明需要装配哪些配置"><a href="#2-指明需要装配哪些配置" class="headerlink" title="2. 指明需要装配哪些配置"></a>2. 指明需要装配哪些配置</h3><p>&emsp;<code>XxxSelector</code>中说明需要加载哪些配置，这个类必须实现 <code>ImportSelector</code> 接口，在 <code>selectImports</code> 方法中指明所需要加载配置的全类名，返回对象是一个全类名数组。</p><p>下面就是加载2个配置：<code>NewExcelExportDubboConfig</code>和<code>ExcelExportTemplateConfig</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> weiguo.liu</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/11/17</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 这里建议直接实现 DeferredImportSelector 接口，他会在所有的 <span class="doctag">@Bean</span> 生效后再执行</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ExcelExportSelector</span> <span class="keyword">implements</span> <span class="title">DeferredImportSelector</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String[] selectImports(AnnotationMetadata importingClassMetadata) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> String[] &#123;NewExcelExportDubboConfig.class.getName(), ExcelExportTemplateConfig.class.getName()&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-提供2中需要导入的配置类"><a href="#3-提供2中需要导入的配置类" class="headerlink" title="3. 提供2中需要导入的配置类"></a>3. 提供2中需要导入的配置类</h3><p>&emsp;<code>NewExcelExportDubboConfig</code>主要是用来注册一个dubboService：newExcelExportService，这样有个好处，接入方不需要在<code>application-consumer.xml</code>去配置消费者就可以使用我们rpc，非常方便。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> weiguo.liu</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/11/17</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> template使用的基础dubbo配置</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NewExcelExportDubboConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;dubbo.reference.version&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String dubboVersion;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> ApplicationConfig applicationConfig;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RegistryConfig registry;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"newExcelExportService"</span>)</span><br><span class="line">    <span class="meta">@ConditionalOnBean</span>(value = &#123;ApplicationConfig.class, RegistryConfig.class&#125;)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> NewExcelExportService <span class="title">getNewExcelExportService</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ReferenceConfig&lt;NewExcelExportService&gt; reference = <span class="keyword">new</span> ReferenceConfig&lt;&gt;();</span><br><span class="line">        reference.setApplication(applicationConfig);</span><br><span class="line">        <span class="comment">// 多个注册中心可以用setRegistries()</span></span><br><span class="line">        reference.setRegistry(registry);</span><br><span class="line">        reference.setInterface(NewExcelExportService.class);</span><br><span class="line">        reference.setVersion(dubboVersion);</span><br><span class="line">        reference.setCheck(<span class="keyword">false</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> reference.get();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再看一下<code>ExcelExportTemplateConfig</code>，这个配置类中主要是注册<code>excelExportTemplate</code> Bean 对象，这个对象中主要是封装了多个像<code>newExcelExportService</code>之类的dubboService，让一些复杂、组合的操作变成一个简单的方式来让接入放调用，所以在注入的时候创建是用的<code>new ExcelExportTemplate(newExcelExportService)</code>。甚至在SDK中我都帮客户端都写好了MQ的监听行为<code>excelExportConsumer</code>，接入想不爽都不行。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> weiguo.liu</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2021/2/23</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ExcelExportTemplateConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> NewExcelExportService newExcelExportService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * excel操作模板</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"excelExportTemplate"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ExcelExportTemplate <span class="title">excelOptTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ExcelExportTemplate(newExcelExportService);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"excelExportConsumer"</span>)</span><br><span class="line">    <span class="meta">@ConditionalOnBean</span>(value = &#123;ExcelExportTemplate.class, ExcelExportHandler.class&#125;)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ExcelExportConsumer <span class="title">excelExportConsumer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ExcelExportConsumer();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>稍微瞄一下MQ的监听类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> weiguo.liu</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/11/24</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> excel业务校验的基础消息消费类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ExcelExportConsumer</span> <span class="keyword">extends</span> <span class="title">BaseConsumer</span> <span class="keyword">implements</span> <span class="title">ApplicationContextAware</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Logger LOGGER = LoggerFactory.getLogger(ExcelExportConsumer.class);</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 业务方需要监听的mq</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> TraceTopic EXCEL_EXPORT_TOPIC = TraceTopic.EXCEL_EXPORT_TASK_CREATE;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ApplicationContext springApplicationContext;</span><br><span class="line">    <span class="keyword">private</span> Map&lt;Integer, ExcelExportHandler&gt; bizTypeContainer = <span class="keyword">new</span> HashMap&lt;&gt;(<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> ExcelExportTemplate excelExportTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;public.rocketmq.nameserver.name&#125;"</span>)</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setNameServer</span><span class="params">(String nameServer)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.nameServer = nameServer;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 特殊场景需要，重新定义</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;rocketmq.consumer.group&#125;"</span>)</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setGroup</span><span class="params">(String group)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.group = group;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostConstruct</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initMethod</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.init();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (CollectionUtils.isEmpty(bizTypeContainer)) &#123;</span><br><span class="line">            fillBizTypeContainer();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PreDestroy</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroyMethod</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.destroy();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getTopic</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> EXCEL_EXPORT_TOPIC.getTopic();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getTags</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> EXCEL_EXPORT_TOPIC.getTags();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">doConsumeMessage</span><span class="params">(String msgId, <span class="keyword">int</span> reconsumeTimes, Serializable message)</span> </span>&#123;</span><br><span class="line">        LOGGER.info(<span class="string">"ExcelExportConsumer receive topic:&#123;&#125; tag:&#123;&#125; message:&#123;&#125;"</span>, EXCEL_EXPORT_TOPIC.getTopic(),</span><br><span class="line">            EXCEL_EXPORT_TOPIC.getTags(), JSON.toJSONString(message));</span><br><span class="line">        <span class="keyword">if</span> (!(message <span class="keyword">instanceof</span> ExcelExportAddNotifyDTO)) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; useless message,msgId=&#123;&#125;, message=&#123;&#125;"</span>, msgId, JSON.toJSONString(message));</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ExcelExportAddNotifyDTO excelTaskNotifyDTO = (ExcelExportAddNotifyDTO)message;</span><br><span class="line">        <span class="keyword">if</span> (!acceptable(msgId, excelTaskNotifyDTO)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果需要自定义检查消息体内容，需要覆盖此方法</span></span><br><span class="line">        <span class="comment">// true 数据合法，业务后端会接着处理业务 | false 数据非法，请求终止，业务方逻辑不会继续处理，直接失败，任务超时</span></span><br><span class="line">        <span class="keyword">if</span> (!customCheck()) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; customCheck failed."</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ExcelExportHandler processHandler = bizTypeContainer.get(excelTaskNotifyDTO.getBizType());</span><br><span class="line">        <span class="keyword">if</span> (processHandler == <span class="keyword">null</span>) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; found no handler -&gt; can not found excel export handler, type:&#123;&#125;"</span>,</span><br><span class="line">                excelTaskNotifyDTO.getBizType());</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> processHandler.process(excelTaskNotifyDTO);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 如果需要自定义检查消息体内容，需要覆盖此方法</span></span><br><span class="line"><span class="comment">     * </span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> true 数据合法，业务后端会接着处理业务 | false 数据非法，请求终止，业务方逻辑不会继续处理，直接失败</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">customCheck</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 简单的数据预处理</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> excelTaskNotifyDTO</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">acceptable</span><span class="params">(String msgId, ExcelExportAddNotifyDTO excelTaskNotifyDTO)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (excelTaskNotifyDTO == <span class="keyword">null</span>) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; excelTaskNotifyDTO is null, msgId:&#123;&#125;"</span>, msgId);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> bizType = excelTaskNotifyDTO.getBizType();</span><br><span class="line">        <span class="keyword">if</span> (!CollectionUtils.isEmpty(bizTypeContainer) &amp;&amp; !bizTypeContainer.containsKey(bizType)) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; not your msg -&gt; your type is &#123;&#125;, msgId:&#123;&#125;, msg's bizType is &#123;&#125;."</span>,</span><br><span class="line">                JSON.toJSONString(bizTypeContainer), msgId, excelTaskNotifyDTO.getBizCondition());</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ExcelExportTaskDO task = excelExportTemplate.getTask(excelTaskNotifyDTO.getId());</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (ExcelExportTaskStatus.isTerminalStatus(task.getStatus())) &#123;</span><br><span class="line">            LOGGER.warn(<span class="string">"&gt;&gt; unexpect result -&gt; task's status arrive terminal status, task:&#123;&#125;"</span>, JSON.toJSONString(task));</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setApplicationContext</span><span class="params">(ApplicationContext applicationContext)</span> <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">        springApplicationContext = applicationContext;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">fillBizTypeContainer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Map&lt;String, Object&gt; excelExportSupportAnnotation =</span><br><span class="line">            springApplicationContext.getBeansWithAnnotation(EnableExcelExportSupport.class);</span><br><span class="line">        <span class="keyword">if</span> (CollectionUtils.isEmpty(excelExportSupportAnnotation)) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Map&lt;String, Object&gt; excelExportHandlerAnnotation =</span><br><span class="line">            springApplicationContext.getBeansWithAnnotation(CustomExcelExportHandler.class);</span><br><span class="line">        Map&lt;Integer, ExcelExportHandler&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;(<span class="number">8</span>);</span><br><span class="line">        excelExportHandlerAnnotation.forEach((k, v) -&gt; &#123;</span><br><span class="line">            Class&lt;?&gt; clazz = v.getClass();</span><br><span class="line">            CustomExcelExportHandler annotation =</span><br><span class="line">                AnnotationUtils.findAnnotation(clazz, CustomExcelExportHandler.class);</span><br><span class="line">            <span class="keyword">if</span> (annotation != <span class="keyword">null</span>) &#123;</span><br><span class="line">                ExcelExportTaskBizType type = annotation.type();</span><br><span class="line">                map.put(type.getCode(), (ExcelExportHandler)v);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        excelExportSupportAnnotation.forEach((key, value) -&gt; &#123;</span><br><span class="line">            Class&lt;?&gt; aClass = value.getClass();</span><br><span class="line">            EnableExcelExportSupport annotation =</span><br><span class="line">                AnnotationUtils.findAnnotation(aClass, EnableExcelExportSupport.class);</span><br><span class="line">            <span class="keyword">if</span> (annotation != <span class="keyword">null</span> &amp;&amp; annotation.types().length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                Arrays.asList(annotation.types()).forEach(o -&gt; bizTypeContainer.put(o.getCode(), map.get(o.getCode())));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的操作完成后，一旦业务方在启动类上添加了注解 <code>@EnableExcelExportSupport</code> 注解后，业务方就会自动注入我们中间件的 dubboSrvice <code>newExcelExportService</code>、封装好的dubboService的操作模板<code>excelExportTemplate</code>以及MQ的消费者。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;Spring 中一直有类似于 &lt;code&gt;@EnbaleXxx&lt;/code&gt; 这样注解一键开启 xxx 功能的支持，甚至连配置都不需要就可以使用，对于接入方来说实在是太爽了。最近在写内部中间件就接触到模块装配的知识点，因为对写过中间件的同学都明白，
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Spring" scheme="https://jacksonary.github.io/tags/Spring/"/>
    
      <category term="模块装配" scheme="https://jacksonary.github.io/tags/%E6%A8%A1%E5%9D%97%E8%A3%85%E9%85%8D/"/>
    
  </entry>
  
  <entry>
    <title>RESTful架构</title>
    <link href="https://jacksonary.github.io/posts/2654e413.html"/>
    <id>https://jacksonary.github.io/posts/2654e413.html</id>
    <published>2020-10-04T01:38:21.000Z</published>
    <updated>2021-06-04T12:41:09.021Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;REST(Representational State Transfer)中文含义是表述性状态转移，它指的是一组架构约束条件和原则(它是一种设计风格而不是标准)，如果一个架构符合REST的约束条件和原则，我们就称它为RESTful架构。理论上REST架构风格并不是绑定在HTTP上，只不过目前HTTP是唯一与REST相关的实例，但不要形成固定思维。目前在三种主流的Web服务实现方案中，因为REST模式的Web服务与复杂的SOAP和XML-RPC对比来讲明显的更加简洁，越来越多的web服务开始采用REST风格设计和实现。REST的设计标准有下面的一些：<a id="more"></a></p><ul><li>网络上的所有事物都被抽象为资源（resource），在REST的世界中，资源即状态，而互联网就是一个巨大的状态机，每个网页是其一个状态；URI是状态的表述；REST风格的应用则是从一个状态迁移到下一个状态的状态转移过程。早期互联网只有静态页面的时候，通过超链接在静态网页间浏览跳转的page-&gt;link-&gt;page-&gt;link…模式就是一种典型的状态转移过程。也就是说早期的互联网就是天然的REST；</li><li>每个资源对应一个唯一的资源标识符（resource identifier），系统中的每一个对象或是资源都可以通过一个唯一的 URI 来进行寻址，URI 的结构应该简单、可预测且易于理解，比如定义目录结构式的URI；</li><li>通过通用的连接器接口（generic connector interface）对资源进行操作，建立创建、检索、更新和删除（CRUD：Create, Retrieve, Update and Delete）操作与HTTP方法之间的一对一映射，主要如下：<ul><li>若要在服务器上创建资源，应该使用POST方法；</li><li>若要检索某个资源，应该使用GET方法；</li><li>若要更新或者添加资源，应该使用PUT方法；</li><li>若要删除某个资源，应该使用DELETE方法。</li></ul></li><li>对资源的各种操作不会改变资源标识符；</li><li>所有的操作都是无状态的（stateless），对服务器端的请求应该是无状态的，完整、独立的请求不要求服务器在处理请求时检索任何类型的应用程序上下文或状态，无状态约束使服务器的变化对客户端是不可见的，因为在两次连续的请求中，客户端并不依赖于同一台服务器。一个客户端从某台服务器上收到一份包含链接的文档，当它要做一些处理时，这台服务器宕掉了，可能是硬盘坏掉而被拿去修理，可能是软件需要升级重启——如果这个客户端访问了从这台服务器接收的链接，它不会察觉到后台的服务器已经改变了。通过超链接实现有状态交互，即请求消息是自包含的（每次交互都包含完整的信息），有多种技术实现了不同请求间状态信息的传输，例如 URI 重新，cookies 和隐藏表单字段等，状态可以嵌入到应答消息里，这样一来状态在接下来的交互中仍然有效。REST 风格应用可以实现交互，但它却天然地具有服务器无状态的特征。在状态迁移的过程中，服务器不需要记录任何 Session，所有的状态都通过 URI 的形式记录在了客户端。更准确地说，这里的无状态服务器，是指服务器不保存会话状态(Session)；而资源本身则是天然的状态，通常是需要被保存的；这里所指无状态服务器均指无会话状态服务器。</li></ul><p>【总结】</p><p>什么是RESTful架构—</p><p>　　（1）每一个URI代表一种资源；</p><p>　　（2）客户端和服务器之间，传递这种资源的某种表现层；</p><p>　　（3）客户端通过四个HTTP动词，对服务器端资源进行操作，实现”表现层状态转化”。</p><h2 id="1-基础"><a href="#1-基础" class="headerlink" title="1. 基础"></a>1. 基础</h2><ul><li>API与用户的通信协议总是用<code>https</code>协议；</li><li>API尽量部署到专用域名下，如<code>https://api.example.com</code>；</li><li>将API的版本号放入URL中，如<code>https://api.example.com/v1/</code>，此外，还可以将其放在HTTP头信息中，但不如放入URL方便和直观；</li><li>路径又称”终点”（endpoint），表示API的具体网址，在RESTful架构中，每个网址代表一种资源（resource），所以<font color="red">网址中不能有动词(具体动词表现在像<code>get</code>、<code>post</code>这些方法上)</font>，只能有名词，而且所用的名词往往与数据库的表格名对应。一般来说，数据库中的表都是同种记录的”集合”（collection），所以API中的名词也应该使用复数，如一个提供动物园信息的API可以像下面的这样：<code>https://api.example.com/v1/zoos</code>、<code>https://api.example.com/v1/animals</code>等；</li><li>对资源进行具体操作时，主要表现在方法层面，比如：<ul><li><code>GET /zoos</code>：列出所有动物园；</li><li><code>POST /zoos</code>：新建一个动物园；</li><li><code>GET /zoos/ID</code>：获取某个指定动物园的信息；</li><li><code>PUT /zoos/ID</code>：更新某个指定动物园的信息（提供该动物园的全部信息）；</li><li><code>PATCH /zoos/ID</code>：更新某个指定动物园的信息（提供该动物园的部分信息）；</li><li><code>DELETE /zoos/ID</code>：删除某个动物园；</li><li><code>GET /zoos/ID/animals</code>：列出某个指定动物园的所有动物；</li><li><code>DELETE /zoos/ID/animals/ID</code>：删除某个指定动物园的指定动物；</li><li><font color="red">【注意】</font><code>PUT</code>和<code>PATCH</code>都是更新资源，但前者需要客户端提供改变后的完整资源，而后者只需要客户端提供改变的属性即可；</li></ul></li><li>过滤信息，API提供参数可以过滤返回结果，常见的参数有：<ul><li><code>?limit=10</code>：指定返回记录的数量，这里是10；</li><li><code>?offset=10</code>：指定返回记录的开始位置；</li><li><code>?page=2&amp;per_page=100</code>：指定第几页，以及每页的记录数；</li><li><code>?sortby=name&amp;order=asc</code>：指定返回结果按照哪个属性排序，以及排序顺序；</li><li><code>?animal_type_id=1</code>：指定筛选条件；</li><li><font color="red">【注意】</font>参数的设计允许存在冗余，即允许API路径和URL参数偶尔有重复；</li></ul></li><li>错误的信息的处理：如果状态码是<code>4xx</code>，就应该向用户返回出错信息。一般来说，返回的信息中将<code>error</code>作为键名，出错信息作为键值即可，如<code>error: &quot;Invalid API key&quot;</code>;</li><li>不同接口调用返回的结果应该符合如下的规范：<ul><li><code>GET /collection</code>：返回资源对象的列表（数组）；</li><li><code>GET /collection/resource</code>：返回单个资源对象；</li><li><code>POST /collection</code>：返回新生成的资源对象；</li><li><code>PUT /collection/resource</code>：返回完整的资源对象；</li><li><code>PATCH /collection/resource</code>：返回完整的资源对象；</li><li><code>DELETE /collection/resource</code>：返回一个空文档；</li></ul></li><li>API的身份认证应该使用<code>OAuth 2.0</code>框架；</li><li>服务器返回的数据格式，应该尽量使用JSON，避免使用XML；</li><li>Hypermedia API，即超媒体API，Hypermedia API的设计被称为HATEOAS。返回结果中提供链接，连向其他API方法，使得用户不查文档，也知道下一步应该做什么，如：向<code>api.example.com</code>请求得到：</li></ul><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"link"</span>: &#123;</span><br><span class="line">        <span class="attr">"rel"</span>: <span class="string">"collection https://www.example.com/zoos"</span>,</span><br><span class="line">        <span class="attr">"href"</span>: <span class="string">"https://api.example.com/zoos"</span>,</span><br><span class="line">        <span class="attr">"title"</span>: <span class="string">"List of zoos"</span>,</span><br><span class="line">        <span class="attr">"type"</span>: <span class="string">"application/vnd.yourformat+json"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>rel</code>表示这个<code>API</code>与当前网址的关系（collection关系，并给出该collection的网址），<code>href</code>表示API的路径，<code>title</code>表示API的标题，<code>type</code>表示返回类型；</p><p>REST API案例(用户和订单)：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/API%E6%A1%88%E5%88%97.png" alt="API案列"></p><p>【总结】</p><p>&emsp;REST是面向资源的(非常重要)，而资源是通过URI进行暴露，URI的设计只要负责把资源通过合理方式暴露出来就可以了，对资源的操作与它无关，操作是通过HTTP动词来体现，所以REST通过URI暴露资源时，会强调不要在URI中出现动词。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;REST(Representational State Transfer)中文含义是表述性状态转移，它指的是一组架构约束条件和原则(它是一种设计风格而不是标准)，如果一个架构符合REST的约束条件和原则，我们就称它为RESTful架构。理论上REST架构风格并不是绑定在HTTP上，只不过目前HTTP是唯一与REST相关的实例，但不要形成固定思维。目前在三种主流的Web服务实现方案中，因为REST模式的Web服务与复杂的SOAP和XML-RPC对比来讲明显的更加简洁，越来越多的web服务开始采用REST风格设计和实现。REST的设计标准有下面的一些：
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="RESTful" scheme="https://jacksonary.github.io/tags/RESTful/"/>
    
      <category term="API" scheme="https://jacksonary.github.io/tags/API/"/>
    
  </entry>
  
  <entry>
    <title>Docker的存储驱动</title>
    <link href="https://jacksonary.github.io/posts/4bf33dda.html"/>
    <id>https://jacksonary.github.io/posts/4bf33dda.html</id>
    <published>2020-04-02T10:06:59.000Z</published>
    <updated>2021-06-04T12:34:00.148Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;存储驱动允许用户在容器的可写层创建数据，但这些数据并不会持久化，随着容器的消亡而消亡，而且存储驱动的赋予这些文件读、写速度都比本地文件系统的性能要慢。</p><h2 id="1-关于-image-和-layer"><a href="#1-关于-image-和-layer" class="headerlink" title="1. 关于 image 和 layer"></a>1. 关于 image 和 layer</h2><p>&emsp;Docker镜像是以一系列的layers为基础构建出来的，镜像中的每个layer都代表着一条指令，除最后一层其他每层都是只读的，所以镜像可以理解为是一个个的只读层堆积起来的。像下面的 Dokcerfile :</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu:<span class="number">18.04</span></span><br><span class="line"><span class="keyword">COPY</span> . /app</span><br><span class="line"><span class="keyword">RUN</span> make /app</span><br><span class="line"><span class="keyword">CMD</span> python /app/app.py</span><br></pre></td></tr></table></figure><a id="more"></a><p>上述的 Dockerfile 就会创建4层 layer，<code>FROM</code>指令从 ubuntu:18.04 镜像创建一个 layer，<code>COPY</code>指令从Docker客户端的当前目录添加文件，<code>RUN</code>指令使用<code>make</code>命令来编译应用，最后一个layer指定了容器中运行的命令。</p><p>&emsp;每一个layer都是一系列和前一个layer不同的东西组成，然后这些layer相互堆积。当创建一个新的容器时，其实这个行为的实质就是在底层的 layer 上添加了一个可写层，这个可写层就是常说的“容器层”，接下来容器内所有的增、改行为（创建、修改文件）都是写入到这个可写层上。存储驱动解决了这些layers之间的通信，不同的存储驱动在不同情况下各有利弊，应该根据业务场景选型。</p><h2 id="2-容器和-layer"><a href="#2-容器和-layer" class="headerlink" title="2. 容器和 layer"></a>2. 容器和 layer</h2><p>&emsp;其实了解了镜像和层的关系后，容器和镜像的最大区别是容器的顶层是可写的，但镜像是只读的，所有写入容器的内容都是存储到可写层的，不会影响镜像的内容，所以当容器消亡时，可写层也随之消亡，那么之前写入的内容会丢失。每个容器都有自己的可写容器层，一个镜像可以创建多个容器，但每个容器都有他们各自的可写层，并不会相互影响（除非指定挂载同一个目录），之间的关系如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%AE%B9%E5%99%A8%E5%92%8C%E9%95%9C%E5%83%8F%E7%9A%84%E5%85%B3%E7%B3%BB.png" alt="容器和镜像的关系"></p><p>可以简单的理解为<code>容器 = 镜像 + 可写层</code>，而Docker的存储驱动就是Docker用来管理镜像层和可写容器层的，每个存储驱动实现方式都不太一样，但所有的存储驱动都是使用可堆积的镜像层和“写时复制”策略（Cow - copy-on-write）。</p><h2 id="3-容器的size"><a href="#3-容器的size" class="headerlink" title="3. 容器的size"></a>3. 容器的size</h2><p>&emsp;使用 <code>docker ps -s</code>中的<code>-s</code>参数可以看到一列名为 SIZE 的列，该列包含的内容大概形式为“0B (virtual 64.2MB)”，0表示容器可写层中的数据量（一般不会为0，除非容器已经停止），括号中的<code>virtual 64.2MB</code>表示镜像+容器可写层的总数据量。因为多个容器可以共享只读的镜像层，如果镜像层是同一个，那它们的只读数据肯定都是共享的，但如果由两个不同的镜像创建的两个容器，但如果这2个镜像中有相同的layer也会共享，因此很难推断总虚拟化的大小（只是简单的进行加和的话），一般都是比加和的总共要小。下面是我简单看了下官方一直叨叨的 the thin writable layer ：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">docker ps -s</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> alpine-base:1.0 是一个包含openjdk8和nodejs12的最小化Linux</span><br><span class="line">CONTAINER ID        IMAGE               COMMAND             CREATED             STATUS              PORTS               NAMES               SIZE</span><br><span class="line">fa689846c376        alpine-base:1.0     "/bin/sh"           2 hours ago         Up 2 hours                              focused_rubin       193B (virtual 153MB)</span><br></pre></td></tr></table></figure><p>可以看到可写容器层的数据量只占用了193B，确实很小很苗条，但个人理解可写层厚薄与否实际取决于应用在可写层操作行为，比如我在容器内生成一个体积较大的块<code>dd if=/dev/zero of=test bs=1M count=1000</code>，然后再看一下可写层的数据量：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">docker ps -s</span><br><span class="line"></span><br><span class="line">CONTAINER ID        IMAGE               COMMAND             CREATED              STATUS              PORTS               NAMES               SIZE</span><br><span class="line">418eb1cbcea5        22de903425e2        "/bin/sh"           About a minute ago   Up About a minute                       angry_newton        1.05GB (virtual 1.2GB)</span><br></pre></td></tr></table></figure><p>瞬间就达到了1.05G，但Docker本身倡导 thin writable container layer，如果你的应用在容器有很大的数据量写入，建议使用数据卷 volume 挂载，这样可以提高I/O效率，而且和应用本身隔离，容器可写层不会占用太多空间，这也是被提倡的。</p><h2 id="4-写时复制-Copy-on-Write"><a href="#4-写时复制-Copy-on-Write" class="headerlink" title="4. 写时复制 Copy-on-Write"></a>4. 写时复制 Copy-on-Write</h2><p>&emsp;写时复制是一种共享和复制文件的策略，能最大程度地提高效率，如果某个文件或目录已经存在于镜像中的底层 layer 中，然后另外一个layer（可以是镜像中的某个layer或直接是容器可写层）需要读取这个文件或目录，那这个文件或目录就会被复制到需要读取该文件的layer中进行相应的读取或修改，这样可以将I/O和每个后续层的大小最小化。</p><h3 id="4-1-最小化镜像大小"><a href="#4-1-最小化镜像大小" class="headerlink" title="4.1 最小化镜像大小"></a>4.1 最小化镜像大小</h3><p>&emsp;当使用<code>docker pull</code>拉取镜像时，或者使用一个不存在的镜像创建容器时，docker 会进行镜像的拉取，可以看到它是一个layer一个layer独立拉取的，存储到Docker本地存储区中（通常是<code>/var/lib/docker</code>目录），每个layer存储到Docker宿主机器的目录中，可以查看<code>/var/lib/docker/&lt;storage-driver&gt;</code>目录，比如 overlay 或者 overlay2，但里面的目录并不是和layer的id一一对应的。注意Docker中的镜像是以layer为基础的，就算2个镜像不同，但如果它们之间有相同的layer时还是会进行共享的，通常可以使用<code>docker history &lt;image-name/image-id&gt;</code>查看镜像中的layer信息，比如下面是 alpine-base:1.0 （一个包含openjdk8和nodejs12的最小化Linux）镜像的Dockerfile：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> alpine:latest</span><br><span class="line"><span class="keyword">MAINTAINER</span> WeiguoLiu &lt;jacksonary@<span class="number">163</span>.com&gt;</span><br><span class="line"><span class="keyword">USER</span> root</span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span> apk update</span><br><span class="line"><span class="keyword">RUN</span> apk fetch openjdk8</span><br><span class="line"><span class="keyword">RUN</span> apk add openjdk8</span><br><span class="line"><span class="keyword">RUN</span> apk add nodejs</span><br></pre></td></tr></table></figure><p>build完再看一下该镜像的layer：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 查看 alpine-base:1.0 的层信息</span><br><span class="line">docker history bec86562d726</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 结果</span><br><span class="line">IMAGE               CREATED             CREATED BY                                      SIZE                COMMENT</span><br><span class="line">bec86562d726        2 days ago          /bin/sh -c apk add nodejs                       29.6MB              </span><br><span class="line">60359c5a2fda        2 days ago          /bin/sh -c apk add openjdk8                     100MB               </span><br><span class="line">cb259c1d667d        2 days ago          /bin/sh -c apk fetch openjdk8                   15.8MB              </span><br><span class="line">ef35bbeb82c2        2 days ago          /bin/sh -c apk update                           1.57MB              </span><br><span class="line">0e8ab980a0c8        2 days ago          /bin/sh -c #(nop)  USER root                    0B                  </span><br><span class="line">2dda369aac80        2 days ago          /bin/sh -c #(nop)  MAINTAINER WeiguoLiu &lt;liu…   0B                  </span><br><span class="line">cc0abc535e36        2 weeks ago         /bin/sh -c #(nop)  CMD ["/bin/sh"]              0B                  </span><br><span class="line">&lt;missing&gt;           2 weeks ago         /bin/sh -c #(nop) ADD file:36fdc8cb08228a870…   5.59MB</span><br></pre></td></tr></table></figure><p>和之前说的一样，Dockerfile中的每个命令都是一个新的layer，上面展现出来的layer信息几乎和Dockerfile中是一样的，而且可以发现除了在使用<code>apk add</code>安装软件外，其他指令几乎不会占用空间（因为大部分的指令仅仅是一个“指令”），此外很多底层镜像都会在<code>FROM XXX</code>引入基础镜像，但这个镜像是在本地所没有的或者是使用另一个系统构建的，那在显式层信息时就会显式上面的<code>&lt;missing&gt;</code>的样式。</p><h3 id="4-2-提高容器效率"><a href="#4-2-提高容器效率" class="headerlink" title="4.2 提高容器效率"></a>4.2 提高容器效率</h3><p>&emsp;当创建容器时，一个薄薄的可写层（感觉docker每次提及 the thin writable layer 时都是可骄傲可骄傲的，哈哈o(<em>￣▽￣</em>)o）将加到镜像的顶层，然后容器内<font color="red">所有文件系统的改变</font>都会存储到这个可写层，而容器中其他任何没有发生改动的文件都不会复制到可写层，所以说这点可以在一定程度上减小了容器可写层的数据量。</p><p>&emsp;在容器内对一个文件进行修改时，那存储驱动就会进行“写时复制”（CoW）的操作，具体操作流程会根据不同的存储驱动而有所不同（而且是强依赖、强相关），比如<code>aufs</code>、<code>overlay</code>和<code>overlay2</code>这3种存储驱动，但写时复制遵循下面大致的顺序：</p><ol><li>从镜像底层查找需要更新的文件，这个过程从镜像的最顶层开始，一直到基础层，当查找完毕后，它们将查找得到的文件添加到缓存以便于后面的操作；</li><li>对第一个查找到的文件副本执行<code>copy_up</code>操作，将这个文件复制到容器的可写层；</li><li>所有的改动都执行到这个副本，容器并不能看到镜像底层只读的文件（因为镜像中的layer都是只读的）；</li></ol><p>&emsp;Btrfs、ZFS和其他的驱动在处理写时复制都有所不同，<code>copy_up</code>操作行为会引发明显的性能开销（开销大小依赖于不同的存储驱动），尤其是在文件量比较大或层次、目录树比较深时，这种消耗可以这样减轻：仅在第一次修改某个文件时进行<code>copy_up</code>。下面是一个计数器镜像的示例（创建4个容器并查看它们所占用的空间）：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%AE%B9%E5%99%A8%E5%9C%A8%E5%AE%BF%E4%B8%BB%E6%9C%BA%E4%B8%8A%E5%AD%98%E5%82%A8.png" alt="容器在宿主机上存储"></p><h2 id="5-存储驱动"><a href="#5-存储驱动" class="headerlink" title="5. 存储驱动"></a>5. 存储驱动</h2><p>&emsp;一般情况下，推荐在容器可写层写入少量数据，如果数据量较大，建议切换成 volume 挂载的方式，但有些情况又必须写入容器层，此时就涉及到了存储驱动。Docker支持多种存储驱动，且以热拔插的方式运行，它控制着宿主机如何存储以及管理镜像和容器。</p><h3 id="5-1-存储驱动的选择"><a href="#5-1-存储驱动的选择" class="headerlink" title="5.1 存储驱动的选择"></a>5.1 存储驱动的选择</h3><p>&emsp;如果内核支持多个存储驱动，如果没有显式配置存储驱动，Docker将使用内置的存储驱动优先级别列表来决定使用哪个。Docker支持的存储驱动有（首选 <font color="red">overlay2</font>）：</p><ul><li><code>overlay2</code>：是首选的存储驱动，适用于所有Linux发行版，不需要额外的配置；</li><li><code>aufs</code>：对于 Ubuntu 14.04 的 3.13 内核，Docker 18.06 及之前的版本，aufs 是首选的存储驱动，但仅支持Ubuntu 和 Debian，可能还需要安装额外的包；</li><li><code>devicemapper</code>：Docker支持该存储驱动，但对于生产环境需要<code>direct-lvm</code>，因为<code>loopback-lvm</code>虽然也是零配置，但性能非常差。对于 centOS 和 RHEL 是推荐使用的存储驱动，因为这两种操作系统的内核不支持<code>overlay2</code>，但它们的版本已经支持<code>overlay2</code>，所以最终还是推荐使用<code>overlay2</code>驱动；</li><li><code>btrfs</code>和<code>zfs</code>：如果 <code>btrfs</code> 和 <code>zfs</code> 存储驱动程序是后备文件系统（安装了Docker的主机的文件系统），则使用它们。这些文件系统有更高级的选项（比如创建快照，但需要更多的维护和配置），这些都依赖于正确配置的支持文件系统，仅支持SLES，而且只有Docker企业版才支持；</li><li><code>vfs</code>：专门为了测试设计的存储驱动，以及无法使用写时复制文件系统的情况才会使用，性能差，不推荐使用；</li></ul><p>&emsp;<strong>根据负载选择合适的驱动</strong>：每个存储驱动程序都有其自己的性能特征，这或多或少地会让不同的存储驱动适合于不同的工作负载。但也有一些共性：</p><ul><li><code>overlay2</code>、<code>aufs</code>、<code>overlay</code>所有的操作都是在文件级别上操作的而不是块上进行，这种方式对于空间的利用率很高，但对于写操作频繁的应用而言，容器可写层占用的空间会增长的比较大；</li><li>类似于<code>devicemapper</code>、<code>btrfs</code>、<code>zfs</code>这种块级别的存储驱动，更适用于写操作频繁的应用，虽然性能没有volume挂载好；</li><li>对于很多少写或容器层级比较多或者文件系统目录树比较深的情况，<code>overlay</code>通常会比<code>overlay2</code>要好，但会消耗更多的inode（类似于用于索引的存储块-索引节点，记录了文件的metadata），可能导致inode耗尽；</li><li><code>btrfs</code>和<code>zfs</code>需要很多内存；</li><li><code>zfs</code>对于高密度的负载是个好选择，比如 PaaS；</li></ul><h3 id="5-2-AUFS存储驱动"><a href="#5-2-AUFS存储驱动" class="headerlink" title="5.2 AUFS存储驱动"></a>5.2 AUFS存储驱动</h3><p>&emsp;aufs 是联合文件系统（advanced multi-layered unification filesystem）的简称，它之前是Ubuntu以及Stretch之前的Debian版本中Docker管理镜像和层的默认存储驱动。如果Linux的内核版本4.0+，那就可以使用 Docker Engine - Community，建议使用更新的 overlay2 驱动（比aufs更具优势），Docker的某些版本是不支持aufs驱动的。如果使用aufs驱动，必须具备一些条件：</p><ul><li>对于Docker Engine - Community，aufs驱动支持Ubuntu以及Stretch之前的Dibian；</li><li>对于Docker EE，aufs驱动支持Ubuntu；</li><li>对于Ubuntu，需要安装额外的包以便将aufs模块加入到内核。注意：如果不安装这些额外的包，就需要使用<code>devicemapper</code>驱动（对于Ubuntu 14.04版本而言，当然这是不被推荐的）或者<code>overlay2</code>（对于Ubuntu 16.04版本而言，可以接受）；</li><li>aufs驱动不能使用下面的备份文件系统：aufs、btrfs以及ecryptfs，换而言之，在<code>/var/lib/docker/aufs</code>中不会出现上面的这些文件系统类型；</li></ul><p>&emsp;aufs的配置也很方便，步骤如下：</p><ol><li>检测是否宿主机器的内核是否支持aufs，直接查看<code>proc/filesystem</code>文件（这个文件中罗列了当前内核支持的所有文件系统，总共两列信息，其中第二列表示支持的文件系统，第一列如果是“nodev”表示对应的文件系统尚未被mount某块设备上，如果第一列为空白，则表示该文件系统已经被挂载到某块设备上了）中是否有<code>aufs</code>关键字：<code>grep aufs /proc/filesystems</code>，如果可以查找到<code>nodev aufs</code>就表示支持aufs，且没有挂载到其他设备上；</li><li>查看当前Docker使用的存储驱动：<code>docker info</code>，可以看到<code>Storage Driver: overlay</code>；</li><li>查看<code>/etc/docker/daemon.json</code>文件是否配置驱动，查看docker启动时是否使用<code>--storage-driver</code>参数指定存储驱动：<code>ps auxw | grep dockerd</code>；</li></ol><p>&emsp;aufs是联合文件系统，可以把多个文件夹的内容合并到一起并提供统一的视图，然后这些目录在aufs专业术语中叫做分支，对应于Docker中就是layer。工作原理如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/aufs_layers.jpg" alt="aufs_layers"></p><p>一般这些信息就是存在<code>/var/lib/docker/&lt;store-driver&gt;</code>目录中（store-driver取决于Docker使用的驱动），然后这个目录里面所有目录和文件都是由Docker自己管理。所有的镜像和容器可写层的信息都是存储在<code>/var/lib/docker/&lt;store-driver&gt;</code>目录中（对应于aufs就是<code>/var/lib/docker/aufs</code>目录），通常该目录中会有如下的子目录：</p><ul><li><code>diff</code>：该目录包含了容器可写层的差异，比如新增或修改的文件；</li><li><code>layers</code>：该目录中存储了容器可写层的父级layer；</li><li><code>mnt</code>：表示每一个运行容器的文件系统的挂载点，它和容器一起出现；</li></ul><p>对应于上图，每个镜像层以及容器可写层在Docker宿主机上都是以<code>/var/lib/docker</code>下面的子目录形式表现，联合安装提供所有层的统一视图，但里面的目录并不是和layer的id一一对应的。</p><p>&emsp;总的来说，aufs驱动特性有：</p><ul><li>总体性能不如 overlay2，但对于类似于PaaS这类高密度的应用而言是一个不错的选择，因为 aufs 可以在多个运行的容器之间实现高效的共享，加快容器的启动，减小磁盘占用；</li><li>aufs在镜像层和容器之间的共享文件非常有效地使用了页面缓存；</li><li>aufs存储驱动在容器写入的延迟更加明显，因为容器在首次写入文件时，需要先定位到该文件再将其复制到容器可写层，而且在该文件存在于很多layer中时或这些文件本身就很大，那这种延迟会越来越严重和复杂；</li></ul><p>如果场景确实需要使用aufs驱动，那最佳实践建议作如下的措施：</p><ul><li>使用SSD（Solid State Devices 固态硬盘）：它可以提供比磁盘更加快速的读、写速度；</li><li>对于写入频繁的应用使用 volume 挂载：数据卷可以为写入频繁的应用提供最佳也是最合适的性能，<font color="red">因为volume挂载绕过了存储驱动程序，并且不会产生任何精简配置和写时复制所带来的潜在开销。</font></li></ul><h3 id="5-3-BTRFS存储驱动"><a href="#5-3-BTRFS存储驱动" class="headerlink" title="5.3 BTRFS存储驱动"></a>5.3 BTRFS存储驱动</h3><p>&emsp;btrfs是下一代支持很多高级存储技术的写时复制文件系统（这对Docker很友好，但只支持Ubuntu和Debian上的社区版Docker和SLES上的社区版、企业版Docker），btrfs包含在Linux主线（及mainline，对应于内核有<code>-ml</code>标识的内核）内核中。btrfs驱动利用了很多btrfs特性来作镜像和容器的管理，像块级别的操作、快照的写时复制、精简配置、便于管理，可以轻松的将多个物理块设备组织成一个独立的btrfs文件系统。</p><p>&emsp;使用btrfs存储驱动有些前提条件：</p><ul><li>对于Docker社区版，如果要使用btrfs，那只推荐在Ubuntu和Dibian上使用；</li><li>对于Docker企业版，btrfs只支持SLES；</li><li>更改存储驱动会使已经运行的容器无法访问本地系统，<code>docker save &lt;container-id&gt;</code>可以存储容器并将已有的镜像推送到DockerHub或自定义的仓库，这样之后就不用重新创建了；</li><li>btrfs需要专用的块存储设备，比如物理磁盘，这些块设备必须因此进行格式化并且挂载到<code>var/lib/docker</code>下。默认SLES是已经格式化为btrfs了，不需要进行格式化，因此对于SLES，不需要使用单独的块设备，但是出于性能考虑，也可以选择使用它；</li><li>检验当前系统是否支持btrfs：<code>cat /proc/filesystem | grep btrfs</code>；</li><li>如果需要管理btrfs文件系统，需要安装<code>btrfsprogs</code>或<code>btrfs-tools</code>使用<code>btrfs</code>指令操作；</li></ul><p>&emsp;将btrfs配置为Docker的存储驱动，步骤如下：</p><ol><li>停止docker；</li><li>备份<code>var/lib/docker</code>目录：<code>cp -au var/lib/docker var/lib/docker.bk</code>，然后清空该目录：<code>rm -rf var/lib/docker</code>；</li><li>将块设备格式化为btrfs：<code>mkfs.btrfs -f &lt;块设备&gt;</code>；</li><li>将上面格式化好的设备挂载到<code>/var/lib/docker</code>：<code>mount -t btrfs &lt;块设备&gt; /var/lib/docker</code>；</li><li>将备份的内容复制到<code>var/lib/docker</code>：<code>cp -au var/lib/docker.bk var/lib/docker</code>；</li><li>创建（没有的话手动创建）配置<code>/etc/docker/daemon.json</code>或在已有文件里面添加：<code>&quot;storage-driver&quot;: &quot;btrfs&quot;</code>；</li><li>启动docker验证：<code>docker info</code>；</li><li>若要添加设备到btrfs：<code>btrfs device add &lt;设备&gt; /var/lib/docker</code>，然后<code>btrfs filesystem balance /var/lib/docker</code>；</li></ol><p>&emsp;btrfs驱动的工作原理，镜像和容器可写层都是存储在<code>var/lib/docker/btrfs/subvolumes</code>目录下，每个镜像或者容器可写层都在这个子目录下有一个自己的目录，从一个layer及其所有父级layer构建的统一文件系统，然后这些子卷都是本地写时复制的并且从基础存储池按需分配空间，特们之间可以是嵌套或者直接是一份快照，下面是一个具有4个子卷的图，其中子卷2、3是属于嵌套，而子卷4展示的是他自己内部的目录树：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/btfs_subvolume.jpg" alt="btfs_subvolume"></p><p>只有一个镜像的基础层可以作为一个子卷树存储，所有其他的层都是以快照的形式存储，这些快照仅仅包含在那个引入layer的差异部分，可以创建快照的快照：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/btfs_snapshots.jpg" alt="btfs_snapshots"></p><p>在磁盘上，快照看着就像是子卷，但实际上它们更小并且空间利用率更高，写时复制的策略就是为了最大化存储效率、最小化layer的体积，然后可以让容器可写层内容以块级别被管理，下面就是子卷以及它的快照共享数据的行为：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/btfs_pool.jpg" alt="btfs_pool"></p><p>为了最大化效率，容器需要更多的空间，以大约1GB的块分配，镜像底层的layer以子卷的形式存储，而其子级镜像layer以及容器则是以快照的形式存储，如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/btfs_container_layer.jpg" alt="btfs_container_layer"></p><p>&emsp;运行btrfs驱动的Docker，它创建镜像或容器的层次为：</p><ol><li>镜像的基础层存储在 btrfs 的子卷下（<code>/var/lib/docker/btrfs/subvolumes</code>）；</li><li>后续的镜像层以 btrfs 快照的形式存储到父级层的子卷或快照下，但是随着这一层的变化，差异存储在块级别；</li><li>容器可写层是最后的layer的btrfs快照，以及运行容器的差异，也是存储在块级别的；</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;存储驱动允许用户在容器的可写层创建数据，但这些数据并不会持久化，随着容器的消亡而消亡，而且存储驱动的赋予这些文件读、写速度都比本地文件系统的性能要慢。&lt;/p&gt;
&lt;h2 id=&quot;1-关于-image-和-layer&quot;&gt;&lt;a href=&quot;#1-关于-image-和-layer&quot; class=&quot;headerlink&quot; title=&quot;1. 关于 image 和 layer&quot;&gt;&lt;/a&gt;1. 关于 image 和 layer&lt;/h2&gt;&lt;p&gt;&amp;emsp;Docker镜像是以一系列的layers为基础构建出来的，镜像中的每个layer都代表着一条指令，除最后一层其他每层都是只读的，所以镜像可以理解为是一个个的只读层堆积起来的。像下面的 Dokcerfile :&lt;/p&gt;
&lt;figure class=&quot;highlight dockerfile&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;FROM&lt;/span&gt; ubuntu:&lt;span class=&quot;number&quot;&gt;18.04&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;COPY&lt;/span&gt; . /app&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;RUN&lt;/span&gt; make /app&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;CMD&lt;/span&gt; python /app/app.py&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Docker" scheme="https://jacksonary.github.io/tags/Docker/"/>
    
      <category term="存储驱动" scheme="https://jacksonary.github.io/tags/%E5%AD%98%E5%82%A8%E9%A9%B1%E5%8A%A8/"/>
    
      <category term="overlay" scheme="https://jacksonary.github.io/tags/overlay/"/>
    
  </entry>
  
  <entry>
    <title>FeignClient问题</title>
    <link href="https://jacksonary.github.io/posts/99b0ef0a.html"/>
    <id>https://jacksonary.github.io/posts/99b0ef0a.html</id>
    <published>2020-03-23T00:43:00.000Z</published>
    <updated>2021-06-04T12:36:30.245Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-主要依赖"><a href="#1-主要依赖" class="headerlink" title="1. 主要依赖"></a>1. 主要依赖</h2><p>&emsp;XXXClient主要依赖的是openfeign：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-openfeign<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.2.2.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="2-FeignClient不支持PATCH方法"><a href="#2-FeignClient不支持PATCH方法" class="headerlink" title="2. FeignClient不支持PATCH方法"></a>2. FeignClient不支持PATCH方法</h2><p>&emsp;引入httpclient依赖（注意时适配feign-core版本）：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.github.openfeign<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>feign-httpclient<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>10.4.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="3-关于服务熔断-hystrix"><a href="#3-关于服务熔断-hystrix" class="headerlink" title="3. 关于服务熔断 hystrix"></a>3. 关于服务熔断 hystrix</h2><p>&emsp;在微服务调用之间，使用FeignClient如果不使用服务熔断，只要xxxClient内部除了异常，调用方得到的信息始终是500（拿不到那些业务相关的异常），为了让xxxClient内部的异常直接抛到调用方，可以如下配置（就算调用方已经定义全局异常处理也不会捕获FeignClient的异常），使用方式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 在定义client时加入fallbackFactory</span></span><br><span class="line"><span class="meta">@FeignClient</span>(value = <span class="string">"test-client"</span>, url = <span class="string">"http://localhost:8087/gjw-shop"</span>, fallbackFactory = TestFeignErrorFallback.class)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">TestClient</span> </span>&#123;</span><br><span class="line">    <span class="comment">//...各种方法</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun1</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 定义TestFeignErrorFallback类（建议一个Client定义一个）</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FeignErrorFallback</span> <span class="keyword">implements</span> <span class="title">FallbackFactory</span>&lt;<span class="title">TestClient</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Logger LOG = LoggerFactory.getLogger(FeignErrorFallback.class);</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String DEFAULT_ERR_MSG = <span class="string">"======= error occurred in server ======="</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> TestClient <span class="title">create</span><span class="params">(Throwable throwable)</span> </span>&#123;</span><br><span class="line">        String msg = throwable == <span class="keyword">null</span> ? DEFAULT_ERR_MSG : throwable.getMessage();</span><br><span class="line">        <span class="keyword">if</span> (!StringUtils.isEmpty(msg)) &#123;</span><br><span class="line">            LOG.error(msg);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> TestClient() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun1</span><span class="params">(Long shopComponentId)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(msg);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 在xxxClient调用方加入服务熔断配置</span></span><br><span class="line"># 开启熔断</span><br><span class="line">feign.hystrix.enabled = <span class="keyword">true</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-主要依赖&quot;&gt;&lt;a href=&quot;#1-主要依赖&quot; class=&quot;headerlink&quot; title=&quot;1. 主要依赖&quot;&gt;&lt;/a&gt;1. 主要依赖&lt;/h2&gt;&lt;p&gt;&amp;emsp;XXXClient主要依赖的是openfeign：&lt;/p&gt;
&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;org.springframework.cloud&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;spring-cloud-starter-openfeign&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;version&lt;/span&gt;&amp;gt;&lt;/span&gt;2.2.2.RELEASE&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;version&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Feign" scheme="https://jacksonary.github.io/tags/Feign/"/>
    
      <category term="RPC" scheme="https://jacksonary.github.io/tags/RPC/"/>
    
      <category term="微服务" scheme="https://jacksonary.github.io/tags/%E5%BE%AE%E6%9C%8D%E5%8A%A1/"/>
    
  </entry>
  
  <entry>
    <title>Hystrix 配置说明</title>
    <link href="https://jacksonary.github.io/posts/732dbc14.html"/>
    <id>https://jacksonary.github.io/posts/732dbc14.html</id>
    <published>2020-03-20T00:41:00.000Z</published>
    <updated>2021-06-04T12:36:57.379Z</updated>
    
    <content type="html"><![CDATA[<p>openFeign先要开启 hystrix 的请求：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 开启服务熔断</span><br><span class="line">feign.hystrix.enabled=true</span><br></pre></td></tr></table></figure><p>主要命令有2个：<a id="more"></a></p><ul><li>HystrixCommand：用于真正执行的hystrix命令；</li><li>HystrixObserableCommand：用于监测hystrix命令的执行结果，如果请求有返回结果的话；</li></ul><p>主要流程如下：调用方在发出请求 —&gt; 监测（observe）该请求 –&gt; 查看cache response 是否存在（存在就直接返回） —&gt; 查看断路器 circuit-breaker 是否开启（若开启直接短路请求尝试返回fallback） –&gt; 检测ThreadPool的队列是否已经开始拒绝请求（即线程池的等待队列已满且线程池中达到最大线程数，若已经开始拒绝请求尝试返回fallback） –&gt; 构建 Command 执行。</p><p>hystrix的相关配置有4个级别：</p><ol><li>全局默认配置，用户不手动配置任何属性，直接使用默认值；</li><li>动态配置默认属性，即下面配置都带有<code>default</code>关键字的属性（推荐）；</li><li>代码中的实例属性，示例见下方；</li><li>动态实例属性（推荐，优先级别最高），可以动态设置特定实例的值，这些值将覆盖前面的三个默认级别，如 <code>hystrix.command.HystrixCommandKey.execution.isolation.strategy</code>；</li></ol><p>注： 代码中实例属性示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">HystrixCommandProperties.Setter().withExecutionTimeoutInMilliseconds(<span class="keyword">int</span> value)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HystrixCommandInstance</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(Setter.withGroupKey(HystrixCommandGroupKey.Factory.asKey(<span class="string">"ExampleGroup"</span>))</span><br><span class="line">        .andCommandPropertiesDefaults(HystrixCommandProperties.Setter()</span><br><span class="line">               .withExecutionTimeoutInMilliseconds(<span class="number">500</span>)));</span><br><span class="line">    <span class="keyword">this</span>.id = id;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HystrixCommandInstance</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(HystrixCommandGroupKey.Factory.asKey(<span class="string">"ExampleGroup"</span>), <span class="number">500</span>);</span><br><span class="line">    <span class="keyword">this</span>.id = id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="1-Command-属性相关属性"><a href="#1-Command-属性相关属性" class="headerlink" title="1. Command 属性相关属性"></a>1. Command 属性相关属性</h2><p>&emsp;以下均为hystrix默认使用的属性，如果不配置该属性，也可以直接动态配置实例属性，即将属性中的<code>default</code>换成<code>HystrixCommandKey</code>即可，如默认属性为 <code>hystrix.command.default.execution.isolation.strategy</code>，那它对应的实例属性就为<code>hystrix.command.HystrixCommandKey.execution.isolation.strategy</code>，其他属性类似。</p><h3 id="1-1-execution相关配置"><a href="#1-1-execution相关配置" class="headerlink" title="1.1 execution相关配置"></a>1.1 execution相关配置</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"># thread 通过线程数量来限制并发请求数，可以提供额外的保护，但有一定的延迟。一般用于网络调用</span><br><span class="line"># semaphore 通过semaphore count来限制并发请求数，适用于无网络的高并发请求</span><br><span class="line"># 默认隔离策略，默认是Thread, 可选 THREAD（推荐，在调用线程之外的另一个线程上单独执行，并发量受限于线程池）｜SEMAPHORE （在调用线程上执行，并发请求受到信号量的限制）</span><br><span class="line">hystrix.command.default.execution.isolation.strategy=THREAD</span><br><span class="line"></span><br><span class="line"># 定义hystrix是否开启超时配置，默认为true</span><br><span class="line">hystrix.command.HystrixCommandKey.execution.timeout.enabled=true</span><br><span class="line"></span><br><span class="line"># 定义hystrix是否允许在超时时允许被打断，默认为true</span><br><span class="line">hystrix.command.default.execution.isolation.thread.interruptOnTimeout=true</span><br><span class="line"></span><br><span class="line"># 定义hystrix是否响应取消操作，默认为false（即hystrix command 在执行时是否可以执行取消行为）</span><br><span class="line">hystrix.command.default.execution.isolation.thread.interruptOnCanc=false</span><br><span class="line"></span><br><span class="line"># 默认调用线程的超时时间（调用方会一直观察该请求直至获取结果或到超时），默认缺省为1000ms</span><br><span class="line">hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds=5000</span><br><span class="line"></span><br><span class="line"># 定义在使用 ExecutionIsolationStrategy.SEMAPHORE 隔离策略时最大允许并发运行的hystrix command命令数（超出的直接丢弃），默认为10</span><br><span class="line">hystrix.command.default.execution.isolation.semaphore.maxConcurrentRequests=10</span><br></pre></td></tr></table></figure><h3 id="1-2-fallback相关配置（控制运行-hystrixCommand-getFallback-方法）"><a href="#1-2-fallback相关配置（控制运行-hystrixCommand-getFallback-方法）" class="headerlink" title="1.2 fallback相关配置（控制运行 hystrixCommand.getFallback() 方法）"></a>1.2 fallback相关配置（控制运行 hystrixCommand.getFallback() 方法）</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># 定义是否在请求失败或被丢弃时尝试去调用getFallback()方法，默认为true</span><br><span class="line">hystrix.command.default.fallback.enabled=true</span><br><span class="line"></span><br><span class="line"># 定义某个调用线程调用hystrixCommand.getFallback()的最大次数，默认为10</span><br><span class="line"># 若达到最大并发请求数，后面的请求将直接被丢弃，此时不会取回定义的fallback</span><br><span class="line">hystrix.command.default.fallback.isolation.semaphore.maxConcurrentRequests=10</span><br></pre></td></tr></table></figure><h3 id="1-3-断路器-circuit-breaker-相关"><a href="#1-3-断路器-circuit-breaker-相关" class="headerlink" title="1.3 断路器 circuit breaker 相关"></a>1.3 断路器 circuit breaker 相关</h3><p>&emsp;断路器的工作流程如下：</p><ol><li>检测 requestVolumeThreshold 是否达到阈值；</li><li>检测失败请求百分比是否达到 errorThresholdPercentag；</li><li>若1、2只要一个达标，则将断路器的状态置为 OPEN；</li><li>断路器状态为 OPEN，它将拦截到所有后续的请求；</li><li>断路器拦截了指定时间（sleepWindowInMilliseconds）后，它将尝试让1个请求通过（此时断路器处于 HALF-OPEN 半开状态）；</li><li>如果5中释放的那个请求失败了，那断路器健康检测直接返回true，表明断路器处于打开状态，然后重复5步骤；</li><li>如果5中释放的那个请求成功了，则将断路器的闭合状态置成关闭状态，后续的请求又重1步骤开始；</li></ol><p>断路器的各个属性配置如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"># 定义是否使用断路器去做断路器的健康检测以及在短路请求时进行跳闸，默认为true</span><br><span class="line"># 这里的健康检测特指hystrix断路器的健康，即检测断路器的开、闭状态</span><br><span class="line">hystrix.command.default.circuitBreaker.enabled=true</span><br><span class="line"></span><br><span class="line"># 定义在一个滚动窗口中触发断路器的最小请求数（阈值），默认为20</span><br><span class="line">hystrix.command.default.circuitBreaker.requestVolumeThreshold=20</span><br><span class="line"></span><br><span class="line"># 定义触发断路器开启的失败请求百分比，默认50，即50%</span><br><span class="line"># X%的请求都失败了就会触发回路短路，此时尝试返回fallback逻辑</span><br><span class="line">hystrix.command.default.circuitBreaker.errorThresholdPercentag=50</span><br><span class="line"></span><br><span class="line"># 定义电路跳闸后拒绝请求的时间，然后允许再次尝试确定电路是否应再次闭合，默认为5000ms</span><br><span class="line">hystrix.command.default.circuitBreaker.sleepWindowInMilliseconds=5000</span><br><span class="line"></span><br><span class="line"># 定义断路器的强制开启状态，默认为false</span><br><span class="line"># 如果为true，它将迫使断路器进入断开（跳闸）状态，在该状态下断路器将拒绝所有请求</span><br><span class="line">hystrix.command.default.circuitBreaker.forceOpen=false</span><br><span class="line"></span><br><span class="line"># 定义断路器的强制关闭状态，默认为false </span><br><span class="line"># 如果为true，它将迫使断路器进入关闭状态，在该状态下断路器将接受所有请求（无论失败请求是否达到触发短路的百分比）</span><br><span class="line">hystrix.command.default.circuitBreaker.forceClosed=false</span><br></pre></td></tr></table></figure><h3 id="1-4-指标-metrics-相关"><a href="#1-4-指标-metrics-相关" class="headerlink" title="1.4  指标 metrics 相关"></a>1.4  指标 metrics 相关</h3><p>&emsp;主要是计算hystrixCommand和HystrixObservableCommand（调用方在请求后会检测请求结果）指标。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"># 定义统计滚动窗口的持续时间（毫秒），这时间是Hystrix保留断路器使用和发布指标的时间</span><br><span class="line">hystrix.command.default.metrics.rollingStats.timeInMilliseconds=10000</span><br><span class="line"></span><br><span class="line"># 定义滚动统计窗口分为的存储桶数，默认为10</span><br><span class="line"># 注： metrics.rollingStats.timeInMilliseconds % metrics.rollingStats.numBuckets == 0 （否则会抛异常）</span><br><span class="line">hystrix.command.default.metrics.rollingStats.numBuckets=10</span><br><span class="line"></span><br><span class="line"># 定义是否跟踪执行延迟并将其计算为百分数，默认为true</span><br><span class="line"># 若禁用，则所有摘要统计信息（平均值，百分位数）都返回-1</span><br><span class="line">hystrix.command.default.metrics.rollingPercentile.enabled=true</span><br><span class="line"></span><br><span class="line"># 定义滚动窗口的持续时间，在该持续时间中保留执行时间进行百分比计算，默认为60000ms</span><br><span class="line">hystrix.command.default.metrics.rollingPercentile.timeInMilliseconds=60000</span><br><span class="line"></span><br><span class="line"># 定义 rollingPercentile 窗口分割成的桶数，默认为6</span><br><span class="line"># 注：metrics.rollingPercentile.timeInMilliseconds % metrics.rollingPercentile.numBuckets == 0 （否则会抛异常）</span><br><span class="line">hystrix.command.default.metrics.rollingPercentile.numBuckets=6</span><br><span class="line"></span><br><span class="line"># 定义每个存储区保留的最大执行时间，如果在这段时间内发生了更多的执行超出了桶的容量，它们将覆盖存储桶的头部内容</span><br><span class="line"># 如果存储桶大小设置为100，并且表示10秒的存储桶窗口，但10s内发生了500次执行，则在该10秒存储桶中将仅保留最近的100次执行</span><br><span class="line"># 加大这个属性值将会需要更多的内存存储以及更多的时间进行排序</span><br><span class="line">hystrix.command.default.metrics.rollingPercentile.bucketSize=100</span><br><span class="line"></span><br><span class="line"># 定义允许进行运行状况快照之间的等待时间（毫秒），该运行状况快照可计算成功率和错误率百分比并影响断路器状态</span><br><span class="line">hystrix.command.default.metrics.healthSnapshot.intervalInMilliseconds=500</span><br></pre></td></tr></table></figure><h3 id="1-5-请求上下文-HystrixRequestContext-相关"><a href="#1-5-请求上下文-HystrixRequestContext-相关" class="headerlink" title="1.5 请求上下文 HystrixRequestContext 相关"></a>1.5 请求上下文 HystrixRequestContext 相关</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># 定义HystrixCommand.getCacheKey()是否应与HystrixRequestCache一起使用</span><br><span class="line"># 这样可以通过请求范围的缓存提供重复数据删除功能，默认为true</span><br><span class="line">hystrix.command.default.requestCache.enabled=true</span><br><span class="line"></span><br><span class="line"># 定义是否将HystrixCommand的执行和事件记录到HystrixRequestLog，默认为true</span><br><span class="line">hystrix.command.default.requestLog.enabled=true</span><br></pre></td></tr></table></figure><h2 id="2-请求合并-request-collapser-相关属性"><a href="#2-请求合并-request-collapser-相关属性" class="headerlink" title="2. 请求合并 request collapser 相关属性"></a>2. 请求合并 request collapser 相关属性</h2><p>&emsp;request collapser 是 hystrix 提供批量请求的合并，及多个<code>Command.execute()/queue()</code>合并成一个 Collapser 然后获取线程池中的一个线程去执行，而不是像通常的一个<code>Command.execute()/queue()</code>就各自占用一个线程去执行，这样可以节省网络带宽和减少线程消耗。</p><p>&emsp;控制HystrixCollapser的行为，以下均为hystrix Collapser 默认使用的属性。如果不配置该属性，需要配置实例属性，即将属性中的<code>default</code>换成<code>HystrixCollapserKey</code>即可，如默认属性为 <code>hystrix.collapser.default.maxRequestsInBatch</code>，那它对应的实例属性就为<code>hystrix.collapser.HystrixCollapserKey.maxRequestsInBatch</code>，其他属性类似。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"># 定义在触发批量请求时允许的最大请求数，默认值 Integer.MAX_VALUE</span><br><span class="line">hystrix.collapser.default.maxRequestsInBatch=Integer.MAX_VALUE</span><br><span class="line"></span><br><span class="line"># 定义在创建批量请求触发时执行的毫秒数</span><br><span class="line">hystrix.collapser.default.timerDelayInMilliseconds=10</span><br><span class="line"></span><br><span class="line"># 设置是否为HystrixCollapser.execute（）和HystrixCollapser.queue（）调用启用请求缓存，默认为true</span><br><span class="line">hystrix.collapser.default.requestCache.enabled=true</span><br></pre></td></tr></table></figure><h2 id="3-线程池ThreadPool相关属性"><a href="#3-线程池ThreadPool相关属性" class="headerlink" title="3. 线程池ThreadPool相关属性"></a>3. 线程池ThreadPool相关属性</h2><p>&emsp;控制Hystrix的ThreadPool属性，hystrix线程池相关概念和Java中的线程池保持一致：核心线程数、最大线程数、队列长度等。以下均为hystrix默认使用的属性，如果不配置该属性，需要配置实例属性，即将属性中的<code>default</code>换成<code>HystrixThreadPoolProperties</code>即可，如 默认属性为 <code>hystrix.threadpool.default.coreSize=10</code>，那它对应的实例属性就为<code>hystrix.threadpool.HystrixThreadPoolProperties.coreSize=10</code>，其他属性类似。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"># 定义线程池的核心线程数，默认10，它可以处理99%的请求，健康状态下，线程池中通常只有一两个存活的线程去服务40ms的调用</span><br><span class="line"># 该数值若要调大，应匹配公式：健康时每秒请求数的峰值 * 99%的延迟（以秒为单位）+一些缓冲数值（可以适度定义）</span><br><span class="line"># 上述公式的是让线程池尽可能的小，因为线程池是减轻负载并防止资源发生延迟的主要手段</span><br><span class="line"># 当该属性正确配置时，HystrixCommand超时现象应该是极少的，但是还是需要有保护措施来减少网络延迟的影响，尤其是在最坏的情况下，connect + read + retry + connect + read的组合仍会超过配置的总体超时</span><br><span class="line">hystrix.threadpool.default.coreSize=10</span><br><span class="line"></span><br><span class="line"># 定义等待队列的容量，若设为-1则使用SynchronousQueue队列，若为正数则使用LinkedBlockingQueue队列并设置为它的容量</span><br><span class="line"># 注：队列容量在初始化后就不能进行resize了，若期望动态控制队列长度，需要配置 queueSizeRejectionThreshold 属性</span><br><span class="line">hystrix.threadpool.default.maxQueueSize=-1</span><br><span class="line"></span><br><span class="line"># 定义拒绝队列的阈值，默认为5，人为设置的最大队列长度，即使尚未达到 maxQueueSize，也会拒绝请求</span><br><span class="line"># 该属性是为了解决BlockingQueue不能动态resize的问题，所以只有hystrix.threadpool.default.maxQueueSize属性为正值时才生效</span><br><span class="line">hystrix.threadpool.default.queueSizeRejectionThreshold=5</span><br><span class="line"></span><br><span class="line"># 定义线程池中线程的存活时间（分钟），默认为1</span><br><span class="line">hystrix.threadpool.default.keepAliveTimeMinutes=1</span><br><span class="line"></span><br><span class="line"># 定义是否启用 maximumSize 属性，默认为false</span><br><span class="line">hystrix.threadpool.default.allowMaximumSizeToDivergeFromCoreSize=false</span><br><span class="line"># 定义线程池的最大线程数，该数值可以大于等于核心线程数，如果该值大于核心线程数时可以维持 maximumSize 的并发，但在不活跃期间可以返回线程，默认为10</span><br><span class="line"># 该参数只有同时配置了 allowMaximumSizeToDivergeFromCoreSize=true参数才会生效</span><br><span class="line">hystrix.threadpool.default.maximumSize=10</span><br><span class="line"></span><br><span class="line"># 定义统计滚动窗口的持续时间（毫秒），默认为10000</span><br><span class="line">hystrix.threadpool.default.metrics.rollingStats.timeInMilliseconds=10000</span><br><span class="line"># 定义滚动统计窗口划分为的桶数，默认为10</span><br><span class="line"># 注：必须满足条件 metrics.rollingStats.timeInMilliseconds % metrics.rollingStats.numBuckets == 0，否则会抛异常</span><br><span class="line">hystrix.threadpool.default.metrics.rollingStats.numBuckets=10</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;openFeign先要开启 hystrix 的请求：&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;# 开启服务熔断&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;feign.hystrix.enabled=true&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;主要命令有2个：
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Hystrix" scheme="https://jacksonary.github.io/tags/Hystrix/"/>
    
      <category term="限流组件" scheme="https://jacksonary.github.io/tags/%E9%99%90%E6%B5%81%E7%BB%84%E4%BB%B6/"/>
    
  </entry>
  
  <entry>
    <title>关于Docker的挂载</title>
    <link href="https://jacksonary.github.io/posts/d135925c.html"/>
    <id>https://jacksonary.github.io/posts/d135925c.html</id>
    <published>2020-01-09T02:48:00.000Z</published>
    <updated>2021-06-04T12:32:49.314Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>底层原理不懂就上手，上手出了问题就懵逼，最近在对接阿里云时遇到Docker存储驱动的神坑，爬了几天爬不出来，最后发现是节点中Docker存储驱动的问题，由此引发此次学习，避免类似问题再次懵逼。<br><a id="more"></a></p></blockquote><p>&emsp;关于镜像images，核心首先必须明确一点，<font color="red">镜像都是只读的</font>，如果需要进行写操作，必须在该镜像上创建一个新的镜像层（我们所有的写操作其实都是在一个可写的镜像层上操作的）。同时镜像也是共享的，那些依赖于同一个 image 的多个容器，并不会将单独复制需要的镜像到自己的容器中进行启动，那样会浪费巨大的空间，实际节点上只会有一个镜像，多个容器是共享这一个镜像的。存储驱动可以将容器中的数据进行持久化同时避免性能问题。存储驱动允许我们在容器的可写层创建数据，这些数据在容器销毁后也就没有了，且这些文件的读写速度都比本地文件系统的性能低。</p><p>&emsp;默认情况下，容器内所有创建的文件都存储在一个可写层，所以这些文件仅仅存活于容器运行时，一旦容器被销毁这些文件也会被销毁。容器的可写层和宿主机器紧密耦合，很难将可写层的文件或数据迁移到非宿主机器外的地方。要将数据写入容器的可写层必须要有一个存储驱动（storage driver）来管理文件系统，存储驱动程序使用Linux内核提供的联合文件系统，这种方式与使用直接写入主机文件系统的数据卷相比降低了性能。Docker提供几种种持久化容器中数据或文件的方式：</p><ul><li><code>volumes</code>：Volumes是持久化Docker中数据最好的方式，存在于宿主机器文件系统的一部分（Linux中位于<code>/var/lib/docker/volumes/</code>目录下的<code>xx.db</code>文件），由Docker自己管理，非Docker进程去修改这部分的文件；</li><li><code>bind mounts</code>：这种方式可以将容器中的数据持久化到宿主机器的任何位置（文件或目录），任何进程都可以进行修改</li><li><code>tmpfs mount</code>：Linux 上专用，tmpfs挂载仅存储在主机系统的内存中，永远不会写入主机系统的文件系统；</li><li><code>named pipe</code>：windowns 上专用，同上；</li></ul><p>关于上述的几种方式的区别，官网形象的图文解释：</p><p><img src="https://img-blog.csdnimg.cn/20200109193632702.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2phY2tzb25hcnk=,size_16,color_FFFFFF,t_70" alt="几种持久化方式"></p><h3 id="1-Volumes（最推荐的挂载方式）"><a href="#1-Volumes（最推荐的挂载方式）" class="headerlink" title="1. Volumes（最推荐的挂载方式）"></a>1. Volumes（最推荐的挂载方式）</h3><p>&emsp;Volumes（数据卷） 由 Docker 自行创建和管理，可以使用<code>docker volume create</code>显式的创建一个Volume，当然如果不人为创建，Docker会在创建容器或服务时自行创建。在使用<code>docker volume create xxx</code>创建 Volume 时，Docker会在宿主机器的<code>/var/lib/docker/volumes/</code>目录下创建<code>xxx</code>文件夹用于容器数据的持久化。当将<code>xxx</code>的Volume挂载到容器上时，此目录就是容器中对应的目录。这种方式和<code>bind mounts</code>很相似，但 Volumes 方式是和宿主机器核心功能隔离并且是由Docker自己管理的。注意<font color="red">同一个Volume是可以同时挂载到多个容器内</font>的，当没有使用该存储卷的容器时，该存储卷并不会自动销毁（因为主要是解决容器可写层数据持久化的问题，当然不会自动销毁），如果需要删除某个存储卷需要手动调用<code>docker volume prune</code>命令，这个命令将会移除所有宿主机器本地的Volumes（谨慎操作）。</p><p>&emsp;当挂载一个 Volume 时，该存储卷可以进行命名或直接匿名，匿名存储卷首次挂载到容器时并不会显式的给它一个名字，而是Docker给它们一个随机名字，且保证该名字是Docker主机中唯一的，匿名和命名的存储卷唯一的区别就是它们名字。</p><p>&emsp;Volumes 是支持 volume driver 的，它允许使用远程主机的存储卷或者其他云供应商提供的数据卷或者其他的存储形式。</p><p>下面是一个示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 创建数据卷</span></span><br><span class="line">docker volume create testvolumes</span><br><span class="line"><span class="meta">#</span><span class="bash"> 直接拉一个ubuntu镜像来实验</span></span><br><span class="line">docker pull ubuntu</span><br><span class="line"><span class="meta">#</span><span class="bash"> 通过 <span class="string">'-v/--volume 存储卷'</span> 的方式即可挂载  /var/lib/docker/volumes/testvolumes:</span></span><br><span class="line">docker run -it -v testvolumes ubuntu:latest</span><br><span class="line"><span class="meta">#</span><span class="bash"> 在容器中的 /testvolumes/_data 目录下创建空文件 addDatas，退出容器并查看宿主机器的 /var/lib/docker/volumes/testvolumes/_data 创建成功</span></span><br><span class="line">touch addDatas</span><br></pre></td></tr></table></figure><p>&emsp;Volum应用场景包含：</p><ul><li>Volume可以让多容器共享的数据（读写或只读），若不显式创建它，会在被挂载前自动创建，Volume只有在显式移除时才会被移除；</li><li>Docker宿主机不具备目录结构或者文件系统，Voulme 可以帮助将Docker宿主机的配置从容器运行时解耦；</li><li>远程存储，这点应该是最广泛的场景，目前很多应用都“上云”，使用的是云提供商或者远程机器，而不是宿主机器本身的存储；</li><li>Volume可以在备份、还原、迁移Dokcer宿主机发挥很人性化的优势，可以先停止使用某个Volume的容器，然后备份Volume（通常目录为<code>/var/lib/docker/volumes/&lt;volume-name&gt;</code>）即可；</li></ul><p>&emsp;Volume 方式有点注意点：</p><ul><li>当将一个空数据卷（volume）挂载到容器中某个已存在的文件或目录时，那么容器该文件或目录中的内容将会被复制到这个空数据卷中；</li><li>当挂载一个不存在的数据卷时，Docker会自动创建一个空的数据卷，这是预先填充另一个容器所需数据的好方法，比如B容器需要一个数据卷VolumeX，他会去检测VolumeX是否存在，那如果A容器在B容器之前启动并且创建了一个VolumeX，那么B的检测就能通过；</li></ul><p>&emsp;数据卷驱动，当创建数据卷<code>docker volume create xxx</code>或启动容器挂载一个还未创建的数据卷时，此时可以指定一个数据卷驱动。下面是一个示例</p><p>&emsp;数据卷 Volume 支持<code>--mount</code>和<code>--volume/-v</code>的语法，当和服务（即swarm services）一起使用数据卷时，仅支持<code>--mount</code>参数，所以官方也是一直推荐使用<code>--mount</code>，完整的示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 1. 单个容器的示例</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 创建数据卷</span></span><br><span class="line">docker volume create my-vol</span><br><span class="line"><span class="meta">#</span><span class="bash"> 查看数据卷列表</span></span><br><span class="line">docker volume ls</span><br><span class="line"><span class="meta">#</span><span class="bash"> 使用ubuntu镜像创建一个容器devtest，将数据卷my-vol挂载到目录/app下，并且该数据卷是只读的</span></span><br><span class="line">docker run -d --name devtest --mount src=my-vol,dst=/app,readonly ubuntu:latest</span><br><span class="line"><span class="meta">#</span><span class="bash"> 检查容器信息（注意查看 Mounts 字段是否和期望的一样）</span></span><br><span class="line">docker volume inspect devtest</span><br><span class="line"><span class="meta">#</span><span class="bash"> 移除数据卷</span></span><br><span class="line">docker container stop devtest</span><br><span class="line">docker container rm devtest</span><br><span class="line">docker volume rm my-vol</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 2. 容器服务的示例</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 将当前节点进行 swarm 初始化，并且成为master</span></span><br><span class="line">docker swarm init</span><br><span class="line"><span class="meta">#</span><span class="bash"> 创建副本为4的service，service仅支持--mount，不支持-v/--volume的形式</span></span><br><span class="line">docker service create -d --replicas=4 --name devtest-service --mount src=my-vol,dst=/app/test ubuntu:latest</span><br><span class="line"><span class="meta">#</span><span class="bash"> 查看service的状态</span></span><br><span class="line">docker service ps devtest-service</span><br><span class="line"><span class="meta">#</span><span class="bash"> 移除所有 devtest-service 的task</span></span><br><span class="line">docker service rm devtest-service</span><br></pre></td></tr></table></figure><h3 id="2-Bind-mounts"><a href="#2-Bind-mounts" class="headerlink" title="2. Bind mounts"></a>2. Bind mounts</h3><p>&emsp;Bind mounts （绑定加载）的方式在Dokcer早期版本就提供了，相对于 Volumes 方式功能有限。Bind mounts 就是将宿主机器中一个文件或者目录挂载到容器中，挂载时必须以该文件或目录的绝对路径指定，而且指定的宿主机器上的路径不一定非要在宿主机器上存在，Docker在加载时会去按需创建，而且绑定加载的性能非常好，但它依赖于宿主机器上特定的目录结构。所以目前为官网还是推荐使用数据卷（Volumes）的形式来作存储驱动。此外，bind mounts 不能使用使用Dokcer命令行直接管理这些绑定加载（bind mounts）。</p><p>&emsp;此外 bind mounts 方式必须对一些敏感文件（比如host文件）有权限，可以通过运行在容器中的进程修改（增长改查）系统host文件，这个功能可能会引发安全问题，因为此时Dokcer对宿主机器上非Docker进程甚至系统级别的文件都可以随意更改。</p><p>下面是一个示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 通过 `-v/--volume 宿主机目录:容器目录` 的形式将宿主机器的 /root/helm 目录挂载到容器的 /usr/<span class="built_in">local</span>/helm 目录</span></span><br><span class="line">docker run --volume /root/helm:/usr/local/helm -it ubuntu:latest</span><br></pre></td></tr></table></figure><p>&emsp;绑定挂载适用的场景有：</p><ul><li>给容器共享宿主机上的配置文件，这也是 Docker 通过将宿主机上的<code>/etc/resolv.conf</code>挂载到容器内从而给容器提供的默认DNS策略；</li><li>在开发环境的宿主机器和容器之间共享代码或者构建的jar包、war包，比如对于maven项目，直接将服务模块的<code>target</code>目录绑定挂载到容器中，这样就不需要要在容器内再次构建了；</li><li>当确保Docker主机的文件或目录结构与绑定挂载所需的容器目录一致时；</li></ul><p>&emsp;绑定挂载和数据卷同时有一个注意点，如果将一个非空宿主机目录挂载到容器中已存在的文件或者目录，那么容器内的这些文件或目录将会被宿主机的文件或者目录将会被遮挡，就像你把文件存在宿主机的<code>/mnt</code>目录，然后将一个USB驱动也挂载到<code>/mnt</code>，那此时<code>/mnt</code>目录中的内容将会被USB驱动遮挡直到USB驱动取消挂载，被遮挡的文件或目录并不会被移除或修改，只是使用绑定挂载或者数据卷形式时这些文件是不可得的；</p><h3 id="3-tmpfs-mounts"><a href="#3-tmpfs-mounts" class="headerlink" title="3. tmpfs mounts"></a>3. tmpfs mounts</h3><p>&emsp;<code>tmpfs</code>挂载不会持久化在磁盘上持久化，也不会存储在宿主机器上，可以在容器的生命声明周期内使用，用于存储不需要持久化或者一些敏感信息，典型应用在内部，群集服务使用<code>tmpfs</code>挂载将机密安装到服务的容器中。使用<code>--tmpfs</code>参数进行挂载。</p><p>&emsp;<code>tmpfs</code>挂载适用于那些不需要在宿主机器或容器中持久化数据的场景，比如可能为了安全原因或者是为了在application 需要写大量非持久化数据时保证容器的性能（我觉得这个原因靠谱点）。</p><h3 id="4-named-pipes"><a href="#4-named-pipes" class="headerlink" title="4. named pipes"></a>4. named pipes</h3><p>&emsp;<code>npipe</code>挂载可以用于宿主机器和Docker容器的通信，典型应用就是在容器中运行第三方工具时使用<code>named pipe</code>连接 Docker Engine 的 API。</p><p>注：上述4种挂载方式在语法上不太一样，使用<code>-v/--volume</code>来使用，在17.06+可以不区分挂载方式直接使用<code>--mount</code>进行挂载（本来这个参数是用于 swarm services，而单个容器使用<code>-v/--volume</code>），因为之前使用的 <code>-v/--volume</code> 参数名和数据卷Volume的形式一样，很容易误导开发者是转专用于数据挂载方式的参数，而且对于参数的定义较为严格，使用<code>--mount</code>参数语义更加清晰，诸如：<code>docker run -it --mount source=testvolumes,target=/usr/local/helm ubuntu:latest</code>（前提是建立自己的Volume）。</p><h3 id="5-关于语法"><a href="#5-关于语法" class="headerlink" title="5.关于语法"></a>5.关于语法</h3><h4 id="5-1-v-–volume"><a href="#5-1-v-–volume" class="headerlink" title="5.1 -v/–volume"></a>5.1 -v/–volume</h4><p>&emsp;<code>-v/--volume</code>命令实际是有三个参数的，之间以<code>:</code>分隔，而且必须以规定的顺序出现，三个参数的具体含义为：</p><ul><li>第一个参数：对于有名字的数据卷表示数据卷的名字，该名字必须在宿主机上唯一；对于匿名数据卷，第一个参数需要省略；</li><li>第二个参数：挂载到容器内的路径；</li><li>第三个参数（可选）：是一系列以<code>,</code>分隔的参数；</li></ul><h4 id="5-2-–mount"><a href="#5-2-–mount" class="headerlink" title="5.2 –mount"></a>5.2 –mount</h4><p>&emsp;<code>--mount</code>命令参数是由多个<code>&lt;key&gt;=&lt;value&gt;</code>形式的键值对组成，之间以<code>,</code>分隔，不要求顺序，主要参数的键值对有：</p><ul><li><code>type=xxx</code>：<code>type</code>参数用来表示挂载的方式，value可以是<code>volume</code>、<code>bind</code>或者<code>tmpfs</code>；</li><li><code>source/src=xxx</code>：<code>source</code>或者<code>src</code>参数表示数据卷（有名字的，匿名数据卷该参数省略），value为数据卷的名字；</li><li><code>target/destination/dst=xxx</code>：<code>target</code>或者<code>destination</code>或者<code>dst</code>参数表示内容器的挂载路径，value即为容器内的目录；</li><li><code>readonly</code>：表示绑定挂载是否只读<code>bind mount</code>，如果出现（该字段非键值对），那容器内对该文件/目录只具备读的能力；</li><li><code>volume-opt</code>：表示除上述参数之外的其他参数，可以出现多个<code>volume-opt</code>参数，每个都以键值对的形式出现；</li></ul><p>&emsp;如果数据卷驱动可以接受多个以<code>,</code>分隔的数组，那在传参时必须在CSV编译器检查语法时正常通过，为此用双引号<code>&quot;&quot;</code>将<code>volume-opt</code>这个参数对象包裹起来，然后整个<code>--mount</code>参数对象使用单引号<code>&#39;&#39;</code>包裹，示例：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> docker service create \</span></span><br><span class="line">     --mount 'type=volume,src=&lt;VOLUME-NAME&gt;,dst=&lt;CONTAINER-PATH&gt;,volume-driver=local,volume-opt=type=nfs,volume-opt=device=&lt;nfs-server&gt;:&lt;nfs-path&gt;,"volume-opt=o=addr=&lt;nfs-address&gt;,vers=4,soft,timeo=180,bg,tcp,rw"'</span><br><span class="line">    --name myservice \</span><br><span class="line">    &lt;IMAGE&gt;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;底层原理不懂就上手，上手出了问题就懵逼，最近在对接阿里云时遇到Docker存储驱动的神坑，爬了几天爬不出来，最后发现是节点中Docker存储驱动的问题，由此引发此次学习，避免类似问题再次懵逼。&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="docker" scheme="https://jacksonary.github.io/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>Redis中的Lua脚本</title>
    <link href="https://jacksonary.github.io/posts/5e479bdd.html"/>
    <id>https://jacksonary.github.io/posts/5e479bdd.html</id>
    <published>2019-12-10T00:47:00.000Z</published>
    <updated>2021-06-04T12:41:01.285Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>最近琢磨分布式锁时接触到的知识点，简单记一下。</p></blockquote><p>&emsp;Redis支持Lua，代码直接发送完整脚本即可。基本语法（redis客户端可以直接执行）：<a id="more"></a></p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&gt; eval <span class="string">"return &#123;KEYS[1],KEYS[2],ARGV[1],ARGV[2]&#125;"</span> <span class="number">2</span> key1 key2 first second</span><br><span class="line"><span class="number">1</span>) <span class="string">"key1"</span></span><br><span class="line"><span class="number">2</span>) <span class="string">"key2"</span></span><br><span class="line"><span class="number">3</span>) <span class="string">"first"</span></span><br><span class="line"><span class="number">4</span>) <span class="string">"second"</span></span><br></pre></td></tr></table></figure><p>注：<code>{KEYS[1],KEYS[2],ARGV[1],ARGV[2]}</code>都是一系列的 key -value 的占位符（<code>KEYS</code>和<code>ARGV</code>都是全局变量），根据实际情况编写，可以是很多这样形式变量。后面的<code>2 key1 first key2 second</code>，首位数字<code>2</code>表示的是 Key 的数量，后面的变量和前面的占位符意义对应（<code>key1</code>对应于<code>KEYS[1]</code>，执行时<code>key1</code>自动注入）</p><h2 id="2-利用Lua操作Redis"><a href="#2-利用Lua操作Redis" class="headerlink" title="2. 利用Lua操作Redis"></a>2. 利用Lua操作Redis</h2><p>&emsp;上述执行借用redis的客户端执行lua脚本，要真正对redis进行读写操作，需要调用<code>redis.call()</code>函数或者<code>redis.pcall()</code>函数，这两个函数比较相似，但如果发生错误时，<code>redis.call()</code>函数将引发一个Lua错误，这又将迫使<code>EVAL</code>向命令调用者返回错误；而<code>redis.pcall()</code>则会捕获异常并返回Lua异常的某个信息码。使用示例如下：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 同样的最后的 <span class="number">0</span> 表示的全局变量 KEYS 中有<span class="number">0</span>个元素 key</span><br><span class="line">eval <span class="string">"return redis.call('set','foo','bar')"</span> <span class="number">0</span></span><br></pre></td></tr></table></figure><p>向redis中写入一个 key 为<code>foo</code>，value为<code>bar</code>的对象。但<code>eval</code>命令将会验证这条命令的语法，具体的验证方式为：将脚本中所有的key替换为全局变量数据<code>KEYS</code>，所以最终验证语法的脚本为<code>eval &quot;return redis.call(&#39;set&#39;,KEYS[1],&#39;bar&#39;)&quot; 1 foo</code>。</p><p>上述2种方式调用出错的情况<br><img src="https://img-blog.csdnimg.cn/20191210163336744.png" alt="call和pcall调用出错的情况"></p><p>当使用<code>redis.call()</code>函数或者<code>redis.pcall()</code>函数操作redis时，Redis返回的值对象将会被转换成Lua语言中的数据类再返回。相似了当执行<code>redis.call()</code>函数或者<code>redis.pcall()</code>函数时，Lua变量的数据类型将会被转成Redis中支持的数据类型（string,list,set,hash,Sorted Set）。</p><h2 id="3-Lua脚本的原子性"><a href="#3-Lua脚本的原子性" class="headerlink" title="3. Lua脚本的原子性"></a>3. Lua脚本的原子性</h2><p>&emsp;Lua脚本在redis中的操作是原子性，redsi使用同一个Lua解释器执行脚本中的所有命令，Redis本身也保证了这个脚本执行的原子性：当该脚本在执行时间区间内，不会有其他的脚本或者命令执行。所以使用Lua执行一个慢脚本是一个很扯的事情（除非你很清楚这样的慢脚本是十分有必要的），一般情况下，因为脚本的开销非常低会很快。</p><h2 id="4-关于-EVALSHA"><a href="#4-关于-EVALSHA" class="headerlink" title="4. 关于 EVALSHA"></a>4. 关于 EVALSHA</h2><p>&emsp;<code>EVAL</code>命令会迫使我们反复发送脚本，但Redis不需要每次都重新编译脚本（因为自己内部的缓存机制），但每次发送脚本需要耗费额外的带宽在很多场景中并不是一种友好的方式。另一方面，使用特殊命令或者通过<code>redis.conf</code>来定义命令也会有一些问题：</p><ul><li>不同的实例可能有该命令的不同实现；</li><li>如果需要确保所有的实例都包含所给的命令，那部署将会很困难，尤其是在分布式环境中；</li><li>读取应用程序代码后，由于应用程序将调用定义在服务器端的命令，因此完整的语义可能不清楚；</li></ul><p>为了解决上述的问题，Redis实现了<code>EVALSHA</code>命令，<code>EVALSHA</code>命令和<code>EVAL</code>命令很相似，但<code>EVALSHA</code>没有以脚本本身作为第一个参数，而是将该脚本的SHA1摘要作为第一个参数，具体行为：</p><ol><li>如果服务端仍然记得和 SHA1 摘要匹配的脚本，那直接执行脚本；</li><li>如果忘了和 SHA1 摘要匹配的脚本，那将会抛出一个异常告诉客户端，使用<code>EVAL</code>命令来执行，不要使用<code>EVALSHA</code>；</li></ol><p>所以客户端最好的方式还是使用<code>EVALSHA</code>命令来执行脚本（即使客户端使用<code>EVAL</code>，脚本实际已经被服务端看到，如果返回<code>NOSCRIPT</code>的错误就会在使用<code>EVAL</code>命令）。</p><p>脚本缓存机制：已执行的脚本会永远存在于所执行的Redis实例的脚本缓存中。这就意味着如果一个<code>EVAL</code>在Redis实例中执行，则后续所有的<code>EVALSHA</code>命令都会调用成功。显式的调用<code>SCRIPT FLUSH</code>将会刷新脚本缓存，也会清除到目前为止所有执行过的脚本（这也是清除脚本缓存的唯一方式）。</p><h2 id="5-常用SCRIPT-命令"><a href="#5-常用SCRIPT-命令" class="headerlink" title="5. 常用SCRIPT 命令"></a>5. 常用<code>SCRIPT</code> 命令</h2><p>&emsp;Redis中脚本本身操作的命令有：</p><ul><li><code>SCRIPT FLUSH</code>：清除Redis中到目前为止所有执行过的脚本缓存；</li><li><code>SCRIPT EXISTS sha1 sha2 ... shaN</code>：验证脚本是否存在缓存中，返回会对应个数的0（缓存中不存在）和1（缓存中存在）；</li><li><code>SCRIPT LOAD script</code>：将脚本注册进入服务端的存储而不需要执行，确保<code>EVALSHA</code>可以找到对应的脚本正常执行，返回该脚本的SHA1加密值；</li><li><code>SCRIPT KILL</code>：打断正在执行的脚本；</li></ul><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 注册脚本</span><br><span class="line">script <span class="built_in">load</span> <span class="string">"return &#123;KEYS[1],KEYS[2],ARGV[1],ARGV[2]&#125;"</span></span><br><span class="line"># 返回sha1值</span><br><span class="line"><span class="string">"a42059b356c875f0717db19a51f6aaca9ae659ea"</span></span><br></pre></td></tr></table></figure><h2 id="6-脚本本地化"><a href="#6-脚本本地化" class="headerlink" title="6. 脚本本地化"></a>6. 脚本本地化</h2><p>&emsp;可以本地编写Lua脚本，直接通过客户端调用<code>redis-cli --eval xxx.lua</code>执行执行<code>xxx.lua</code>脚本。如实际Linux中操作经常用到<code>redis-cli -h hostname -p port -a password SCRIPT LOAD &quot;$(cat lua_script_file_location)&quot;</code>类似的命令将指定脚本缓存到Redis的服务端，已便下次直接通过返回的摘要直接执行脚本。<br>t lua_script_file_location)”`类似的命令将指定脚本缓存到Redis的服务端，已便下次直接通过返回的摘要直接执行脚本。</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;最近琢磨分布式锁时接触到的知识点，简单记一下。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&amp;emsp;Redis支持Lua，代码直接发送完整脚本即可。基本语法（redis客户端可以直接执行）：
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Redis" scheme="https://jacksonary.github.io/tags/Redis/"/>
    
      <category term="Lua" scheme="https://jacksonary.github.io/tags/Lua/"/>
    
  </entry>
  
  <entry>
    <title>Java中几种锁</title>
    <link href="https://jacksonary.github.io/posts/6f26ce44.html"/>
    <id>https://jacksonary.github.io/posts/6f26ce44.html</id>
    <published>2019-11-20T00:46:00.000Z</published>
    <updated>2021-06-04T12:38:06.951Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>文章内容来自并发编程网的文章阅读，部分示例代码已修改。</p></blockquote><h2 id="1-自旋锁"><a href="#1-自旋锁" class="headerlink" title="1. 自旋锁"></a>1. 自旋锁</h2><p>&emsp;自旋锁就是它的名字一样，让当前线程不停的在一个循环体内执行，当循环的条件被其他线程改变时才能进入临界区。自旋锁的示例代码（仅以非公平锁为例）如下：<br><a id="more"></a><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpinLock</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 临界区的owner</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> AtomicReference&lt;Thread&gt; owner = <span class="keyword">new</span> AtomicReference&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Thread current = Thread.currentThread();</span><br><span class="line">        <span class="comment">// CAS原子操作预测原来的值为空，在循环体内空循环</span></span><br><span class="line">        <span class="keyword">while</span> (!owner.compareAndSet(<span class="keyword">null</span>, current)) &#123;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Thread current = Thread.currentThread();</span><br><span class="line">        <span class="comment">// CAS原子操作预测原来的值为当前线程</span></span><br><span class="line">        owner.compareAndSet(current, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>当第一个线程调用<code>lock()</code>时，临界区的owner为空，没有线程占用，原值为空，CAS将owner置为当前线程A，此时第二个线程再来调用<code>lock()</code>时，由于临界区的owner此时是第一个线程A，不为空，因此<code>while()</code>中的条件<code>owner.compareAndSet(null, current)</code>由于预测（<code>null</code>）和实际（线程A）不符不会设置新的owner，返回<code>false</code>（取反为<code>true</code>），所以一直为在那里空循环。直到第一个线程A调用<code>unlock()</code>方法将临界区的<code>owner</code>置为null，第二个线程会退出空循环成为临界区的新owner。</p><p>注：自旋锁只是将当前线程不停的执行空循环，不进行线程状态的改变，所以响应速度很快，但当线程数不断增加时，性能会明显下降，因为每个线程都要自旋，占用CPU时间。若线程竞争不激烈，并保持锁的时间段，比较适合使用自旋锁。</p><h3 id="1-1-常见3类自旋锁"><a href="#1-1-常见3类自旋锁" class="headerlink" title="1.1 常见3类自旋锁"></a>1.1 常见3类自旋锁</h3><p>&emsp;自旋锁中有3种常见的锁形式：TicketLock，CLHlock 和 MCSlock。TicketLock主要解决的访问顺序的问题；CLHlock和MCSlock则是两类类似的公平锁，以链表形式进行排序。其中CLHlock会不停的查询前驱变量（隐式队列），所以它不适合在NUMA架构（在该结构下，每个线程分布在不同的物理内存区域）下使用，CLHLock的全称为Craig, Landin, and Hagersten lock，属于自旋锁的一种，它可以确保无饥饿，提供先入先出的公平按序服务，基于链表实现。MCSLock则是对本地变量的节点进行循环（显式队列），不存在CLHlock的问题。重入锁JUC ReentrantLock默认就是使用的CLH锁。3种锁的示例代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/10/16 9:11</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 自旋锁的一种 由于每次都要查询ServiceNum服务号会影响性能（必须要到主存读取并阻止其他cpu修改）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TicketLock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> AtomicInteger serviceNum = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line">    <span class="keyword">private</span> AtomicInteger ticketNum = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;Integer&gt; LOCAL = <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> myTicket = ticketNum.getAndIncrement();</span><br><span class="line">        LOCAL.set(myTicket);</span><br><span class="line">        <span class="keyword">while</span> (myTicket != serviceNum.get()) &#123;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> myTicket = LOCAL.get();</span><br><span class="line">        serviceNum.compareAndSet(myTicket, myTicket + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/10/16 19:54</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> CLHLock 会不停查询前驱变量</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CLHLock</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 在创建节点时，初始化行为就将标识符置为 true，表示该节点需要锁，</span></span><br><span class="line"><span class="comment">    * 只要新创建节点就默认表示它需要同步锁</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CLHNode</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">boolean</span> isLocked = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> CLHNode tail;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;CLHNode&gt; LOCAL = <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line">    <span class="comment">// 采用链表的形式</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> AtomicReferenceFieldUpdater&lt;CLHLock, CLHNode&gt; UPDATER = AtomicReferenceFieldUpdater.newUpdater(CLHLock.class, CLHNode.class, <span class="string">"tail"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 1. 创建一个CLHNode</span></span><br><span class="line">        CLHNode node = <span class="keyword">new</span> CLHNode();</span><br><span class="line">        LOCAL.set(node);</span><br><span class="line">        <span class="comment">// 2. 线程对tail调用 getAndSet 方法使自己成为 tail，同时获取它前驱节点的引用 preNode</span></span><br><span class="line">        CLHNode preNode = UPDATER.getAndSet(<span class="keyword">this</span>, node);</span><br><span class="line">        <span class="comment">// 3. 在前驱节点 preNode 上进行旋转，直至前驱节点释放锁</span></span><br><span class="line">        <span class="keyword">if</span> (preNode != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">while</span> (preNode.isLocked) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 4. 将前驱节点置为 null</span></span><br><span class="line">            preNode = <span class="keyword">null</span>;</span><br><span class="line">            LOCAL.set(node);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        CLHNode node = LOCAL.get();</span><br><span class="line">        <span class="comment">// 释放锁时，将当前节点的 isLocked 标识符置为false，表示此时该线程不需要锁</span></span><br><span class="line">        <span class="keyword">if</span> (!UPDATER.compareAndSet(<span class="keyword">this</span>, node, <span class="keyword">null</span>)))&#123;</span><br><span class="line">            node.isLocked = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 将 node 节点置为 null</span></span><br><span class="line">        node = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/10/17 10:46</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> MCSLock是对本地变量的节点进行循环</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MCSLock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MCSNode</span> </span>&#123;</span><br><span class="line">        <span class="keyword">volatile</span> MCSNode next;</span><br><span class="line">        <span class="keyword">volatile</span> <span class="keyword">boolean</span> isLocked = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;MCSNode&gt; NODE = <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line">    <span class="meta">@SuppressWarnings</span>(<span class="string">"unused"</span>)</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> MCSNode queue;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> AtomicReferenceFieldUpdater&lt;MCSLock, MCSNode&gt; UPDATER = AtomicReferenceFieldUpdater.newUpdater(MCSLock.class, MCSNode.class, <span class="string">"queue"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        MCSNode current = <span class="keyword">new</span> MCSNode();</span><br><span class="line">        NODE.set(current);</span><br><span class="line">        MCSNode preNode = UPDATER.getAndSet(<span class="keyword">this</span>, current);</span><br><span class="line">        <span class="keyword">if</span> (preNode != <span class="keyword">null</span>) &#123;</span><br><span class="line">            preNode.next = current;</span><br><span class="line">            <span class="keyword">while</span> (current.isLocked) &#123;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        MCSNode currentNode = NODE.get();</span><br><span class="line">        <span class="keyword">if</span> (currentNode.next == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (UPDATER.compareAndSet(<span class="keyword">this</span>, currentNode, <span class="keyword">null</span>)) &#123;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">while</span> (currentNode.next == <span class="keyword">null</span>) &#123;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            currentNode.next.isLocked = <span class="keyword">false</span>;</span><br><span class="line">            currentNode.next = <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-阻塞锁"><a href="#2-阻塞锁" class="headerlink" title="2. 阻塞锁"></a>2. 阻塞锁</h2><p>&emsp;阻塞锁会让线程进入阻塞状态，当获得响应的信号时才能进入线程的准备就绪状态，能够和阻塞相关的关键字和方法有：<code>sychronized</code>、<code>ReentrantLock</code>、<code>Object.wait()</code>以及<code>LockSupport.park()/unpark</code>。阻塞锁的代码示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BlockLock</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">BlockNode</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">volatile</span> Thread isLocked;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> BlockNode tail;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;BlockNode&gt; LOCAL = <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> AtomicReferenceFieldUpdater&lt;BlockLock, BlockNode&gt; UPDATER = AtomicReferenceFieldUpdater.newUpdater(BlockLock.class, BlockNode.class, <span class="string">"tail"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        BlockNode node = <span class="keyword">new</span> BlockNode();</span><br><span class="line">        LOCAL.set(node);</span><br><span class="line">        BlockNode preNode = UPDATER.getAndSet(<span class="keyword">this</span>, node);</span><br><span class="line">        <span class="keyword">if</span> (preNode != <span class="keyword">null</span>) &#123;</span><br><span class="line">            preNode.isLocked = Thread.currentThread();</span><br><span class="line">            LockSupport.park(<span class="keyword">this</span>);</span><br><span class="line">            preNode = <span class="keyword">null</span>;</span><br><span class="line">            LOCAL.set(node);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        BlockNode node = LOCAL.get();</span><br><span class="line">        <span class="keyword">if</span> (UPDATER.compareAndSet(<span class="keyword">this</span>, node, <span class="keyword">null</span>)) &#123;</span><br><span class="line">            System.out.println(<span class="string">"unlock\t"</span> + node.isLocked.getName());</span><br><span class="line">            <span class="comment">// 使用 LockSupport.unpark() 阻塞锁</span></span><br><span class="line">            LockSupport.unpark(node.isLocked);</span><br><span class="line">        &#125;</span><br><span class="line">        node = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>阻塞锁的优势在于：阻塞的线程不会占用CPU的时间，但进入时间和恢复时间比自旋锁慢。在竞争激烈的情况下阻塞锁的性能明显高于自旋锁（阻塞的线程不会额外占用CPU的时间），理想场景下，竞争不激烈可以选用自旋锁，响应比较及时，竞争激烈使用阻塞锁。</p><h2 id="3-可重入锁"><a href="#3-可重入锁" class="headerlink" title="3. 可重入锁"></a>3. 可重入锁</h2><p>&emsp;可重入锁，也叫递归锁，本意为一个线程可以重复获取锁，指同一个线程外层函数获取锁后，内层递归函数仍然有获取该锁的代码，但不受影响。在java语言中<code>ReentrantLock</code>和<code>sychronized</code>都是可重入锁。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">TestLock testLock;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    testLock.lock();</span><br><span class="line">    doSth();</span><br><span class="line">    testLock.unLock();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    testLock.lock();</span><br><span class="line">    test1();</span><br><span class="line">    doSth();</span><br><span class="line">    testLock.unLock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在方法<code>test1()</code>和<code>test2()</code>方法中都有对同一个锁<code>testLock</code>获取的行为，如果上述的TestLock不是重入锁，调用<code>test2()</code>方法时肯定会出现死锁问题，但如果它是可重入锁，那没有问题。但不要认为所有的死锁问题都可以使用重入锁来代替非重入锁的方式来解决。给出问题场景：如果<code>test2()</code>和<code>test()1</code>都在操作同一个共享变量，此时 testLock 是可重入锁，在上述2个方法中同时更改该变量，此时就会出现问题。下面是<code>ReentrantLock</code>和<code>sychronized</code>使用的示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 可重入锁测试，结果是每个线程的getter和setter方法都可以输出，没有出现死锁的情况，得出可重入的结论</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/11/1 14:22</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReentrantTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> THREAD_COUNT = <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ThreadPoolExecutor executor = <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">5</span>, <span class="number">10</span>, <span class="number">10</span>, TimeUnit.SECONDS, <span class="keyword">new</span> LinkedBlockingQueue&lt;&gt;(<span class="number">10</span>), <span class="keyword">new</span> MyFactoryBuilder().build());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为了让2种场景下的结果观测更明显，使用串行的方式进行测试（因为懒，所以写到一个main方法内）</span></span><br><span class="line">        CountDownLatch synchronizedCount = <span class="keyword">new</span> CountDownLatch(THREAD_COUNT);</span><br><span class="line">        SynchronizedRunnable synchronizedRunnable = <span class="keyword">new</span> SynchronizedRunnable(synchronizedCount);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; THREAD_COUNT; i++) &#123;</span><br><span class="line">            executor.execute(synchronizedRunnable);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            synchronizedCount.await();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ReentrantLockRunnable reentrantLockRunnable = <span class="keyword">new</span> ReentrantLockRunnable();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; THREAD_COUNT; i++) &#123;</span><br><span class="line">            executor.execute(reentrantLockRunnable);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * synchronized 关键字可重入测试类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SynchronizedRunnable</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> CountDownLatch countDownLatch;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">SynchronizedRunnable</span><span class="params">(CountDownLatch countDownLatch)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.countDownLatch = countDownLatch;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        get();</span><br><span class="line">        countDownLatch.countDown();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SynchronizedRunnable.get(): "</span> + Thread.currentThread());</span><br><span class="line">        set();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SynchronizedRunnable.set(): "</span> + Thread.currentThread());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * ReentrantLock 可重入测试类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ReentrantLockRunnable</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 可重入锁</span></span><br><span class="line">    <span class="keyword">private</span> ReentrantLock myLock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="comment">//     自旋锁，不可重入锁，测试自旋锁的不可重入性使用这个Lock，会出现死锁的现象，在getter获取锁后，其内部函数无法再次获取lock产生自旋，而外层的getter则始终持有锁</span></span><br><span class="line"><span class="comment">//    private SpinLock myLock = new SpinLock();</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        get();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        myLock.lock();</span><br><span class="line">        System.out.println(<span class="string">"ReentrantLockRunnable.get(): "</span> + Thread.currentThread());</span><br><span class="line">        set();</span><br><span class="line">        myLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        myLock.lock();</span><br><span class="line">        System.out.println(<span class="string">"ReentrantLockRunnable.set(): "</span> + Thread.currentThread());</span><br><span class="line">        myLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可重入锁重点解决的问题是死锁。</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;文章内容来自并发编程网的文章阅读，部分示例代码已修改。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;1-自旋锁&quot;&gt;&lt;a href=&quot;#1-自旋锁&quot; class=&quot;headerlink&quot; title=&quot;1. 自旋锁&quot;&gt;&lt;/a&gt;1. 自旋锁&lt;/h2&gt;&lt;p&gt;&amp;emsp;自旋锁就是它的名字一样，让当前线程不停的在一个循环体内执行，当循环的条件被其他线程改变时才能进入临界区。自旋锁的示例代码（仅以非公平锁为例）如下：&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Java" scheme="https://jacksonary.github.io/tags/Java/"/>
    
      <category term="锁" scheme="https://jacksonary.github.io/tags/%E9%94%81/"/>
    
      <category term="并发编程" scheme="https://jacksonary.github.io/tags/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>并发控制笔记</title>
    <link href="https://jacksonary.github.io/posts/5f1eaa70.html"/>
    <id>https://jacksonary.github.io/posts/5f1eaa70.html</id>
    <published>2019-11-13T01:25:52.000Z</published>
    <updated>2021-06-04T12:32:41.026Z</updated>
    
    <content type="html"><![CDATA[<p>线程这一块是前段时间面试过程中遇到很多的问题，学校里面接触有限，吃了一些亏，抽空最近在看并发编程编程的挑战这本书并作一些简单记录。有两个常见的概念：</p><ul><li>临界资源：临界资源是一次仅允许一个进程使用的共享资源，各进程之间对共享资源的使用方式是互斥的；（常罗列的临界资源硬件上有打印机、磁带机，软件上有消息队列等）</li><li>临界区：每个进程中访问临界资源的那段代码就是临界区。每次只允许一个进程进入临界区，且此时其他进程无法进入。多个进程涉及到同一个临界资源的的临界区称为相关临界区；</li></ul><p>注：多线程操作共享资源时就可以理解为使用临界区，一般不允许线程占用临界区的时间太长，只要运行在临界区的线程还没有离开，其他进入此临界区的线程都会被挂起处于等待状态，影响程序的运行性能。<br><a id="more"></a></p><h2 id="1-多线程"><a href="#1-多线程" class="headerlink" title="1. 多线程"></a>1. 多线程</h2><p>&emsp;并发的目的是为了让程序更快的执行，但是有一点必须明确的是，<font color="red">并不是多线程就一定比单线程运行的快！</font>我记得这个问题是当初面试群硕时面试官提的问题，逐渐向后挖掘这个问题并不局限于软件问题了。这里注意：</p><ul><li>上下文的切换：单核CPU也是支持多线程，通过给各个线程分配时间片完成，所以就涉及到每个线程切换之前需要记录该线程的任务状态（为了下次切回这个线程继续执行，这就是创建上下文的过程）。这样的任务从保存到再加载就是一个上下文的切换过程，多线程不可避免的会多出一部分时间来做上下文的创建和切换；</li><li>影响多线程效率的因素：多线的运行效率受限于上下文的切换（切换次数和时长，可以用Lmbench和vmstat查看）、死锁、硬、软件资源等多种问题的影响；</li></ul><p>【总结】：多线程不一定比单线程执行快，受限于上下文的创建、切换、硬件资源等因素。</p><p><strong>如何减少上下文切换次数</strong><br></p><p>&emsp;由于频繁切换上下文会影响线程效率，所以可以着手减少上下文切换的次数来提高效率，方法主要有：</p><ul><li>无锁并发：多线程在竞争锁时，会引起上下文的切换，所以可以在多线程中可以避免使用锁（使用其他方法来替代，如将ID按照Hash算法取模分段，然后不同的线程处理不同段的数据）；</li><li>CAS算法：concurrent.atomic包使用CAS算法更新数据，不需要加锁；</li><li>最小线程：避免创建不必要的线程，如任务较少却创建了很多线程，这种行为将造成大量线程处于等待状态；</li><li>协程：在单线程里实现多任务的调度，并在单线程中维持多任务间的切换；</li></ul><p>注：上述的几种方式，多线程中除使用锁保证线程安全外还可以使用的其他几种方法。</p><p><strong>避免死锁</strong><br></p><p>&emsp;在复杂业务场景下，可能出现死锁，A线程由于某些原因没有释放锁，锁一直被A线程持有，那其他需要相同锁的线程都会持续等待，这样整个业务可能会因此宕掉，为了避免死锁的发生，有些常见的方法：</p><ul><li>避免在一个线程中同时获取多个锁；</li><li>避免在一个线程在锁内同时占用多个资源，尽量每个锁只占用一个资源；</li><li>尽量使用定时锁，使用<code>locak.tryLock(long time, TimeUnit unit)</code>替代内部锁机制；</li><li>数据库锁，加锁、释放锁必须在一个数据库连接里，否则释放锁可能失败；</li></ul><h2 id="2-并发原理"><a href="#2-并发原理" class="headerlink" title="2. 并发原理"></a>2. 并发原理</h2><h3 id="2-1-volatile"><a href="#2-1-volatile" class="headerlink" title="2.1 volatile"></a>2.1 volatile</h3><p>&emsp;可以理解为轻量级的<code>sychronized</code>，因为<code>volatile</code>不会引起上下文的切换和调度，可以保证该关键字修饰的变量在内存中的改变对于其他线程而言是即时可见的（即共享变量的“可见性”）。如果某个变量被声明为<code>volatile</code>，Java线程内存模型确保所有线程看到这个变量的值是一致的。书中对于<code>volatile</code>有如下描述：</p><blockquote><p>Java编程语言允许线程访问共享变量，为了确保共享变量能准确和一致地更新，线程应该确保通过排他锁单独获取这个变量。</p></blockquote><h3 id="2-2-synchronized"><a href="#2-2-synchronized" class="headerlink" title="2.2 synchronized"></a>2.2 synchronized</h3><p>&emsp;相对于<code>volatile</code>而言可以理解为重量级的锁，主要用于同步方法和同步代码块。但随着Java的优化也没有之前那么重了，在1.6+版本中为了减少获取锁、释放锁的性能消耗引入了偏向锁、轻量级锁、锁的存储结构和升级过程。</p><p>&emsp;<code>synchronized</code>经常在同步方法或者同步块中使用，Java中每个对象都可以作为锁，有如下3种形式：</p><ul><li>普通同步方法，锁对象是当前实例对象；</li><li>静态同步方法，锁对象是当前类的Class对象；</li><li>同步方法块，锁是<code>synchronized(A)</code>中的对象A；</li></ul><p>注：注意上述普通方法和静态方法加锁的区别，面试中经常会问。</p><h3 id="2-3-锁升级"><a href="#2-3-锁升级" class="headerlink" title="2.3 锁升级"></a>2.3 锁升级</h3><p>&emsp;在Java不断优化的过程中，引入了偏向锁和轻量级锁的概念，锁共有4种状态，级别从低到高依次为：</p><ol><li>无锁状态；</li><li>偏向锁状态；</li><li>轻量级锁状态；</li><li>重量级锁状态；</li></ol><p>上述的4种锁状态会随着竞争情况逐渐升级，但锁不能降级（只能升级不能降级），锁不能降级的机制是为了提高锁的获得、释放的效率。各自的优缺点如下：</p><table><thead><tr><th>锁类</th><th>优点</th><th>缺点</th><th>适用场景</th></tr></thead><tbody><tr><td>偏向锁</td><td>加锁、释放锁不需要额外的消耗，和执行非同步方法相比只存在纳秒级别的差距</td><td>如果线程间存在锁竞争会有额外消耗</td><td>适用于只有一个线程访问同步块的场景</td></tr><tr><td>轻量级锁</td><td>竞争的线程不会阻塞，可以提高线程的响应速度</td><td>如果线程得不到锁将会自旋，消耗CPU</td><td>适用于追求响应速度、同步块执行速度非常快</td></tr><tr><td>重量级锁</td><td>线程竞争不使用自旋，不会消耗CPU</td><td>线程阻塞，响应慢</td><td>适用于追求吞吐量、同步块执行速度较慢</td></tr></tbody></table><h4 id="2-3-1-偏向锁"><a href="#2-3-1-偏向锁" class="headerlink" title="2.3.1 偏向锁"></a>2.3.1 偏向锁</h4><p>&emsp;很多情况中，多线程会竞争锁，而且锁会由同一个线程多次获取，<font color="red">为了降低线程获取锁的代价</font>引入了偏向锁。当一个线程访问同步块并获取锁时，会在对象头和栈帧中的锁记录中存储锁偏向的线程ID，以后该线程在进入和退出同步块时不需要进行CAS操作来加锁、释放锁，只需要简单地测试对象头中Mark Word中是否存储着指向当前线程的偏向锁，如果测试失败，就再测试一下Mark Word中偏向锁的标识是否设置为1（标识当前是偏向锁）：如果没有设置则使用CAS竞争锁，否则尝试使用CAS将对象头的偏向锁指向当前线程。偏向锁的机制：等到竞争出现才会释放锁，当其他线程尝试竞争偏向锁时，持有偏向锁的线程才会释放锁。</p><p>注：偏向锁在JDK6、7中默认启用，但它在应用程序启动几秒后才会激活，如果确定程序中所有的锁基本都处于竞争状态，可以通过JVM参数关闭偏向锁：<code>-XX:-UseBiasedLocking=false</code>，程序会默认进入轻量级锁状态。</p><h4 id="2-3-2-轻量级锁"><a href="#2-3-2-轻量级锁" class="headerlink" title="2.3.2 轻量级锁"></a>2.3.2 轻量级锁</h4><p>&emsp;线程在执行同步块之前，JVM当前线程的栈帧中创建存储锁记录的空间并将对象头中的Mark Word复制到锁记录中（Displaced Mark Word）。然后线程尝试使用CAS对象头中的Mark Word替换为指向锁纪录的指针，如果成功，当前线程获得锁，如果失败，其他线程竞争锁，当前线程尝试使用自旋来获取锁。</p><p>&emsp;在轻量级锁释放锁时，将使用CAS操作把Displaced Mark Word替换到对象头，如果成功表示没有竞争发生，如果失败，表示当前锁存在竞争，锁将会膨胀为重量级锁。</p><p>&emsp;自旋会消耗CPU，因为已获取锁的线程由于某些原因被阻塞，那在等待锁的其他线程会一直处于自旋状态，为了避免无用的自旋，一旦锁升级为重量级锁，就不会恢复到轻量级锁，当锁处于这个状态下，其他线程尝试获取这个锁时就会被阻塞，当持有锁的线程释放锁后会唤醒这些线程，被唤醒的线程会进行新的一轮锁竞争。</p><h3 id="2-4-关于原子操作"><a href="#2-4-关于原子操作" class="headerlink" title="2.4 关于原子操作"></a>2.4 关于原子操作</h3><p>&emsp;原子操作，原子在已经是最小粒子，所以原子操作也就表示不可中断的一个或一系列的操作。在Java中，可以通过如下的方式实现原子操作：</p><ul><li>锁：锁保证了只有获得锁的线程才能操作锁定区域的内存，在JVM中除了偏向锁外，其他所有的锁都使用了循环CAS方式来释放锁；</li><li>CAS：java中的自旋CAS操作就是循环进行CAS操作直到成功为止；</li></ul><p>注：目前java并发包<code>java.util.concurrent.atomic</code>中有很多类支持原子操作，诸如<code>AtomicBoolean</code>、<code>AtomicInteger</code>等。</p><h4 id="2-4-1-CAS（比较交换-Compare-And-Swap）"><a href="#2-4-1-CAS（比较交换-Compare-And-Swap）" class="headerlink" title="2.4.1 CAS（比较交换 Compare And Swap）"></a>2.4.1 CAS（比较交换 Compare And Swap）</h4><p>&emsp;CAS 是 Compare And Swap 的简写，即比较交换。标准的CAS操作需要2个输入值，一个是操作前期望的值、一个是新值，在CAS操作过程中先比较旧值是否发生变化，如果传入的期望值和实际获取的值不相等（即发生了变化），就不设置新值，如果相等则设置为新值。对应于Java中的<code>compareAndSet(expect,update)</code>方法，返回一个<code>boolean</code>值，底层调用<code>compareAndSwapInt(this, valueOffset, expect, update)</code>方法，第一个参数表示变量的内存位置，它完成的作用是在<code>set</code>时先比较expect值和valueOffset内存中的值是否相等，如果相等就将valueOffset内存中的值更新为值update，否则不执行任何操作，从而保证了原子性操作，<code>java.util.concurrent.atomic</code>包中的很大一部分的类都是使用了CAS操作，可以翻一下源码，比如<code>AtomicInteger</code>类的<code>getAndUpdate</code>方法底层的实现代码如下</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/20191113_getAndUpdate.png" alt="这里写图片描述"></p><p><strong>【CAS操作中存在的问题】</strong><br></p><p>&emsp;CAS方式虽然解决了原子性操作问题，但也存在下面三个基本问题：</p><ol><li>ABA问题；</li><li>循环时间长、开销大；</li><li>只能保证一个共享变量的原子操作；</li></ol><p><strong>1.ABA问题</strong><br></p><p>&emsp;CAS操作时，会首先检查该内存上的值有没有发生变化，如果没有变化则更新，否则不更新，但是值的更新存在一个问题：比如原来的值为A，一个人改成了B，然后一混蛋偷偷又改成了A，这时候CAS在检查时发现该内存中值没有改变，这就混淆视听了，CAS就不会去更新这个值。要防止ABA问题的发生，可以在变量前面追加版本号，每次更新版本号加1，那么上述ABA的流程就变成了1A–&gt;2B–&gt;3A。当然，JDK1.5 以后提供了<code>AtomicStampedReference</code>类解决ABA问题，它的<code>compareAndSet</code>方法可以首先检查当前引用是否等于预期标志，只有全部相等才会以原子方式将该引用和该标志的值设置为给定的更新值。源码如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/20191113_compareAndSet.png" alt="这里写图片描述"></p><p><strong>2.循环时间长、开销大</strong><br></p><p>&emsp;自旋CAS操作如果长时间不成功，那会处于自旋状态，长时间的自旋会给CPU带来非常大的执行开销。</p><p><strong>3.只能保证一个共享变量的原子操作</strong><br></p><p>&emsp;通过循环CAS的方法可以保证<font color="red">单个共享变量</font>的原子操作，但对于<font color="red">多个共享变量</font>无法保证它们的原子操作，对于多个共享变量的原子操作，提供下面2种的方案：</p><ul><li>通过加锁；</li><li>将多个共享变量合并为一个共享变量来操作，比如两个共享变量<code>a=1,b=2</code>，那么可合并为ab=12,然后直接对ab进行CAS操作，由于1.5以后有了<code>AtomicStampedReference</code>，可以将多个变量放到一个对象里进行操作；</li></ul><h2 id="3-内存模型"><a href="#3-内存模型" class="headerlink" title="3. 内存模型"></a>3. 内存模型</h2><h3 id="3-1-内存模型的基础"><a href="#3-1-内存模型的基础" class="headerlink" title="3.1 内存模型的基础"></a>3.1 内存模型的基础</h3><p>&emsp;线程问题是围绕共享变量展开的，多线程（指并发执行的活动实体）中有2个核心概念：</p><ul><li>通信：指线程之间交换信息的机制，Java中线程之间的通信是通过共享内存的方式（隐式通信）实现的，通信过程对开发人员完全透明；</li><li>同步：<font color="red">指程序中用于控制不同线程之间操作发生相对顺序的机制</font>，在共享内存模型中同步是显式的，开发人员必须通过指定某个方法或者某段代码需要在线程之间互斥执行；</li></ul><p>注：在内存共享并发模型中，线程之间共享程序的公共状态，通过写、读内存中的公共状态进行<font color="red">隐式通信</font>，相对于隐式通信是消息传递模型（命令式编程中另一种线程通信的机制）中的显示通信，即线程之间通过发送消息进行通信。Java中线程间的通信就是通过共享内存的方式进行的，因此Java中线程通信总是隐式进行的，整个通信过程对开发人员完全透明。Java中线程同步是显式的，而在消息传递模型中，同步是隐式的，因为消息的发送必须在消息接收之前。文中也提及如果不理解内存共享模型中的隐式通信，可能会遇见一些内存可见性的问题。第一遍时，对于上述的可见性不是很理解，后来又理了理出现如下的图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%86%85%E5%AD%98%E5%8F%AF%E8%A7%81%E6%80%A7.png" alt="内存可见性"></p><p>&emsp;java内存模型（JMM）决定了一个线程对共享变量的写入何时对另一个线程可见，结合上图，JMM定义了线程和主存之间的抽象关系：线程之间的共享变量存储在主存中，每个线程都有一个私有的本地内存（即上图中各个线程的工作内存），本地内存存储了共享变量的副本。注意：本地内存是JMM中的抽象概念，实际并不存在，准确点说它是一种逻辑存在，涵盖了缓存、写缓存区、寄存器、硬件、编辑器优化等。结合上图，线程1和线程2之间要通信的话，必须经历2个步骤：</p><ol><li>线程1将本地内存中的更新过的A1刷回主存中；</li><li>线程2到主存中将已更新的A读取到自己的本地内存中；</li></ol><p>从上述的2个步骤可以看到线程1和线程2通信的方式必须通过主存，JMM就是通过控制主存和各个线程的本地内存之间的交换来为开发人员提供内存可见性的保证。</p><p>&emsp;Java中，所有的实例域、静态域和数组元素都存储在堆内存中，对内存在线程之间共享（线程中的“共享变量”指的就是实例域、静态域和数组元素）。而局部变量、方法定义参数和异常处理参数不会在线程之间共享，它们不会有内存可见性的问题。</p><p>&emsp;在执行程序时，为了提高性能，编译器和处理器通常会对指令作重排序，指令的重排序分为一下的3种：</p><ul><li>编译器优化的重排序（属于编译器重排序）：编译器在不改变单线程程序语义的前提下，重新安排语句的执行顺序；</li><li>指令级并行的重排序（属于处理器重排序）：目前处理器采用指令级并行技术来将多条指令重叠执行，若无数据依赖，处理器可以改变语句对应机器指令的执行顺序；</li><li>内存系统的重排序（属于处理器重排序）：处理器使用缓存和读、写缓冲区，这使得加载和存储操作上可能是乱序执行的；</li></ul><p>在Java中从源代码到最终执行的指令序列都会经历上述3种重排序，这些重排序可能导致多线程出现内存可见性的问题，对于编译器，JMM的编译器会禁止特定类型的编译器重排序。对于处理器重排序，JMM处理器规则会要求Java编译器在生成指令序列时，插入特定类型的内存屏障（Memory Barriers）指令，通过内存屏障指令来禁止特定类型的处理器重排序。</p><p>&emsp;JMM属于语言级别的内存模型，它确保在不同的编译器和不同的处理器平台上，通过禁止特定类型的编译器重排序和处理器重排序，为开发人员提供一致的内存可见性保证。</p><p><strong>关于happens-before规则（JMM的核心，很重要）</strong><br></p><p>&emsp;在JMM中，如果一个操作执行的结果需要对另一个操作可见，那两个行为（可以在一个线程内，也可以在不同线程内）之间必须要存在happens-before关系，happen-before规则如下：</p><ul><li>程序顺序规则：一个线程中的每个操作，happens-before于该线程中的任意后续操作；</li><li>监视器锁规则：对于一个锁的释放，happens-before于随后对这个锁的加锁；</li><li><code>volatile</code>变量规则：对一个<code>volatile</code>域的写，happens-before于后续对这个<code>volatile</code>域的读；</li><li>传递性：如果A happens-before B，且B happens-before C，那A happens-before C；</li></ul><h3 id="3-2-重排序"><a href="#3-2-重排序" class="headerlink" title="3.2 重排序"></a>3.2 重排序</h3><p>&emsp;重排序是指编译器和处理器为了优化程序性能而对指令序列进行重新排序的一种手段。</p><h4 id="3-2-1-数据的依赖性"><a href="#3-2-1-数据的依赖性" class="headerlink" title="3.2.1 数据的依赖性"></a>3.2.1 数据的依赖性</h4><p>&emsp;在2种操作同时访问同一个变量，且两个操作中有一个操作涉及写操作，那就称这两个操作之间存在数据依赖。数据依赖的类型分为下面的三种：写后读、写后写、读后写，只要对2个操作的执行顺序进行重排序，程序的执行结果就会改变。注：数据依赖性针对的是单个处理器中执行的指令序列和单个线程中执行的操作，不同处理器之间和不同线程之间的数据依赖性不被编译器和处理器考虑。</p><h4 id="3-2-2-as-if-serial"><a href="#3-2-2-as-if-serial" class="headerlink" title="3.2.2 as-if-serial"></a>3.2.2 as-if-serial</h4><p>&emsp;as-if-serial 表示不管怎么重排序，单线程的执行结果不能被改变。编译器、runtime和处理器都必须遵守as-if-serial语义，为了遵守as-if-serial语义，<font color="red">编译器和处理器不会对存在数据依赖关系的操作重排序</font>（因为重排序会改变执行结果）。但如果操作之间不存在数据依赖关系，这些操作可能会被编译器和处理器中重排序。</p><p>&emsp;as-if-serial语义将单线程程序保护起来，这样可以无需担心重排序会干扰程序和内存可见性的问题。</p><p>【总结】重排序对多线程的影响：单线程中，对有数据依赖得操作进行重排序，不会改变执行结果；但在多线程中，对数据依赖得操作进行重排序，可能会改变程序得执行结果。</p><h3 id="3-3-顺序一致性"><a href="#3-3-顺序一致性" class="headerlink" title="3.3 顺序一致性"></a>3.3 顺序一致性</h3><p>&emsp;顺序一致性内存模型是一个理论参考模型，在设计时，处理器和编程语言得内存模型都会以顺序一致性内存模型。当程序未正确同步时就可能出现数据竞争，Java内存模型对于数据竞争的定义为：在一个线程中写一个变量，在另一个线程中读同一个变量，而写和读没有通过同步来排序。当一个多线程能正确同步时，这个程序中是没有数据竞争的。JMM对多线程程序的内存一致性的正确同步有如下保证：</p><blockquote><p>若程序是正确同步的，程序的执行将具有顺序一致性（即程序的执行结果与该程序在顺序一致性模型中的执行结果相同）。这里的同步包含了同步原语（<code>synchronized</code>和<code>volatile</code>以及<code>final</code>）的正确使用。</p></blockquote><p>&emsp;在JMM中，未同步程序是无序的，而且所有线程看到的操作顺序也可能不一样，一个线程在自己本地缓存中修改数据但还未刷到主存前，这个写操作是仅对当前线程可见的，其他线程则是无法看到的，只有在对应的线程将这个变化刷新到主存中其他线程才能看到。即当前线程和其他线程看到的顺序并非一致。</p><h3 id="3-4-volatile的内存语义"><a href="#3-4-volatile的内存语义" class="headerlink" title="3.4 volatile的内存语义"></a>3.4 volatile的内存语义</h3><p>&emsp;当共享变量声明为<code>volatile</code>后，该变量的修改对其他线程是即时可见的，在随后对该变量的所有读、写操作都将会使用一个锁来同步（同步的含义就是控制线程的执行顺序）。如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义为 volatile 变量a</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">volatile</span> <span class="keyword">int</span> a;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.a = a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 上述的getter/setter方法等同于下面的方式</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.a = a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">int</span> <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>锁的 happens-before 规则保证了锁释放和获取锁的两个线程之间的内存可见性，这就可以对<code>volatile</code>变量进行读操作时，总是可以看到任意线程中对改变量的最后写入值。<code>volatile</code>变量具备如下的特性：</p><ul><li>可见性：对于<code>volatile</code>变量的读，总是可以看到任意线程中对该变量的最后写入值；</li><li>原子性：对任意单个<code>volatile</code>变量的读、写具有原子性，但类似于<code>a++</code>这种符合操作不具有原子性；</li></ul><p>&emsp;<code>volatile</code>对于线程的内存可见性的影响比<code>volatile</code>自身的特性更重要，它可以实现线程之间的通信，它的读、写与锁的释放、获取有相同的内存效果：<code>volatile</code>写和对应于锁的释放、读对应于锁的获取。</p><p>&emsp;当一个<code>volatile</code>变量在修改时，JMM会把该线程对本地内存中的共享变量刷新到主存中。当对一个<code>volatile</code>变量读操作时，JMM会把该线程对应的本地内存置为无效，然后从主存中读取共享变量，这样一来其他线程读取的<code>volatile</code>变量就是和主存中共享变量是一致的。</p><p>【<code>volatile</code>读写的内存语义总结】</p><ul><li>线程A对<code>volatile</code>变量进行写操作时，实质上是线程A需要先读这个<code>volatile</code>变量的某个线程发出的消息（即对共享变量修改的消息，Java中是隐式通信）；</li><li>线程B读取<code>volatile</code>变量，实质上是接受了之前某个线程发出的消息（在写这个<code>volatile</code>变量前对共享变量所作的修改）；</li><li>线程A写<code>volatile</code>变量，其他线程B读取这个变量时，实质是线程A通过主存向线程B发送了消息；</li></ul><p>&emsp;为了实现<code>volatile</code>内存语义，JMM会分别限制重排序中的编译器重排序和处理器重排序。</p><p>注：后面省略，看不懂了……</p><h3 id="3-5-锁的内存语义"><a href="#3-5-锁的内存语义" class="headerlink" title="3.5 锁的内存语义"></a>3.5 锁的内存语义</h3><p>&emsp;锁可以让临界区互斥执行。对于临界资源和临界区的概念：</p><ul><li>临界资源：一次仅允许一个线程使用的共享资源，各进程采取互斥方式、实现共享的资源就是临界资源（硬件中打印机、软件中的消息缓存队列等都是临界资源）；</li><li>临界区：每个线程访问临界资源的程序就是临界区；</li></ul><p>&emsp;锁是可以同步，除了可以让临界区互斥外，还可以让释放锁的线程向获取同一个锁的线程发送消息。当线程获取锁时，JMM会把该线程对应的本地内存置为无效，从而使被监视器保护的临界区代码必须从主存中读取共享变量。锁释放和<code>volatile</code>变量写具有相同的内存语义，而锁的获取和<code>volatile</code>变量的读取也具有相同的内存语义。</p><p>【总结】</p><ul><li>线程A释放一个锁，实质上是线程A向接下来将要获取这个锁的某个线程发出了消息（线程A对共享变量所作的修改）；</li><li>线程B获取一个锁，实质上是线程B接受了之前某个线程发出的消息；（在释放这个锁之前对共享变量的修改）；</li><li>线程A释放锁，随后线程B获取这个锁，这个过程是线程A通过主存向线程B发送消息；</li></ul><p>&emsp;在分析可重入锁<code>ReentrantLock</code>时，它的用法基本会经过如下获取锁和释放锁的2个过程：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">ReentrantLock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取锁</span></span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// 做一些操作</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="comment">// 释放锁</span></span><br><span class="line">    lock.unlock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>ReentrantLock</code>的实现依赖于Java同步器框架AbstractQueuedSychronizer（AQS），AQS使用了一个整型的<code>volatile</code>变量（名为<code>state</code>）维护同步状态。<code>RenntrantLock</code>分为公平锁和非公平锁。在创建<code>ReentrantLock</code>对象时，有2种构造器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 默认无参构造器使用的锁是非公平锁</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ReentrantLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    sync = <span class="keyword">new</span> NonfairSync();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过fair参数指定锁的类型</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ReentrantLock</span><span class="params">(<span class="keyword">boolean</span> fair)</span> </span>&#123;</span><br><span class="line">    sync = fair ? <span class="keyword">new</span> FairSync() : <span class="keyword">new</span> NonfairSync();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在ReentantLock加锁时过程如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%8A%A0%E9%94%81%E8%BF%87%E7%A8%8B.png" alt="ReentantLock加锁过程"></p><p>公平锁种<code>tryAcquire</code>方法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 加锁</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">    <span class="comment">// 先获取 volatile 变量 state</span></span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;</span><br><span class="line">            compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">        <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">        <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        setState(nextc);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-6-final-的内存语义"><a href="#3-6-final-的内存语义" class="headerlink" title="3.6 final 的内存语义"></a>3.6 final 的内存语义</h3><p>&emsp;对于 final 域，编译器和处理器需要遵守2个重排规则：</p><ul><li>在构造函数内对一个 final 域的写入，与随后把这个被构造对象的引用赋值给一个引用变量，这两个操作之间不能重排；</li><li>初次读取一个包含 final 对象的引用和随后初次读这个 final 域，两个操作之间不能重排；</li></ul><p>【待重读】</p><h3 id="3-7-双重检验加锁（DCL）和延迟初始化"><a href="#3-7-双重检验加锁（DCL）和延迟初始化" class="headerlink" title="3.7 双重检验加锁（DCL）和延迟初始化"></a>3.7 双重检验加锁（DCL）和延迟初始化</h3><p>&emsp;多线程中，如果需要采用延迟某些类（往往这些类是高开销的类）的初始化来降低初始化类和创建对象的开销，并且只有在使用这些对象时才进行初始化，<strong>双重检验加锁是一种常用的延迟初始化的方式</strong>，但它是一种错误的用法。下面以单例模式作分析：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">            instance  = <span class="keyword">new</span> Singleton();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述的单例模存在线程安全问题，为了解决这个问题，在<code>getInstance()</code>方法上加上<code>sychronized</code>，这样作可以解决上述的问题但会如果<code>getInstance()</code>方法被多线程频繁调用将导致性能开销，使程序性能下降（当然如果该方法不被多线程频繁调用上述的方式可以接受）。然后为了降低上述方式带来的性能开销，<strong>双重检查锁定</strong>出现了，通过双重检查锁定来降低同步的开销，代码为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Singleton instance;          <span class="comment">// 1</span></span><br><span class="line">                                                <span class="comment">// 2</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;     <span class="comment">// 3</span></span><br><span class="line">        <span class="comment">// 第一次检查</span></span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;                 <span class="comment">// 4</span></span><br><span class="line">            <span class="comment">// 加锁</span></span><br><span class="line">            <span class="keyword">synchronized</span> (Singleton.class) &#123;    <span class="comment">// 5</span></span><br><span class="line">                <span class="comment">// 第二次检查</span></span><br><span class="line">                <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;         <span class="comment">// 6</span></span><br><span class="line">                    instance = <span class="keyword">new</span> Singleton(); <span class="comment">// 7</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种方式比之前的优化方式具备的有点是：仅有初次未被实例化时才会进入同步块，而不是像之前那样不管有没有实例化，只要你要获取都必须同步一下。在第一次检查时可能会有多线程进入，但进入后需要经历一个同步块，在同步块中再次检查这样就排除了第一次检查中进入的多个线程的情况。但书中作者提到：</p><blockquote><p>在执行到第4行，代码读取到的 instance 不为 null时，instance 引用的对象可能还没有完成初始化。</p></blockquote><p>在对象初始化的过程中被分解为三个流程：[1.分配对象的内存空间] -&gt; [2.初始化对象] -&gt; [3.设置instance指向刚分配的内存地址]，但这个过程可能会被重排（为了提高程序的执行性能）为：[1.分配对象的内存空间] -&gt; [3.设置instance指向刚分配的内存地址] -&gt; [2.初始化对象]，此时如果一个线程获取锁后进入上述的初始化流程第2步（重排序后的第2步），此时另一个线程过来可以得到<code>instance == null</code>为<code>false</code>，它直接去获取实例对象时可能获取不到，导致作者所提出的问题。为了解决这个问题，2种方法：</p><ol><li>禁止对象初始化时第2、3步骤的重排序：在声明<code>instance</code>变量时加上<code>volatile</code>关键字即可；</li><li>允许第2、3重排序，但不让其他线程看到这个重排序的过程：基于类初始化；</li></ol><p>上述的方法2中使用的方式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingletonFactory</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">InstanceHolder</span> </span>&#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">static</span> Singleton instance = <span class="keyword">new</span> Singleton();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 会自动去初始化 InstanceHolder 类</span></span><br><span class="line">        <span class="keyword">return</span> InstanceHolder.instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那在使用时直接使用<code>SingletonFactory.getInstance()</code>即可获取实例对象，这样以后就部不允许非构造线程看到初始化的重排序（但允许初始化流程被重排序）。使用<code>volatile</code>关键字的方式，除了可以对静态字段实现延迟初始化外，还可以对实例字段实现延迟初始化。所以对于上述2种解决方法也有不同的使用场景：</p><ol><li>使用<code>volatile</code>关键字：需要对实例字段使用线程安全的延迟初始化；</li><li>使用类初始化的方式：需要对静态字段使用线程安全的延迟初始化；</li></ol><h2 id="4-并发基础"><a href="#4-并发基础" class="headerlink" title="4. 并发基础"></a>4. 并发基础</h2><h3 id="4-1-关于线程"><a href="#4-1-关于线程" class="headerlink" title="4.1 关于线程"></a>4.1 关于线程</h3><p>&emsp;线程是操作系统调度的最小单元，多线程可以并行执行，这种工作方式可以显著提升程序性能（在多核环境中表现明显）。在运行一个程序时操作系统会为其创建一个进程，一个进程中可以创建多个线程（也叫轻量级进程），通过Java的线程管理类JMX来获取线程信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// java线程管理类</span></span><br><span class="line">    ThreadMXBean threadMXBean = ManagementFactory.getThreadMXBean();</span><br><span class="line">    <span class="comment">// 获取线程和堆栈信息</span></span><br><span class="line">    ThreadInfo[] threadInfos = threadMXBean.dumpAllThreads(<span class="keyword">false</span>, <span class="keyword">false</span>);</span><br><span class="line">    <span class="keyword">for</span> (ThreadInfo t: threadInfos) &#123;</span><br><span class="line">        System.out.println(<span class="string">"["</span> + t.getThreadId() + <span class="string">"] "</span> + t.getThreadName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行的不仅仅只有 main() 方法，而是 main() 线程和其他多个线程同时运行，获取的线程信息如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[8] JDWP Command Reader</span><br><span class="line">[7] JDWP Event Helper Thread</span><br><span class="line">[6] JDWP Transport Listener: dt_socket</span><br><span class="line">[5] Attach Listener</span><br><span class="line">[4] Signal Dispatcher</span><br><span class="line">[3] Finalizer</span><br><span class="line">[2] Reference Handler</span><br><span class="line">[1] main</span><br></pre></td></tr></table></figure><h3 id="4-2-线程的优先级"><a href="#4-2-线程的优先级" class="headerlink" title="4.2 线程的优先级"></a>4.2 线程的优先级</h3><p>&emsp;操作系统基本采用时分的形式调度运行的线程，操作系统会分出一个个的时间片，线程会被分配到若干时间片，当线程的时间片用完后就会发生线程调度，并等待下一次分配，线程分配的时间片多少决定了线程使用处理器资源的多少，线程的优先级就是用来<strong>决定线程需要分配处理器资源的或多或少</strong>，优先级属性是用来控制线程获得处理器资源的因素，它跟线程本身属性没啥关系。</p><p>&emsp;在代码表现上，优先级在<code>Thread</code>类中使用一个<code>int</code>型的属性表示，范围1-10，数值越大优先级越高，默认为5，理论上优先级的高的线程分配的时间片比优先级低的线程分配的时间片要多，但在不同的JVM或者操作系统上，线程规划会有些差异，甚至会忽略对线程优先级的设置。</p><h3 id="4-3-线程状态"><a href="#4-3-线程状态" class="headerlink" title="4.3 线程状态"></a>4.3 线程状态</h3><p>&emsp;线程状态，即线程的生命周期：</p><ol><li>new：初始状态，线程被创建，但未调用<code>start()</code>方法；</li><li>runnable：运行状态，Java线程将操作系统中的就绪和运行两种状态笼统的称作“运行中”；</li><li>blocked：阻塞状态，即未获取锁；</li><li>waiting：等待状态，线程需要等待其他线程的一些行为（通知或中断）；</li><li>time_waiting：超时等待状态，可以在指定时间自动返回；</li><li>terminated：终止状态，表示当前线程已经执行完毕；</li></ol><p>各个状态之间的转换关系图如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%BA%BF%E7%A8%8B%E7%8A%B6%E6%80%81%E8%BD%AC%E6%8D%A2%E5%9B%BE.png" alt="线程状态转换图"></p><p><strong>关于中断</strong><br></p><p>中断：表示一个运行中的线程是否被其他线程进行了中断操作，某种意义上是线程的一个属性。中断的主要方法有：</p><ul><li><code>t.interrupt()</code>：当前线程将对 t 线程进行中断操作；</li><li><code>t.isInterrupted()</code>：判断 t 线程是否被中断。注：若 t 线程的状态为终止状态，那该方法将始终返回<code>false</code>，不论是否曾经被中断过；</li><li><code>Thread.interrupted()</code>：静态方法，用于对当前线程的中断标识符进行复位；</li></ul><p>注：若一个运行中得线程被其他线程调用<code>t.interrupt()</code>方法打断，如果抛出<code>InterruptedException</code>异常，JVM会将该线程的中断标识符清除，然后再抛出这个异常，所以<code>isInterrupted()</code>会返回<code>false</code>。</p><h3 id="4-4-Daemon线程（即守护线程）"><a href="#4-4-Daemon线程（即守护线程）" class="headerlink" title="4.4 Daemon线程（即守护线程）"></a>4.4 Daemon线程（即守护线程）</h3><p>&emsp;Daemon线程是一种支持型线程，区别于用户线程的守护线程（GC是典型的守护线程），主要用于程序中后台调度或者支持性工作，当JVM中不存在非Daemon线程时，JVM将退出，可以通过<code>Thread.setDaemon(boolean on)</code>设置线程是否为守护线程。</p><p>&emsp;Daemon线程中finally块中的代码未必会被执行，因为如果在执行finally块之前可能用户线程就退出了，此时整个JVM都将退出，不会执行finall块，所以在守护线程中不能依靠finally块来作一些关闭或资源清理工作。示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Runnable countRunnable = <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println(count++);</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"finally"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    Thread t = <span class="keyword">new</span> Thread(countRunnable);</span><br><span class="line">    t.setDaemon(<span class="keyword">true</span>);</span><br><span class="line">    t.start();</span><br><span class="line">    <span class="comment">// 如果想观察finally块的内容，打开此注释</span></span><br><span class="line">    <span class="comment">//Thread.sleep(1000);</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-5-过期的suspend-、resume-和stop"><a href="#4-5-过期的suspend-、resume-和stop" class="headerlink" title="4.5 过期的suspend()、resume()和stop()"></a>4.5 过期的suspend()、resume()和stop()</h3><p>&emsp;线程的启动是调用<code>start()</code>方法（千万不要手动去掉<code>run()</code>方法），随着<code>run()</code>执行完毕而完毕，线程终止。之前的API提供了线程的停止、继续、停止功能，对应于<code>suspend()</code>、<code>resume()</code>和<code>stop()</code>方法，示例如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Runnable suspend = <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                System.out.println(LocalDateTime.now().format(DateTimeFormatter.ofPattern(<span class="string">"yyyy-MM-dd HH:mm:ss"</span>)));</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">1</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    Thread t = <span class="keyword">new</span> Thread(suspend);</span><br><span class="line">    t.start();</span><br><span class="line">    TimeUnit.SECONDS.sleep(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">"==========suspend 3s=========="</span>);</span><br><span class="line">    t.suspend();</span><br><span class="line">    TimeUnit.SECONDS.sleep(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">"==========resume=========="</span>);</span><br><span class="line">    t.resume();</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">"==========stop=========="</span>);</span><br><span class="line">    TimeUnit.SECONDS.sleep(<span class="number">3</span>);</span><br><span class="line">    t.stop();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但上述的3种方法都已经丢弃了，因为在调用<code>suspend()</code>暂停线程时，线程不会释放已占用的资源（比如锁），<code>stop()</code>方法在停止线程时也不会保证线程占用的资源全部被释放，这样的方式会导致线程状态不可知，而且可能导致死锁，因此不建议使用，而是建议使用<code>wait()</code>/<code>notify()</code>来替代。</p><h3 id="4-6-停止线程"><a href="#4-6-停止线程" class="headerlink" title="4.6 停止线程"></a>4.6 停止线程</h3><p>&emsp;线程的停止已经不推荐使用<code>stop()</code>方法了，安全的停止线程推荐使用标识符的方式来控制：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    TimerRunnable timerRunnable = <span class="keyword">new</span> TimerRunnable();</span><br><span class="line"></span><br><span class="line">    Thread t = <span class="keyword">new</span> Thread(timerRunnable);</span><br><span class="line">    t.start();</span><br><span class="line">    TimeUnit.SECONDS.sleep(<span class="number">10</span>);</span><br><span class="line">    timerRunnable.stopRunner();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">TimerRunnable</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">boolean</span> run = <span class="keyword">true</span>;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (run) &#123;</span><br><span class="line">            System.out.println(LocalDateTime.now().format(DateTimeFormatter.ofPattern(<span class="string">"yyyy-MM-dd HH:mm:ss"</span>)));</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                TimeUnit.SECONDS.sleep(<span class="number">1</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stopRunner</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        run = <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过标识符的方式可以在改变标识符的位置作一些资源释放和清理工作，而不是去停止。</p><h3 id="4-7-线程通信"><a href="#4-7-线程通信" class="headerlink" title="4.7 线程通信"></a>4.7 线程通信</h3><p>&emsp;线程通信是线程之间协作的重要方式，它可以让各个线程不仅仅是独立的完成自己工作，而且可以做到各个线程之间协作，相比各个线程孤立的干活要提供额外一些应用场景。因为每个线程都有自己的工作内存，在多线程访问的变量实际是这个变量的拷贝（这样可以加速程序的执行效率额），换而言之，多线程中未必可以看到这个变量的最新值。使用<code>volatile</code>变量修饰的变量就是告诉程序要获取这个变量必须从共享内存中获取，若修改变量也必须同步刷新到共享内存中，这样可以保证所有线程对变量的即时可见性。</p><p><strong>等待/通知</strong><br></p><p>A线程修改了一个对象的值，另一个线程B感知到变化后执行相应的行为，整个流程就是开始于一个线程，而执行的是另一个线程，前者是生产者，后者是消费者，是典型的生产者/消费者模式，这种模式它隔离了“做什么”和“怎么做”的问题。简单的方式就是设置一个标识符不断循环检测，条件满足时就去做，否则不做：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 条件不满足时，睡眠1秒，这样可以避免消耗不必要的处理器资源</span></span><br><span class="line"><span class="keyword">while</span>(falg != desire) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        TimeUnit.SECONDS.sleep(<span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 否则执行某些事</span></span><br><span class="line">dosth()</span><br></pre></td></tr></table></figure><p>上述睡眠行为是为了防止过快的无效尝试，虽然功能层面可以解决这个问题，但存在一些缺陷：</p><ol><li>非实时性。虽然睡眠可以有效地避免处理器的消耗（基本不消耗），但这个时间如果不能合理的设置，很容易造成标识符的不及时判断；</li><li>难以降低开销。根据1的阐述，可以通过降低睡眠时间来降低非实时性，但如果时间睡眠时间太短，反而会消耗更多的资源（因为线程会在短时间内反复的睡眠、唤醒）；</li></ol><p>&emsp;经历过上述的问题，Java内置的 等待/通知 机制可以解决问题，<code>wait()</code>/<code>notify()</code>方法是定义在<code>Object</code>类上，注意其中<code>wait()</code>方法会释放锁（若不释放锁，其他线程无法获取锁来唤醒它，将一直处于 waiting 状态），简单示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WaitAndNotify</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object LOCK = <span class="keyword">new</span> Object();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> flag = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Runnable wait = () -&gt; &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (LOCK) &#123;</span><br><span class="line">                <span class="keyword">while</span> (flag) &#123;</span><br><span class="line">                    printlnMessage(Thread.currentThread(), <span class="string">" flag: true. wait"</span>);</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="comment">// wait 时需要持有锁对象</span></span><br><span class="line">                        LOCK.wait();</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                printlnMessage(Thread.currentThread(), <span class="string">" flag: false"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        Runnable notify = () -&gt; &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (LOCK) &#123;</span><br><span class="line">                printlnMessage(Thread.currentThread(), <span class="string">" hold lock. notify"</span>);</span><br><span class="line">                <span class="comment">// 调用 notify 必须持有 wait 时的锁对象</span></span><br><span class="line">                LOCK.notify();</span><br><span class="line">                flag = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">5</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">synchronized</span> (LOCK) &#123;</span><br><span class="line">                printlnMessage(Thread.currentThread(), <span class="string">" hold lock again. sleep"</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">5</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        Thread tw = <span class="keyword">new</span> Thread(wait, <span class="string">"wait-thread"</span>);</span><br><span class="line">        Thread tn = <span class="keyword">new</span> Thread(notify, <span class="string">"notify-thread"</span>);</span><br><span class="line"></span><br><span class="line">        tw.start();</span><br><span class="line">        tn.start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">printlnMessage</span><span class="params">(Thread t, String message)</span> </span>&#123;</span><br><span class="line">        System.out.println(t + message + <span class="string">" @ "</span> + LocalDateTime.now().format(DateTimeFormatter.ofPattern(<span class="string">"yyyy-MM-dd HH:mm:ss"</span>)));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在使用<code>wait()</code>/<code>notify()</code>方法时，必须要注意几点：</p><ul><li>使用<code>wait()</code>、<code>notify()</code>和<code>notifyAll()</code>等方法时，<strong>必须先对调用对象加锁</strong>；</li><li>使用<code>wait()</code>方法后，线程状态由running变成waiting，并将当前线程放置到对象的等待队列中；</li><li><code>notify()</code>或<code>notifyAll()</code>调用后，等待的线程不会立刻waiting状态返回，必须等到之前调用<code>notify()</code>或<code>notifyAll()</code>的线程释放锁之后；</li><li><code>notify()</code>方法将等待队列中的一个线程移到同步队列中，<code>notifyAll()</code>是将等待队列中所有的线程全部移到同步队列中，被移动的线程状态由waiting变成blocked；</li></ul><p><strong>关于<code>join()</code></strong><br></p><p>若在当线程执行了<code>t.join()</code>，那当前线程会变成 waiting 的状态，它会等待线程 t 终止后才会继续执行，内部的实现也是利用<code>wait()</code>/<code>notify()</code>进行的，源码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 加锁当前线程</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">join</span><span class="params">(<span class="keyword">long</span> millis)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    <span class="keyword">long</span> base = System.currentTimeMillis();</span><br><span class="line">    <span class="keyword">long</span> now = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (millis &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"timeout value is negative"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (millis == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 若当前线程存活则让其一直等待，0表示的是没有时间限制</span></span><br><span class="line">        <span class="keyword">while</span> (isAlive()) &#123;</span><br><span class="line">            wait(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">while</span> (isAlive()) &#123;</span><br><span class="line">            <span class="keyword">long</span> delay = millis - now;</span><br><span class="line">            <span class="keyword">if</span> (delay &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            wait(delay);</span><br><span class="line">            now = System.currentTimeMillis() - base;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-8-ThreadLocal"><a href="#4-8-ThreadLocal" class="headerlink" title="4.8 ThreadLocal"></a>4.8 ThreadLocal</h3><p>&emsp;<code>ThreadLocal</code>即线程变量，它以<code>ThreadLocal</code>对象为键、任意对象为值的存储结构。这个结构被附带在线程上，一个线程可以根据一个ThreadLocal对象查询到绑定在这个线程上的一个值。一定注意，<code>ThreadLLocal</code>和它的名字一样，是线程局部变量，只能在这个线程内被读写，其他线程无法访问，<code>ThreadLocal</code>定义的通常都是和线程关联的私有静态字段（即<code>private static final</code>）。</p><h3 id="4-8-管道的输入-输出"><a href="#4-8-管道的输入-输出" class="headerlink" title="4.8 管道的输入/输出"></a>4.8 管道的输入/输出</h3><h3 id="4-9-线程池"><a href="#4-9-线程池" class="headerlink" title="4.9 线程池"></a>4.9 线程池</h3><p>&emsp;如果服务端每次接受到一个任务就创建一个线程去处理，对于几万个请求同时过来时，上述的方式会创建几万个线程去跑，显然不是太合理，这个么多线程将会导致系统频繁的进行上下文的切换，系统负载增加，而且线程的创建和消亡也需要消耗资源。所以为了解决上面的问题，线程池出来了，它可以预先创建出指定数量的线程，而不是由用户直接对线程进行控制，这样重复使用固定的或数目固定的线程来完成任务的执行。线程池的出现解决了频繁创建和销毁线程的系统资源开销的问题，同时也能让过量的任务能够平稳的劣化（啥意思？）。</p><p>&emsp;</p><h2 id="5-Java中的锁"><a href="#5-Java中的锁" class="headerlink" title="5. Java中的锁"></a>5. Java中的锁</h2><h3 id="5-1-Lock接口"><a href="#5-1-Lock接口" class="headerlink" title="5.1 Lock接口"></a>5.1 Lock接口</h3><p>&emsp;在多线程中，通常的理解是利用锁来限制同一时刻只有一个线程可以对共享资源进行操作，但是有些锁是允许多线程对多线程同时访问共享资源的（仅仅是访问，也就是读操作，也能理解，同时读也没啥毛病），比如提的比较多的读写锁。在jdk1.5以后提供了除<code>synchronized</code>关键字以外的<code>Lock</code>接口来实现锁，这个接口提供了与<code>synchronized</code>类似的功能，但是它<font color="red">需要显示的获取锁和释放锁</font>，虽然没有像同步方法或者同步代码块那样隐式的获取锁、释放锁方便（隐式锁操作是<code>sychronized</code>的一个优势），但是拥有获取、释放锁的多种灵活性。对于<code>synchronized</code>提供的方式，锁的操作流程是被固化的，必定是先获取再释放，灵活性不强。而Lock相比之下要灵活得多，主要使用方法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//创建锁</span></span><br><span class="line">Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="comment">//获取锁，重要：不要将获取锁的步骤写在try块中，防止在获取锁的过程中抛出异常，锁无故被释放</span></span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">   <span class="comment">// ....</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="comment">// 释放锁的操作应该写在 finally 块中，保证最终能够被释放</span></span><br><span class="line">    lock.unlock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>Lock</code>接口相对于<code>synchronized</code>关键字主要比较鲜明的特性有：</p><ul><li>尝试非阻塞地获取锁：当线程尝试获取锁时，若此时锁没有被其他线程占用，则成功获取锁；</li><li>能中断地获取锁：获取到锁的线程能够响应中断，若获取到锁的线程被中断，中断异常将会抛出，同时锁被释放；</li><li>超时获取锁：在指定时间内获取锁，若超时则直接返回。</li></ul><p><code>Lock</code>接口提供了锁的获取、释放基本操作，方法有：</p><ul><li><code>lock()</code>：获取锁，若该锁不可用，则该线程不可调度；</li><li><code>lockInterruptibly()</code>：获取锁，但该方法会响应中断，即可以在获取锁的过程中中断当前线程；</li><li><code>tryLock()</code>：尝试非阻塞的获取锁，该方法会尝试获取锁，若此刻锁被占用，直接返回<code>false</code>，否则返回<code>true</code>；</li><li><code>tryLock(long time, TimeUnit unit)</code>：和上面<code>tryLock()</code>类似，区别在于不是立刻返回，而是在指定时间内尝试获取，且可以响应中断；</li><li><code>unlock()</code>：释放锁；</li><li><code>Condition newCondition()</code>：获取等待通知组件，该组件和当前的锁绑定，只有当前线程获取了锁才能调用组件的<code>wait()</code>方法，调用后，当前线程释放锁；</li></ul><h3 id="5-2-队列同步器"><a href="#5-2-队列同步器" class="headerlink" title="5.2 队列同步器"></a>5.2 队列同步器</h3><p>&emsp;同步器（<code>AbstractQueuedSynchronizer</code>，是一个基础抽象类），主要用于构建锁或者其他同步组件的基础框架，经常在手动创建线程池时使用。该队列使用一个<code>int</code>类型的成员变量表示同步，通过内置的FIFO队列完成资源获取线程的排队工作，同步器提供下面3个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//1.获取当前同步状态，返回long类型</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">long</span> <span class="title">getState</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> state;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//2.设置当前同步状态</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">setState</span><span class="params">(<span class="keyword">long</span> newState)</span> </span>&#123;</span><br><span class="line">    state = newState;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//3.使用CAS设置当前状态，该方法能保证状态设置的原子性</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">compareAndSetState</span><span class="params">(<span class="keyword">long</span> expect, <span class="keyword">long</span> update)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// See below for intrinsics setup to support this</span></span><br><span class="line">    <span class="keyword">return</span> unsafe.compareAndSwapLong(<span class="keyword">this</span>, stateOffset, expect, update);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同步器的使用方式是子类通过继承队列同步器并实现它的抽象方法来管理同步状状态，注意：<font color="red">推荐定义为自定义同步组件的静态内部类</font>，重写它的抽象方法来自定义管理同步状态，同步器中定义了若干获取和设置同步状态的方法给自定义同步组件使用，它既支持独占式获取同步状态，也可支持共享式获取同步状态。同步器是实现锁的关键，在锁的实现中聚合同步器，利用同步器实现锁的语义：锁是面向使用者的，它定义了使用者与锁交互的接口，隐藏细节；而同步器是锁的实现者，它简化了锁的实现，屏蔽了同步状态管理、线程的排队、等待与唤醒等底层操作。锁和同步器隔离了使用者和实现者需要关注的领域。</p><h4 id="5-2-1-队列同步的实现"><a href="#5-2-1-队列同步的实现" class="headerlink" title="5.2.1 队列同步的实现"></a>5.2.1 队列同步的实现</h4><p>&emsp;同步器內部依靠同步队列（一个FIFO的双向队列）实现同步状态的管理，当前线程获取同步状态失败时，同步器会将当前线程以及等待状态等信息构造成一个Node节点并加入同步队列中，同时阻塞当前线程，当同步状态释放时，会将首节点的线程唤醒，让其再次尝试获取同步状态。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 等队列中头节点（懒加载）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node head;</span><br><span class="line"><span class="comment">// 等待队列中的尾节点（懒加载）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node tail;</span><br><span class="line"><span class="comment">// 同步的状态</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> state;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 标识共享锁模式中等待的节点</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> Node SHARED = <span class="keyword">new</span> Node();</span><br><span class="line">    <span class="comment">// 标识独占式中等待的节点</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> Node EXCLUSIVE = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 表示线程因为超时或被中断的状态</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CANCELLED =  <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 当前节点的线程若释放了同步状态或取消，会通知后继处于等待状态的节点，让后继节点的线程得以运行</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> SIGNAL    = -<span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 表示当前节点的线程正在等待 Condition</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CONDITION = -<span class="number">2</span>;</span><br><span class="line">     <span class="comment">// 表示下一次共享的不同状态将会无条件的传播下去</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> PROPAGATE = -<span class="number">3</span>;</span><br><span class="line">    <span class="comment">// 表示的等待状态，它的值可能为上面的4种情况，如果都不是就为0</span></span><br><span class="line">    <span class="keyword">volatile</span> <span class="keyword">int</span> waitStatus;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 4个核心属性</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// 1. 前驱节点</span></span><br><span class="line">    <span class="keyword">volatile</span> Node prev;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 后继节点</span></span><br><span class="line">    <span class="keyword">volatile</span> Node next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 表示获取同步状态（锁）的线程</span></span><br><span class="line">    <span class="keyword">volatile</span> Thread thread;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 表示下一个正在等待 condition 或者在共享模式中的Node</span></span><br><span class="line">    Node nextWaiter;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 判断是否为共享模式节点</span></span><br><span class="line">    <span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">isShared</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> nextWaiter == SHARED;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 返回前一个节点，若无则抛异常</span></span><br><span class="line">    <span class="function"><span class="keyword">final</span> Node <span class="title">predecessor</span><span class="params">()</span> <span class="keyword">throws</span> NullPointerException </span>&#123;</span><br><span class="line">        Node p = prev;</span><br><span class="line">        <span class="keyword">if</span> (p == <span class="keyword">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> p;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化</span></span><br><span class="line">    Node() &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于添加等待状态线程的节点，即等待队列</span></span><br><span class="line">    Node(Thread thread, Node mode) &#123;</span><br><span class="line">        <span class="keyword">this</span>.nextWaiter = mode;</span><br><span class="line">        <span class="keyword">this</span>.thread = thread;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于添加等待 Condition 线程的节点</span></span><br><span class="line">    Node(Thread thread, <span class="keyword">int</span> waitStatus) &#123;</span><br><span class="line">        <span class="keyword">this</span>.waitStatus = waitStatus;</span><br><span class="line">        <span class="keyword">this</span>.thread = thread;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过节点构成同步队列，上述节点的数据结构是构成同步队列的基础，同步器具有首节点和尾节点，没有成功获取同步状态的线程会加入到该队列的尾部，队列的基本结构如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%90%8C%E6%AD%A5%E9%98%9F%E5%88%97%E7%9A%84%E7%BB%93%E6%9E%84.png" alt="同步队列的结构"></p><p>首先明确一点，同步队列中都是在当前时刻未能获取同步状态的线程构造出来的Node，每次有新的线程无法获取同步状态时都会加入这个同步队列的尾部（通过同步器提供的CAS操作<code>compareAndSetTail(...)</code>方法）。整个队列遵循FIFO，在首节点成功获取状态后，原首节点将会唤醒它的后继节点，当这个后继节点在自己成功获取同步状态时将自己设置为新的首节点（设置新首节点是通过获取同步状态的线程操作的），和设置尾节点时不同的是，设置新首节点不需要CAS操作（【疑问】我同样发现<code>compareAndSetHead(...)</code>方法）。</p><h4 id="5-2-2-独占式同步状态"><a href="#5-2-2-独占式同步状态" class="headerlink" title="5.2.2 独占式同步状态"></a>5.2.2 独占式同步状态</h4><p>&emsp;同步器中的<code>acquire(int arg)</code>方法可以获取到同步状态，对中断不敏感，内部并不是直接去获取同步状态，而是非阻塞的去获取，若获取失败就将其构造为一个节点（这里是构造为独占模式的节点<code>addWaiter(Node.EXCLUSIVE)</code>）加入到同步队列的队尾，然后调用<code>acquireQueued(final Node node, int arg)</code>方法让该节点以死循环的方式获取同步状态。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取同步状态</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp; acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">        selfInterrupt();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 加入队列尾部</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">addWaiter</span><span class="params">(Node mode)</span> </span>&#123;</span><br><span class="line">    Node node = <span class="keyword">new</span> Node(Thread.currentThread(), mode);</span><br><span class="line">    Node pred = tail;</span><br><span class="line">    <span class="keyword">if</span> (pred != <span class="keyword">null</span>) &#123;</span><br><span class="line">        node.prev = pred;</span><br><span class="line">        <span class="comment">// 可以确保节点可以在线程安全的场景下添加</span></span><br><span class="line">        <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123;</span><br><span class="line">            pred.next = node;</span><br><span class="line">            <span class="keyword">return</span> node;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    enq(node);</span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 该方法可以将并行行为变成串行了</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 通过死循环保证节点正确添加，只有通过CAS将节点成功添加为尾节点后才返回，否则将不断尝试</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        Node t = tail;</span><br><span class="line">        <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> Node()))</span><br><span class="line">                tail = head;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            node.prev = t;</span><br><span class="line">            <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;</span><br><span class="line">                t.next = node;</span><br><span class="line">                <span class="keyword">return</span> t;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">        <span class="comment">// 同样在死循环中尝试获取同步状态</span></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">            <span class="comment">// 维护FIFO的原则，</span></span><br><span class="line">            <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</span><br><span class="line">                setHead(node);</span><br><span class="line">                p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                failed = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">return</span> interrupted;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                parkAndCheckInterrupt())</span><br><span class="line">                interrupted = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (failed)</span><br><span class="line">            cancelAcquire(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>只有前驱节点是头节点的才能尝试获取同步状态，这种行为有下面2点保证：</p><ul><li>在队列中获取同步状态的节点只能是前驱节点是头节点的节点，当等待队列的头节点成功获取同步状态后再释放锁，最后唤醒它的后继节点，该节点中的线程被唤醒后会去检测自己的前驱节点是否为头节点；</li><li>维护同步队列的FIFO原则；</li></ul><p>&emsp;当前线程获取同步状态并执行了内部逻辑后就需要释放同步状态，使得后继节点能够继续获取同步状态，通过调用同步器的<code>reslease</code>方法释放锁，它释放锁后会唤醒后继节点，让后继节点重新尝试获取同步状态：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">        Node h = head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">            <span class="comment">// 该方法用于唤醒处于等待的线程</span></span><br><span class="line">            unparkSuccessor(h);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此外独占式获取同步状态的方式还有一个超时获取的方式<code>doAcquireNanos(int arg, long nanosTimeout)</code>，原理一样，只是额外加了一个超时限制，响应中断，在等待的同步状态时，如果该线程被中端，会立即返回并抛出<code>InterruptedException</code>异常。示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 测试独占锁</span></span><br><span class="line"><span class="comment"> * 在自定义的队列同步器中主要是独占式的父类方法进行重写</span></span><br><span class="line"><span class="comment"> * 然后在锁Mutex中通过层性向上调用自定义的同步器的独占式方法实现</span></span><br><span class="line"><span class="comment"> * 单线程限制的独占式的控制，以后调用锁的时候直接用即可</span></span><br><span class="line"><span class="comment"> * created by WeiguoLiu on 2017/12/26</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Mutex</span> <span class="keyword">implements</span> <span class="title">Lock</span> </span>&#123;</span><br><span class="line">    <span class="comment">//利用静态内部类自定义同步器，则是推荐的方式</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Sync</span> <span class="keyword">extends</span> <span class="title">AbstractQueuedSynchronizer</span> </span>&#123;</span><br><span class="line">        <span class="comment">//重写判断同步器是否处于独占式占用状态,0表示未被占用，1表示被占用</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">isHeldExclusively</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> getState() == <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//重写同步器获取锁的方式，当状态为0的时候获取锁</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="comment">//CAS操作，如果该内存位置上的值为0（即没有被占用），那么将它更新为1，</span></span><br><span class="line">            <span class="comment">// 表示被调用的线程占用，否则不更新</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, <span class="number">1</span>)) &#123;</span><br><span class="line">                setExclusiveOwnerThread(Thread.currentThread());</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//重写独占式释放同步状态的方法，将状态置为0</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (getState() == <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">            setExclusiveOwnerThread(<span class="keyword">null</span>);</span><br><span class="line">            setState(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//返回一个Condition，每个condition都包含一个condition队列</span></span><br><span class="line">        <span class="function">Condition <span class="title">newCondition</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> ConditionObject();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将需要的操作代理到Sync上，定义一个同步器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Sync sync = <span class="keyword">new</span> Sync();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        sync.acquire(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sync.tryAcquire(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        sync.release(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Condition <span class="title">newCondition</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sync.newCondition();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">siLocked</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sync.isHeldExclusively();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">hasQueuedThreads</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sync.hasQueuedThreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lockInterruptibly</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        sync.acquireInterruptibly(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">(<span class="keyword">long</span> time, TimeUnit unit)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sync.tryAcquireNanos(<span class="number">1</span>, unit.toNanos(time));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>【总结】在获取同步状态时，同步器维护一个同步队列，获取同步状态失败的线程会被构造成一个节点加入队列尾部进行自旋。移出队列（停止自旋）的条件是前驱节点为头节点且成功获取同步状态，在获取同步状态时，同步器调用<code>tryRelease(int arg)</code>方法释放同步状态，然后唤醒头节点的后继节点。</p><h4 id="5-2-3-共享式同步状态"><a href="#5-2-3-共享式同步状态" class="headerlink" title="5.2.3 共享式同步状态"></a>5.2.3 共享式同步状态</h4><p>&emsp;共享式获取同步状态和独占式获取同步状态的主要区别在于同一时刻是否可以有多个线程同时获取到同步状态。在共享式访问资源时，其他共享式的访问被允许，而独占式访问会被阻塞；当独占式访问资源时，同一时刻其他所有资源的访问方式都将被阻塞。通过<code>acquireShared(int arg)</code>可以共享式的获取同步状态：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquireShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 当 tryAcquireShared 方法返回值大于0时，表示可以获取同步状态</span></span><br><span class="line">    <span class="comment">// 能够成功获取到同步状态并退出自旋的条件就是该方法的返回值 &gt;= 0 </span></span><br><span class="line">    <span class="keyword">if</span> (tryAcquireShared(arg) &lt; <span class="number">0</span>)</span><br><span class="line">        doAcquireShared(arg);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doAcquireShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Node node = addWaiter(Node.SHARED);</span><br><span class="line">    <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">        <span class="comment">// 死循环的自旋</span></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">            <span class="comment">// 当前驱节点为头节点时，尝试获取同步状态</span></span><br><span class="line">            <span class="keyword">if</span> (p == head) &#123;</span><br><span class="line">                <span class="keyword">int</span> r = tryAcquireShared(arg);</span><br><span class="line">                <span class="keyword">if</span> (r &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    setHeadAndPropagate(node, r);</span><br><span class="line">                    p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    <span class="keyword">if</span> (interrupted)</span><br><span class="line">                        selfInterrupt();</span><br><span class="line">                    failed = <span class="keyword">false</span>;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">                parkAndCheckInterrupt())</span><br><span class="line">                interrupted = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (failed)</span><br><span class="line">            cancelAcquire(node);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 释放共享式同步状态</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">releaseShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryReleaseShared(arg)) &#123;</span><br><span class="line">        doReleaseShared();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-2-4-自定义同步组件-TwinsLock"><a href="#5-2-4-自定义同步组件-TwinsLock" class="headerlink" title="5.2.4 自定义同步组件 TwinsLock"></a>5.2.4 自定义同步组件 TwinsLock</h4><p>&emsp;直接示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/9/10 8:30</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> 设计一个同步工具，同一时刻之多只有2个线程同时访问，超过则阻塞</span></span><br><span class="line"><span class="comment"> * 分析：支持2个线程同时访问，显然只有共享模式符合，可以通过重写相关Shared方法来完成</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TwinsLock</span> <span class="keyword">implements</span> <span class="title">Lock</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定义同步器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Sync</span> <span class="keyword">extends</span> <span class="title">AbstractQueuedSynchronizer</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 初始化时将状态置为对应的共享线程数，此处为2</span></span><br><span class="line">        Sync(<span class="keyword">int</span> count) &#123;</span><br><span class="line">            <span class="keyword">if</span> (count &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"count must larger than 0."</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 设置 state 字段</span></span><br><span class="line">            setState(count);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// 死循环</span></span><br><span class="line">            <span class="keyword">for</span> (; ; ) &#123;</span><br><span class="line">                <span class="keyword">int</span> current = getState();</span><br><span class="line">                <span class="keyword">int</span> newCount = current - arg;</span><br><span class="line">                <span class="comment">// 当一个线程获取同步状态时减一，0表示已有2个线程获取同步状态</span></span><br><span class="line">                <span class="comment">// 修改同步状态时调用 compareAndSetState 方法可以保证原子性的更新</span></span><br><span class="line">                <span class="keyword">if</span> (newCount &lt; <span class="number">0</span> || compareAndSetState(current, newCount)) &#123;</span><br><span class="line">                    <span class="keyword">return</span> newCount;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryReleaseShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (; ; ) &#123;</span><br><span class="line">                <span class="keyword">int</span> current = getState();</span><br><span class="line">                <span class="keyword">int</span> newCount = current + arg;</span><br><span class="line">                <span class="comment">// 当一个线程释放同步状态时加一</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(current, newCount)) &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Sync sync = <span class="keyword">new</span> Sync(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        sync.acquireShared(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        sync.releaseShared(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 其他方法略</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> liuwg-a</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/9/10 8:42</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span> TwinsLock 的测试类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TwinsLockTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ExecutorService executor = <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">10</span>, <span class="number">20</span>, <span class="number">50</span>, TimeUnit.MILLISECONDS, <span class="keyword">new</span> LinkedBlockingQueue&lt;&gt;(), <span class="keyword">new</span> MyThreadFactory(<span class="keyword">new</span> MyFactoryBuilder().namePrefix(<span class="string">"twins-lock"</span>)));</span><br><span class="line">        <span class="keyword">final</span> Lock lock = <span class="keyword">new</span> TwinsLock();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            executor.execute(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    lock.lock();</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        System.out.println(System.currentTimeMillis() + <span class="string">" -&gt; "</span> + Thread.currentThread().getName());</span><br><span class="line">                        TimeUnit.SECONDS.sleep(<span class="number">1</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        lock.unlock();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                TimeUnit.SECONDS.sleep(<span class="number">1</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同一时刻只有2个线程的输出信息表示同时刻只有2个线程可以获取同步状态，输出结果大致的样式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">1568078312377 -&gt; twins-lock-pool-1-thread-2</span><br><span class="line">1568078312377 -&gt; twins-lock-pool-1-thread-1</span><br><span class="line"></span><br><span class="line">1568078313378 -&gt; twins-lock-pool-1-thread-2</span><br><span class="line">1568078313378 -&gt; twins-lock-pool-1-thread-1</span><br><span class="line"></span><br><span class="line">1568078314379 -&gt; twins-lock-pool-1-thread-1</span><br><span class="line">1568078314379 -&gt; twins-lock-pool-1-thread-2</span><br><span class="line"></span><br><span class="line">1568078315379 -&gt; twins-lock-pool-1-thread-1</span><br><span class="line">1568078315379 -&gt; twins-lock-pool-1-thread-2</span><br></pre></td></tr></table></figure><h3 id="5-3-重入锁-ReentrantLock"><a href="#5-3-重入锁-ReentrantLock" class="headerlink" title="5.3 重入锁 ReentrantLock"></a>5.3 重入锁 ReentrantLock</h3><h4 id="5-3-1-重入锁"><a href="#5-3-1-重入锁" class="headerlink" title="5.3.1 重入锁"></a>5.3.1 重入锁</h4><p>&emsp;重入锁<code>ReentrantLock</code>，这种锁支持重进入的锁，表示能<font color="red">支持一个线程对资源的重复加锁</font>。此外，重入锁还支持获取锁的公平和非公平选择。当一个线程调用<code>lock()</code>方法获取锁之后，若再次调用<code>lock()</code>方法将会被自己阻塞，这种情况就是典型的非重入锁，非重入锁在调用<code>tryAcquire(int arg)</code>方法时没有考虑已占有锁的线程再次获取锁的场景，导致方法返回<code>false</code>。</p><p>&emsp;常见的<code>syqnchronized</code>关键字是隐式支持重新进入的，如用<code>synchronized</code>修饰递归方法时，线程在开始的时就获取的了锁，随着递归深度的增加，仍然可以连续的不断获取锁，所以它是隐式地支持重入行为的。在重入锁中，已经获取锁的线程还是可以继续调用<code>lock()</code>方法获取锁而不会出现被自己阻塞的情况，注意这里重入的定义是仅仅局限于同一个线程而言，不要和独占这个词搞混了。<code>ReentrantLock</code>没有像<code>sychronized</code>支持隐式重入，但可以通过反复调用<code>lock()</code>方法获取锁而不被阻塞。</p><p>&emsp;<strong>获取锁的公平性</strong>。公平锁其实就是指先到先得原则（获取锁的顺序是顺序获取，等待时间最长的线程优先获取锁），即先请求获取锁的线程会先获取，后请求获取锁的线程会后获取，反之锁就是不公平的。公平锁的效率往往没有非公平锁的效率高，但共公平锁的机制能够减少“饥饿”的发生。<code>ReentrantLock</code>提供额外的带参构造方法<code>ReentrantLock(boolean fair)</code>来控制锁是否公平（无参构造方法默认使用非公平锁）。</p><h4 id="5-3-2-重入原理"><a href="#5-3-2-重入原理" class="headerlink" title="5.3.2 重入原理"></a>5.3.2 重入原理</h4><p>&emsp;重入表示的是任意线程在获取到锁之后能够再次获取该锁而不会被阻塞，重入原理主要解决如下的2个问题：</p><ul><li>锁如何辨认后面请求的线程为当前持有锁的线程，如果是则获取成功，否则获取失败；</li><li>最终锁如何释放，线程重复获取锁，需要对获取的次数计数，获取时自增，释放时自减，当计数器为0时，锁释放成功；</li></ul><p>&emsp;<code>ReentrantLock</code>的<code>tryLock()</code>方法实现如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 获取锁</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 默认为非公平锁</span></span><br><span class="line">    <span class="keyword">return</span> sync.nonfairTryAcquire(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="comment">// 如果 getState() 为0，表示当前线程是初次获取锁</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 记录当前线程并返回true</span></span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 若当前线程和之前记录的线程是同一个，则计数器增加，返回 true</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">        <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">        <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        <span class="comment">// 设置同步状态值</span></span><br><span class="line">        setState(nextc);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 释放锁</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    sync.release(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">        Node h = head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">            unparkSuccessor(h);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 只要c不为0，锁就没有完全释放</span></span><br><span class="line">    <span class="keyword">int</span> c = getState() - releases;</span><br><span class="line">    <span class="keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">    <span class="keyword">boolean</span> free = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        free = <span class="keyword">true</span>;</span><br><span class="line">        setExclusiveOwnerThread(<span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    setState(c);</span><br><span class="line">    <span class="keyword">return</span> free;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-3-3-公平锁和非公平锁"><a href="#5-3-3-公平锁和非公平锁" class="headerlink" title="5.3.3 公平锁和非公平锁"></a>5.3.3 公平锁和非公平锁</h4><p>&emsp;公平性与否是针对获取锁而言的，若获取锁的顺序符合绝对时间顺序（即FIFO）。对于非公平锁，只要CAS设置同步状态成功，则表示当前线程获取了锁，非公平锁不太一样，如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// hasQueuedPredecessors 用于判断当前节点在同步队列中有前驱节点</span></span><br><span class="line">        <span class="comment">// 如果有前驱节点则等待前驱节点线程获取并释放锁之后才能继续获取锁</span></span><br><span class="line">        <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp; compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">        <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">        <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        setState(nextc);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>公平性锁每次都是从同步队列中的第一个节点获取锁，而非公平锁则有可能某个线程或某些线程连续获取锁的情况，原因如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(current);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">        <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">        <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        setState(nextc);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因为重入锁存在，当一个线程请求锁时，只要获取同步状态（成功获取锁），刚释放锁的线程再次获取同步状态的几率会很大（不是很懂……）。</p><p>&emsp;非公平锁可能会使线程“饥饿”，但非公平锁作为默认实现是有它的优势：上下文切换次数少，性能开销低，保证了更大的吞吐量。反观公平锁保证了FIFO，但是代价是付出了大量的线程切换的消耗。</p><h3 id="5-4-读写锁"><a href="#5-4-读写锁" class="headerlink" title="5.4 读写锁"></a>5.4 读写锁</h3><p>&emsp;读写锁允许同一时刻多个线程对访问共享资源，但是在一个线程在进行写操作的时候，所有的读线程和其他的写线程将全被阻塞（只有写线程释放锁后才能允许读，这样有效的避免了数据的脏读），还有一点是需要注意，就是一个或者多个线程获取读锁之后，其他线程是无法获取写锁的。<font color="red">读写锁维护的是一对锁</font>，而不是一个锁，拥有一个读锁和一个写锁，将读锁和写锁进行分离，使得线程的并发性得到了较大的提高。</p><p>&emsp;之前的可重入锁<code>ReentrantLock</code>属于排他锁，同一时刻只允许一个线程进行访问。一般对于读写锁而言，性能要高于排他锁，因为多数场景是读多于写，在此场景下，它可以提供比排他锁更好的并发量和吞吐量。读写锁对于的类为<code>ReentrantReadWriteLock</code>，它有如下特性：</p><ul><li>支持公平性选择：和重入锁一样，都除无参构造方法外提供了带参构造方法指定公平性选择<code>ReentrantReadWriteLock(boolean fair)</code>，默认是非公平锁；</li><li>支持重入：看他的名字就知道是支持可重入的，不管是内部的写锁还是读锁都是支持重入的；</li><li>锁降级：遵循获取写锁、读锁再释放写锁的次序，写锁能降级成读锁；</li></ul><p>&emsp;<code>ReentrantReadWriteLock</code>获取锁的方式相较于其他类的<code>lock()</code>分裂出获取读锁和获取写锁两种方式：获取读锁<code>readLock()</code>、获取写锁<code>writeLock()</code>。也提供了一些可监控内部状态的方法（读、写锁几乎一一对应的）：</p><ul><li><code>getReadLockCount()</code>：获取当前读锁被获取的次数，由于是可重入锁，注意不是线程数；</li><li><code>getReadHoldCount()</code>：获取当前线程获取读锁的次数。</li></ul><p>使用示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 利用 Map 和读写锁实现线程安全的缓存</span></span><br><span class="line"><span class="comment"> * created by WeiguoLiu on 2017/12/28</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Cache</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Map&lt;String, Object&gt; map = <span class="keyword">new</span> HashMap&lt;String, Object&gt;();</span><br><span class="line">    <span class="keyword">static</span> ReentrantReadWriteLock rwl = <span class="keyword">new</span> ReentrantReadWriteLock();</span><br><span class="line">    <span class="keyword">static</span> Lock r = rwl.readLock();</span><br><span class="line">    <span class="keyword">static</span> Lock w = rwl.writeLock();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取一个key对应的value</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Object <span class="title">get</span><span class="params">(String key)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//首先获取读锁</span></span><br><span class="line">        r.lock();</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            <span class="keyword">return</span> map.get(key);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">//最后资源的释放</span></span><br><span class="line">            r.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//设置key对应的value，并返回旧值的value</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Object <span class="title">put</span><span class="params">(String key, Object value)</span> </span>&#123;</span><br><span class="line">        w.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> map.put(key, value);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            w.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//清空缓存内容</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">//清空是一种写操作，获取写锁，阻塞索所有线程的读操作和其他线程的写操作</span></span><br><span class="line">        w.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            map.clear();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            w.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-4-1-读写锁原理"><a href="#5-4-1-读写锁原理" class="headerlink" title="5.4.1 读写锁原理"></a>5.4.1 读写锁原理</h3><p>&emsp;这里涉及到的读写锁暂时默认为<code>ReentrantReadWriteLock</code>，因为内部维护的是一对锁（读锁、写锁）。这一对锁是围绕读写状态来工作，读写锁的自定义同步器需要在同步状态（一个整型变量）上维护多个读线程和一个写线程的状态，这一点就读写锁的关键。</p><p>&emsp;读写锁是将这一位整型变量，整型变量是4位32字节，切割成高16位（负责读）和低16位（负责写）2部分，这样就可以使用一个变量来维护读、写2种状态。若一个线程已经获取了写锁，且反复获取了2次写锁（即重入了2次），此时的读、写状态可以通过位运算来操作。</p><p>【写锁】</p><p>&emsp;写锁是支持重入的排他锁，若当前线程已获取写锁，则增加写状态，若当前线程在获取写锁时，读锁已被获取或该线程不是获取读写锁的线程，则当前线程进入等待状态。读写锁需要保证写锁操作对读锁的可见性，如果允许读锁在已被获取的情况下对写锁进行获取，那正在运行的其他线程是无法感知当前写线程的操作。因此当其他线程都释放了读锁，写锁才能被当前线程获取，而写锁一旦被获取，那后面的不论是读线程还是写线程都会被阻塞。在释放写锁时，每次都会减少写状态，当状态为0时表示锁已被释放。</p><p>【读锁】</p><p>&emsp;读锁是支持重入的共享锁，可以被多个线程同时获取，获取读锁时会进行读状态的线程安全的增加。若在获取读锁时，写锁已被其他线程获取，则进入等待状态；</p><p>【锁降级】</p><p>&emsp;锁降级指的是写锁降级为读锁。而且注意这个程过中写锁降级成读锁也不是直接性质上的转变（写锁转成读锁），本质上也是获取锁和释放锁的过程：锁降级是指当前线程仍然把握着当前拥有的写锁，然后再获取读锁，最后将写锁释放（注意：<font color="red">一定是把握写锁的后去获取读锁！</font>），流程为：获取写锁 -&gt; 获取读锁 -&gt; 释放写锁。如果当前线程拥有写锁，然后将其释放再获取读锁，我们的锁降级可不是指的这种逗逼情况。锁降级的这一操作流程是必不可少，主要是为了当前线程可以感知变量的修改即时可见。</p><p>&emsp;<code>ReentrantReadWriteLock</code>不支持锁升级（获取读锁、获取写锁，最后释放读锁），原因也是为了数据的可见性，如果读锁已经被多个线程获取，任意一个线程成功获取写锁并更新了数据，那此时这种更新对其他获取读锁的线程是不可见的。</p><h3 id="5-4-LockSupport工具"><a href="#5-4-LockSupport工具" class="headerlink" title="5.4 LockSupport工具"></a>5.4 LockSupport工具</h3><p>&emsp;在需要阻塞或者唤醒一个线程的时候，都会使用<code>LockSupport</code>工具类完成相应的工作，它提供了一组公共的静态方法来完成上述工作，将<code>park</code>方法定义为阻塞，将<code>unpark</code>定义为唤醒，似乎还挺形象的，运行的线程就像是一辆辆的车，线程的阻塞和唤醒对应于车辆的停下和启动。来看一下jdk8中源码吧：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 下面 blocker 参数为标识当前线程等待的对象</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//唤醒处于阻塞的线程，但是最起码线程已经启动了吧</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">unpark</span><span class="params">(Thread thread)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (thread != <span class="keyword">null</span>)</span><br><span class="line">        UNSAFE.unpark(thread);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//阻塞当前线程,blocker是负责该线程阻塞的同步器</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">park</span><span class="params">(Object blocker)</span> </span>&#123;</span><br><span class="line">    Thread t = Thread.currentThread();</span><br><span class="line">    setBlocker(t, blocker);</span><br><span class="line">    UNSAFE.park(<span class="keyword">false</span>, <span class="number">0L</span>);</span><br><span class="line">    setBlocker(t, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//阻塞当前线程，最长不超过nanos纳秒，前者的blocker就是同步器</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">parkNanos</span><span class="params">(Object blocker, <span class="keyword">long</span> nanos)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (nanos &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        Thread t = Thread.currentThread();</span><br><span class="line">        setBlocker(t, blocker);</span><br><span class="line">        UNSAFE.park(<span class="keyword">false</span>, nanos);</span><br><span class="line">        setBlocker(t, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//阻塞当前线程直到deadline时间</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">parkUntil</span><span class="params">(Object blocker, <span class="keyword">long</span> deadline)</span> </span>&#123;</span><br><span class="line">    Thread t = Thread.currentThread();</span><br><span class="line">    setBlocker(t, blocker);</span><br><span class="line">    UNSAFE.park(<span class="keyword">true</span>, deadline);</span><br><span class="line">    setBlocker(t, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 下面是上述方法的重载，上面多出来的同步器，可以从线程dump结果得到阻塞对象的更多信息，</span></span><br><span class="line"><span class="comment">* 方便定位，上面的方法主要问题排查和系统监控</span></span><br><span class="line"><span class="comment">* /</span></span><br><span class="line"><span class="comment">    //阻塞当前线程</span></span><br><span class="line"><span class="comment">    public static void park() &#123;</span></span><br><span class="line"><span class="comment">        UNSAFE.park(false, 0L);</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    //阻塞当前线程，最长不超过nanos纳秒</span></span><br><span class="line"><span class="comment">    public static void parkNanos(long nanos) &#123;</span></span><br><span class="line"><span class="comment">        if (nanos &gt; 0)</span></span><br><span class="line"><span class="comment">            UNSAFE.park(false, nanos);</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    //阻塞当前线程直到deadline时间</span></span><br><span class="line"><span class="comment">    public static void parkUntil(long deadline) &#123;</span></span><br><span class="line"><span class="comment">        UNSAFE.park(true, deadline);</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br></pre></td></tr></table></figure><p>推荐使用含阻塞对象的方法。</p><h3 id="5-5-Condition接口"><a href="#5-5-Condition接口" class="headerlink" title="5.5 Condition接口"></a>5.5 Condition接口</h3><p>&emsp;<code>Condition</code>接口在<code>Lock</code>接口中的<code>newCondition()</code>方法返回的就是这个东西。任何Java对象都有一组监视器的方法，通常是定义子在<code>java.lang.Object</code>上，包含了<code>wait()</code>、<code>wait(long timeout)</code>、<code>notify()</code>、<code>notifyAll()</code>方法，将这些方法和<code>synchronized</code>关键字结合可以实现等待/通知模式。<code>Contidition</code>接口中同样提供了类似于Object的监视器方法，和<code>Lock</code>配合可以实现等待/通知的模式。需要注意的是在线程在调用这些方法时，需要提前获取<code>Condition</code>对象关联的锁，它的锁是由<code>Lock</code>对象创建（<font color="red">调用<code>Lock</code>对象的<code>newCondition()</code></font>）。虽然<code>Object</code>和<code>Condition</code>都可以实现等待/通知，但两者有些区别：</p><table><thead><tr><th>对比项</th><th>Object 类的监视器</th><th>Condition 类的监视器</th></tr></thead><tbody><tr><td>前提条件</td><td>获取对象的锁</td><td>通过 <code>Lock.lock()</code> 获取锁，通过 <code>Lock.newCondition()</code> 获取 <code>Condition</code> 对象</td></tr><tr><td>调用方式</td><td>直接调用，如 <code>obj.wait()</code></td><td>直接调用，如 <code>cond.await()</code></td></tr><tr><td>等待队列个数</td><td>一个</td><td>多个</td></tr><tr><td>当前线程释放锁并进入等待状态</td><td>支持</td><td>支持</td></tr><tr><td>当前线程释放锁并进入等待状态，等待状态中不响应中断</td><td>不支持</td><td>支持</td></tr><tr><td>当前线程释放锁并进入超时等待状态</td><td>不支持</td><td>支持</td></tr><tr><td>当前线程释放锁并进入等待状态到某个时间</td><td>不支持</td><td>支持</td></tr><tr><td>唤醒等待队列中的一个线程</td><td>支持</td><td>支持</td></tr><tr><td>唤醒等待队列中的全部线程</td><td>支持</td><td>支持</td></tr></tbody></table><p>基本API，Condition接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Condition</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当前线程进入等待状态直到被通知（即 signal()/signalAll() 方法）或中断</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当前线程进入等待状态直到被通知，对中断不敏感</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">awaitUninterruptibly</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当前线程进入等待状态直到被通知、中断或超时，入参表示超时时间</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">long</span> <span class="title">awaitNanos</span><span class="params">(<span class="keyword">long</span> nanosTimeout)</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 同上面的方式，只是更加灵活，可自由指定时间单位</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">await</span><span class="params">(<span class="keyword">long</span> time, TimeUnit unit)</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当前线程进入等待状态直到被通知、中断或到某个时间</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">awaitUntil</span><span class="params">(Date deadline)</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 唤醒一个等待在 Condition 上的线程，该线程从等待方法返回前必须获得与 Condition 相关联的锁</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">signal</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 唤醒所有等待在 Condition 上的线程，同样使用前必须先获取相关联的锁</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">signalAll</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;<code>Condition</code>定义了2种类型的等待/通知方法，当前线程调用这些方法时，需要提前获取到 <code>Condition</code> 对象关联的锁，<code>Condition</code> 对象是由 <code>Lock</code> 对象创建（通过<code>Lock.newCondition()</code>），换而言之，<code>Condition</code>是依赖<code>Lock</code>对象的。<code>Condition</code>使用比较简单，但<font color="red">一定需要在调用方法前必须先获取锁</font>，使用示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConditionUseCase</span> </span>&#123;</span><br><span class="line">    Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    Condition condition = lock.newCondition();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">conditionWait</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 先获取锁，再对 condition 操作</span></span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 当前线程释放锁并在此等待</span></span><br><span class="line">            condition.await();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">conditionSignal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 现获取锁，再对 condition 操作</span></span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 通知当前线程，让之前 await 的线程从等待状态退出</span></span><br><span class="line">            condition.signal();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 测试类</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConditionUseCaseMain</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> ThreadPoolExecutor executor = <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">5</span>, <span class="number">10</span>, <span class="number">10</span>, TimeUnit.MILLISECONDS, <span class="keyword">new</span> LinkedBlockingQueue&lt;&gt;(<span class="number">2</span>), <span class="keyword">new</span> MyFactoryBuilder().namePrefix(<span class="string">"condition-use-case"</span>).build());</span><br><span class="line"></span><br><span class="line">        ConditionUseCase conditionUseCase = <span class="keyword">new</span> ConditionUseCase();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 共创建10个线程进行等待行为</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            executor.execute(() -&gt; &#123;</span><br><span class="line">                System.out.println(Thread.currentThread() + <span class="string">" has been created! ThreadPool's thread count: "</span> + executor.getActiveCount());</span><br><span class="line">                conditionUseCase.conditionWait();</span><br><span class="line">                System.out.println(Thread.currentThread() + <span class="string">" has been waked up! ThreadPool's thread count: "</span> + executor.getActiveCount());</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">"main-thread will sleep 5s"</span>);</span><br><span class="line">        SleepUtils.sleep(<span class="number">5</span>);</span><br><span class="line">        System.out.println(<span class="string">"main-thread waked up after sleep 5s"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 主线程睡眠5s 后对线程池中等待的线程进行唤醒操作，1s一次</span></span><br><span class="line">        <span class="keyword">while</span> (executor.getActiveCount() != <span class="number">0</span>) &#123;</span><br><span class="line">            System.out.println(<span class="string">"taskCount: "</span> + executor.getTaskCount() + <span class="string">", activeCount: "</span> + executor.getActiveCount());</span><br><span class="line">            conditionUseCase.conditionSignal();</span><br><span class="line">            SleepUtils.sleep(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"======================end======================"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在测试的时候，发现一个奇怪的问题，就是我的线程池中最后总会有5个线程处于等待状态（知道的同学给个解释呗，后面查看得知在线程池种创建的核心线程数一旦创建就不会变化了，即不论线程是否执行完毕都会有corePoolSize个线程会一直阻塞在线程池中，直到去等待队列中可以获取到任务）。这个结论是通过JProfiler观察得到的，但通过打印得到的线程数量为1。通常情况下，会将<code>Condition</code>对象作为成员变量，在<strong>调用<code>await()</code>后，当前线程会释放锁并且在此等待</strong>，而其他线程<strong>调用<code>signal()</code>后，通知当前线程后，当前线程才会从<code>await()</code>返回,终止等待的状态</strong>，并且在返回时就已经获取了锁。下面是有个利用 Condition 实现的有界队列，比上面的靴微地好理解点：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.hhu.threads;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Condition;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Contion的案例：有界队列的实现</span></span><br><span class="line"><span class="comment"> * created by WeiguoLiu on 2017/12/29</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BoundedQueue</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Object[] items;</span><br><span class="line">    <span class="comment">//增加、移除、数组中的数据量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> addIndex, removeIndex, count;</span><br><span class="line">    <span class="keyword">private</span> Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    <span class="keyword">private</span> Condition notEmpty = lock.newCondition();</span><br><span class="line">    <span class="keyword">private</span> Condition notFull = lock.newCondition();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BoundedQueue</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//在构造方法中进行数组的初始化工作</span></span><br><span class="line">        items = <span class="keyword">new</span> Object[size];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getSize</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//往里面添加一个元素</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(T t)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="comment">//首先获取锁</span></span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//如果队列已满，那么添加数据的线程将会被阻塞，直到队列有空位</span></span><br><span class="line">            <span class="keyword">while</span>(count==items.length) &#123;</span><br><span class="line">                notFull.await();</span><br><span class="line">            &#125;</span><br><span class="line">            items[addIndex] = t;</span><br><span class="line">            <span class="keyword">if</span>(++addIndex==items.length) &#123;</span><br><span class="line">                addIndex = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            ++count;</span><br><span class="line">            <span class="comment">//通知正在await的线程</span></span><br><span class="line">            notEmpty.signal();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">//最后释放锁</span></span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">remove</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">while</span>(count==<span class="number">0</span>) &#123;</span><br><span class="line">                notEmpty.await();</span><br><span class="line">            &#125;</span><br><span class="line">            Object x = items[removeIndex];</span><br><span class="line">            <span class="keyword">if</span>(++removeIndex==items.length) &#123;</span><br><span class="line">                removeIndex = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            --count;</span><br><span class="line">            notFull.signal();</span><br><span class="line">            <span class="keyword">return</span> (T)x;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-5-2-Condition-原理"><a href="#5-5-2-Condition-原理" class="headerlink" title="5.5.2 Condition 原理"></a>5.5.2 Condition 原理</h4><p>&emsp;ConditionObject是同步器 AbstractQueuedSynchronizer 的内部类，因为 Condition 操作需要获取相关联的锁，所以作为同步器的内部类也比较合理。每个 Condition 对象都包含着一个队列（即等待队列），这个队列是实现等待/通知的关键。</p><p>【等待队列】</p><p>&emsp;等待队列是一个FIFO的队列，在队列中的每个节点都包含一个线程（即在 Condition 对象上等待的线程）的引用，如果一个线程调用了 <code>Condition.await()</code> 方法，那该线程将会释放锁、构造成节点加入等待队列并进入等待状态。在节点中定义复用了同步器中节点的定义，即同步队列和等待队列中节点类型都是同步器的静态内部类 <code>AbstractQueuedSychronizer.Node</code>。Condition 包含的等待队列和之前的同步队列类似，拥有首尾节点的引用，当新增节点时只要将原尾节点的 next 指向这个新构造的节点即可。但这个过程并没有使用CAS保证，原因在调用 <code>await()</code> 方法的线程必定已经获取了锁，因此上述的过程是通过锁来保证线程安全的。</p><p>&emsp;<code>Object</code>的监视器模型上，一个对象拥有一个同步队列和等待队列，而<code>Lock</code>拥有一个同步队列和等待队列。Condition实现的同步器的内部类，每个Condition 实列都能访问同步器提供的方法，相当于都拥有所属同步器的引用。</p><p>注：<strong>等待队列和同步队列的区别</strong> - 等待队列和同步队列两者不是一个东西，一般只有通过<code>wait()</code>或<code>await()</code>方法才会进入等待队列，它是处于等待的状态，而同步队列中的线程不是处于等待状态，而是处于竞争锁的状态，当等待队列中的线程被唤醒后才能进入同步队列参与锁的竞争。</p><p>【等待/通知】</p><p>&emsp;等待 <code>Condition</code> 的 <code>await()</code> 方法，会使当前线程进入等待队列并释放锁，同时线程变成等待状态，当从<code>await()</code>方法返回时，当前线程一定是获取了 Condition 相关联的锁。Condition 的 <code>signal()</code> 方法会唤醒在等待队列中等待时间最长的节点（即首节点，新构造的节点是加入队列尾部），将该节点移至同步队列中，当然 <code>signal()</code> 调用前也必须获取锁</p><h2 id="4-Java并发容器和框架"><a href="#4-Java并发容器和框架" class="headerlink" title="4. Java并发容器和框架"></a>4. Java并发容器和框架</h2><h3 id="4-1-ConcurrentHashMap"><a href="#4-1-ConcurrentHashMap" class="headerlink" title="4.1 ConcurrentHashMap"></a>4.1 ConcurrentHashMap</h3><p>&emsp;关于<code>HashMap</code>和<code>Hashtable</code>这俩儿货一直是面试过程中属于阴魂不散的，最本质的区别就是前者不是线程安全的，而后者是线程安全的，通过查看jdk的源码可以看到在<code>Hashtable</code>提供的大部分方法都加上了<code>synchronized</code>关键字，所以明白它为啥是线程安全的吧，但是一直说<code>synchronized</code>是不好的，因为它重啊，阻塞式的保证同一时刻只有一个线程可以获取唯一的一把锁，所以可想而知在并分量达到一定数值时，<code>Hashtable</code>的效率实在让人失望，老版本中的<code>HashMap</code>还存在并发环境下执行<code>put</code>操作出现死循环的问题（不过我用jdk8中并没有测试出来这个问题，不知道是不是源码修改了，后面再研究），处于这样的尴尬情况，所以<code>ConcurrentHashMap</code>出现了（线程安全并且效率比<code>Hashtable</code>高）。</p><p>&emsp;<code>ConcurrentHashMap</code>主要通过分段锁的技术提高了并发访问的效率，分段锁就是<code>ConcurrentHashMap</code>这个容器中不像<code>Hashtable</code>只有一把锁抢起来太费劲了，它以数据分段为单位，为每一段数据提供一把锁，在获取不同数据段锁的时候，就大大降低了竞争发生的概率和竞争的激烈程度，当A线程访问1号数据段的时候，那么B线程可以访问2号数据段呀，完全不要去竞争，多和谐！</p><p>&emsp;在<code>ConcurrentHashMap</code>中，包含了一个<code>Segment</code>数据结构和一个<code>HashEntry</code>数组。<code>Segment</code>是一种重入锁（<code>ReentrantLock</code>），而<code>HashEntry</code>是用于存储数据的键值对，哦对了，<code>Segment</code>也是一种键值对的数据形式，在其中扮演着锁的角色，包含着<code>HashEntry</code>数组，而<code>HashEntry</code>又是一种链表结构，每个<code>Segment</code>都将守护着<code>HashEntry</code>数组，当每次要对<code>HashEntry</code>里面的数据进行更改时必须先获取守护它的<code>Segment</code>锁。我们可以形象的将<code>ConcurrentHashMap</code>的结构描述为（鬼画符啊）：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/20191113_ConcurrentHashMap.png" alt="这里写图片描述"></p><p><code>ConcurrentHashMap</code>中的<code>get</code>操作高效在于<code>get</code>操作不需要加锁，除非读到的值为空才会加锁重读，而<code>Hashtable</code>中的<code>get</code>操作是需要加锁的，在<code>ConcurrentHashMap</code>的get方法中将要使用的共享变量都定义为<code>volatile</code>类型（比如用于统计当前<code>Segment</code>大小的<code>count</code>字段和用于存储值的<code>HashEntry</code>的<code>value</code>）；在<code>ConcurrentHashMap</code>的<code>put</code>操作中，在操作共享变量时，为了线程安全，必须加锁，<code>put</code>方法首先定位到<code>Segment</code>，然后在<code>Segment</code>里面进行插入操作，插入操作主要经过两个步骤：</p><ol><li>判断是否需要对<code>Segment</code>里面的<code>HashEntry</code>数组进行扩容（<code>HashMap</code>中扩容时是先插入元素再判断是否达到容量值，而<code>Segment</code>的扩容是在插入前先判断再插入，这样就避免了扩容后没有元素插入的尴尬情况，<code>HashMap</code>扩容时是将其容量阔成为原来的2倍，而在<code>ConcurrentHashMap</code>中，为了高效，并不会对整个<code>ConcurrentHashMap</code>进行扩容，而是仅对某个<code>Segment</code>进行扩容）；</li><li>其次定位添加元素的位置，然后将其放在<code>HashEntry</code>数组中。</li></ol><h3 id="4-2-队列"><a href="#4-2-队列" class="headerlink" title="4.2 队列"></a>4.2 队列</h3><p>&emsp;在线程安全队列中，有阻塞算法和非阻塞算法，阻塞算法可以使用一个锁（入队和出队用同一把锁）也可以使用两把锁（出队和入队使用不同的锁）；非阻塞算法则是使用循环CAS方式实现。</p><h4 id="4-2-1-ConcurrentLinkedQueue"><a href="#4-2-1-ConcurrentLinkedQueue" class="headerlink" title="4.2.1 ConcurrentLinkedQueue"></a>4.2.1 ConcurrentLinkedQueue</h4><p>&emsp;<code>ConcurrentLinkedQueue</code>是基于链接节点的无界线程安全队列，采用先进先出的规则对节点进行排序，新增元素添加队列尾部；获取元素时，返回的时队列头部的元素，采用“wait-free”算法（即CAS算法，在原有的 Michael&amp;Scott 算法上进行了修改）。<code>ConcurrentLinkedQueue</code>主要由head节点和tail2个节点组成（综合来看Node这个内部类出现频率相当高，内部维护了一个元素item和一个next节点），各个节点之间通过next进行关联，以链表的形式组成队列（默认head节点为空，tail节点等于head节点）。在入队过程中，先将入队节点设置为当前队尾节点的next，如果tail的next为空，则将入队节点设置为tail的next，所以tail不总是尾节点。</p><p><strong>2.阻塞队列</strong><br>阻塞队列是一个支持两个附加操作的队列，这两个附加的操作支持阻塞的插入和移除的方法。首先阻塞的插入方法指：当队列满时，队列会阻塞插入元素的线程，直到线程不满；支持阻塞的移除方法是指：在队列为空时，获取元素的线程会等待队列变为非空。在阻塞队列不可用时，上述的两种操作提供了如下的4种处理方式（都是针对是不同线程对该队列进行取、放操作）：<br>1.抛出异常：当队列满时再有线程执行插入操作，会抛出IllegalStateException异常，当队列为空时再获取元素会抛出NoSuchElementException异常；<br>2.返回特殊值：当线程插入成功时返回true，移除则是从队列中取出一个元素，如果没有则返回null;<br>3.一只阻塞：当阻塞队列满时再插入（put）时，队列会一直被阻塞，当队列为空时，线程获取元素将会，那么此线程将会被一直阻塞。<br>4.超时退出：当阻塞队列满时，当有线程向里面插入时，队列将会阻塞它一段时间，如果超出了指定时间将会使该线程退出。</p><p><strong>在JDK7中提供了7中阻塞队列，主要如下：</strong></p><ol><li>ArrayBlockingQueue<br> ArrayBlockingQueue是用数组实现的有界阻塞队列，按照先进先出的原则进排序，默认不保证线程公平的访问队列（即不保证阻塞的先后顺序访问队），阻塞的线程可以任意争夺这个资源，可能最先被阻塞的线程一直到最后才访问到队列资源。通常情况下，为了保证公平性会降低吞吐量</li><li>LinkedBlockingQueue<br>LinkedBlockingQueue是利用链表实现的有界阻塞队列，默认和最大的长度为Integer.MAX_VALUE，按照先进先出的原则进行排序。</li><li>PriorityBlockingQueue<br>PriorityBlockingQueue是一个支持优先级的无界阻塞队列，默认情况下元素采取自然顺序升序排序，也可以自定义类实现compareTo()方法指定排序规则，或者初始化PriorityBlockingQueue时，制定构造参数Comparator对元素进行排序，但是<font color="red">不能保证同优先级别的顺序</font>。</li><li>DelayQueue<br>DelayQueue支持延时获取元素的无界阻塞队列，队列使用PriorityQueue实现，但是队列中的元素必须实现Delayed接口，在创建元素时可以指定多久才能从队列中获取当前元素，只有在延迟期满时才能从队列从提取元素。这玩意儿很有用，可以运用在缓存系统的设计和定时任务调度的场景下</li><li>SynchronousQueue<br>SynchronousQueue是一个不存储元素阻塞队列，每个put操作必须等待一个take操作，否则不能继续添加元素，它支持公平访问队列，默认是采用非公平的性策略访问，使用如下的构造方法进行初始化的时候会进行相应的设置</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">public synchronousQueue(boolean fair) &#123;</span><br><span class="line">transferer = fair ? new TransferQueue():new TransferStack();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>SynchronusQueue队列本身并不存储任何元素，只是将线程处理过的</p><ol start="6"><li>LinkedTransferQueue</li><li>LinkedBlockingDeque</li></ol><h1 id="第五章-Java中的原子操作类"><a href="#第五章-Java中的原子操作类" class="headerlink" title="第五章 Java中的原子操作类"></a>第五章 Java中的原子操作类</h1><p>多线程除了利用同步监视器和同步代码块来保证线程之间的共享资源的安全外，在JDK5后的版本中提供了原子操作类来更为简单且性能高效、线程安全的变量方式，这个问题也是在之前面试过程中被问懵逼的一个技术点，由于在学校对于Java的接触面一直比较狭隘，仅仅知道有个线程安全的概念，所以根本不知到有这么个东西，Java提供的原子操作都是在</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java.util.concurrent.atomic</span><br></pre></td></tr></table></figure><p>这个包中，总共13个原子操作类，每个类都有基本都是实用Unsafe实现的包装类，主要分为4种类型：原子更新基本类型、原子更新数组、原子更新引用和原子更新属性。</p><h2 id="1-原子更新基本类型类"><a href="#1-原子更新基本类型类" class="headerlink" title="1.原子更新基本类型类"></a>1.原子更新基本类型类</h2><p>原子更新基本类型的类，Atomic主要提供下面的3个类：<br>AtomicInteger:原子更新整型数<br>AtomicLong:原子更新长整型数<br>AtomicBoolean:原子更新布尔类型数<br>下面是关于AtomicInteger的一Demo：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.Atomic;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.atomic.AtomicInteger;</span><br><span class="line">import java.util.concurrent.atomic.AtomicIntegerArray;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/14 9:34</span><br><span class="line"> */</span><br><span class="line">public class AtomicIntegerTest &#123;</span><br><span class="line">    //定义原子整型数</span><br><span class="line">    static AtomicInteger ai = new AtomicInteger(1);</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        //AtomicInteger中的getAndIncrement方法是将当前值加1并返回自增操作前的旧值</span><br><span class="line">        System.out.println(ai.getAndIncrement());</span><br><span class="line">        //获取当前值</span><br><span class="line">        System.out.println(ai.get());</span><br><span class="line">        //进行加和操作并返回加和之前的值</span><br><span class="line">        System.out.println(ai.getAndAdd(2));</span><br><span class="line">        System.out.println(ai.get());</span><br><span class="line">        //进行加和操作并返回结果，注意和上面的方法的区别，其实就是顺序，上面是先获取再加和</span><br><span class="line">        System.out.println(ai.addAndGet(2));</span><br><span class="line">        //CAS操作返回原值和预期值是否相同，相同就更新并返回true,否者不更新返回false</span><br><span class="line">        System.out.println(ai.compareAndSet(5, 8));</span><br><span class="line">        System.out.println(ai.get());</span><br><span class="line">        //返回旧值并设置新值</span><br><span class="line">        System.out.println(ai.getAndSet(5));</span><br><span class="line">        System.out.println(ai.get());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-原子更新数组"><a href="#2-原子更新数组" class="headerlink" title="2.原子更新数组"></a>2.原子更新数组</h2><p>对数组的更新类似于上述的操作，Atomic提供下面的4个类：<br>1.AtomicIntegerArray:原子更新整型数组里面的元素；<br>2.AtomicLongArray:原子更新长整型数组元素；<br>3.AtomicReferenceArray:原子更新引用类型数组里面的元素；<br>下面的是原子更新整型数组的Demo：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.Atomic;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.atomic.AtomicInteger;</span><br><span class="line">import java.util.concurrent.atomic.AtomicIntegerArray;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/14 9:34</span><br><span class="line"> */</span><br><span class="line">public class AtomicIntegerArrayTest &#123;</span><br><span class="line">    //定义原子整型数组</span><br><span class="line">    static int[] value= new int[]&#123;1, 2&#125;;</span><br><span class="line">    //这里将数组以构造方法的方式传进去，Atomic在操作时，会把这个数组在内部复制一份在</span><br><span class="line">    //然后在内部对它复制的数组进行操作而不会去影响原来的数组</span><br><span class="line">    static AtomicIntegerArray aa = new AtomicIntegerArray(value);</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        //获取数组中的第一个数，并设置新值为3</span><br><span class="line">        aa.getAndSet(0, 3);</span><br><span class="line">        //对内部有影响</span><br><span class="line">        System.out.println(aa.get(0));</span><br><span class="line">        //这里对原有的数组不会影响</span><br><span class="line">        System.out.println(value[0]);</span><br><span class="line">        //将对应索引号的元素进行加和操作</span><br><span class="line">        System.out.println(aa.addAndGet(0, 3));</span><br><span class="line">        //数组中的CAS操作和单个的Atomic基本类型操作类似，但是必须提供数组的索引号</span><br><span class="line">        System.out.println(aa.compareAndSet(0, 5, 9));</span><br><span class="line">        System.out.println(aa.get(0));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-原子更新引用类型"><a href="#3-原子更新引用类型" class="headerlink" title="3.原子更新引用类型"></a>3.原子更新引用类型</h2><p>原子更新多个变量，可以使用原子更新引用类型的类，不像原子更新的基本类型，Atomic包中提供了下面的3个类：<br>1.AtomicReference:原子更新引用类型；<br>2.AtomicReferenceFieldUpdater:原子更新引用类型里面的字段<br>3.AtomicMarkableReference:原子更新带有标记位的引用类型，可以更新布尔类型的标记位和引用类型，构造方法为<code>AtomicMarkableReference(V initialRef, boolean initialMark)</code><br>下面是AtomicReference的一个Demo:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.Atomic;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.atomic.AtomicReference;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 测试原子更新引用类型</span><br><span class="line"> * 由于AtomicInteger只能对单个变量进行更新，对多个变量的更新</span><br><span class="line"> * 就需要使用这个原子更新的引用类,这是对一个引用的整体更新，而非局部的字段更新</span><br><span class="line"> *</span><br><span class="line"> * AtomicReference:原子更新引用类型</span><br><span class="line"> * AtomicReferenceFieldUpdater:原子更新引用类型里面的字段</span><br><span class="line"> * AtomicMarkableReference:原子更新带有标记位的引用类型，可以原子更新布尔类型的标记位和引用类型</span><br><span class="line"> * 构造方法是AtomicMarkableReference(V initialRef, boolean initialMark)</span><br><span class="line"> *</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/14 10:20</span><br><span class="line"> */</span><br><span class="line">public class AtomicReferenceTest &#123;</span><br><span class="line">    public static AtomicReference&lt;User&gt; af = new AtomicReference&lt;User&gt;();</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        User user = new User(&quot;jack&quot;, 15);</span><br><span class="line">        af.set(user);</span><br><span class="line">        User updateUser = new User(&quot;air&quot;, 18);</span><br><span class="line">        //调用CAS进行更新，先判断原变量是否还是user，是的话就更新为新值，否则不更新</span><br><span class="line">        af.compareAndSet(user, updateUser);</span><br><span class="line">        System.out.println(af.get().getName());</span><br><span class="line">        System.out.println(af.get().getAge());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    static class User &#123;</span><br><span class="line">        private String name;</span><br><span class="line">        private int age;</span><br><span class="line"></span><br><span class="line">        public User(String name, int age) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">            this.age = age;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public String getName() &#123;</span><br><span class="line">            return name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public int getAge() &#123;</span><br><span class="line">            return age;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-原子更新字段"><a href="#4-原子更新字段" class="headerlink" title="4.原子更新字段"></a>4.原子更新字段</h2><p>注意和上面原子更新引用类型的区别<br>原子更新对<font color="red">某个类中的某个字段</font>，Atomic包中主要提供了下面的3个类：<br>1.AtomicIntegerFieldUpdater:原子更新整型数的更新器<br>2.AtomicLongFieldUpdater:原子更行长整型的更新器<br>3.AtomicStampedReference:原子更新带有版本号的引用类型，这个类将整数值与引用关联，用于原子的更新数据和版本号，这就解决了CAS操作中可能出现的ABA问题。</p><p>原子更新字段的主要分为两个步骤：<br>1.因为原子更新字段类都是抽象类，所以每次使用的时候必须使用静态方法new Updater()来创建一个更新器，并且指定其中需要更新的类和该类实例的某个字段属性名；<br>2.更新类的某个属性字段必须需要设置为public volatile才能对此字段进行更新；<br>上述的更新过程就是不断对更新类的中待更新的字段的不断的创建的更新器，并且将其中更新的字段名设置进去即可，下面是更新器的一个Demo：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.Atomic;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.atomic.AtomicIntegerFieldUpdater;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 原子更新某个类的某个字段，在Atomic包中主要提供的3个类：</span><br><span class="line"> * AtomicIntegerFieldUpdater:原子更新整型的字段更新器</span><br><span class="line"> * AtomicLongFieldUpdater:原子更新长整形字段的更新器</span><br><span class="line"> * AtomicStampedReference:原子更新带有本版号的引用类型，可以解决CAS操作的ABA问题</span><br><span class="line"> *</span><br><span class="line"> * 原子更新字段类的步骤如下：</span><br><span class="line"> * 1.因为原子更新字段类都是抽象类，每次使用的时候必须使用静态方法newUpdater()创建一个更新器</span><br><span class="line"> * 并设置想要更新的类和属性</span><br><span class="line"> *</span><br><span class="line"> * 2.更新类的字段必须使用public volatile修饰</span><br><span class="line"> *</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/14 10:44</span><br><span class="line"> */</span><br><span class="line">public class AtomicIntegerFieldUpdaterTest &#123;</span><br><span class="line">    //首先创建原子更新器，并设置需要更新的对象和对象的字段</span><br><span class="line">    private static AtomicIntegerFieldUpdater&lt;User&gt; afu =  AtomicIntegerFieldUpdater.newUpdater(User.class, &quot;age&quot;);</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        User user = new User(&quot;conan&quot;, 10);</span><br><span class="line">        //获取更字段更新器对User的年龄进行更新（自增1，获取自增前的值）</span><br><span class="line">        System.out.println(afu.getAndIncrement(user));</span><br><span class="line">        //获取更更行器中类的字段的属性值</span><br><span class="line">        System.out.println(afu.get(user));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class User &#123;</span><br><span class="line">        private String name;</span><br><span class="line">        //必须将更新的字段设置为public volatile</span><br><span class="line">        public volatile int age;</span><br><span class="line"></span><br><span class="line">        public User(String name, int age) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">            this.age = age;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public String getName() &#123;</span><br><span class="line">            return name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public int getAge() &#123;</span><br><span class="line">            return age;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="第六章-Java并发工具类"><a href="#第六章-Java并发工具类" class="headerlink" title="第六章 Java并发工具类"></a>第六章 Java并发工具类</h1><p>jdk并发包提供了一些并发工具类：CountDownLatch、CyclicBarrier和Semaphore工具类提供了并发流程控制的手段，Exchanger提供了在线程间交换数据的一种手段。</p><h2 id="1-等待多线程完成的CountDownLatch"><a href="#1-等待多线程完成的CountDownLatch" class="headerlink" title="1.等待多线程完成的CountDownLatch"></a>1.等待多线程完成的CountDownLatch</h2><p>场景：多线程解析EXCEL，每个线程负责一个sheet，当所有线程解析完成后给出提示，所以这个提示必须是在所有线程完成之后才可以，下面是利用join方法完成的案例，直接将每个sheet分配给各个线程，然后在主线程中调用各个线程的join方法来阻塞主线程，在主线程中给出提示即可，Demo如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 场景：多线程解析EXCEL，每个线程负责一个sheet，当所有线程解析完成后给出提示</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 8:52</span><br><span class="line"> */</span><br><span class="line">public class JoinCountDownLatchTest &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Thread parser1 = new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                System.out.println(&quot;parser1 has finshed task1!&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        Thread parser2 = new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                System.out.println(&quot;parser2 has finshed task2!&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        parser1.start();</span><br><span class="line">        parser2.start();</span><br><span class="line">        try &#123;</span><br><span class="line">            //这里注意join的线程必须在启动后才能生效，其原理是不停地检查</span><br><span class="line">            //join线程是否存活，如果存活则让当前线程永远等待，也可以指定等待的时间，</span><br><span class="line">            //在join线程终止后，线程的this.notifyAll()会被调用（这个方法在JVM中，需查看JVM源码），</span><br><span class="line">            parser1.join();</span><br><span class="line">            parser2.join();</span><br><span class="line">        &#125; catch (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(&quot;Excel parse has finshed!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，下面利用JDK提供的CountDownLatch并发工具类完成上述的工作，就是等地，代码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.CountDownLatch;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 利用JDK中提供的CountDownLatch实现</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 9:18</span><br><span class="line"> */</span><br><span class="line">public class CountDownLatchTest &#123;</span><br><span class="line">    //这里传入参数作为计数器，等待N个点完成就传入N即可，这里将线程的数量传入，必须大于0</span><br><span class="line">    static CountDownLatch c = new CountDownLatch(2);</span><br><span class="line">    public static void main(String[] args) throws InterruptedException &#123;</span><br><span class="line">        new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                System.out.println(&quot;Task1 has complated!&quot;);</span><br><span class="line">              </span><br><span class="line">                /*</span><br><span class="line">                每次调用CountDownLatch的countDown方法，计数器就会执行减1操作</span><br><span class="line">                这个方法可以在任意地方执行，所以上面计数器的N可以是线程的数量，也可以</span><br><span class="line">                也可以是1个线程里的N个步骤</span><br><span class="line">                 */</span><br><span class="line">                c.countDown();</span><br><span class="line">                System.out.println(&quot;Task2 has complated!&quot;);</span><br><span class="line">                c.countDown();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">        /*CountDownLatch的await的方法会阻塞当前线程，直到CountDownLatch的计数器变为0</span><br><span class="line">        如果有某个线程执行速度比较慢，也可以指定等地的时间await(long timeout, TimeUnit unit)</span><br><span class="line">         */</span><br><span class="line">        c.await();</span><br><span class="line">        System.out.println(&quot;Task has all complated!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-同步屏障CyclicBarrier"><a href="#2-同步屏障CyclicBarrier" class="headerlink" title="2.同步屏障CyclicBarrier"></a>2.同步屏障CyclicBarrier</h2><p>CyclicBarrier用于线程屏障的阻塞，当线程到达屏障点后调用CyclicBarrier的await方法告诉CyclicBarrier该线程已经到达屏障点并且被阻塞，等待其他线程全部到达屏障点后在全部运行，至于各个线程是哪个先运行哪个后运行，全凭自己去抢，优势多线程之战了，下面是一个简单的Demo:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.BrokenBarrierException;</span><br><span class="line">import java.util.concurrent.CyclicBarrier;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 可循环屏障的案例</span><br><span class="line"> * CyclicBarrier的默认构造方法需要传入一个整型数，一般指线程数</span><br><span class="line"> * 每个线程达到屏障点的时候会被阻塞，直到最后一个线程达到屏障时，屏障才会开门</span><br><span class="line"> * 这时所有的线程才会继续运行</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 9:40</span><br><span class="line"> */</span><br><span class="line">public class CyclicBarrierTest &#123;</span><br><span class="line">    //这里设置的有几个屏障点就必须有几个线程去调用CyclicBarrier的await的方法</span><br><span class="line">    //否则suo</span><br><span class="line">    static CyclicBarrier c = new CyclicBarrier(2);</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                try &#123;</span><br><span class="line">                    //每个线程调用await()告诉CyclicBarrier我已经到达屏障点了然后被阻塞，</span><br><span class="line">                    //这里是随意放了一个位置作为阻塞屏障点</span><br><span class="line">                    c.await();</span><br><span class="line">                &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; catch (BrokenBarrierException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(&quot;Task1 has complated!&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line"></span><br><span class="line">        try &#123;</span><br><span class="line">            c.await();</span><br><span class="line">        &#125; catch (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; catch (BrokenBarrierException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(&quot;Task has complated!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>【注】这里到底是先输出哪个字符串是不一定的，而且设置的CyclicBarrier设置的屏障点的数量一定要和后面调用CyclicBarrier调用其await的次数一致，否则其他的线程到达屏障点后会一直被阻塞在屏障点，直到await的调用次数达到设置的值。<br>当然CyclicBarrier也提供高级的构造方法CyclicBarrier(int parties, Runnable barrierAction)，在所有线程到达屏障点后优先执行barrierAction，案例如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.BrokenBarrierException;</span><br><span class="line">import java.util.concurrent.CyclicBarrier;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * CyclicBarrier还提供了比较高级的构造函数CyclicBarrier(),</span><br><span class="line"> * 用于达到屏障时优先执行，方便复杂业务场景</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 10:00</span><br><span class="line"> */</span><br><span class="line">public class CyclicBarrierTest2 &#123;</span><br><span class="line">    /*CyclicBarrier提供的更为高级的构造方法CyclicBarrier(int parties, Runnable barrierAction)，</span><br><span class="line">    在各个线程到达屏障点后，会优先执行barrierAction</span><br><span class="line">     */</span><br><span class="line">    static CyclicBarrier c = new CyclicBarrier(2, new A());</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                try &#123;</span><br><span class="line">                    /*这里放一个屏障点，线程执行到这个地方时会告诉CyclicBarrier我已经到达屏障点了</span><br><span class="line">                    然后就会被阻塞在这里，等待其他的线程到达各个的屏障点</span><br><span class="line">                     */</span><br><span class="line">                    c.await();</span><br><span class="line">                &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; catch (BrokenBarrierException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(&quot;Task1 has complated!&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">        try &#123;</span><br><span class="line">            c.await();</span><br><span class="line">        &#125; catch (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; catch (BrokenBarrierException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(&quot;Task2 has complated!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    static class A implements Runnable &#123;</span><br><span class="line">        @Override</span><br><span class="line">        public void run() &#123;</span><br><span class="line">            System.out.println(&quot;Task3 has complated!&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同理，上述Demo中的只有Task3是优先执行的，至于Task1和Task2谁先执行，看运气咯。下面是多线程处理银行流水的Demo，处理方式主要和Excel差不太多，各个线程完成各个Sheet的流水处理，然后由优先线程来统计银行总的流水信息，主要代码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.Map;</span><br><span class="line">import java.util.concurrent.*;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 场景：每隔Sheet保存一个银行账户的近一年的银行流水，现在要统计</span><br><span class="line"> * 用户的日均银行流水，先用多线程处理每个Sheet里面的银行流水，操作</span><br><span class="line"> * 完得到每个Sheet里面的流水，最后用CyclicBarrier里面的barrierAction执行计算结果</span><br><span class="line"> *</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 10:45</span><br><span class="line"> */</span><br><span class="line">public class BankWaterService implements Runnable &#123;</span><br><span class="line"></span><br><span class="line">    //构造CyclicBarrier，4个屏障点，优先执行本服务线程，即下面重写的run方法</span><br><span class="line">    private CyclicBarrier c = new CyclicBarrier(4, this);</span><br><span class="line"></span><br><span class="line">    //构造定长线程池，只启动4个线程，注意线程池的构建方式</span><br><span class="line">    private Executor executor = Executors.newFixedThreadPool(4);</span><br><span class="line"></span><br><span class="line">    //用于保存每个线程处理流水的结果，用于后面本服务线程的统计</span><br><span class="line">    private ConcurrentHashMap&lt;String, Integer&gt; sheetBanWaterCount = new ConcurrentHashMap&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line">    private void count() &#123;</span><br><span class="line">        //这里循环创建的线程池的4个线程执行任务</span><br><span class="line">        for (int i = 0; i &lt; 4; i++) &#123;</span><br><span class="line">            executor.execute(new Runnable() &#123;</span><br><span class="line">                @Override</span><br><span class="line">                public void run() &#123;</span><br><span class="line">                    //这里是将线程名和值1放入Map中</span><br><span class="line">                    sheetBanWaterCount.put(Thread.currentThread().getName(), 1);</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + &quot; has processed task&quot;);</span><br><span class="line">                    try &#123;</span><br><span class="line">                        /*到达线程屏障点，线程被阻塞，等待其他线程到达屏障点，这里是一个线程一个屏障点，</span><br><span class="line">                         * 由于是多线程，循环创建4个线程，每个线程一个屏障点，4个线程4个屏障点，完美</span><br><span class="line">                         * 在每个线程完成流水统计后设置屏障点，当前线程的等待</span><br><span class="line">                         */</span><br><span class="line">                        c.await();</span><br><span class="line">                    &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125; catch (BrokenBarrierException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /*该线程是CyclicBarrier优先执行的线程任务，在所有计算流水线程处理完毕到达屏障点后，优先执行这个综合</span><br><span class="line">    各个线程的结果即可</span><br><span class="line">     */</span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        int result = 0;</span><br><span class="line">        //便利流水处理结果进行加和处理</span><br><span class="line">        for (Map.Entry&lt;String, Integer&gt; sheet : sheetBanWaterCount.entrySet()) &#123;</span><br><span class="line">            result += sheet.getValue();</span><br><span class="line">        &#125;</span><br><span class="line">        sheetBanWaterCount.put(&quot;result&quot;, result);</span><br><span class="line">        System.out.println(&quot;All threads have complated, result = &quot; + result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        BankWaterService bankWaterCount = new BankWaterService();</span><br><span class="line">        bankWaterCount.count();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然在上面的Demo中已经可以看到CountDownLatch和CyclicBarrier两者之间的区别，但是两者最主要的区别在与前者是一次性的操作，它的计数器只能用一次，在下次使用的时候必须重新进行初始化，而 后者的则是可以直接调用它的reset()方法重置，CyclicBarrier还提供更为有用的方法，比如getNumberWaitting来获取正在等待的线程数，还有isBroken()方法来了解阻塞线程是否被中断，下面是一个简单的Demo：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.BrokenBarrierException;</span><br><span class="line">import java.util.concurrent.CyclicBarrier;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 测试CountDownlatch和CyclicBarrier的区别，前者只可以应用一次，初始化，在计数器用完就无法再次调用</span><br><span class="line"> * 后者是可以进行重置计数器reset()，可以用于更为复杂的场景,而且提供很多方法</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 11:26</span><br><span class="line"> */</span><br><span class="line">public class CyclicBarrierTest3 &#123;</span><br><span class="line">    //创建屏蔽的点的计数器，初始化的屏蔽点为2</span><br><span class="line">    static CyclicBarrier c = new CyclicBarrier(2);</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        //创建处理每个Sheet业务子线程</span><br><span class="line">        Thread t = new Thread(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                try &#123;</span><br><span class="line">                    //设置屏蔽点，处理该业务和线程阻塞等待</span><br><span class="line">                    c.await();</span><br><span class="line">                &#125; catch (Exception e) &#123;</span><br><span class="line">                    System.out.println(&quot;error1 has happend!&quot;);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        t.start();</span><br><span class="line">        t.interrupt();</span><br><span class="line">        try &#123;</span><br><span class="line">            c.await();</span><br><span class="line">        &#125; catch (Exception e) &#123;</span><br><span class="line">            //通过这个方法可以了解到阻塞线程是否被中断</span><br><span class="line">            System.out.println(c.isBroken());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-控制并发线程数的Semaphore"><a href="#3-控制并发线程数的Semaphore" class="headerlink" title="3.控制并发线程数的Semaphore"></a>3.控制并发线程数的Semaphore</h2><p>Semaphore也就是信号量，还是比较形象的，它负责协调各个线程来保证公共资源的合理使用，可以理解为在设置信号量以后，只有看到信号量的线程才有资格执行，其他线程是没有资格执行的，就好比一电梯中有一个限重的概念，一个电梯只允许有在13个人同时在里面，超过这个数之外的人只能在外面等，只有里面的出来之后，才可以进去等量的人，类比到这里就是，信号量控制着最大并发执行的线程数，超过这个数量的线程只能在外面等待，在Semaphore里面也提供了其他的一些方法，比如：<br>availablePermits()可以返回信号量中可用的许可证数；<br>getQueueLength()获取正在等待的许可证数；<br>s.hasQueuedThreads()是否有现成正在等待获取许可证；<br>简单的案例如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.ExecutorService;</span><br><span class="line">import java.util.concurrent.Executors;</span><br><span class="line">import java.util.concurrent.Semaphore;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 测试Semaphore的案例,信号量的可以理解为红绿灯，在一个场景下，一条马路上只允许一个有100辆车。</span><br><span class="line"> * 那么在信号乱量Semaphore的控制下，只有当前路上的100辆车看的信号量是允许放行的状态，而对于</span><br><span class="line"> * 100辆以外的车来说，这个信号量都是不允许放行的状态，这个说下来邮更有点像某个场地了，有人数限制，</span><br><span class="line"> * 只有场地里面的人离开了才会让外面等量的人进来</span><br><span class="line"> *</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 13:35</span><br><span class="line"> */</span><br><span class="line">public class SemaphoreTest &#123;</span><br><span class="line">    private static final int THREAD_COUNT = 30;</span><br><span class="line">    //创建定长线程池</span><br><span class="line">    private static ExecutorService threadPool = Executors.newFixedThreadPool(THREAD_COUNT);</span><br><span class="line">    //规定一次只能有10个的信号量</span><br><span class="line">    private static Semaphore s = new Semaphore(10);</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        for (int i = 0; i &lt; THREAD_COUNT; i++) &#123;</span><br><span class="line">            threadPool.execute(new Runnable() &#123;</span><br><span class="line">                @Override</span><br><span class="line">                public void run() &#123;</span><br><span class="line">                    try &#123;</span><br><span class="line">                        //获取许可证，只有获取许可证，线程才能正常执行</span><br><span class="line">                        s.acquire();</span><br><span class="line">                        //availablePermits方法获信号量中可用的许可证，getQueueLength是获取正在等待许可证的线程数</span><br><span class="line">                        System.out.println(&quot;save date&quot; + s.availablePermits() + &quot; &quot; + s.getQueueLength());</span><br><span class="line"></span><br><span class="line">                        //上诉业务不太复杂看不出效果，加睡眠时间</span><br><span class="line">                        Thread.currentThread().sleep(5000);</span><br><span class="line"></span><br><span class="line">                        //运行完释放许可证给其他线程使用</span><br><span class="line">                        s.release();</span><br><span class="line">                    &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            //这里无需手动释放，否则报错</span><br><span class="line">//            threadPool.shutdown();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-线程间交换数据的Exchanger"><a href="#4-线程间交换数据的Exchanger" class="headerlink" title="4.线程间交换数据的Exchanger"></a>4.线程间交换数据的Exchanger</h2><p>Exchanger主要用于线程间的数据交换，它为线程提供了一个数据交换和同步点，在某个线程到达同步时会调用Exchanger中的exchange()的方法，传入本线程的需要给对方的数据并且返回对方在同步站点传入的数据（在某一个线程到达先达到同步点后会等待另一个线程到达同步点才进行数据的交换），这个工具类可以用于遗传算法（两人进行交配时会交换数据，并使用交叉规则得出交配结果）和数据的校对（两个人同时进行同一个工作，在某个点进行两人数据的比对，提高正确率），下面是简单的数据校对的Demo:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threadutil;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.Exchanger;</span><br><span class="line">import java.util.concurrent.ExecutorService;</span><br><span class="line">import java.util.concurrent.Executors;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * Exchanger是线程协同工作的工具类，主要用于线程间的数据交换。它提供了一个同步点，在这个同步点，</span><br><span class="line"> * 两个线程可以进行数据的交换，在某一个线程先到达exchange方法，它会一直等待第二个线程到同步点，</span><br><span class="line"> * 当两个线程都到达同步点的时候，两者可以进行数据的交换，下面是两个线程的数据校对的案案例</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 14:32</span><br><span class="line"> */</span><br><span class="line">public class ExchangerTest &#123;</span><br><span class="line">    private static final Exchanger&lt;String&gt; exchanger = new Exchanger&lt;String&gt;();</span><br><span class="line">    private static ExecutorService threadPool = Executors.newFixedThreadPool(2);</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        threadPool.execute(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                String A = &quot;Bannker A&quot;;</span><br><span class="line">                try &#123;</span><br><span class="line">                    //这里将A传给同步点的另一个线程，并且返回对方传入的数据B</span><br><span class="line">                    String re1 = exchanger.exchange(A);</span><br><span class="line">                    Thread.sleep(2000);</span><br><span class="line">                    System.out.println(re1);</span><br><span class="line">                &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        threadPool.execute(new Runnable() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line">                String B = &quot;Bannker B&quot;;</span><br><span class="line">                String A = null;</span><br><span class="line">                try &#123;</span><br><span class="line">                    //将B传入给同步点的两个线程并且返回对方传入的数据</span><br><span class="line">                    A = exchanger.exchange(B);</span><br><span class="line">                &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(&quot;A equals B ? &quot; + A.equals(B) + &quot;\n A=&quot; + A + &quot;; B=&quot; + B);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        threadPool.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>【注】如果两个线程只要有一个没有执行exchange方法，那另一个就会一直等待，为了避免这种等待无限持续，可以设定最大的等待时长，可以使用exchange(V x, long timeout, TimeUnit unit)</p><h1 id="第七章-线程池"><a href="#第七章-线程池" class="headerlink" title="第七章 线程池"></a>第七章 线程池</h1><p>在实际开发涉及到多线程时，往往会搞一个线程池统一管理，这样做的和数据库连接池那一块的东西有些类似了，有如下的一些好处：</p><ol><li>降低资源的消耗：重复利用已创建的线降低线程创建和销毁的资源消耗</li><li>提高响应速度：当任务到达时不需要再等待线程创建这个过程</li><li><p>提高线程的可管理性：线程是稀缺资源，反复创建会消耗系统资源，降低系统的稳定性，线程池可以统一分配、调优和监控。</p></li><li><p>线程池原理</p></li></ol><hr><p>线程池处理任务的流程主要如下：<br>提交新的任务后，首先会判断核心线程池是否已满，如果不满则创建线程执行任务，如果满了再判断工作队列是否已满，如果不满则将该任务存储到这个工作队列中，如果工作队列已满则再对线程池进行判断，如果不满创建新的工作线程执行任务，如果已满则把这个任务交给饱和策略进行处理，呐，上面就是线程池处理提交任务的完整流程了，下面对上述的一些名词做一个简单的说明：<br>1.<strong>核心线程池</strong>：corePool，在创建线程池后默认里面是没有任何线程的，只有任务来的时候才创建线程执行任务，除非调用prestartAllCoreThreads()或者prestartCoreThread()方法预创建线程，在任务还没有到来时就创建corePoolSize个线程或一个线程，当线程池中线程的数据达到corePoolSize后就会把之后到来的任务放到缓存队列中；<br>2.<strong>工作队列</strong>：workQueue是一种阻塞式队列（BlockingQueue），用于存储等待执行的任务，主要有如下的几种阻塞队列：</p><p><font color="red">LinkedBlockingQueue</font>:一种基于链表结构的有界阻塞队列，按FIFO排序，吞吐量一般高于ArrayBlockingQueue，其中定长线程池Executors.newFixedThreadPool()就是使用的这个队列；</p><p><font color="red">SynchronousQueue</font>:一种不存储元素的阻塞队列，每个插入操作必须等到等一个线程调用移除操作，否则会阻塞在那里，吞吐量一般高于LinkedBlockingQueue，可缓存线程池Executors.newCachedThreadPool()就是只用的这种队列；<br>ArrayBlockingQueue:一种基于数组结构的有界阻塞队列，按FIFO原则排序；<br>PriorityBlockingQueue:一种具有优先级的无限阻塞队列。<br>【注】以上四种阻塞队列，前两种LinkedBlockingQueue和SynchronousQueue比较常用，另外两个不常用，另外建议使用有界队列（它能增加系统的稳定性和预警能力）。<br>3.<strong>饱和策略</strong>：RejectedExecutionHandler是在队列和线程池都已满的状态时，表示线程池已经处于饱和状态，这个时候任务再来的时候就必须采取一种策略来处理提交的新任务，主要的饱和策略有如下的四种：<br>ThreadPoolExecutor.AbortPolicy()：直接抛出异常；<br>ThreadPoolExecutor.CallerRunsPolicy()：只用调用者所在的线程来执行任务<br>ThreadPoolExecutor.DiscardOldestPolicy()：丢弃队列中最近的一个任务并执行当前任务；<br>ThreadPoolExecutor.DiscardPolicy()：不处理，直接丢弃。</p><p><strong>第一步：创建线程池</strong><br>ThreadPoolExecutor类是Java线程池中最核心的类，继承自AbstractExecutorService，AbstractExecutorService是一个抽象类，它实现了ExecutorService接口（所以在创建线程池的时候可以写成<code>ExecutorService threadpool = new ThreadPoolExecutor(...)</code>的形势），当然除此之外，在JDK8中可以追踪到的最根本的接口是Executor接口，这里简单提一下其他的与ThreadPoolExecutor类并列的几个类：</p><p>ThreadPoolExecutor：线程池的核心类，用于执行被提交的任务；<br>ScheduledThreadPoolExecutor：可以在给定延迟后再运行，或者定期执行命令</p><p>ThreadPoolExecutor提供了四种构造方法，主要源码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line">public class ThreadPoolExecutor extends AbstractExecutorService &#123;</span><br><span class="line">...</span><br><span class="line">    public ThreadPoolExecutor(int corePoolSize,</span><br><span class="line">                              int maximumPoolSize,</span><br><span class="line">                              long keepAliveTime,</span><br><span class="line">                              TimeUnit unit,</span><br><span class="line">                              BlockingQueue&lt;Runnable&gt; workQueue) &#123;</span><br><span class="line">        this(corePoolSize, maximumPoolSize, keepAliveTime, unit, workQueue,</span><br><span class="line">             Executors.defaultThreadFactory(), defaultHandler);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public ThreadPoolExecutor(int corePoolSize,</span><br><span class="line">                              int maximumPoolSize,</span><br><span class="line">                              long keepAliveTime,</span><br><span class="line">                              TimeUnit unit,</span><br><span class="line">                              BlockingQueue&lt;Runnable&gt; workQueue,</span><br><span class="line">                              ThreadFactory threadFactory) &#123;</span><br><span class="line">        this(corePoolSize, maximumPoolSize, keepAliveTime, unit, workQueue,</span><br><span class="line">             threadFactory, defaultHandler);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public ThreadPoolExecutor(int corePoolSize,</span><br><span class="line">                              int maximumPoolSize,</span><br><span class="line">                              long keepAliveTime,</span><br><span class="line">                              TimeUnit unit,</span><br><span class="line">                              BlockingQueue&lt;Runnable&gt; workQueue,</span><br><span class="line">                              RejectedExecutionHandler handler) &#123;</span><br><span class="line">        this(corePoolSize, maximumPoolSize, keepAliveTime, unit, workQueue,</span><br><span class="line">             Executors.defaultThreadFactory(), handler);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    public ThreadPoolExecutor(int corePoolSize,</span><br><span class="line">                              int maximumPoolSize,</span><br><span class="line">                              long keepAliveTime,</span><br><span class="line">                              TimeUnit unit,</span><br><span class="line">                              BlockingQueue&lt;Runnable&gt; workQueue,</span><br><span class="line">                              ThreadFactory threadFactory,</span><br><span class="line">                              RejectedExecutionHandler handler) &#123;</span><br><span class="line">        if (corePoolSize &lt; 0 ||</span><br><span class="line">            maximumPoolSize &lt;= 0 ||</span><br><span class="line">            maximumPoolSize &lt; corePoolSize ||</span><br><span class="line">            keepAliveTime &lt; 0)</span><br><span class="line">            throw new IllegalArgumentException();</span><br><span class="line">        if (workQueue == null || threadFactory == null || handler == null)</span><br><span class="line">            throw new NullPointerException();</span><br><span class="line">        this.corePoolSize = corePoolSize;</span><br><span class="line">        this.maximumPoolSize = maximumPoolSize;</span><br><span class="line">        this.workQueue = workQueue;</span><br><span class="line">        this.keepAliveTime = unit.toNanos(keepAliveTime);</span><br><span class="line">        this.threadFactory = threadFactory;</span><br><span class="line">        this.handler = handler;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><p>上面的几个构造方法corePoolSize表示核心线程池的大小，maximumPoolSize表示线程池最大线程池，keepAliveTime表示线程最大空闲时间（即线程如果没有执行任何任务会存活多长时间，往往提高这个参数可以提高线程的复用率），unit表示前面keepAliveTime参数的单位，workQueue表示工作队列（阻塞队列），threadFactory表示创建线程的线程工厂，handler表示拒绝任务的策略，通常可以用上述ThreadPoolExecutor的四种初始化方法进行线程池的创建。</p><p><strong>第二步：提交任务</strong><br>呐，线程池创建后提交任务可以用如下的两种方式：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">threadPool.execute(new Runnable());</span><br><span class="line">threadPool.submit(new Runnable());</span><br></pre></td></tr></table></figure><p>其中execute()方法没有返回值，无法知道提交的任务是否被线程池执行，而submit()方法提交任务会返回Future类型的对象，并且可以通过它的get()方法判断任务是否成功执行，但是这个方法会阻塞当前线程直到任务完成</p><p><strong>第三步：关闭线程池</strong><br>线程池的关闭可以通过调用线程池的shutdownNow()或者shutdown()方法，但是shutdown()是直接interrupt所有线程没有正在执行任务的线程，不再接受新的任务，它会等待所有正在执行的任务执行完毕，一般都是调用这个方法；而shutdownNow()会首先尝试将线程池的状态设置为STOP，然后尝试停止所有正在执行或者暂停的任务，并返回等待执行的任务列表；<br>【注意】性质不同的任务应当用不同规模的线程池处理，CPU密集的任务应该配置尽可能小的线程，通过<code>Runtime.getRuntime().availableProcessors()</code>可以获取当前设备的CPU个数。</p><p>好啦，来看个小例子：<br>注意这里最最好存在12个任务对不对，池中10个，工作队列中12个<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line">package com.hhu.threads;</span><br><span class="line"></span><br><span class="line">import java.util.concurrent.*;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 测试线程池</span><br><span class="line"> *</span><br><span class="line"> * @author WeiguoLiu</span><br><span class="line"> * @date 2018/1/15 16:06</span><br><span class="line"> */</span><br><span class="line">public class ThreadPoolTest &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        //获取当前设备中CPU的个数</span><br><span class="line">        System.out.println(&quot;CPU: &quot; + Runtime.getRuntime().availableProcessors());</span><br><span class="line">        //创建线程池，核心池为5个线程，最大线程数为10，线程最大空闲存活时间3秒，工作队列是基于数组的有界队列2个元素，饱和策略选择直接抛弃超出的任务，不予处理</span><br><span class="line">        ThreadPoolExecutor threadPool = new ThreadPoolExecutor(5, 10, 3, TimeUnit.SECONDS, new ArrayBlockingQueue(2), new ThreadPoolExecutor.DiscardPolicy());</span><br><span class="line">        //创建线程池的另一种方式，面向接口编程</span><br><span class="line">        ExecutorService threadPool2 = new ThreadPoolExecutor(5, 10, 3, TimeUnit.SECONDS, new ArrayBlockingQueue(2), new ThreadPoolExecutor.DiscardPolicy());</span><br><span class="line"></span><br><span class="line">        //这里提交20个线程做测试</span><br><span class="line">        for (int i = 0; i &lt; 20; i++) &#123;</span><br><span class="line">            System.out.println(&quot;需要执行： &quot; + threadPool.getTaskCount() + &quot;; 已完成： &quot; + threadPool.getCompletedTaskCount() + &quot;; 池中存在： &quot; +</span><br><span class="line">            threadPool.getPoolSize());</span><br><span class="line">            threadPool.execute(new A());</span><br><span class="line">            System.out.println();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">//        threadPool.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    static class A implements Runnable &#123;</span><br><span class="line">        @Override</span><br><span class="line">        public void run() &#123;</span><br><span class="line">            System.out.println(&quot;The thread has started: &quot; + Thread.currentThread().getName() );</span><br><span class="line">            try &#123;</span><br><span class="line">                Thread.currentThread().sleep(3000);</span><br><span class="line">            &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>输出的主要内容如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">&quot;D:\Program Files\Java\jdk1.8.0_101\bin\java&quot; &quot;-javaagent:D:\Program Files (x86)\ideaIU-2017.3.2\lib\idea_rt.jar=8374:D:\Program Files (x86)\ideaIU-2017.3.2\bin&quot; -Dfile.encoding=UTF-8 -classpath &quot;D:\Program Files\Java\jdk1.8.0_101\jre\lib\charsets.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\deploy.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\access-bridge-64.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\cldrdata.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\dnsns.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\jaccess.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\jfxrt.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\localedata.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\nashorn.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\sunec.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\sunjce_provider.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\sunmscapi.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\sunpkcs11.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\ext\zipfs.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\javaws.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\jce.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\jfr.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\jfxswt.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\jsse.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\management-agent.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\plugin.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\resources.jar;D:\Program Files\Java\jdk1.8.0_101\jre\lib\rt.jar;D:\我的文档\IdeaWorkSpace\JavaProject\target\classes;D:\Mavenrepository\repository\junit\junit\4.12\junit-4.12.jar;D:\Mavenrepository\repository\org\hamcrest\hamcrest-core\1.3\hamcrest-core-1.3.jar;D:\Mavenrepository\repository\org\springframework\spring-core\4.3.7.RELEASE\spring-core-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\commons-logging\commons-logging\1.2\commons-logging-1.2.jar;D:\Mavenrepository\repository\org\springframework\spring-web\4.3.7.RELEASE\spring-web-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-aop\4.3.7.RELEASE\spring-aop-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-beans\4.3.7.RELEASE\spring-beans-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-context\4.3.7.RELEASE\spring-context-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-webmvc\4.3.7.RELEASE\spring-webmvc-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-expression\4.3.7.RELEASE\spring-expression-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\springframework\spring-websocket\4.3.7.RELEASE\spring-websocket-4.3.7.RELEASE.jar;D:\Mavenrepository\repository\org\apache\poi\poi\3.16\poi-3.16.jar;D:\Mavenrepository\repository\commons-codec\commons-codec\1.10\commons-codec-1.10.jar;D:\Mavenrepository\repository\org\apache\commons\commons-collections4\4.1\commons-collections4-4.1.jar;D:\Mavenrepository\repository\com\baidu\aip\java-sdk\3.4.1\java-sdk-3.4.1.jar;D:\Mavenrepository\repository\org\json\json\20160810\json-20160810.jar;D:\Mavenrepository\repository\commons-httpclient\commons-httpclient\3.1\commons-httpclient-3.1.jar;D:\Mavenrepository\repository\mysql\mysql-connector-java\5.1.37\mysql-connector-java-5.1.37.jar&quot; com.hhu.threads.ThreadPoolTest</span><br><span class="line">CPU: 4</span><br><span class="line">需要执行： 0; 已完成： 0; 池中存在： 0</span><br><span class="line">需要执行： 1; 已完成： 0; 池中存在： 1</span><br><span class="line">需要执行： 2; 已完成： 0; 池中存在： 2</span><br><span class="line">The thread has started: pool-1-thread-1</span><br><span class="line">需要执行： 3; 已完成： 0; 池中存在： 3</span><br><span class="line">需要执行： 4; 已完成： 0; 池中存在： 4</span><br><span class="line">The thread has started: pool-1-thread-2</span><br><span class="line">需要执行： 5; 已完成： 0; 池中存在： 5</span><br><span class="line">需要执行： 6; 已完成： 0; 池中存在： 5</span><br><span class="line">需要执行： 7; 已完成： 0; 池中存在： 5</span><br><span class="line">需要执行： 8; 已完成： 0; 池中存在： 6</span><br><span class="line">需要执行： 9; 已完成： 0; 池中存在： 7</span><br><span class="line">需要执行： 10; 已完成： 0; 池中存在： 8</span><br><span class="line">需要执行： 11; 已完成： 0; 池中存在： 9</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">需要执行： 12; 已完成： 0; 池中存在： 10</span><br><span class="line">The thread has started: pool-1-thread-3</span><br><span class="line">The thread has started: pool-1-thread-4</span><br><span class="line">The thread has started: pool-1-thread-5</span><br><span class="line">The thread has started: pool-1-thread-6</span><br><span class="line">The thread has started: pool-1-thread-7</span><br><span class="line">The thread has started: pool-1-thread-8</span><br><span class="line">The thread has started: pool-1-thread-9</span><br><span class="line">The thread has started: pool-1-thread-10</span><br><span class="line">The thread has started: pool-1-thread-5</span><br><span class="line">The thread has started: pool-1-thread-4</span><br><span class="line"></span><br><span class="line">Process finished with exit code 0</span><br></pre></td></tr></table></figure><h1 id="第八章-Executor框架"><a href="#第八章-Executor框架" class="headerlink" title="第八章 Executor框架"></a>第八章 Executor框架</h1><p> Executor框架主要包含了：ThreadPoolExecutor、ScheduledThreadPoolExcutor、Future接口、Runnable接口、Callable接口和Executors。</p><ol><li><p><strong>ThreadPoolExecutor</strong><br>ThreadPoolExecutor是Executor框架的核心，通常使用工厂类Executors来创建，Executors可以创建三种类型的ThreadPoolExecutor：<br>单线程化线程池SingleThreadPool、定长线程池FixedThreadPool、可缓存线程池CachedThreadPool。</p></li><li><p><strong>ScheduledThreadPoolExcutor</strong><br>ScheduledThreadPoolExcutor继承自ThreadPoolExecutor，通常使用工厂类Executors来创建，Executors可以创建2种类型的ScheduledThreadPoolExcutor：<br>ScheduledThreadPoolExcutor：包含若干个线程的ScheduledThreadPoolExcutor<br>SingleThreadPoolExcutor：只包含一个线程的ScheduledThreadPoolExcutor</p></li><li><p><strong>Future接口</strong><br>Future接口和实现Future接口的FutureTask类（该类也实现了Runnable接口）用来表示异步计算的结果，当向线程池使用submit提交任务时就是返回的一个FutureTask对象</p></li></ol><p>4.<strong>Runnable接口和Callable接口</strong><br>Runnbale接口和Callable接口的实现类都可以被ThreadPoolExecutor或者ScheduledThreadPoolExecutor执行，主要是执行Runnable接口不会返回结果，而执行Callable接口是可以返回结果的，当然除了自己去实现Callable接口外，Executor类也提供了把Runnable封装为Callable：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Callable&lt;Object&gt; <span class="title">callable</span><span class="params">(Runnable task)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (task == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> RunnableAdapter&lt;Object&gt;(task, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;线程这一块是前段时间面试过程中遇到很多的问题，学校里面接触有限，吃了一些亏，抽空最近在看并发编程编程的挑战这本书并作一些简单记录。有两个常见的概念：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;临界资源：临界资源是一次仅允许一个进程使用的共享资源，各进程之间对共享资源的使用方式是互斥的；（常罗列的临界资源硬件上有打印机、磁带机，软件上有消息队列等）&lt;/li&gt;
&lt;li&gt;临界区：每个进程中访问临界资源的那段代码就是临界区。每次只允许一个进程进入临界区，且此时其他进程无法进入。多个进程涉及到同一个临界资源的的临界区称为相关临界区；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;注：多线程操作共享资源时就可以理解为使用临界区，一般不允许线程占用临界区的时间太长，只要运行在临界区的线程还没有离开，其他进入此临界区的线程都会被挂起处于等待状态，影响程序的运行性能。&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="Java" scheme="https://jacksonary.github.io/tags/Java/"/>
    
      <category term="并发编程" scheme="https://jacksonary.github.io/tags/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
    
      <category term="多线程" scheme="https://jacksonary.github.io/tags/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>docker利用阿里云拉取墙外镜像</title>
    <link href="https://jacksonary.github.io/posts/9b3ef1e.html"/>
    <id>https://jacksonary.github.io/posts/9b3ef1e.html</id>
    <published>2019-07-19T01:38:00.000Z</published>
    <updated>2021-06-04T12:35:18.712Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;K8S很多镜像都是国内无法拉取的，利用阿里的镜像仓库可以很容易拉取这些镜像，我的方式如下：</p><h3 id="1-创建存放Dockerfile的仓库"><a href="#1-创建存放Dockerfile的仓库" class="headerlink" title="1.创建存放Dockerfile的仓库"></a>1.创建存放<code>Dockerfile</code>的仓库</h3><p>&emsp;很简单，直接创建一个仓库用于存放<code>Dockerfile</code>的仓库，比如我的<a href="https://github.com/Jacksonary/docker-ali-autobuild" rel="external nofollow noopener noreferrer" target="_blank">docker-ali-autobuild</a>，经过不断摸索，建议文件结构采用如下方式（即 image-name -&gt; version -&gt; Dockerfile）比较合理<br><a id="more"></a><br><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvcndXd3RSUDUvaW1hZ2UucG5n" alt="目录结构"></p><p><code>Dockerfile</code>中直接使用<code>FROM</code>指令引入我们所需要的镜像即可，比如我这里的<code>quay.io/kubernetes-ingress-controller/nginx-ingress-controller:0.25.0</code>。</p><h3 id="2-创建阿里云镜像仓库并绑定代码源"><a href="#2-创建阿里云镜像仓库并绑定代码源" class="headerlink" title="2.创建阿里云镜像仓库并绑定代码源"></a>2.创建阿里云镜像仓库并绑定代码源</h3><p>&emsp;命名空间随便搞，比如我这里就是<code>hhu</code>，然后创建对应镜像的仓库，代码源选择第一步创建的docker-ali-autobuild 即可，过程如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%88%9B%E5%BB%BA%E9%95%9C%E5%83%8F%E4%BB%93%E5%BA%93.gif" alt></p><p>注：务必选中“使用海外机器构建”。</p><h3 id="3-创建镜像构建任务"><a href="#3-创建镜像构建任务" class="headerlink" title="3.创建镜像构建任务"></a>3.创建镜像构建任务</h3><p>&emsp;由于结构原因我们不能使用阿里云的自动构建的功能，但可以tag的方式来构建，创建tag并推送：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">liuwg-a@liuwg-a-p01 MINGW64 /f/个人相关/docker-ali-autobuild (master)</span><br><span class="line">$ git tag -a release-v0.25.0 -m &quot;nginx-ingress-controller:0.25.0&quot;</span><br><span class="line"></span><br><span class="line">liuwg-a@liuwg-a-p01 MINGW64 /f/个人相关/docker-ali-autobuild (master)</span><br><span class="line">$ git tag</span><br><span class="line">release-v0.25.0</span><br><span class="line"></span><br><span class="line">liuwg-a@liuwg-a-p01 MINGW64 /f/个人相关/docker-ali-autobuild (master)</span><br><span class="line">$ git push origin release-v0.25.0</span><br><span class="line">Enumerating objects: 1, done.</span><br><span class="line">Counting objects: 100% (1/1), done.</span><br><span class="line">Writing objects: 100% (1/1), 179 bytes | 89.00 KiB/s, done.</span><br><span class="line">Total 1 (delta 0), reused 0 (delta 0)</span><br><span class="line">To github.com:Jacksonary/docker-ali-autobuild.git</span><br><span class="line"> * [new tag]         release-v0.25.0 -&gt; release-v0.25.0</span><br></pre></td></tr></table></figure><p>创建构建规则并开始构建</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvS2o4WWtaS0wvaW1hZ2UucG5n" alt="创建规则并构建"></p><p>构建完成后，引用该镜像即可从阿里云拉取。</p><p>【附】：<code>git tag</code>的简单语法</p><ul><li>创建tag：<code>git tag -a &lt;tag-name&gt; -m &quot;some describe&quot;</code></li><li>查看本地tag：<code>git tag</code></li><li>推送tag：<code>git push origin &lt;tag-name&gt;</code></li><li>本地删除tag：<code>git tag -d &lt;tag-name&gt;</code></li><li>删除远程tag：<code>git push origin :refs/tags/&lt;tag-name&gt;</code></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;K8S很多镜像都是国内无法拉取的，利用阿里的镜像仓库可以很容易拉取这些镜像，我的方式如下：&lt;/p&gt;
&lt;h3 id=&quot;1-创建存放Dockerfile的仓库&quot;&gt;&lt;a href=&quot;#1-创建存放Dockerfile的仓库&quot; class=&quot;headerlink&quot; title=&quot;1.创建存放Dockerfile的仓库&quot;&gt;&lt;/a&gt;1.创建存放&lt;code&gt;Dockerfile&lt;/code&gt;的仓库&lt;/h3&gt;&lt;p&gt;&amp;emsp;很简单，直接创建一个仓库用于存放&lt;code&gt;Dockerfile&lt;/code&gt;的仓库，比如我的&lt;a href=&quot;https://github.com/Jacksonary/docker-ali-autobuild&quot; rel=&quot;external nofollow noopener noreferrer&quot; target=&quot;_blank&quot;&gt;docker-ali-autobuild&lt;/a&gt;，经过不断摸索，建议文件结构采用如下方式（即 image-name -&amp;gt; version -&amp;gt; Dockerfile）比较合理&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="docker" scheme="https://jacksonary.github.io/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>Ingress笔记及经验</title>
    <link href="https://jacksonary.github.io/posts/21ee6688.html"/>
    <id>https://jacksonary.github.io/posts/21ee6688.html</id>
    <published>2019-07-05T00:51:00.000Z</published>
    <updated>2021-06-04T12:37:07.452Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;Ingress是管理外部网咯访问K8S集群中Service的API对象（典型就是HTTP），它可以提供负载均衡、SSL终端以及基于名字的虚拟host。简单点来说它就是外网访问集群应用的媒介，流程如下：<br><a id="more"></a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+----------+  Ingress   +---------+</span><br><span class="line">| internet | ---------&gt; | Service |</span><br><span class="line">+----------+            +---------+</span><br></pre></td></tr></table></figure><p>Ingress Controller 负责实现入口（即ingress的意思），通常使用负载均衡器，但它也可以配置边缘路由器或其他前端以帮助处理流量。Ingress不会随意暴露端口或协议，向Internet公开HTTP和HTTPS以外的服务通常使用<code>Service.Type=NodePort</code>或<code>Service.Type=LoadBalancer</code>类型的服务。</p><p>&emsp;和其他K8S资源一样，Ingress必须要有一个 Ingress Controller，仅有 Ingress Resource 是无效的，Ingress也是无法正常工作的。Ingress Controller不会随着集群的启动而自动启动，且Ingress Controller有多种，目前K8S只支持和维护GCE（Google Kubernetes Engine）和nginx2种Controller。</p><p>【Ingress Resource】</p><p>&emsp;Ingress Resource 则和大部分的K8S资源差不多，需要<code>apiVersion</code>、<code>kind</code>和<code>metadata</code>等字段，下面一个最小ingress resource的示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">test-ingress</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - path:</span> <span class="string">/testpath</span></span><br><span class="line"><span class="attr">        backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">test</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>Ingress通常都是使用注解<code>annotations</code>来配置某些选项，具体可配置选项、支持哪些注解取决于Ingress Controller类型，上述是一个<code>rewrite-target</code>的注解，<code>spec</code>字段包含了负载均衡器或代理服务器的配置，最重要的是它包含了所有传入请求的匹配规则。注：Ingress Resource仅支持HTTP通信的规则。</p><p>&emsp;Ingress rules是Ingress中比较重要的一部分，即上述示例中的<code>rules</code>字段的含义（它是一个可以包含多种<code>rule</code>对象的数组），每个Http的<code>rule</code>都应该包含如下的信息：</p><ul><li><code>host</code>：可选参数，用于限定rule适用的host，如指定为<code>foo.bar.com</code>，那该rule只应用于这个host，上述示例中未指定，则该<code>rule</code>适用于通过指定IP地址的所有入站HTTP流量；</li><li><code>-paths</code>：一系列的path对象组成的数组，上述示例中只有一个path为<code>/testpath</code>，每个path都有一个<code>backend</code>对象（代表的是后端服务）与之对应；</li><li><code>backend</code>：是一个<code>serviceName</code>和<code>servicePort</code>的组合对象，只有<code>host</code>和<code>path</code>都匹配成功的情况下，LoadBalancer才会将流量导向引用的服务。</li></ul><p>&emsp;通常Ingress会有一个 Default Backend，当在上述我们自定义的Ingress Resource没有匹配的<code>rule</code>对象时，流量就会被导向这个Default Backend。当然这个default backend需要我们自己去配，通常不会在Ingress Resource中配置default backend，而是应该在Ingress Ctontroller中进行指定。</p><p>【Ingress的类型】</p><p><strong>1.Single Service Ingress</strong></p><p>&emsp;即单服务Ingress，K8S当前是支持暴露单个服务的，可以通过在没有规则的情况下指定默认后端来使用ingress来完成此操作，如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ingress.yaml</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">test-ingress</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  backend:</span></span><br><span class="line"><span class="attr">    serviceName:</span> <span class="string">testsvc</span></span><br><span class="line"><span class="attr">    servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>注：</p><p>安装完ingress后，还要执行：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 创建命名空间 ingress-nginx</span><br><span class="line">kubectl create namespace ingress-nginx</span><br><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/master/deploy/rbac.yaml</span><br><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/master/deploy/with-rbac.yaml</span><br></pre></td></tr></table></figure><p><code>kubectl apply -f ingress.yaml</code>即可，通过<code>kubectl get ingress test-ingress</code>可以查看详情，包括Ingress分配给这个default backend的IP。</p><p><strong>2.Simple fanout</strong></p><p>&emsp;fanout基于请求的HTTP URI将配置流量从单个IP路由到多个服务，Ingress 是允许将负载平衡器的数量降到最低，如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">foo.bar.com -&gt; 178.91.123.132 -&gt; / foo    service1:4200</span><br><span class="line">                                 / bar    service2:8080</span><br></pre></td></tr></table></figure><p>上述的过程需要一个如下的Ingress（同一个域名配置了2个path）：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">simple-fanout-example</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">foo.bar.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - path:</span> <span class="string">/foo</span></span><br><span class="line"><span class="attr">        backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">service1</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">4200</span></span><br><span class="line"><span class="attr">      - path:</span> <span class="string">/bar</span></span><br><span class="line"><span class="attr">        backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">service2</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">8080</span></span><br></pre></td></tr></table></figure><p><strong>3.基于名字虚拟host</strong></p><p>&emsp;和2类似，这种类型的Ingress支持将HTTP流量路由到在同一个IP上的多个host name，流程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">foo.bar.com --|                 |-&gt; foo.bar.com s1:80</span><br><span class="line">              | 178.91.123.132  |</span><br><span class="line">bar.foo.com --|                 |-&gt; bar.foo.com s2:80</span><br></pre></td></tr></table></figure><p>此时的Ingress Resource应该为：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">name-virtual-host-ingress</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">foo.bar.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">service1</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">bar.foo.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">service2</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>此时Ingress会告诉负载均衡器将请求根据Host header路由到指定地方。如果不指定host，任意到Ingress Controller IP地址的Web流量都可以匹配，而不需要基于名称的虚拟主机。</p><p><strong>4.TLS</strong></p><p>&emsp;可以指定包含一对TLS私钥、证书的方式来保护Ingress，当前Ingress只支持TLS 443端口和TLS终端，TLS必须包含<code>tls.crt</code>（即证书）和<code>tls.key</code>（即私钥），如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Secret</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">testsecret-tls</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="string">tls.crt:</span> <span class="string">base64</span> <span class="string">encoded</span> <span class="string">cert</span></span><br><span class="line">  <span class="string">tls.key:</span> <span class="string">base64</span> <span class="string">encoded</span> <span class="string">key</span></span><br><span class="line"><span class="attr">type:</span> <span class="string">kubernetes.io/tls</span></span><br></pre></td></tr></table></figure><p>在Ingress中引用这个密钥将告诉Ingress Controller使用TLS保护Ingress从客户端到负载平衡器的通道。此外，还需要确保创建的TLS机密来自包含CN的证书，如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">networking.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">tls-example-ingress</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  tls:</span></span><br><span class="line"><span class="attr">  - hosts:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">sslexample.foo.com</span></span><br><span class="line"><span class="attr">    secretName:</span> <span class="string">testsecret-tls</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">    - host:</span> <span class="string">sslexample.foo.com</span></span><br><span class="line"><span class="attr">      http:</span></span><br><span class="line"><span class="attr">        paths:</span></span><br><span class="line"><span class="attr">        - path:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">          backend:</span></span><br><span class="line"><span class="attr">            serviceName:</span> <span class="string">service1</span></span><br><span class="line"><span class="attr">            servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>注：TLS对于nginx和GCE Controller稍有不同。在实际配置中可能在配置完整证书后想让<code>http</code>请求也强制转到<code>https</code>，此后可使用如下注解：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在配置了证书时，重定向到https</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/ssl-redirect:</span> <span class="string">"true"</span></span><br></pre></td></tr></table></figure><p>若没有配置证书仍然想强制跳转<code>https</code>，需要使用注解<code>nginx.ingress.kubernetes.io/force-ssl-redirect</code>。上述重定向到<code>https</code>时默认状态码 308 或者 307，老版本浏览器可能不支持，可选择性配置状态码为 301。</p><p>【Update Ingress】</p><p>&emsp;更新一个已有的Ingress，可以使用如下的流程：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 查看已有配置</span><br><span class="line">kubectl describe ingress test</span><br><span class="line"><span class="meta">#</span> 修改配置</span><br><span class="line">kubectl edit ingress test</span><br></pre></td></tr></table></figure><p>保存后将会更新资源，它将告诉Ingress Controller重新配置负载均衡器，当然也可以使用<code>kubectl replace -f xx.yaml</code>来重新载入配置即可。</p><p>注：<br>ingress的文档比较好用，贴一下<a href="https://kubernetes.github.io/ingress-nginx/user-guide/nginx-configuration/annotations" rel="external nofollow noopener noreferrer" target="_blank">Annotations</a>，这里有个坑，如果使用rancher进行配置，那所有字段尽量不要再用<code>&#39;&#39;</code>或<code>&quot;&quot;</code>包括，rancher的解析做的不是很好，他会将引号作为内容的一部分设置为对应的注解值，很坑！</p><h3 id="附：常用ingress配置"><a href="#附：常用ingress配置" class="headerlink" title="附：常用ingress配置"></a>附：常用ingress配置</h3><p>&emsp;常用的ingress配置模板如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.deploy.app</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">&#123;&#123;-</span> <span class="string">range</span> <span class="string">$key,</span> <span class="string">$value</span> <span class="string">:=</span> <span class="string">.Values.service.ingress.annotations</span> <span class="string">&#125;&#125;</span></span><br><span class="line">      <span class="string">&#123;&#123;</span> <span class="string">$key</span> <span class="string">&#125;&#125;:</span> <span class="string">&#123;&#123;</span> <span class="string">$value</span> <span class="string">| quote &#125;&#125;</span></span><br><span class="line"><span class="string">    <span class="template-variable">&#123;&#123;- end &#125;&#125;</span></span></span><br><span class="line"><span class="string"></span><span class="attr">  labels:</span></span><br><span class="line"><span class="attr">    service-cluster:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.deploy.serviceCluster</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    app:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.deploy.app</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    project-type:</span> <span class="string">java</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line">  <span class="comment"># 可以有多个</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">&#123;&#123;</span> <span class="string">.hosts</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line">        <span class="comment"># 可以有多个（可以正则）</span></span><br><span class="line"><span class="attr">        - path:</span> <span class="string">&#123;&#123;</span> <span class="string">.path</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">          backend:</span></span><br><span class="line"><span class="attr">            serviceName:</span> <span class="string">&#123;&#123;</span> <span class="string">.serviceName</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">            servicePort:</span> <span class="string">&#123;&#123;</span> <span class="string">.servicePort</span> <span class="string">&#125;&#125;</span></span><br><span class="line">  <span class="comment"># 可以有多个</span></span><br><span class="line"><span class="attr">  tls:</span></span><br><span class="line">    <span class="comment"># 可以有多个</span></span><br><span class="line"><span class="attr">    - hosts:</span></span><br><span class="line">      <span class="string">&#123;&#123;-</span> <span class="string">range</span> <span class="string">.hosts</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="bullet">        -</span> <span class="string">&#123;&#123;</span> <span class="string">.</span> <span class="string">&#125;&#125;</span></span><br><span class="line">      <span class="string">&#123;&#123;-</span> <span class="string">end</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">      secretName:</span> <span class="string">&#123;&#123;</span> <span class="string">.secretName</span> <span class="string">&#125;&#125;</span></span><br></pre></td></tr></table></figure><h4 id="1-跨域（单个或多个）"><a href="#1-跨域（单个或多个）" class="headerlink" title="1. 跨域（单个或多个）"></a>1. 跨域（单个或多个）</h4><p>&emsp;在配置跨域时，如果使用rancher可视化面板配置<code>nginx.ingress.kubernetes.io/enable-cors</code>和<code>nginx.ingress.kubernetes.io/cors-allow-origin</code>注解时千万不能加引号，不然找半天找不到错误在哪。但写Chart的yaml配置文件时，该怎么写就怎么写，<code>string</code>类型的还是需要加上引号，跨域的示例配置为：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 方式1：直接使用注解（但此时只能配置一个域跨域）</span></span><br><span class="line"><span class="attr">annotations:</span> </span><br><span class="line">  <span class="comment"># 使用rewrite配置contextPath</span></span><br><span class="line">  <span class="string">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/account/$1</span></span><br><span class="line">  <span class="comment"># 配置跨域</span></span><br><span class="line">  <span class="string">nginx.ingress.kubernetes.io/enable-cors:</span> <span class="string">"true"</span></span><br><span class="line">  <span class="comment"># 这个注解只能配一个具体的值或者*</span></span><br><span class="line">  <span class="string">nginx.ingress.kubernetes.io/cors-allow-origin:</span> <span class="string">"http://k8s-www.hhu.com"</span></span><br><span class="line">  <span class="string">nginx.ingress.kubernetes.io/cors-allow-methods:</span> <span class="string">"GET, PUT, POST, DELETE, PATCH, OPTIONS"</span></span><br><span class="line">  <span class="string">nginx.ingress.kubernetes.io/cors-allow-headers:</span> <span class="string">"DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式2：使用自定代码段注解，自定写跨域（这种方式可以配置多个域名跨域）</span></span><br><span class="line"><span class="comment"># 配置server片段</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/server-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">  # 配置跨域（多个，单个可用注解）</span></span><br><span class="line"><span class="string">  set $corsHost '';</span></span><br><span class="line"><span class="string">  set $httpHost '';</span></span><br><span class="line"><span class="string">  set $httpHeaders 'DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization,Content-Type,X-Forwarded-For,xfilecategory,xfilename,xfilesize';</span></span><br><span class="line"><span class="string">  if ( $http_origin ~ http://k8s-www.hhu.com)&#123;</span></span><br><span class="line"><span class="string">          set $corsHost $http_origin;</span></span><br><span class="line"><span class="string">          set $httpHost 'k8s-www.hhu.com';</span></span><br><span class="line"><span class="string">  &#125;</span></span><br><span class="line"><span class="string">  if ( $http_origin ~ http://k8s-m.hhu.com)&#123;</span></span><br><span class="line"><span class="string">          set $corsHost $http_origin;</span></span><br><span class="line"><span class="string">          set $httpHost 'k8s-m.hhu.com';</span></span><br><span class="line"><span class="string">  &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  add_header Access-Control-Allow-Origin $corsHost always;</span></span><br><span class="line"><span class="string">  add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS, PATCH';</span></span><br><span class="line"><span class="string">  add_header Access-Control-Allow-Headers $httpHeaders;</span></span><br><span class="line"><span class="string">  add_header Access-Control-Allow-Credentials 'true';</span></span><br><span class="line"><span class="string"># 配置location片段，如果是OPTIONS请求（预检）直接返回200</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/configuration-snippet: |</span></span><br><span class="line"><span class="string">  if ($request_method = OPTIONS ) &#123;</span></span><br><span class="line"><span class="string">    return 200;</span></span><br><span class="line"><span class="string">  &#125;</span></span><br></pre></td></tr></table></figure><p>比较推崇的是上述第二种的配置方式，注意，<font color="red">在添加<code>Access-Control-Allow-Origin</code>的header时必须接上<code>always</code>，否则不生效</font>。对于上述2种跨域的配置方式，有时会有配置二级泛域名的需求，我们可以将上述第二种方式的变形来配置，如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"> <span class="comment"># 原配置</span></span><br><span class="line"> <span class="string">if</span> <span class="string">(</span> <span class="string">$http_origin</span> <span class="string">~</span> <span class="attr">http://k8s-www.hhu.com)&#123;</span></span><br><span class="line">         <span class="string">set</span> <span class="string">$corsHost</span> <span class="string">$http_origin;</span></span><br><span class="line">         <span class="string">set</span> <span class="string">$httpHost</span> <span class="string">'k8s-www.hhu.com'</span><span class="string">;</span></span><br><span class="line"> <span class="string">&#125;</span></span><br><span class="line"> <span class="string">if</span> <span class="string">(</span> <span class="string">$http_origin</span> <span class="string">~</span> <span class="attr">http://k8s-m.hhu.com)&#123;</span></span><br><span class="line">         <span class="string">set</span> <span class="string">$corsHost</span> <span class="string">$http_origin;</span></span><br><span class="line">         <span class="string">set</span> <span class="string">$httpHost</span> <span class="string">'k8s-m.hhu.com'</span><span class="string">;</span></span><br><span class="line"> <span class="string">&#125;</span></span><br><span class="line"></span><br><span class="line"> <span class="comment"># 修改为如下方式，支持http和https的跨域</span></span><br><span class="line"><span class="string">if</span> <span class="string">(</span> <span class="string">$http_origin</span> <span class="string">~*</span> <span class="string">^(https?:\/\/.*\.hhu\.com)</span> <span class="string">)&#123;</span></span><br><span class="line"> <span class="string">set</span> <span class="string">$corsHost</span> <span class="string">$http_origin;</span></span><br><span class="line"> <span class="string">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="2-ingress中的rewrite"><a href="#2-ingress中的rewrite" class="headerlink" title="2. ingress中的rewrite"></a>2. ingress中的rewrite</h4><p>&emsp;ingress中配置rewrite有一个自己的注解<code>nginx.ingress.kubernetes.io/rewrite-target</code>，可以自己配置为指定值，我工作中更习惯将其配置为各个服务自己的contextPath，比如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">    annotations:</span> </span><br><span class="line">      <span class="string">...</span></span><br><span class="line">      <span class="string">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/account/$1</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line">  <span class="comment"># 可以有多个</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">www.hhu.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line">        <span class="comment"># 可以有多个（可以正则）</span></span><br><span class="line"><span class="attr">        - path:</span> <span class="string">/($/.*)</span></span><br><span class="line"><span class="attr">          backend:</span></span><br><span class="line"><span class="attr">            serviceName:</span> <span class="string">server-account</span></span><br><span class="line"><span class="attr">            servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>上述<code>path</code>使用的是一个正则<code>/($/.*)</code>，可以匹配<code>/</code>或者任意以<code>/</code>开头任意多个字符，我比较推荐使用这个正则<code>/($/.*)</code>，而不推荐使用<code>/(.*)</code>，因为<code>.*</code>要求你至少匹配1个字符，即匹配不到<code>/</code>的path。可以配置<code>www.hhu.com</code>域名下任意路径，在其前面加上<code>/account</code>（比如访问的是<code>www.hhu.com/info</code>会被路由到<code>www.hhu.com/account/info</code>）。</p><p>&emsp;ingress中的rewrite同样可以使用<code>nginx.ingress.kubernetes.io/server-snippet</code>注解来更加多样化的配置，示例如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">nginx.ingress.kubernetes.io/server-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">  # analyzer</span></span><br><span class="line"><span class="string">  rewrite /behavior/data($|.*) /analyzer/behavior/data$1 break;</span></span><br><span class="line"><span class="string">  rewrite /hhu/event($|.*) /analyzer/hhu/event$1 break;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # mine 或者写成 rewrite /private(/|$)(.*) /mine/private$1$2 break;</span></span><br><span class="line"><span class="string">  rewrite /private($|.*) /mine/private$1 break; </span></span><br><span class="line"><span class="string">  rewrite /mine($|.*) /mine$1 break; </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # shop</span></span><br><span class="line"><span class="string">  rewrite /shops($|.*) /shops$1 break; </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # task</span></span><br><span class="line"><span class="string">  rewrite /internal($|.*) /task/internal$1 break; </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # portal</span></span><br><span class="line"><span class="string">  rewrite /($|.*) /portal/$1 break;</span></span><br></pre></td></tr></table></figure><p>上述注意，<code>break</code>千万不能丢，像类似于<code>/($|.*)</code>全匹配的正则一定要写到最下面，否则它会最先匹配（原因未知，不是说好的路径最长匹配吗……）。</p><h4 id="3-https配置"><a href="#3-https配置" class="headerlink" title="3. https配置"></a>3. https配置</h4><p>&emsp;hhtps的配置和跳转很简单，添加完证书参照官网配置即可，这里略过，从http重定向到https，可以使用注解：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 方式1：该注解只在配置了HTTPS之后才会生效进行跳转</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/ssl-redirect:</span> <span class="string">"true"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式2：强制跳转到https，不论是否配置了https证书</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/force-ssl-redirect:</span> <span class="string">"true"</span></span><br></pre></td></tr></table></figure><p>在配置HTTPS过程中，其他的照常操作，并且在业务应用的ingress配置了https相关协议：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">nginx.ingress.kubernetes.io/server-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">  ssl_protocols SSLv3 TLSv1 TLSv1.1 TLSv1.2;</span></span><br><span class="line"><span class="string">  ssl_ciphers RC4:HIGH:!aNULL:!MD5;</span></span><br><span class="line"><span class="string">  ssl_prefer_server_ciphers on;</span></span><br></pre></td></tr></table></figure><p>但在桌面端应用连接HTTPS时出现：<code>The request was aborted: Could not create SSL/TLS secure channel</code>，通过fiddler抓取https握手过程，发现客户端发送clienthello后，抛出上述异常，握手失败，抓取结果如下</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvMXpmWFk5Y00vaW1hZ2UucG5n" alt="握手失败抓包"></p><p>很明显客户端在和服务端建立通道时出现了问题，正常https（即http+SSL/TLS）的请求过程如下（图片源自网络）</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvWlJzY3pZMVIvaHR0cHMucG5n" alt="https请求过程"></p><p>对比发现在第三步断开了，在第一步客户端和服务端进行握手过程中，客户端表明自身只支持TLS协议的1.0版本，但服务端并未有响应，握手失败，推测服务端不支持TLS1.0导致，但我已经在ingress配置了对TLS1.0的支持（即<code>TLSv1</code>），到容器中查看<code>nginx.conf</code>，也确实出现在配置文件中了，为了确切的验证是否<code>ssl_protocols SSLv3 TLSv1 TLSv1.1 TLSv1.2</code>生效，又通过<a href="https://www.ssllabs.com/ssltest/index.html" rel="external nofollow noopener noreferrer" target="_blank">SSL Server Test</a>检测，然后问题发现了，发现服务端确实不支持TLS1.0协议（只支持1.2）：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvZHRUSnBDd3Avc2NyZWVuc2hvdC0yLnBuZw" alt="SSL检测结果"></p><p>最终苦战2天发现和技术支持确认了一个wingress默认只支持<code>TLS1.2</code>，如何需要开启其他的，必须通过configMap配置如下的data：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">kind:</span> <span class="string">ConfigMap</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">nginx-config</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line"><span class="attr">  ssl-ciphers:</span> <span class="string">"ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-DSS-AES128-GCM-SHA256:kEDH+AESGCM:ECDHE-RSA-AES128-SHA256:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA:ECDHE-ECDSA-AES128-SHA:ECDHE-RSA-AES256-SHA384:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA:ECDHE-ECDSA-AES256-SHA:DHE-RSA-AES128-SHA256:DHE-RSA-AES128-SHA:DHE-DSS-AES128-SHA256:DHE-RSA-AES256-SHA256:DHE-DSS-AES256-SHA:DHE-RSA-AES256-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA256:AES256-SHA256:AES128-SHA:AES256-SHA:AES:CAMELLIA:DES-CBC3-SHA:!aNULL:!eNULL:!EXPORT:!DES:!RC4:!MD5:!PSK:!aECDH:!EDH-DSS-DES-CBC3-SHA:!EDH-RSA-DES-CBC3-SHA:!KRB5-DES-CBC3-SHA"</span></span><br><span class="line"><span class="attr">  ssl-protocols:</span> <span class="string">"TLSv1 TLSv1.1 TLSv1.2"</span></span><br></pre></td></tr></table></figure><p>当然上述的加密套件<code>ssl-ciphers</code>可以在configMap配置，也可以在各个应用中的server去自定义配置，所以配置过上述的ConfigMap后即可，但我在工作过程中是只在ConfigMap中配置了支持的协议<code>ssl-protocols</code>，加密套件是放在各个应用的Ingress中配置的：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 方式1，注解搞定，这个配置是nginx官方给出来的加密套件</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/ssl-ciphers:</span> <span class="string">"ALL:!aNULL:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式2，自定义服务片段</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/server-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">ssl_ciphers ALL:!aNULL:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;</span></span><br><span class="line"><span class="string">ssl_prefer_server_ciphers on;</span></span><br></pre></td></tr></table></figure><p>我不太确定的是使用注解配置时<code>ssl_prefer_server_ciphers</code>（指定在使用sslv3和tls协议时，服务器密码应优先于客户端密码）是否开启。调式通过后的握手过程如下：</p><p><img src="https://imgconvert.csdnimg.cn/aHR0cHM6Ly9pLnBvc3RpbWcuY2MvdENjNG4wSHgvaW1hZ2UucG5n" alt="https正常握手抓包"></p><p>服务端响应ClientHello的结果包含了SSL版本以及加密组件，服务器的加密组件内容是从客户端发来的加密组件列表中选出来的。</p><p>附：关于https的简介（其实是我自己不知道(+_+)?，入职后第一次遇到这么让我无从下手的问题）</p><blockquote><p>https，即http+SSL/TLS，https就是在http外层套了一层TSL（Transport Layer Security-传输层安全协议）或者SSL（Secure Socket Layer-安全套接字层）的壳</p></blockquote><h4 id="4-ingress暴露自定义header"><a href="#4-ingress暴露自定义header" class="headerlink" title="4. ingress暴露自定义header"></a>4. ingress暴露自定义header</h4><p>&emsp;有时候某些服务可能会往response中塞一些自定义的header，比如域名<code>api.hhu.com</code>指向后端服务A，<code>www.hhu.com</code>指向一个前端ssr服务B，B中向A服务的某个请求时，A向客户端响应中塞了一个自定义header <code>x-page-count</code>，那此时<code>api.hhu.com</code>不但需要配置允许<code>www.hhu.com</code>跨域，而且需要额外配置<code>api.hhu.com</code>响应中的<code>x-page-count</code>对请求方暴露，否则前端服务是无法读取这个header的，应该在api域名的ingress服务端片段中额外配置一个<code>Access-Control-Expose-Headers</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 用于指明哪些头在跨域时不会被过滤掉，Access-Control-Expose-Headers 中罗列的 header 都是认为是安全的，可以暴露给前端</span></span><br><span class="line"><span class="string">add_header</span> <span class="string">Access-Control-Expose-Headers</span> <span class="string">'x-page-count'</span><span class="string">;</span></span><br></pre></td></tr></table></figure><p>注：之前一直以为在配置跨域时，如果配置<code>add_header Access-Control-Allow-Headers</code>，那前端一定能拿到该字段中的所有header，实则不然，查了以下MDN，得出下面的结论：</p><ul><li><code>Access-Control-Allow-Headers</code>：用于一个预检请求指示哪个HTTP标头将通过提供 <code>Access-Control-Expose-Headers</code> 使实际的请求时，所以它只是用来预检的；</li><li><code>Access-Control-Expose-Headers</code>：用于指明哪些头在跨域时不会被过滤掉，<code>Access-Control-Expose-Headers</code> 中罗列的 header 都被是认为是安全的，会暴露给调用方；</li></ul><h4 id="5-注解说明"><a href="#5-注解说明" class="headerlink" title="5. 注解说明"></a>5. 注解说明</h4><p>&emsp;虽然ingress的文档比较全（这里的全是指覆盖面广，但解释不细致，很多注解需要自己去摸索），下面对某些注解作一些记录说明：</p><h5 id="5-1-nginx-ingress-kubernetes-io-affinity-quot-cookie-quot"><a href="#5-1-nginx-ingress-kubernetes-io-affinity-quot-cookie-quot" class="headerlink" title="5.1 nginx.ingress.kubernetes.io/affinity: &quot;cookie&quot;"></a>5.1 <code>nginx.ingress.kubernetes.io/affinity: &quot;cookie&quot;</code></h5><p>&emsp;该注解用作于session绑定，主要用来在nginx层生成一个黏性cookie以黏住上游服务，正常线上会有多个Pod，多多少少会遇到分布式session如何共享的问题，因为一个请求过来指向的后端服务可能有多个Server，典型的场景就是请求登录时服务端A向response中写了个cookie（可以视作session <code>JSESSIONID</code>），然后让客户端每次请求时都带上这个cookie，但后续的某些请求发送到了相同Servier的服务器A1了，此时A1不能识别请求带过来的这个Cookie，导致出错。官方的解释如下：</p><blockquote><p>The annotation nginx.ingress.kubernetes.io/affinity enables and sets the affinity type in all Upstreams of an Ingress. This way, a request will always be directed to the same upstream server. The only affinity type available for NGINX is cookie.If more than one Ingress is defined for a host and at least one Ingress uses nginx.ingress.kubernetes.io/affinity: cookie, then only paths on the Ingress using nginx.ingress.kubernetes.io/affinity will use session cookie affinity. All paths defined on other Ingresses for the host will be load balanced through the random selection of a backend server.</p></blockquote><p>实际发现不生效，有些疑问，使用的示例yaml如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">nginx-test</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="comment"># nginx只支持 cookie 类型</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/affinity:</span> <span class="string">"cookie"</span></span><br><span class="line">    <span class="comment"># 自定义黏性cookie的名字，默认为 INGRESSCOOKIE</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/session-cookie-name:</span> <span class="string">"route"</span></span><br><span class="line">    <span class="comment"># 配置黏性cookie的path，但好像不生效</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/session-cookie-path:</span> <span class="string">"/"</span></span><br><span class="line">    <span class="comment"># 对应于 Max-Age</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/session-cookie-expires:</span> <span class="string">"172800"</span></span><br><span class="line">    <span class="comment"># 对应于 Expires</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/session-cookie-max-age:</span> <span class="string">"172800"</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">stickyingress.example.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">http-svc</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">        path:</span> <span class="string">/</span></span><br></pre></td></tr></table></figure><p>在后续调试过程中，需要查看<code>route</code>这个cookie的Path，默认是ingress中匹配到的路由路径（即<code>path</code>），其中黏性<code>route</code>的值是根据路由到的Server生成随机随机值，如果有多个ingress指向相同的Service，那必须在多个ingress同时配置黏性cookie，如果只有其中一个或某些ingress配置了黏性cookie，那只会应用这些配置黏性cookie的ingress，那些未配置的ingress将不会被应用。</p><h5 id="5-2-改写cookie的pathnginx-ingress-kubernetes-io-proxy-cookie-path"><a href="#5-2-改写cookie的pathnginx-ingress-kubernetes-io-proxy-cookie-path" class="headerlink" title="5.2 改写cookie的pathnginx.ingress.kubernetes.io/proxy-cookie-path"></a>5.2 改写cookie的path<code>nginx.ingress.kubernetes.io/proxy-cookie-path</code></h5><p>&emsp;在代理返回结果时，有时需要更改cookie的path，即可使用<code>nginx.ingress.kubernetes.io/proxy-cookie-path</code>注解，它的语法为<code>&lt;currentCookiePath&gt; &lt;targetPath&gt;</code>，否则不生效，示例为<code>nginx.ingress.kubernetes.io/proxy-cookie-path: &quot;/hhu /&quot;</code>，即对应于ngixn中的<code>proxy_cookie_path /bops /;</code>。</p><h5 id="5-3-定制服务器片段nginx-ingress-kubernetes-io-server-snippet"><a href="#5-3-定制服务器片段nginx-ingress-kubernetes-io-server-snippet" class="headerlink" title="5.3 定制服务器片段nginx.ingress.kubernetes.io/server-snippet"></a>5.3 定制服务器片段<code>nginx.ingress.kubernetes.io/server-snippet</code></h5><p>&emsp;该注解中可以做一些较为复杂的定制，对应于nginx中的服务器块（即<code>server</code>那个块），如上述中的多个<code>rewrite</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">nginx.ingress.kubernetes.io/server-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">  # shop</span></span><br><span class="line"><span class="string">  rewrite /shops($|.*) /shops$1 break; </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # task</span></span><br><span class="line"><span class="string">  rewrite /internal($|.*) /task/internal$1 break; </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">  # portal</span></span><br><span class="line"><span class="string">  rewrite /($|.*) /portal/$1 break;</span></span><br></pre></td></tr></table></figure><p>对应生成的nginx.conf就为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line"> server_name k8s-xx.xxx.com ;</span><br><span class="line"></span><br><span class="line"> listen 80;</span><br><span class="line"></span><br><span class="line"> set $proxy_upstream_name &quot;-&quot;;</span><br><span class="line"></span><br><span class="line"> rewrite /shops($|.*) /shops$1 break;</span><br><span class="line"> rewrite /internal($|.*) /task/internal$1 break;</span><br><span class="line"> rewrite /($|.*) /portal/$1 break;</span><br><span class="line"></span><br><span class="line"> location /xxx &#123;</span><br><span class="line">   ...</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="5-4-定制位置片段nginx-ingress-kubernetes-io-configuration-snippet"><a href="#5-4-定制位置片段nginx-ingress-kubernetes-io-configuration-snippet" class="headerlink" title="5.4 定制位置片段nginx.ingress.kubernetes.io/configuration-snippet"></a>5.4 定制位置片段<code>nginx.ingress.kubernetes.io/configuration-snippet</code></h5><p>&emsp;该注解用于nginx中的<code>location</code>自定义配置，会应用于所有的<code>location</code>（<code>location /</code>可能需要适当调整），示例如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 示例1：在响应中创建一个名为Request-Id的header</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/configuration-snippet:</span> <span class="string">|</span></span><br><span class="line"><span class="string">  more_set_headers "Request-Id: $req_id";</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 示例2，替换路由目标的Service</span></span><br><span class="line"><span class="string">nginx.ingress.kubernetes.io/configuration-snippet: |</span></span><br><span class="line"><span class="string">  serviceName=&lt;myservice1&gt;</span></span><br><span class="line"><span class="string">  # Example location snippet</span></span><br><span class="line"><span class="string">  proxy_request_buffering off;</span></span><br><span class="line"><span class="string">  rewrite_log on;</span></span><br><span class="line"><span class="string">  proxy_set_header "x-additional-test-header" "location-snippet-header";</span></span><br><span class="line"><span class="string">  &lt;EOS&gt;</span></span><br><span class="line"><span class="string">  serviceName=&lt;myservice2&gt;</span></span><br><span class="line"><span class="string">  proxy_set_header Authorization "";</span></span><br><span class="line"><span class="string">  &lt;EOS&gt;</span></span><br></pre></td></tr></table></figure><p>但注意这个注解的配置偶尔不是即时生效的！！</p><h5 id="5-5-nginx-ingress-kubernetes-io-rewrite-target注解"><a href="#5-5-nginx-ingress-kubernetes-io-rewrite-target注解" class="headerlink" title="5.5 nginx.ingress.kubernetes.io/rewrite-target注解"></a>5.5 <code>nginx.ingress.kubernetes.io/rewrite-target</code>注解</h5><p>&emsp;之前其他项目组的同事有这么的需求，同一个域名下有多个服务，但这多个服务都没有context-path，通过如下的方式进行转发：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"># 他们的访问方式</span><br><span class="line">api.xxx.com/comment/go  ---&gt; comment服务 ---&gt; 对应于 10.5.3.28:8080/go</span><br><span class="line">api.xxx.com/account/go  ---&gt; account服务 ---&gt; 对应于 10.5.3.28:8081/go</span><br><span class="line">api.xxx.com/bops/go ---&gt; bops服务 ---&gt; 对应于 10.5.3.28:8082/go</span><br><span class="line"></span><br><span class="line"># 我们的方式方式</span><br><span class="line">api.xxx.com/comment/go  ---&gt; comment服务 ---&gt; 对应于 10.5.3.28:8080/comment/go</span><br></pre></td></tr></table></figure><p>注意域名都是相同的，那怎么做到这样的需求呢？原来我们服务都是在ingress中rewrite后通过context-path去直接转发到对应的服务，但它这里不一样，暴露出来的URL和真正访问服务的path不一样，如果不处理会出现404，通过<code>nginx.ingress.kubernetes.io/rewrite-target</code>注解可以实现这个功能，配置如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/rewrite-target:</span> <span class="string">/$2</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">rewrite</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">rewrite.bar.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">http-svc</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">        path:</span> <span class="string">/something(/|$)(.*)</span></span><br></pre></td></tr></table></figure><p>最终请求Service的是<code>/something</code>或者<code>/something/</code>后面的内容，但最终一个域名会出现多个ingress（不易维护），最后还是建议在项目中加上context-path来做各个应用的区分。</p><h5 id="5-6-nginx-ingress-kubernetes-io-from-to-www-redirect-quot-true-quot"><a href="#5-6-nginx-ingress-kubernetes-io-from-to-www-redirect-quot-true-quot" class="headerlink" title="5.6 nginx.ingress.kubernetes.io/from-to-www-redirect: &quot;true&quot;"></a>5.6 <code>nginx.ingress.kubernetes.io/from-to-www-redirect: &quot;true&quot;</code></h5><p>&emsp;这个注解可以让<code>domain.com</code>跳转到<code>www.domain.com</code>，主站域名可以关注一下。</p><h4 id="6-ingress作内部反向代理"><a href="#6-ingress作内部反向代理" class="headerlink" title="6. ingress作内部反向代理"></a>6. ingress作内部反向代理</h4><p>&emsp;需求是同一个域名，根据<code>http_user_agent</code>将请求路由到不同的ssr上（分为移动端和网站端2个ssr服务），可以这么在服务端片段（<code>nginx.ingress.kubernetes.io/server-snippet</code>）中这么配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">proxy_set_header</span> <span class="string">Host</span> <span class="string">$host;</span></span><br><span class="line"><span class="string">proxy_set_header</span> <span class="string">X-Real-IP</span> <span class="string">$remote_addr;</span></span><br><span class="line"><span class="string">proxy_set_header</span> <span class="string">X-Forwarded-For</span> <span class="string">$proxy_add_x_forwarded_for;</span></span><br><span class="line"></span><br><span class="line"><span class="string">location</span> <span class="string">/go</span> <span class="string">&#123;</span></span><br><span class="line"><span class="string">if</span> <span class="string">($http_user_agent</span> <span class="string">~*</span> <span class="string">"(.*(a|A)ndroid.*)|(.*i(p|P)hone.*)|(.*(w|W)indows (p|P)hone.*)$"</span><span class="string">)&#123;</span></span><br><span class="line"><span class="string">proxy_pass</span> <span class="attr">http://&lt;svc1&gt;.&lt;namespace&gt;.svc.cluster.local;</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string">proxy_pass</span> <span class="attr">http://&lt;svc2&gt;.&lt;namespace&gt;.svc.cluster.local;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><p>主要这个其实就是从nginx衍生的，没啥新东西，注：在<code>/etc/resolv.conf</code>可以看到对应的代理地址。</p><h4 id="关于安装"><a href="#关于安装" class="headerlink" title="关于安装"></a>关于安装</h4><p>&emsp;目前Ingress的安装很简单，参照官网作如下步骤：</p><ol><li>拉取yaml文件：<code>wget https://raw.githubusercontent.com/kubernetes/ingress-nginx/master/deploy/static/mandatory.yaml</code>；</li><li>修改镜像，将ingress镜像<code>quay.io/kubernetes-ingress-controller/nginx-ingress-controller:0.25.0</code>更改为<code>registry.cn-shanghai.aliyuncs.com/hhu/nginx-ingress-controller:0.25.0</code>（已公开）；</li><li>直接安装<code>kubectl apply -f mandatory.yaml</code>；</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;Ingress是管理外部网咯访问K8S集群中Service的API对象（典型就是HTTP），它可以提供负载均衡、SSL终端以及基于名字的虚拟host。简单点来说它就是外网访问集群应用的媒介，流程如下：&lt;br&gt;
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="K8S" scheme="https://jacksonary.github.io/tags/K8S/"/>
    
      <category term="Kubernetes" scheme="https://jacksonary.github.io/tags/Kubernetes/"/>
    
      <category term="Ingress" scheme="https://jacksonary.github.io/tags/Ingress/"/>
    
  </entry>
  
  <entry>
    <title>K8S经验总结</title>
    <link href="https://jacksonary.github.io/posts/23062039.html"/>
    <id>https://jacksonary.github.io/posts/23062039.html</id>
    <published>2019-07-01T00:55:00.000Z</published>
    <updated>2021-06-04T12:39:00.710Z</updated>
    
    <content type="html"><![CDATA[<h3 id="1-资源配额"><a href="#1-资源配额" class="headerlink" title="1. 资源配额"></a>1. 资源配额</h3><p><strong>1. 预留和限制的概念</strong></p><ul><li>预留：即至少需要的资源，对应于<code>requestsCpu</code>和<code>requestsMemory</code>；</li><li>资源限制：最大资源限制，对应于<code>limitsCpu</code>和<code>limitsMemory</code>；<a id="more"></a></li></ul><p>注：这里提一下个人理解，对于上述2个参数刚接触的时候感觉理解了又不理解，不知道新人是不是也是这种感觉，后来接触多了，感觉这样理解会好一点：<code>requestsXxx</code>表示必须的资源，在K8S对Pod进行调度时，只有节点上的资源至少满足<code>requestsXxx</code>的值，那么这个Pod才有可能调度到这些满足需求的节点上，所以很多相关译文上将<code>requestsXxx</code>翻译为“预留XXX”很到位；而当Pod已经运行在某个节点上时，就没<code>requestsXxx</code>参数什么事了，最终该Pod可以获得宿主机多少资源取决自身，但不会超过<code>limitsXxx</code>限制的值。所以可以粗略地理解为<code>requestsXxx</code>是为了Pod调度，而<code>limitsXxx</code>是为了遏制Pod对宿主机资源的索取最大限度。K8S对于CPU资源管理的策略分为<code>none</code>（默认，通过CFS配额 cgroups-control groups）和<code>static</code>（为节点上具有某些特征的 pod 赋予增强的 CPU 亲和性和独占性，针对配置资源为整数型的POD）</p><p><strong>2. 单位的含义</strong></p><ul><li>内存：常用的单位为<code>MiB</code>和<code>Gi</code>，和常规单位的关系如下为：</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1 MiB = 2^20 bytes = 1024 kibibytes = 1048576 bytes（以2为底数）</span><br><span class="line">1 MB = 1000KB（以10为底数）</span><br></pre></td></tr></table></figure><ul><li>CPU：常用单位为毫核（<code>m</code>或<code>milli</code>），或者直接是核（没有单位，如1核直接表示为<code>1</code>），换算关系为：1个核=1000m，当然也可以使用占比来表示，如：1/4个核=0.25，半个核=0.5，1个整核=1。</li></ul><p>注：在rancher中部署应用时，经常遇到<code>deployment does not have minimum availability</code>异常，此时适当调大资源限额可以解决大部分的问题，下面是Chart模板中常用的配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">resources:</span></span><br><span class="line"><span class="attr">  limits:</span></span><br><span class="line"><span class="attr">    memory:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.resources.limits.memory</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    cpu:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.resources.limits.cpu</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">  requests:</span></span><br><span class="line"><span class="attr">    memory:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.resources.requests.memory</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    cpu:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.resources.requests.cpu</span> <span class="string">&#125;&#125;</span></span><br></pre></td></tr></table></figure><h3 id="2-关于探针"><a href="#2-关于探针" class="headerlink" title="2. 关于探针"></a>2. 关于探针</h3><p>&emsp;K8S中的探针应该是必不可少的一部分，参照文档，理了理，探针主要分为2类：</p><ul><li>liveness probe：存活探针，它可以<font color="red">决定何时重启容器</font>，比如存活探针在应用运行过程中遇到死锁（应用确实是正在运行的，但无法获取结果），这种情况往往是由于代码中存在bug，尽管如此，但存活探针的存在可以让应用更加具有可用性；</li><li>readiness probe：就绪探针，它可以决定一个容器何时就绪，准备接受外部的流量。如果说某个Pod已经就绪表示该Pod中的所有容器都已经就绪，当一个Pod处于未就绪的状态，他将会从负载均衡中移除；</li></ul><p>实际观测，只有当2类探针同时检测通过，Pod才算启动成功，下面是探针的配置方式：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">readinessProbe:</span></span><br><span class="line"><span class="attr">  enabled:</span> <span class="literal">true</span></span><br><span class="line"><span class="attr">  httpGet:</span></span><br><span class="line"><span class="attr">    path:</span> <span class="string">"/healthcheck"</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">20</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">60</span></span><br><span class="line"><span class="attr">  failureThreshold:</span> <span class="number">3</span></span><br><span class="line"></span><br><span class="line"><span class="attr">livenessProbe:</span></span><br><span class="line"><span class="attr">  enabled:</span> <span class="literal">true</span></span><br><span class="line"><span class="attr">  httpGet:</span></span><br><span class="line"><span class="attr">    path:</span> <span class="string">"/healthcheck"</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">180</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">30</span></span><br><span class="line"><span class="attr">  failureThreshold:</span> <span class="number">3</span></span><br></pre></td></tr></table></figure><p>实际开发中，发现后端的Server启动用这套探针没啥问题，但前端的Server（使用node）启动的过程非常不稳定，build和拉取依赖的过程耗时特别长，基本都会重启好几遍容器才能成功，我曾经试图去调节存活探针的<code>initialDelaySeconds</code>参数，但如果这个过程快的话我岂不是要浪费一些时间，最终发现调节失败的阈值<code>failureThreshold</code>会更加方便。</p><p>&emsp;具体探针的行为可以有如下的一些：</p><p><strong>1.HTTP</strong><br><br>java中探针往往都是通过调用类似于<code>go</code>的HTTP接口，示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">livenessProbe:</span></span><br><span class="line"><span class="attr">  httpGet:</span></span><br><span class="line"><span class="attr">    path:</span> <span class="string">/healthz</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">    httpHeaders:</span></span><br><span class="line"><span class="attr">    - name:</span> <span class="string">Custom-Header</span></span><br><span class="line"><span class="attr">      value:</span> <span class="string">Awesome</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">3</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">3</span></span><br></pre></td></tr></table></figure><p><strong>2.具体指令</strong><br><br>探针检测的具体行为可以自己定义，比如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">livenessProbe:</span></span><br><span class="line"><span class="attr">  exec:</span></span><br><span class="line"><span class="attr">    command:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">cat</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">/tmp/healthy</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">5</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">5</span></span><br></pre></td></tr></table></figure><p>上述是将探针的行为定义为Linux中的<code>cat</code>命令</p><p><strong>3.TCP</strong><br><br>比如一些ES的连接可能会用到TCP</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">readinessProbe:</span></span><br><span class="line"><span class="attr">  tcpSocket:</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">5</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">10</span></span><br><span class="line"><span class="attr">livenessProbe:</span></span><br><span class="line"><span class="attr">  tcpSocket:</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">  initialDelaySeconds:</span> <span class="number">15</span></span><br><span class="line"><span class="attr">  periodSeconds:</span> <span class="number">20</span></span><br></pre></td></tr></table></figure><h3 id="3-Pod日志查看"><a href="#3-Pod日志查看" class="headerlink" title="3. Pod日志查看"></a>3. Pod日志查看</h3><h4 id="3-1-进入指定Pod中的指定容器"><a href="#3-1-进入指定Pod中的指定容器" class="headerlink" title="3.1 进入指定Pod中的指定容器"></a>3.1 进入指定Pod中的指定容器</h4><p>&emsp;在rancher中，权限一般给的很严，一般不会使用<code>docker</code>命令直接进入容器，而是要使用<code>kubectl</code>命令才能进入：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">kubectl exec -it &lt;podName&gt; -c &lt;containerName&gt; -n &lt;namespace&gt; -- -- /bin/bash</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 退出容器</span><br><span class="line">exit</span><br></pre></td></tr></table></figure><p>如果Pod中只有1个容器，上述命令可以直接简写为<code>kubectl exec -it &lt;podName&gt; -n &lt;namespace&gt; -- -- /bin/bash</code>，进入容器后即可为所欲为了(●ˇ∀ˇ●)。</p><p>【注】上述通过<code>kubectl exec</code>可以拓展为任意容器中的命令，形如<code>kubectl exec &lt;pod-name&gt; -n &lt;name-space&gt; -- &lt;command&gt; xxx</code>，只要容器中可以执行<code>&lt;command&gt; xxx</code>命令，那就可以通过上述的方式在不进入容器的情况下对容器执行一些命令。</p><h4 id="3-2-查看Pod日志"><a href="#3-2-查看Pod日志" class="headerlink" title="3.2 查看Pod日志"></a>3.2 查看Pod日志</h4><p>&emsp;Pod日志可以直接通过<code>logs</code>或<code>describe</code>查看，具体示例如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 1. logs方式</span><br><span class="line">kubectl logs &lt;pod_name&gt; -c &lt;container_name&gt; -n &lt;namespace&gt;</span><br><span class="line"><span class="meta">#</span> 2. describe方式</span><br><span class="line">kubectl describe pod &lt;pod_name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><p>得到的结果也是2个不同的，<code>logs</code>方式得到的日志是我们是部署应用时指定挂载目录位置中的日志，如deployment中在指定容器规约时：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  containers:</span></span><br><span class="line">  <span class="string">...</span></span><br><span class="line"><span class="attr">    volumeMounts:</span></span><br><span class="line"><span class="attr">    - mountPath:</span> <span class="string">/data/logs/paas</span></span><br><span class="line"><span class="attr">      name:</span> <span class="string">log-dir</span></span><br></pre></td></tr></table></figure><p>那此时看到的日志就是<code>/data/logs/paas/xxx.log</code>的内容。</p><p>&emsp;<code>describe</code>方式得到的日志是关于pod行为的日志，最典型的就是Pod节点的分配<code>Scheduled</code>、镜像的拉取<code>Pulling</code>/<code>Pulled</code>、容器的创建<code>Created</code>及运行<code>Started</code>、探针检测<code>Unhealthy</code>/<code>Healthy</code>。</p><h3 id="4-版本"><a href="#4-版本" class="headerlink" title="4. 版本"></a>4. 版本</h3><p>&emsp;和Helm类似的，K8S集群和K8S集群客户端是两回事，在写Chart时需要一个属性<code>kubeVersion</code>，查看版本信息：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Client Version: version.Info&#123;Major:"1", Minor:"12", GitVersion:"v1.12.7", GitCommit:"6f482974b76db3f1e0f5d24605a9d1d38fad9a2b", GitTreeState:"clean", BuildDate:"2019-03-25T02:52:13Z", GoVersion:"go1.10.8", Compiler:"gc", Platform:"linux/amd64"&#125;</span><br><span class="line">Server Version: version.Info&#123;Major:"1", Minor:"11", GitVersion:"v1.11.5", GitCommit:"753b2dbc622f5cc417845f0ff8a77f539a4213ea", GitTreeState:"clean", BuildDate:"2018-11-26T14:31:35Z", GoVersion:"go1.10.3", Compiler:"gc", Platform:"linux/amd64"&#125;</span><br></pre></td></tr></table></figure><p>我们需要的是服务端版本，指的是<code>GitVersion:&quot;v1.11.5&quot;</code>，所以<code>kubeVersion: 1.11.5</code></p><h3 id="5-使用-ExternalName-Service-代理OSS静态"><a href="#5-使用-ExternalName-Service-代理OSS静态" class="headerlink" title="5. 使用 ExternalName Service 代理OSS静态"></a>5. 使用 <code>ExternalName</code> Service 代理OSS静态</h3><p>&emsp;通常使用Service去发现Server，对于正常应用都可以应对，但当代理OSS静态资源时，有点尴尬，集群内是没有明确的Server对应，此时必须使用<code>ExternalName</code>类型的Service去解决，示例如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 额外的ExternalName Service用于OSS静态代理</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">hhu-static</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  type:</span> <span class="string">ExternalName</span></span><br><span class="line">  <span class="comment"># 此处为OSS访问域名，取回CNAME与之映射</span></span><br><span class="line"><span class="attr">  externalName:</span> <span class="string">hhu-prod-static.oss-cn-beijing-internal.aliyuncs.com</span></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">server-hhu</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">kubernetes.io/ingress.class:</span> <span class="string">nginx-ingress</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  rules:</span></span><br><span class="line"><span class="attr">  - host:</span> <span class="string">k8s-www.hhu.com</span></span><br><span class="line"><span class="attr">    http:</span></span><br><span class="line"><span class="attr">      paths:</span></span><br><span class="line"><span class="attr">      - path:</span> <span class="string">/*</span></span><br><span class="line"><span class="attr">        backend:</span></span><br><span class="line"><span class="attr">          serviceName:</span> <span class="string">hhu-static</span></span><br><span class="line"><span class="attr">          servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>注：OSS还需要额外绑定<code>k8s-www.hhu.com</code>域名，否则无法进行代理。</p><h3 id="6-让Master节点参与调度"><a href="#6-让Master节点参与调度" class="headerlink" title="6. 让Master节点参与调度"></a>6. 让Master节点参与调度</h3><p>&emsp;默认情况下，k8s集群是不让master节点参与Pod的调度，通过如下的命令可以改变这种行为：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 让master节点参与调度</span><br><span class="line">kubectl taint node &lt;k8s-master-hostname&gt; node-role.kubernetes.io/master-</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 恢复master节点的Master Only状态</span><br><span class="line">kubectl taint node &lt;k8s-master-hostname&gt; node-role.kubernetes.io/master=""</span><br></pre></td></tr></table></figure><h3 id="7-删除Pod"><a href="#7-删除Pod" class="headerlink" title="7. 删除Pod"></a>7. 删除Pod</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 正常删除</span><br><span class="line">kubectl delete pod xxx</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 强删</span><br><span class="line">kubectl delete pod &lt;pod name&gt; --force --grace-period=0 -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><h3 id="8-删除命名空间"><a href="#8-删除命名空间" class="headerlink" title="8. 删除命名空间"></a>8. 删除命名空间</h3><p>&emsp;删除命名空间的准确步骤如下：</p><ol><li>查看待删除的命名空间中是否存在资源：<code>kubectl get all -n xxx</code>，然后清空资源（务必确认这个步骤）；</li><li>删除namespace：<code>kubectl delete ns xxx</code>；</li><li>确认命名空间已删除：<code>kubectl get ns</code>；</li></ol><p>【注】：</p><ul><li>如果上述第一步没有完全删除待删除命名空间下的资源，那第三步可能出现该命名空间一直处于 Terminating 的状态；</li><li>无法删除 Pod 时可采用强制手段：<code>kubectl delete pod &lt;pod name&gt; --force --grace-period=0 -n &lt;namespace&gt;</code>；</li><li>如果上述手段无效解决 Terminating 状态的namespace，可以直接升级namespace，去除yaml文件中的<code>finalizers</code>属性：</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 编辑命名空间的配置文件</span><br><span class="line">kubectl edit ns xxx</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 删除如下属性保存退出即可</span><br><span class="line">  finalizers:</span><br><span class="line">  - kubernetes</span><br></pre></td></tr></table></figure><h3 id="9-创建拉取镜像的凭证"><a href="#9-创建拉取镜像的凭证" class="headerlink" title="9. 创建拉取镜像的凭证"></a>9. 创建拉取镜像的凭证</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 创建凭证</span><br><span class="line">kubectl -n &lt;name-space&gt; create secret docker-registry &lt;secret-name&gt; \</span><br><span class="line">--docker-server=registry.xxx.com \</span><br><span class="line">--docker-username=&lt;user-name&gt; \</span><br><span class="line">--docker-password=&lt;password&gt; \</span><br><span class="line">--docker-email=xxx@xx.com</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 删除密码</span><br><span class="line">kubectl delete secret &lt;secret-name&gt;</span><br></pre></td></tr></table></figure><p>在控制器的规约中指定拉取镜像的密码名字即可，在spec中直接使用：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">spec:</span> </span><br><span class="line"><span class="attr">  template:</span> </span><br><span class="line"><span class="attr">    spec:</span> </span><br><span class="line"><span class="attr">      imagePullSecrets:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">&lt;secret-name&gt;</span></span><br></pre></td></tr></table></figure><p>【注】上述的<code>--docker-xxx</code>选项中的值可能包含了一些特殊的值（比如<code>#</code>或者<code>@</code>等符号），此时需要使用英文的引号<code>&#39;xxx&#39;</code>来包裹，这种写法也是比较推荐的，不会出现问题。</p><h3 id="10-升级deployment-svc等（通用）"><a href="#10-升级deployment-svc等（通用）" class="headerlink" title="10. 升级deployment/svc等（通用）"></a>10. 升级deployment/svc等（通用）</h3><p>&emsp;比如升级deployment：<code>kubectl edit deployment apollo-portal</code>修改保存后就会自动升级，然后通过<code>kubectl rollout status deployment apollo-portal</code>即可看到更新的状态（不动不是卡住，是正在执行一个阶段），直到<code>... rolled out</code>即表示deployment升级成功，其对应的Pod也会重新创建并运行，下面是升级apollo-portal的日志：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"># 升级 apollo-portal 的deployment</span><br><span class="line">kubectl edit deployment apollo-portal</span><br><span class="line"># 修改保存后退出出现下面的打印结果</span><br><span class="line">deployment.extensions/apollo-portal edited</span><br><span class="line"></span><br><span class="line"># 查看升级过程</span><br><span class="line">kubectl rollout status deployment apollo-portal</span><br><span class="line"># 下面是升级日志</span><br><span class="line">Waiting for deployment &quot;apollo-portal&quot; rollout to finish: 1 old replicas are pending termination...</span><br><span class="line">Waiting for deployment &quot;apollo-portal&quot; rollout to finish: 1 old replicas are pending termination...</span><br></pre></td></tr></table></figure><h3 id="11-在Pod中添加host"><a href="#11-在Pod中添加host" class="headerlink" title="11. 在Pod中添加host"></a>11. 在Pod中添加host</h3><p>&emsp;测试环境中，由于少了域名解析的这一层，通常需要额外绑定一些host，此时可以通过<code>hostAliases</code>向Pod中添加指定的host，形如：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  hostAliases:</span></span><br><span class="line"><span class="attr">  - ip:</span> <span class="string">"127.0.0.1"</span></span><br><span class="line"><span class="attr">    hostnames:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">"foo.local"</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">"bar.local"</span></span><br><span class="line"><span class="attr">  - ip:</span> <span class="string">"10.1.2.3"</span></span><br><span class="line"><span class="attr">    hostnames:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">"foo.remote"</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">"bar.remote"</span></span><br></pre></td></tr></table></figure><p>通过名命令<code>kubectl exec server-746ddf6747-9kmx7 -n &lt;name-space&gt; -- cat /etc/hosts</code>可以检验。</p><h3 id="12-开启远程debug"><a href="#12-开启远程debug" class="headerlink" title="12. 开启远程debug"></a>12. 开启远程debug</h3><p>&emsp;在k8s中为了可以进行remote debug，可以做如下配置：</p><ol><li>java的启动参数增加：<code>-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=&lt;remote-debug-port&gt;</code>；</li><li>在Service中指定一个端口（使用<code>nodePort</code>类型）映射到<code>remote-debug-port</code>；</li><li>直接使用集群中任意节点的IP加nodePort形式即可（<code>&lt;nodeIp&gt;:&lt;nodePort&gt;</code>），nodeIp可以是任意节点IP；</li></ol><p>【注】<br><br>在上述第2步中，由于remote-debug使用的端口使用的是<code>NodePort</code>，所以在定义Service时需要额外将<code>type</code>也指定为<code>NodePort</code>，否者默认是<code>ClusterIP</code>类型，在该类型不允许使用<code>nodePort</code>来做映射，即类似于：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="comment"># 省略部分配置</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  ports:</span></span><br><span class="line"><span class="attr">  - port:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">    targetPort:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.service.port</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">http</span></span><br><span class="line"><span class="attr">  - port:</span> <span class="string">&#123;&#123;.Values.remoteDebugPort&#125;&#125;</span></span><br><span class="line"><span class="attr">    targetPort:</span> <span class="string">&#123;&#123;.Values.remoteDebugPort&#125;&#125;</span></span><br><span class="line"><span class="attr">    nodePort:</span> <span class="string">&#123;&#123;.Values.remoteDebugNodePort&#125;&#125;</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">remote-debug</span></span><br><span class="line"><span class="attr">  selector:</span></span><br><span class="line"><span class="attr">    service-cluster:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.deploy.serviceCluster</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    app:</span> <span class="string">&#123;&#123;</span> <span class="string">.Values.deploy.app</span> <span class="string">&#125;&#125;</span></span><br><span class="line"><span class="attr">    project-type:</span> <span class="string">java</span></span><br><span class="line"><span class="attr">  type:</span> <span class="string">NodePort</span></span><br></pre></td></tr></table></figure><p>K8S在创建Service时，将会给其分配IP地址（默认是<code>cluster IP</code>，即只能在集群中访问），Service可以将任意请求到<code>port</code>的端口映射到<code>targetPort</code>端口，完成最终请求，默认情况下，为了便利，<code>targetPort</code>设置为和<code>port</code>一样的值。</p><p>&emsp;<strong>ServiceType</strong>，即在定义Service时，<code>spec.type</code>字段，默认为<code>ClusterIP</code>，相关释义如下：</p><ul><li><code>ClusterIP</code>：使用集群内部的IP暴露服务，只能在集群内部访问服务，默认值；</li><li><code>NodePort</code>：将服务暴露在集群中每个节点的一个指定静态端口上（即<code>port.NodePort</code>），将会自动创建一个<code>ClusterIp</code>的Service，然后将<code>NodePort</code>的服务路由到上面，此时集群外部可以访问集群内的服务；</li><li><code>LoadBalancer</code>：使用云提供商（如阿里云）提供的负载均衡暴露服务，指向集群外部负载均衡的<code>NodePort</code>和<code>ClusterIp</code>Service会被自动创建；</li><li><code>ExternalName</code>（CoreDNS1.7+）：<code>externalName</code>字段通过返回CNAME记录的方式，将Service映射到上面。</li><li><h3 id="13-定向调度"><a href="#13-定向调度" class="headerlink" title="13. 定向调度"></a>13. 定向调度</h3></li></ul><p>&emsp;有些场景需要将Pod调度到指定节点上，步骤如下：</p><ol><li>给节点添加标签：<code>kubectl label node &lt;node_name&gt; &lt;your_label&gt;</code>；</li><li>在定义Pod时，直接通过<code>NodeSelector: &lt;your_label&gt;</code>即可；</li></ol><h3 id="14-预览chart包生成对应的K8S资源"><a href="#14-预览chart包生成对应的K8S资源" class="headerlink" title="14. 预览chart包生成对应的K8S资源"></a>14. 预览chart包生成对应的K8S资源</h3><p>&emsp;由于使用使用的go模板方式，写完之后最好验证一下最终会生成什么样的K8S资源，在排查chart包问题时十分管用，使用<code>helm install --dry-run --debug</code>命令：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 查看渲染后的文件， nginx-ingress 是chart包工程名，不是文件夹名，然后 nginx-ingress.yaml 是具体资源配置使用的yaml文件，如tiller在远程，通过 --host=x.x.x.x:xxxx 来指定</span><br><span class="line">helm install --dry-run --debug nginx-ingress -f nginx-ingress.yaml</span><br></pre></td></tr></table></figure><h3 id="15-集群中DNS解析异常或超时"><a href="#15-集群中DNS解析异常或超时" class="headerlink" title="15. 集群中DNS解析异常或超时"></a>15. 集群中DNS解析异常或超时</h3><p>&emsp;问题出现场景：在k8s集群中，项目中使用OKHttpClient发送请求时出现DNS无法解析的异常（在频繁发送时），异常日志如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">20:03:20.907 [http-nio-8080-exec-3] ERROR [OKHttpClient.java:201] - gosspublic.alicdn.com: Try again</span><br><span class="line">java.net.UnknownHostException: gosspublic.alicdn.com: Try again</span><br></pre></td></tr></table></figure><p>在Pod上查看内 DNS 策略 <code>cat /etc/resolv.conf</code>：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> nameserver 用于指定DNS服务器的IP</span><br><span class="line">nameserver 10.247.3.10</span><br><span class="line"><span class="meta">#</span> search 用于指定解析域名时可能会带上的前缀</span><br><span class="line">search hhu-test.svc.cluster.local svc.cluster.local cluster.local</span><br><span class="line"><span class="meta">#</span> 如果域名中的点号 . 少于5个，将会尝试将 search 中的那些前缀加到用户ping的域名前面来尝试解析</span><br><span class="line">options ndots:5 single-request-reopen timeout:2</span><br></pre></td></tr></table></figure><p>注：关于resolve中的配置作如下说明：</p><ul><li><code>nameserver</code>：用于指定DNS服务器；</li><li><code>search</code>：用于指定若用户域名中的点号<code>.</code>的数量小于<code>ndots</code>参数指定的值，那它将会尝试带上这些<code>search</code>指定的前缀去解析用户域名，若执行<code>ping test</code>（该域名中没有点号），实际它会帮我们同时去<code>ping test</code>、<code>ping hhu-test.svc.cluster.local.test</code>、<code>ping svc.cluster.local.test</code>、<code>ping cluster.local.test</code>，系统会尝试都去解析这些域名；</li><li><code>ndots</code>：用于指定用户域名中的点号<code>.</code>小于这个指定值，那系统会自动帮我们加上<code>search</code>中的各个前缀作域名解析；</li><li><code>single-request-reopen</code>和<code>single-request</code>：为了解决A记录和AAAA记录解析冲突或超时使用的；</li></ul><p><code>single-request-reopen</code>和<code>single-request</code>区别：</p><ul><li><code>single-request-reopen</code> (since glibc 2.9): A 和 AAAA 请求使用不同的 socket 来发送，这样它们的源 Port 就不同，五元组也就不同，避免了使用同一个 conntrack 表项；</li><li><code>single-request</code> (since glibc 2.10): A 和 AAAA 请求改成串行，没有并发，从而也避免了冲突；</li></ul><blockquote><p>single-request-reopen (since glibc 2.9)<br>                     Sets RES_SNGLKUPREOP in _res.options.  The resolver<br>                     uses the same socket for the A and AAAA requests.  Some<br>                     hardware mistakenly sends back only one reply.  When<br>                     that happens the client system will sit and wait for<br>                     the second reply.  Turning this option on changes this<br>                     behavior so that if two requests from the same port are<br>                     not handled correctly it will close the socket and open<br>                     a new one before sending the second request.</p></blockquote><blockquote><p>single-request (since glibc 2.10)<br>                     Sets RES_SNGLKUP in _res.options.  By default, glibc<br>                     performs IPv4 and IPv6 lookups in parallel since<br>                     version 2.9.  Some appliance DNS servers cannot handle<br>                     these queries properly and make the requests time out.<br>                     This option disables the behavior and makes glibc<br>                     perform the IPv6 and IPv4 requests sequentially (at the<br>                     cost of some slowdown of the resolving process).</p></blockquote><p>DNS 区域采用资源记录的形式存储信息。每条资源记录均具有一个类型 , 表明其保留的数据类型。<br>– A : 名称至 IPv4 地址<br>– AAAA : 名称至 IPv6 地址<br>– CNAME : 名称至 ”规范名称 “ ( 包含 A/AAAA 记录的另一个名称 )<br>– PTR : IPv4/IPv6 地址至名称<br>– MX : 用于名称的邮件交换器 ( 向何处发送其电子邮件 )<br>– NS : 域名的名称服务器<br>– SOA :” 授权起始 “ , DNS 区域的信息 ( 管理信息 )</p><p>K8S中DNS配置：</p><ol><li><code>Default</code>：默认继承所在Node的DNS策略，<font color="red">但它并不是Pod的默认DNS策略，如果pod的DNS配置缺省，默认是ClusterFirst</font>；</li><li><code>ClusterFirst</code>：不符合已配置的domain的后缀的域名，将会转交给从所在Node继承的nameserver DNS去作解析；</li><li><code>ClusterFirstWithHostNet</code>：使用hostNetwork的Pod必须显式的设置<code>ClusterFirstWithHostNet=true</code>；</li><li><code>None</code>：Pod会直接忽略K8S环境中的DNS设置，所有的DNS配置应该在Pod规约中（pod-&gt;spec）使用<code>dnsConfig</code>指定；</li></ol><p>示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">service/networking/custom-dns.yaml</span> </span><br><span class="line"></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Pod</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">dns-example</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  containers:</span></span><br><span class="line"><span class="attr">    - name:</span> <span class="string">test</span></span><br><span class="line"><span class="attr">      image:</span> <span class="string">nginx</span></span><br><span class="line"><span class="attr">  dnsPolicy:</span> <span class="string">"ClusterFirst"</span></span><br><span class="line"><span class="attr">  dnsConfig:</span></span><br><span class="line"><span class="attr">    nameservers:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="number">1.2</span><span class="number">.3</span><span class="number">.4</span></span><br><span class="line"><span class="attr">    searches:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">ns1.svc.cluster-domain.example</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">my.dns.search.suffix</span></span><br><span class="line"><span class="attr">    options:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">ndots</span></span><br><span class="line"><span class="attr">        value:</span> <span class="string">"2"</span></span><br><span class="line"><span class="attr">   - name:</span> <span class="string">timeout</span></span><br><span class="line"><span class="attr">     value:</span> <span class="string">"5"</span></span><br><span class="line">     <span class="comment"># 这里没有语法错误，注意空格和索引</span></span><br><span class="line"><span class="attr">     - name:</span> <span class="string">single-request-reopen</span></span><br></pre></td></tr></table></figure><p>一般集群中的DNS出现解析异常或解析超时等问题，可以尝试加上如下的dns的配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">dnsConfig:</span></span><br><span class="line"><span class="attr">  options:</span></span><br><span class="line"><span class="attr">  - name:</span> <span class="string">timeout</span></span><br><span class="line"><span class="attr">    value:</span> <span class="string">"5"</span></span><br><span class="line"><span class="attr">  - name:</span> <span class="string">ndots</span></span><br><span class="line"><span class="attr">    value:</span> <span class="string">"2"</span></span><br><span class="line"><span class="attr">  - name:</span> <span class="string">single-request-reopen</span></span><br><span class="line"><span class="attr">dnsPolicy:</span> <span class="string">ClusterFirst</span></span><br></pre></td></tr></table></figure><p>核心思想是将超时时间变长、减少不必要的域名解析（将增加域名前缀的域名条件变成点号为2）、以及减少冲突。</p><h3 id="16-关于时区"><a href="#16-关于时区" class="headerlink" title="16. 关于时区"></a>16. 关于时区</h3><p>&emsp;在基于alpine镜像中，配置基本的时区：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">apk add tzdata</span><br><span class="line">cp /usr/share/zoneinfo/Asia/Shanghai /etc/localtime</span><br><span class="line"><span class="meta">#</span> 可选，删除可以最小化镜像</span><br><span class="line">apk del tzdata</span><br></pre></td></tr></table></figure><h3 id="附：一些常用的命令"><a href="#附：一些常用的命令" class="headerlink" title="附：一些常用的命令"></a>附：一些常用的命令</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span> 使用 -o yaml 选项查看 deployment 的yaml文件</span><br><span class="line">kubectl get deployment my-nginx -n hhu-pro -o yaml</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 使用 -o wide 选项查看 pod 的详细信息，可查看到分配的worker节点</span><br><span class="line">kubectl get pods -n hhu-pro -o wide</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span> 运行时动态修改Pod副本数量，controller-type可能为deployment或者rc，controller-name为controller的名字</span><br><span class="line">kubectl scale &lt;controller-type&gt; &lt;controller-name&gt; --replicas=3</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;1-资源配额&quot;&gt;&lt;a href=&quot;#1-资源配额&quot; class=&quot;headerlink&quot; title=&quot;1. 资源配额&quot;&gt;&lt;/a&gt;1. 资源配额&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;1. 预留和限制的概念&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;预留：即至少需要的资源，对应于&lt;code&gt;requestsCpu&lt;/code&gt;和&lt;code&gt;requestsMemory&lt;/code&gt;；&lt;/li&gt;
&lt;li&gt;资源限制：最大资源限制，对应于&lt;code&gt;limitsCpu&lt;/code&gt;和&lt;code&gt;limitsMemory&lt;/code&gt;；
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="K8S" scheme="https://jacksonary.github.io/tags/K8S/"/>
    
      <category term="Kubernetes" scheme="https://jacksonary.github.io/tags/Kubernetes/"/>
    
  </entry>
  
  <entry>
    <title>UML学习记录</title>
    <link href="https://jacksonary.github.io/posts/4abd9277.html"/>
    <id>https://jacksonary.github.io/posts/4abd9277.html</id>
    <published>2019-05-30T12:34:00.000Z</published>
    <updated>2021-06-04T12:42:23.295Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1.基本概念"></a>1.基本概念</h2><p>&emsp;UML即统一建模语言(Unified Modeling Language)，具备如下特点：</p><ul><li>面向对象；</li><li>可视化，表达能力强；</li><li>独立于过程；</li><li>独立于程序设计；</li><li>容易掌握使用；<a id="more"></a></li></ul><p>&emsp;从UML整个架构来看，是由视图（View）、图（Diagrams）、模型元素（Model elements）和通用机制等几个部分构成：</p><ul><li>视图：用来表示被建模系统的各个方面。由多个图构成，它不是一个图片，而是在某一个抽象层上，对系统的抽象表示。如果要为系统建立一个完整的模型图，只需定义一定数量的视图，每个视图表示系统的一个特殊方面就可以了。视图还把建模语言和系统开发时选择的方法或过程连接起来。</li><li>图：由各种图片构成，用来描述一个视图的内容。UML语言定了9中不同的图的类型，把它们有机地结合起来就可以描述系统的所有视图。</li><li>模型元素：代表面向对象中的类、对象、消息和关系等概念，是构成图的最基本的常用概念。</li><li>通用机制：用于表示其他信息，比如注释、模型元素的语义等。它还提供扩展机制，使UML语言能够适应一个特殊的方法（或过程）、或扩充至一个组织或用户。</li></ul><p>&emsp;UML系统可以由不同的用户使用，可以是开发人员、测试人员、商务人士、分析师等等，所以要设计一个系统的体系结构，最重要的是做到从不同的角度来看，实现可视化系统，这样也可以让我们自己更好的了解系统，让它达到一个更好的效果。从不同的角度使用 UML 定义一个系统的起着重要的作用，这些角度是：</p><ul><li>设计</li><li>实现</li><li>处理</li><li>部署</li></ul><p>该中心是连接所有这四个用例视图，一个用例代表了系统的功能。因此，其他的角度连接使用的情况下：</p><ul><li>系统设计包括类，接口和协作。UML类图，对象图支持；</li><li>实现定义的组件组装在一起，使一个完整的物理系统。UML组件图是用来支持实施的角度；</li><li>流程定义了系统的流动。因此，在设计中所用的相同的元件也可用来支持当前角度看；</li><li>部署代表物理节点的硬件系统构成。UML部署图是用来支持这个角度来看；</li></ul><h2 id="2-UML中的构建模块"><a href="#2-UML中的构建模块" class="headerlink" title="2. UML中的构建模块"></a>2. UML中的构建模块</h2><p>&emsp;UML主要由如下的三部分组成：</p><ul><li>事物(Things)：UML模型中最基本的构成元素，是具有代表性成分的抽象；</li><li>关系(Relationships)：关系把事物紧密联系在一起；</li><li>图(Diagrams )：图是事物和关系的可视化表示；</li></ul><h3 id="2-1-事物"><a href="#2-1-事物" class="headerlink" title="2.1 事物"></a>2.1 事物</h3><p>&emsp;事物在实体抽象化的结果，是UML构件模块中最重要的组成部分，事物主要分为：结构事物、行为事物、分组事物、注释事物。</p><ul><li>结构事物：结构事物是模型中的静态部分，用以呈现概念或实体的表现元素，是软件建模中最常见的元素；</li><li>行为事物：行为事物时UML模型中的动态部分，代表语句中的“动词”，表示模型中随着时空不断变化的部分；</li><li>分组事物：可以将分组事物看作一个盒子，模型在其中可以被分解，目前只有一种分组事物，即包(package)，结构事物、动作事物甚至分组事物都有可能放在一个包中。<font color="red">包纯粹是概念上的，只存在于开发阶段，而组件在运行时存在</font>；</li><li>注释事物：注释事物可以被定义为一种机制来捕捉 UML 模型元素的言论，说明和注释，注释是唯一一个注释事物；</li></ul><p>关于事物在UML中的表示，具体如下所示：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/UML%E4%B8%AD%E7%9A%84%E4%BA%8B%E7%89%A9.png" alt="UML中的事物"></p><h3 id="2-2-关系"><a href="#2-2-关系" class="headerlink" title="2.2 关系"></a>2.2 关系</h3><p>&emsp;关系是UML中又一重要构建块，它表示元素之间的关联关系，UML中定义了四种关系：</p><ul><li>依赖关系：依赖是两件事物之间的语义联系，其中一个事物的变化也影响到另一个事物；</li><li>协作：一种描述一组对象之间连接的结构关系，如聚合关系（描述了整体和部分间的结构关系）；</li><li>泛化：泛化可以被定义为一个专门的元件连接关系与一个广义的元素，它基本上描述了在对象世界中的继承关系，是一种一般化-特殊化的关系；</li><li>实现：类之间的语义关系，其中的一个类指定了由另一个类保证执行的契约；</li></ul><p>关于关系的四种表示如下所示：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/UML%E4%B8%AD%E5%85%B3%E7%B3%BB.png" alt="UML中关系"></p><h3 id="2-3-图"><a href="#2-3-图" class="headerlink" title="2.3 图"></a>2.3 图</h3><p>&emsp;UML的核心是图表，主要分为结构图和行为图：结构图是静态图(如类图、对象图)，行为图是动态图(如序列图、协作图)。关于不同图的简单描述如下：</p><ul><li>类图：描述系统所包含的类、类的内部结构以及类之间的关系；</li><li>对象图：对象图是类图一个具体的实例，所以和类图的基本要素是类似的。对象图是由对象和链接组成，在一个特定的时刻，它捕获该系统的实例。对象图用于原型设计，逆向工程和实际场景建模；</li><li>组件图：描述代码部件的物理结构以及各部件之间的依赖关系，是一种特殊的 UML 图，用于描述系统的静态实现视图。组件图包括物理组件，如库、档案、文件夹等。组件图是从实施的角度。使用一个以上的元件图来表示整个系统，正向和逆向工程技术的使用，使可执行文件组件图；</li><li>部署图：定义系统中软硬件的物理体系结构，部署图是由节点和它们之间的关系构成，一个高效的部署图是应用软件开发的一个组成部分；</li><li>用例图：从用户角度描述系统功能、需求，并指出各功能的操作者，展示系统外部各类角色与系统内部的各种用例之间的关系，用来捕捉系统的动态性质。一个高层次的设计用例图是用来捕捉系统的要求，因此它代表系统的功能和流向。虽然用例图的正向和反向工程是不是一个很好的选择，但他们仍然在一个稍微不同的方法来模拟它；</li><li>交互图：交互图用于捕获系统的动态性质。交互图包括序列图和协作图，其中：序列图显示对象之间的动态合作关系，它强调对象之间消息发送的顺序，同时显示对象之间的交互；协作图描述对象间的协作关系，协作图跟时序图相似，显示对象间的动态合作关系；</li><li>状态图：描述一类对象的所有可能的状态以及事件发生时状态的转移条件，用于模拟一个对象的整个生命周期。一个对象的状态被定义为对象所在的条件下，特定的时间和对象移动对其他状态，在某些事件发生时。状态图还用于正向和反向工程，它着重描述从一个状态到另一个状态的流程，主要有外部事件的参与；</li><li>活动图：描述系统中各种活动的执行顺序，是 UML 的动态模型的一种图形，一般用来描述相关用例图，活动图是一种特殊的状态图。准确的活动图定义如下：活动图描述满足用例要求所要进行的活动以及活动间的约束关系，有利于识别并行活动。活动图是一种特殊的状态图，它对于系统的功能建模特别重要，强调对象间的控制流程；</li><li>顺序图：表示对象之间的动态合作关系；</li><li>协作图：描述对象之间的协作关系；</li></ul><h2 id="3-UML建模"><a href="#3-UML建模" class="headerlink" title="3. UML建模"></a>3. UML建模</h2><p>&emsp;UML建模可以把在复杂世界的许多重要的细节给抽象出。为了区分UML模型，UML建模用不同类型的不同的图，其中UML建模中三个重要类型的建模为：</p><p><strong>结构建模</strong></p><p>&emsp;结构建模具有捕捉静态的功能，包括下列各项：</p><ul><li>类图</li><li>对象图</li><li>部署图</li><li>包图</li><li>复合结构图</li><li>组件图</li></ul><p>&emsp;结构模型代表的系统架构，这个框架的所有其他组件存在的地方。因此，类图，组件图和部署图的部分结构建模。它们都代表的元素和机制将它们组装。但是，从来没有的结构模型描述系统的动态行为。类图中是最广泛使用的结构图。</p><p><strong>行为建模</strong></p><p>&emsp;行为建模描述了在系统中的相互作用，代表之间的交互的结构图，行为建模显示系统的动态性质，包括下列各项：</p><ul><li>活动图</li><li>交互图</li><li>用例图</li></ul><p>所有上述的显示在一个系统中流动的动态序列。</p><p><strong>架构建模</strong></p><p>&emsp;架构建模表示的是一个系统的总体框架，它包含了系统的结构和行为的元素。架构建模可以被定义为整个系统的蓝图。包图是根据架构模型进行的。</p><h2 id="4-UML的基本操作"><a href="#4-UML的基本操作" class="headerlink" title="4. UML的基本操作"></a>4. UML的基本操作</h2><h3 id="4-1-结构事物"><a href="#4-1-结构事物" class="headerlink" title="4.1 结构事物"></a>4.1 结构事物</h3><p>用图形表示法中使用的“结构事物”是UML中使用最广泛的，这些被认为是为UML模型的名词，UML中主要的结构名词有：</p><ul><li>类 Class：在UML中，类（类用来表示对象，对象可以是任何性质和职责）的图在上面学习事物的章节已经涉及到了，主要是Class Name、Attributes和Operation组成，可能还有一些附加组件，注意的权限修饰符的表示法，<code>Public</code>使用<code>+</code>表示，<code>Protected</code>使用<code>#</code>表示(<code>friendly</code>是归到这个表示方式中)，<code>Private</code>使用<code>-</code>表示，属性和方法权限修饰符都是通用的(对象是类的一个具体实例，表示方法一样，只是类名使用下划线标识)；</li><li>接口 Interface：接口只是描述功能，而不执行(无方法体)，使用圆表示接口，接口名写在下面；</li><li>协作 Association：协作表示和接口表示类似，圆变成椭圆，外框变为虚线；</li><li>用例 Use Case：用例可能包含更多职责，它用于捕捉系统的高层次功能；</li><li>活动类：活动类用于描述系统的并发行为；</li><li>组件 Component：必要时，可以添加额外元素；</li><li>节点 Node：节点表示一个物理的系统组件，如服务器、网络；</li></ul><p>它们各自的表示法如下图所示：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%BB%93%E6%9E%84%E4%BA%8B%E7%89%A9%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="结构事物表示法"></p><h3 id="4-2-行为事物"><a href="#4-2-行为事物" class="headerlink" title="4.2 行为事物"></a>4.2 行为事物</h3><p>&emsp;相互作用的类型包括顺序(顺序图)和协作(协作图)两种。</p><p><strong>交互表示法</strong></p><p>交互是两个UML组件之间的信息交换，下面是交互的小栗子：</p><p><strong>状态机表示法</strong></p><p>状态机描述的是组件在声明周期中的不同状态（状态可以是活动的、空闲的或其他情况），下面是小栗子：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%8A%B6%E6%80%81%E6%9C%BA%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="状态机表示法"></p><h3 id="4-3-分组事物"><a href="#4-3-分组事物" class="headerlink" title="4.3 分组事物"></a>4.3 分组事物</h3><p>UML中只有“包”可以用于分组，其他不能用于分组。</p><h3 id="4-4-UML关系"><a href="#4-4-UML关系" class="headerlink" title="4.4 UML关系"></a>4.4 UML关系</h3><p>&emsp;UML中提供了不同类型的关系：依赖、协作、泛化，下面是三种关系的表示法：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%85%B3%E7%B3%BB%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="关系表示法"></p><p>上述协作关系两端的<code>* 1</code>表示的是一对多的关系，这种关联关系还有一些，如下：</p><table><thead><tr><th>表示方式</th><th>说明</th></tr></thead><tbody><tr><td><code>1...1</code></td><td>表示一对一关联关系(表示两端代表的对象之间的比例)</td></tr><tr><td><code>0...*</code></td><td>表示零对多关联关系</td></tr><tr><td><code>1...*</code></td><td>表示一对多关联关系</td></tr><tr><td><code>0...1</code></td><td>表示零对多关联关系</td></tr><tr><td><code>m...n</code></td><td>表示m对n关联关系</td></tr></tbody></table><h2 id="5-UML标准图"><a href="#5-UML标准图" class="headerlink" title="5. UML标准图"></a>5. UML标准图</h2><p>&emsp;UML中的图分为两大类：</p><ul><li>结构图:<ul><li>类图；</li><li>对象图；</li><li>组件图；</li><li>部署图；</li></ul></li><li>行为图：<ul><li>用例图；</li><li>序列图；</li><li>协作图；</li><li>状态图；</li><li>活动图；</li></ul></li></ul><p>关于各个图的说明在<a href="#23-图">2.3章节</a>已经学习过了。</p><h3 id="5-1-类图"><a href="#5-1-类图" class="headerlink" title="5.1 类图"></a>5.1 类图</h3><p>&emsp;类图是面向对象系统建模中最常用和最重要的图，是定义其它图的基础，主要用来显示系统中的类、接口以及它们之间的静态结构和关系的一种静态模型，不仅用于可视化描述和记录系统的不同方面，也为构建可执行代码的软件应用程序，类图描述一类的属性和操作，也是对系统的约束，被广泛应用于类图的建模的面向对象的系统中，因为它们是唯一的，可以直接映射到面向对象的语言的UML图，类图显示集合的类，接口，关联，协作和约束，也被称为作为结构图。使用类图的目的可以概括为：</p><ul><li>分析和设计应用程序的静态视图；</li><li>描述一个系统的责任；</li><li>基地组件图和部署图；</li><li>正向和逆向工程；</li></ul><p>在绘制类图时，应该注意下面的一些点：</p><ul><li>类图中的名称应该是有意义的描述，并且是面向系统的；</li><li>画类图前应先确定每个元素之间的关系；</li><li>类图中的每个类职责（属性和方法）应该清晰标明；</li><li>对于每个类的属性的最小数量应符合规定，不必要的属性将使图表复杂；</li><li>使用注释来描述图中的某些方面，它应该是可以理解的开发者/编码器；</li></ul><p>下面是一个订单的类图表示法：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%B1%BB%E5%9B%BE%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="类图表示法"></p><h3 id="5-2-对象图"><a href="#5-2-对象图" class="headerlink" title="5.2 对象图"></a>5.2 对象图</h3><p>&emsp;UML对象图和类图一样反映系统的静态过程，但它是从实际的或原型化的情景来表达的，显示某时刻对象和对象之间的关系，对象图是类图的实例，所以使用的标识基本相同，对象存在生命周期，因此UML对象图只能在系统某一时间段存在（其实就类似于系统某时刻的快照），使用上述订单的栗子，某时刻可以消费者在特定时间购买，存在的对象和上面类图类似，下面是对象图的小栗子：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%AF%B9%E8%B1%A1%E5%9B%BE%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="对象图表示法"></p><h3 id="5-3-UML组件图"><a href="#5-3-UML组件图" class="headerlink" title="5.3 UML组件图"></a>5.3 UML组件图</h3><p>&emsp;UML组件图（Component Diagram）又称为构件图，描述的是在软件系统中遵从并实现的一组接口的物理、可替换的软件模块，组件图 = 构件（Component）+接口（Interface）+关系（Relationship）+端口（Port）+连接器（Connector），组件图的目的如下：</p><ul><li>可视化系统的组成部分；</li><li>构建的可执行文件，使用正向和反向工程；</li><li>描述的组织和组件的关系；</li></ul><p>如果还以上述订单为例，组件图内部的构件就是文件，所以组件图是显示了应用程序的文件以及它们之间的关系，实际组件图还包含dll文件、库、文件夹等。</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%BB%84%E4%BB%B6%E5%9B%BE%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="组件图表示法"></p><h3 id="5-4-UML部署图"><a href="#5-4-UML部署图" class="headerlink" title="5.4 UML部署图"></a>5.4 UML部署图</h3><p>&emsp;部署图由节点以及节点之间的关系组成，描述的是系统运行时的结构，展示了硬件的配置及其软件如何部署到网络结构中，用于可视化软件组件部署的系统中的物理组件的拓扑结构，部署图的元素主要如下：</p><ul><li>节点(Node)：存在于运行时的代表计算机资源的物理元素，可以是硬件也可以是运行其上的软件系统，比如64主机、Windows server 2008操作系统、防火墙等；</li><li>节点实例(Node Instance)：与节点的区别在于名称有下划线和结点类型前面有冒号，冒号前面可以有实例名称也可以没有实例名称；</li><li>节点类型(Node Stereotypes)：节点类型有：cdrom、cd-rom、computer、disk array、pc、pc client、pc server、secure、server、storage、unix server、user pc，并在节点的右上角有不同的图标；</li><li>物件(Artifact)：是软件开发过程中的产物，包括过程模型（比如用例图、设计图等等）、源代码、可执行程序、设计文档、测试报告、需求原型、用户手册等等；</li><li>连接(Association)：结点之间的连线表示系统之间进行交互的通信路径，这个通信路径称为连接(Association)，连接中有网络协议；</li><li>节点容器(Node as Container)：一个结点可以包括其他的结点，比如组件或者物件，就形成了节点容器；</li></ul><p>部署元素的表示法如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E9%83%A8%E7%BD%B2%E5%9B%BE%E5%85%83%E7%B4%A0%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="部署图元素表示法"></p><p>高效的部署图将控制系统：性能、可扩展性、可维护性、可移植性，绘制部署图前应确定节点和节点之间的关系两个构件。以订单系统为例绘制下面的部署图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E9%83%A8%E7%BD%B2%E5%9B%BE%E8%A1%A8%E7%A4%BA%E6%B3%95.png" alt="部署图表示法"></p><h3 id="5-5-UML用例图"><a href="#5-5-UML用例图" class="headerlink" title="5.5 UML用例图"></a>5.5 UML用例图</h3><p>&emsp;用例图捕捉了模拟系统中的动态行为，并且描述了用户、需求以及系统功能单元之间的关系，展示了一个外部用户能够观察到的系统功能模型图，由主角，用例和它们之间的关系组成。用例图的目的是捕捉到一个系统的动态方面，用来收集系统的要求，包括内部和外部的影响，上述的订单系统的用例图如下：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%94%A8%E4%BE%8B%E5%9B%BE%E7%A4%BA%E4%BE%8B.png" alt="用例图示例"></p><h3 id="5-6-UML交互图"><a href="#5-6-UML交互图" class="headerlink" title="5.6 UML交互图"></a>5.6 UML交互图</h3><p>&emsp;UML交互图描述的是对象之间的动态合作关系以及合作过程中的行为次序，常常用来描述一个用例的行为，显示该用例中所涉及的对象以及这些对象之间的消息传递情况(即一个用例的实现过程)，主要包括对象和消息两类元素，创建交互图的过程实际上就是向对象分配任务的过程，是可视化系统的交互行为。UML交互图包括两种：</p><ul><li>序列图 ：显示对象之间的关系，强调对象之间消息的时间顺序，显示对象之间的交互；</li><li>协作图 ：描述对象之间的交互关系；</li></ul><p><strong>序列图</strong></p><p>下面是订单系统的序列图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%BA%8F%E5%88%97%E5%9B%BE.png" alt="序列图"></p><p><strong>协作图</strong></p><p>协作图显示对象的组织，下面是订单系统的协作图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E5%8D%8F%E4%BD%9C%E5%9B%BE.png" alt="协作图"></p><h3 id="5-7-UML状态图"><a href="#5-7-UML状态图" class="headerlink" title="5.7 UML状态图"></a>5.7 UML状态图</h3><p>&emsp;UML状态图是图表本身的名称，主要用于描述对象具有的各种状态、状态之间的转换过程以及触发状态转换的各种事件和条件。UML状态图描述了一个状态机，可以被定义为一台机器，它定义了一个对象，这些状态控制外部或内部事件的不同状态，它可以捕获对象、子系统和系统的生命周期，可以告知一个对象可以拥有的状态，并且事件(如消息的接收，时间的流逝、错误、条件为真等)会怎样随着时间的推移来影响这些状态。状态图绘制之前，需要明确几点：</p><ul><li>识识别对象，以进行分析；</li><li>识别状态；</li><li>识别的事件；</li></ul><p>以订单系统为例，下面是状态图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E7%8A%B6%E6%80%81%E6%9C%BA%E8%A1%A8%E7%A4%BA%E6%B3%952.png" alt="状态机表示法"></p><h3 id="5-8-UML活动图"><a href="#5-8-UML活动图" class="headerlink" title="5.8 UML活动图"></a>5.8 UML活动图</h3><p>&emsp;UML活动图是UML的动态模型的一种图形，一般用来描述满足用例要求所要进行的活动以及活动间的约束关系，活动图能够捕捉到该系统的动态行为，UML中其它的四个图是用来显示从一个对象到另一个消息流，但活动图是用来显示消息流从一个活动到另一个活动图，下面是订单系统的活动图：</p><p><img src="https://gitee.com/weiguoliu/image_bed/raw/master/image/%E6%B4%BB%E5%8A%A8%E5%9B%BE.png" alt="活动图"></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-基本概念&quot;&gt;&lt;a href=&quot;#1-基本概念&quot; class=&quot;headerlink&quot; title=&quot;1.基本概念&quot;&gt;&lt;/a&gt;1.基本概念&lt;/h2&gt;&lt;p&gt;&amp;emsp;UML即统一建模语言(Unified Modeling Language)，具备如下特点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;面向对象；&lt;/li&gt;
&lt;li&gt;可视化，表达能力强；&lt;/li&gt;
&lt;li&gt;独立于过程；&lt;/li&gt;
&lt;li&gt;独立于程序设计；&lt;/li&gt;
&lt;li&gt;容易掌握使用；
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="UML" scheme="https://jacksonary.github.io/tags/UML/"/>
    
  </entry>
  
  <entry>
    <title>dockerfile</title>
    <link href="https://jacksonary.github.io/posts/1979f904.html"/>
    <id>https://jacksonary.github.io/posts/1979f904.html</id>
    <published>2019-05-19T04:15:00.000Z</published>
    <updated>2021-06-04T12:35:45.398Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-关于docker-build"><a href="#1-关于docker-build" class="headerlink" title="1. 关于docker build"></a>1. 关于<code>docker build</code></h2><p>&emsp;<code>docker build</code>可以基于Dockerfile和context打包出一个镜像，其中context是一系列在<code>PATH</code>或<code>URL</code>中指定的位置中的文件（context是递归的，包含子目录下的文件，build时会将context中的全部内容传递给docker daemon）。其中<code>PATH</code>是指本地文件系统，<code>URL</code>则是git仓库的地址。比如在当前目录下创建镜像：<code>docker build .</code>，此时context就是当前目录<code>.</code>。 <a id="more"></a> build的运行是由docker daemon操作的，并不是CLI（Command-Lin Interface）。通常build时强烈建议创建一个空目录，然后将Dockerfile文件放入，最后只将build需要用到的文件放到该目录下。注：千万不要用root或者<code>/</code>作为PATH，docker daemon会将整个目录下的文件读取。为了尽可能提高Docker的build性能，和git一样可以在上述目录中添加<code>.dockerignore</code>文件排除一些不要的文件，比如正常打包时都会排除掉Dockerfile：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./Dockerfile*</span><br></pre></td></tr></table></figure><p>&emsp;默认build时会基于context的根目录下Dockerfile文件打包（如果不存在会报错），当然可以通过<code>-f DockerfilePath</code>的方式指定任意位置的Dockerfile位置，但后面的context必须在Dockerfile所在位置的目录或者父目录（指定为其他目录会报错，比如指定为<code>~/k8s/</code>），比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ~/docker/test1/Dockerfil指定Dockerfile位置，~/docker/指定context一定是Dockerfile所在目录或父目录</span></span><br><span class="line">docker build -f ~/docker/test1/Dockerfile ~/docker/</span><br></pre></td></tr></table></figure><p>&emsp;此外还可以指定如果镜像构建成功存放的仓库和标签（即repository和tag），如构建时打上阿里云的仓库标签：<code>docker build -t registry.cn-shanghai.aliyuncs.com/hhu/redis:4.0-alpine3.9 .</code>。注：这里可以不断追加<code>-t 标签</code>的方式打上多个仓库仓库标签，比如：<code>docker build -t registry.cn-shanghai.aliyuncs.com/hhu/redis:4.0-alpine3.9 -t test1/redis:4.0-alpine3.9 -t test2/redis:4.0-alpine3.9 .</code>，上述栗子一下就会出现3个标签的镜像，但它们的ID是相同的，仅仅是标签不同。</p><p>&emsp;和大部分的应用类似，docker daemon会在运行Dockerfile中的指令时会先验证文件的可行性，比如语法错误就会返回error。<font color="red">Dockerfile中的每条指令都是独立运行的（比如<code>RUN cd /tmp</code>并不会影响下一条指定的执行），并不会创建新的镜像。</font>不论何时，docker都会尽可能重复应用缓存的中间镜像以便加快build的过程，所以有时在build时可能会在控制台看到<code>Using cache</code>的字样。可以使用的缓存镜像只有在本地具备父链才能使用（即缓存的镜像先前被创建过或者整个镜像链都使用<code>docker load</code>加载过）。如果在build时希望使用某个特定的镜像缓存可以使用<code>--cache-from</code>选项，注：使用<code>--cache-from</code>时不需要具备本地父链，可能会从其他的注册中心拉下来。</p><p>注：<code>docker run</code>如果不显式的指定<code>-it</code>进行交互的话，此时创建的容器会直接在后台运行，如果没有前台进行，docker会让容器自动停止并移除，所以很多时候为了让一些只有操作系统的镜像创建的容器不被docker自动移除，我们往往会在创建镜像dockerfile的时候加上一个<code>CMD</code>：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建 centOS 空系统工具经常加这个CMD</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> /usr/sbin/init</span></span><br><span class="line"><span class="comment"># 创建 Alpine 空系统工具经常加这个CMD</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> init</span></span><br></pre></td></tr></table></figure><p>他们目的都是在系统中挂一个前台进程，这样docker就不会认为该容器处于空闲状态去移除它，目的都是一样的，当然我们也可以人为造出一些死循环来达到这种目的。</p><h2 id="2-语法"><a href="#2-语法" class="headerlink" title="2. 语法"></a>2. 语法</h2><p>&emsp;Dockerfile的语法为<code>INSTRUCTION arguments</code>，不区分大小写，但为了区分指令和参数，约定指令全部大写、参数尽量小写。docker会按序执行Dockerfile中的指令，<font color="red">Dockerfile文件必须以<code>FROM</code>指令开头（除指令解释器外）</font>，它指定了当前构建镜像的基础镜像。通常Dockerfile中也允许注释，注释行以<code>#</code>开头，但指令解释器除外，比如<code></code>。</p><h4 id="2-1-指令解释器"><a href="#2-1-指令解释器" class="headerlink" title="2.1 指令解释器"></a>2.1 指令解释器</h4><p>指令解释器是可选的，它会影响下面指令的处理方式，但不会在增加层layer，也不会作为构建的步骤展示出来。<font color="red">docker一旦处理了注释、空行或生成器指令，就不会再看是它否分析器指令了</font>，就算下面还有指令解释器，docker也会将其视为注释（都是以<code>#</code>开头），所以Dockerfile中只要有指令解释器，请务必尽可能的靠前声明（约定解释器为小写），应该Dockerfile的第一行。指令解释器不支持行继续的行为（Linux中表现为<code>\</code>），同时指令解释器后应该空一行。总结一下，应该遵循如下的规则：</p><ol><li><code>#</code>开头，指令解释器小写，如果有位于Dockerfile的第一行；</li><li>不支持行继续操作行为，即<code>\</code>；</li><li>解释器后应留一个空白行；</li></ol><p>下面是一些反例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 反例1：使用了行继续 \</span></span><br><span class="line"><span class="comment"># direc \</span></span><br><span class="line">tive=value</span><br><span class="line"></span><br><span class="line"><span class="comment"># 反例2：只会识别第一个，value2不应该出现会被当成注释</span></span><br><span class="line"><span class="comment"># directive=value1</span></span><br><span class="line"><span class="comment"># directive=value2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> ImageName</span><br><span class="line"></span><br><span class="line"><span class="comment"># 反例3：指令解释器非首行</span></span><br><span class="line"><span class="keyword">FROM</span> ImageName</span><br><span class="line"><span class="comment"># directive=value</span></span><br></pre></td></tr></table></figure><p>注：指令解释器允许使用空格符，比如下面的几种方式都是一样有效的：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#directive=value</span></span><br><span class="line"><span class="comment"># directive =value</span></span><br><span class="line"><span class="comment">#directive= value</span></span><br><span class="line"><span class="comment"># directive = value</span></span><br></pre></td></tr></table></figure><p>&emsp;Dockerfile支持的指令解释器有：<code>syntax</code>和<code>escape</code>。其中<code>syntax</code>仅支持下一代构建工具BuildKit，这里暂不进行捣鼓，直接看<code>escape</code>，它定义的是Dockerfile中的转义字符，有2种定义方式：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># escape=\</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># escape=`</span></span><br></pre></td></tr></table></figure><p>如果不指定默认转义字符为<code>\</code>。转义字符既用于转义一行中的字符，也用于转义换行符。这使得Dockerfile指令可以跨越多行，无论转义分析器指令是否包含在Dockerfile中，转义都不会在<code>RUN</code>命令中执行，除非在行尾执行。在windows上，\ 是目录之间的分隔符，将转义字符设置为 ` 是非常有用的，它和 PowerShell 一致，比如在windows的powerShell中：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> microsoft/nanoserver</span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> testfile.txt c:\\</span></span><br><span class="line"><span class="bash">RUN dir c:\</span></span><br></pre></td></tr></table></figure><p>没有指定指令解释器，默认为<code>\</code>，那此时第二行中<code>COPY testfile.txt c:\\</code>就出现了转义字符（<code>c:\\</code>中的第一个<code>\</code>），实际意思就是<code>COPY testfile.txt c:\</code>，那剩下的那个<code>\</code>就变成了行继续的操作，所以第二行和第三行是一行实际为：<code>COPY testfile.txt c:\RUN dir c:</code>第三行的转义字符在行末属于可执行范围。在powershell中，为了避免这个问题可以使用 ` 作为转义字符就不会出现上述的问题：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># escape=`</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> microsoft/nanoserver</span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> testfile.txt c:\\</span></span><br><span class="line"><span class="bash">RUN dir c:\</span></span><br></pre></td></tr></table></figure><p>上述Dockerfile拆解成了3个指令（除去指令解释器，之前是2条指令）。</p><h4 id="2-2-环境替换"><a href="#2-2-环境替换" class="headerlink" title="2.2 环境替换"></a>2.2 环境替换</h4><p>&emsp;docker中可以使用<code>ENV</code>为特定的指令声明环境变量，转义字符也被处理为将类似变量的语法包含到字面上的语句中。环境变量在Dockerfile中使用<code>$variable_name</code>或<code>${variable_name}</code>标注，上述2标注方式是一样的，第2种<code>{xx}</code>的方式通常用于解决变量名没有空格的问题（不是很理解），比如<code>${foo}_bar</code>，此外这种大括号的方式还支持一些标准的<code>bash</code>修饰符，比如（<code>word</code>只是随意取的值）：</p><ul><li><code>${variable:-word}</code>表示如果<code>variable</code>设置了，那它的值就是设置的值；如果<code>variable</code>没有设置，那<code>word</code>就是它的值（有点三目运算的意思）。</li><li><code>${variable:+word}</code>表示如果<code>variable</code>设置了，那<code>word</code>就是它的值；如果<code>variable</code>没有设置，那它的值就是空字符串。</li></ul><p>注：变量上也是可以使用转义字符的，比如使用的转义字符是默认的<code>\</code>，那在变量中<code>\$foo</code>或<code>\${foo}</code>，那就表示它就是个普通的字符串<code>$foo</code>或<code>${foo}</code>，而不是对应foo的值。</p><p>&emsp;环境变量支持如下的指令：<code>ADD</code>、<code>COPY</code>、<code>ENV</code>、<code>EXPOSE</code>、<code>FROM</code>、<code>LABEL</code>、<code>STOPSIGNAL</code>、<code>USER</code>、<code>VOLUME</code>、<code>WORKDIR</code>、<code>ONBUILD</code>（1.4版本之后该指令只有在和上述其他指令结合使用时才能使用环境变量）。下面是一个示例（解析的指令在<code>#</code>后已经标出）：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> busybox</span><br><span class="line"><span class="comment"># 声明环境变量foo，表示的值为/bar</span></span><br><span class="line"><span class="keyword">ENV</span> foo /bar</span><br><span class="line"><span class="comment"># 等同于 WORKDIR /bar</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> <span class="variable">$&#123;foo&#125;</span></span></span><br><span class="line"><span class="comment"># 等同于 ADD . /bar</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> . <span class="variable">$foo</span></span></span><br><span class="line"><span class="comment"># 等同于 COPY $foo /quux</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> \<span class="variable">$foo</span> /quux</span></span><br></pre></td></tr></table></figure><p>&emsp;环境变量的替换将在整个指令中对每个变量使用相同的值，比如下面的示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ENV</span> abc=hello</span><br><span class="line"><span class="keyword">ENV</span> abc=bye def=$abc</span><br><span class="line"><span class="keyword">ENV</span> ghi=$abc</span><br></pre></td></tr></table></figure><p>上述的定义过程中，<code>def</code>的值为<code>hello</code>，而不是<code>bye</code>；<code>ghi</code>的值为<code>bye</code>，因为它不是将<code>abc</code>设置为<code>bye</code>那条指令的一部分。可能有点绕人，但只要记住一点就行了，在使用变量时，变量的值永远都是前面最靠近使用该变量的指令的定义（但不包含本身），如第2条指令使用<code>def=$abc</code>，向前寻找变量<code>abc</code>，排除自身，发现最靠近的<code>abc</code>定义在第一条指令中<code>abc=hello</code>，所以<code>def=hello</code>；同样第3条指令使用<code>ghi=$abc</code>，向前寻找变量<code>abc</code>，发现第2条指令最靠近它，虽然第1条指令也有<code>abc</code>的声明，但没有第2条指令靠近，所以取第2条指令之中<code>abc</code>的声明<code>bye</code>，所以<code>ghi=bye</code>。</p><h4 id="2-3-dockerignore文件"><a href="#2-3-dockerignore文件" class="headerlink" title="2.3 .dockerignore文件"></a>2.3 <code>.dockerignore</code>文件</h4><p>&emsp;和<code>.gitignore</code>类似，在CLI（Command-Lin Interface）将context发送给docker daemon时，它会关注一下context根目录下的<code>.dockerignore</code>文件，如果存在，CLI将修改context以排除与该文件中匹配的文件和目录。这样可以避免将一些不必要的大文件和敏感发送给docker daemon并通过<code>ADD</code>或<code>COPY</code>将这些文件加入到镜像中。同样的<code>.dockerignore</code>文件中也是允许注释的，行首用<code>#</code>标注即可，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># comment</span></span><br><span class="line">*/temp*</span><br><span class="line">*/*/temp*</span><br><span class="line">temp?</span><br></pre></td></tr></table></figure><p>上述的<code># comment</code>是一个注释，CLI将不会管它；<code>*/temp*</code>表示CLI将排除context根目录中的任何一级子目录中名称以<code>temp</code>开头的文件或目录；<code>*/*/temp*</code>表示CLI将从context根目录下任何两级子目录中排除以<code>temp</code>开头的文件或目录（如<code>/somedir/subdir/temporary.txt</code>）；<code>temp?</code>表示CLI将排除context根目录中名为<code>temp</code>的单字符扩展名的文件和目录（比如<code>/tempa</code>）。此外它还支持<code>**</code>通配符（匹配0或多个目录，注意是用于匹配目录的！！），如<code>**/*.go</code>就是匹配context目录（包括它所有的子孙目录）下所有以<code>.go</code>结尾的文件。</p><p>&emsp;如果行首是<code>!</code>可以用于排除例外的情况，示例如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">*.md</span><br><span class="line">!README.md</span><br></pre></td></tr></table></figure><p>上述就是排除所有以<code>.md</code>结尾的文件，但<code>README.md</code>除外不用排除。</p><p>&emsp;<code>!</code>的位置会影响<code>.dockerignore</code>文件中与特定文件匹配的属性决定它是包含的还是排除的最后一行，下面是2个示例（有点懵懂）：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 示例1：除了README-secret.md之外，排除上下文中含任何其他以.md结尾的文件。</span></span><br><span class="line">*.md</span><br><span class="line">!README*.md</span><br><span class="line">README-secret.md</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例2：所有README文件都包含在内，中间的 README-secret.md 没有效果，因为 !readme*.md 与 readme-secret.md 匹配却在最后</span></span><br><span class="line">*.md</span><br><span class="line">README-secret.md</span><br><span class="line">!README*.md</span><br></pre></td></tr></table></figure><p>&emsp;在<code>.dockerignore</code>中甚至可以将<code>Dockerfile</code>和<code>.dockerignore</code>排除掉，但它们仍然会被发送到daemon因为docker需要这些进行job，但<code>ADD</code>和<code>COPY</code>指令不会将它们复制到镜像中。</p><h4 id="2-4-具体指令-只包含18-09版本最新指令"><a href="#2-4-具体指令-只包含18-09版本最新指令" class="headerlink" title="2.4 具体指令(只包含18.09版本最新指令)"></a>2.4 具体指令(只包含18.09版本最新指令)</h4><h5 id="2-4-1-FROM"><a href="#2-4-1-FROM" class="headerlink" title="2.4.1 FROM"></a>2.4.1 <code>FROM</code></h5><p>&emsp;<code>FROM</code>指令初始化一个新的构建阶段，它为后续的指令设置了一个基本镜像，因此它必须是在所有指令的最前面定义的（除指令解释器），这个基本镜像可以是任何一个合法的镜像。语法如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 方式1</span></span><br><span class="line"><span class="keyword">FROM</span> &lt;image&gt; [AS &lt;name&gt;]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式2</span></span><br><span class="line"><span class="keyword">FROM</span> &lt;image&gt;[:&lt;tag&gt;] [AS &lt;name&gt;]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式3</span></span><br><span class="line"><span class="keyword">FROM</span> &lt;image&gt;[@&lt;digest&gt;] [AS &lt;name&gt;]</span><br></pre></td></tr></table></figure><p><code>FROM</code>可以在一个<code>Dockerfile</code>文件中出现多次，创建多个镜像或使用一个生成阶段作为另一个的依赖。只需在每条新的<code>FROM</code>指令之前记录提交所输出的最后一个镜像的ID，每个<code>FROM</code>指令会清除之前其他指令创建的状态。<code>name</code>是可选的，可以通过在<code>FROM</code>指令中添加<code>AS name</code>给一个新的构建阶段起一个名字，这个名字可以在后来的<code>FROM</code>指令和<code>COPY --from=&lt;name|index&gt;</code>指令中引用对应的镜像。同样<code>tag</code>和<code>digest</code>也是可选的，如果不指定，默认为<code>latest</code>。</p><h5 id="2-4-2-ARG"><a href="#2-4-2-ARG" class="headerlink" title="2.4.2 ARG"></a>2.4.2 <code>ARG</code></h5><p>&emsp;<code>ARG</code>是唯一一个可以在<code>FROM</code>指令之前的，<code>FROM</code>指令是支持在第一个<code>FROM</code>之前由<code>ARG</code>指令声明的变量，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 声明变量 CODE_VERSION，值为latest</span></span><br><span class="line"><span class="keyword">ARG</span>  CODE_VERSION=latest</span><br><span class="line"><span class="comment"># 在FROM指令中使用由ARG声明的变量</span></span><br><span class="line"><span class="keyword">FROM</span> base:$&#123;CODE_VERSION&#125;</span><br><span class="line"><span class="keyword">CMD</span><span class="bash">  /code/run-app</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在FROM指令中使用由ARG声明的变量</span></span><br><span class="line"><span class="keyword">FROM</span> extras:$&#123;CODE_VERSION&#125;</span><br><span class="line"><span class="keyword">CMD</span><span class="bash">  /code/run-extras</span></span><br></pre></td></tr></table></figure><p>在<code>FROM</code>指令之前<code>ARG</code>变量的声明是属于构建阶段之外的，因此不能在<code>FROM</code>指令之后的其他指令中使用。如果要使用第一个<code>From</code>之前声明的<code>ARG</code>值，需要在构建阶段内使用没有指定值的<code>ARG</code>指令，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ARG</span> VERSION=latest</span><br><span class="line"><span class="keyword">FROM</span> busybox:$VERSION</span><br><span class="line"><span class="comment"># 声明一个没有默认值的变量 ARG 指令</span></span><br><span class="line"><span class="keyword">ARG</span> VERSION</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="variable">$VERSION</span> &gt; image_version</span></span><br></pre></td></tr></table></figure><h5 id="2-4-3-RUN"><a href="#2-4-3-RUN" class="headerlink" title="2.4.3 RUN"></a>2.4.3 <code>RUN</code></h5><p>&emsp;<code>RUN</code>指令将会在当前镜像的顶部的一个新层layer中执行命令并提交结果，提交镜像的结果将用于Dockerfile文件中的下一步骤。分层<code>RUN</code>并生成新的新的结果符合Docker的核心思想（即提交的成本很低且可以从镜像历史的任何一点创建容器，很像源代码管理）。语法如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：shell的形式，命令运行在shell中，Linux中默认运行 /bin/sh -c，win中默认运行 cmd /S /C</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> &lt;<span class="built_in">command</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2：exec的形式</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>]</span></span><br></pre></td></tr></table></figure><p>exec形式可以避免对shell字符串进行munging，可以在不包含指定可执行shell的基本镜像中运行<code>RUN</code>指令。shell形式默认的shell可以使用<code>SHELL</code>命令更改，比如要使用除<code>/bin/sh</code>之外的shell，就需要使用传入所需shell的exec形式：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="bash"> [<span class="string">"/bin/bash"</span>, <span class="string">"-c"</span>, <span class="string">"echo hello"</span>]</span></span><br></pre></td></tr></table></figure><p>在shell形式中可以使用<code>\</code>以继续运下一行的 RUN 指令（即使用<code>\</code>作为行继续操作符，可以跨行运行），比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="bash"> /bin/bash -c <span class="string">'source $HOME/.bashrc; \</span></span></span><br><span class="line"><span class="bash"><span class="built_in">echo</span> <span class="variable">$HOME</span><span class="string">'</span></span></span><br></pre></td></tr></table></figure><p>上述使用的是行继续操作符，实际是一个指令，等同于：<code>RUN /bin/bash -c &#39;source $HOME/.bashrc; echo $HOME&#39;</code>。exec形式会被编译成JSON形式，所以需要使用双引号<code>&quot;</code>而不是单引号<code>&#39;</code>，此外在exec形式中应该避免<code>\</code>，如<code>RUN [&quot;c:\windows\system32\tasklist.exe&quot;]</code>会出错，正确的写法应该为<code>RUN [&quot;c:\\windows\\system32\\tasklist.exe&quot;]</code>。</p><p>&emsp;和shell形式不同，exec形式不会调用shell，所以平常在shell正常处理的在exec形式中可能无法处理，比如：<code>RUN [ &quot;echo&quot;, &quot;$HOME&quot; ]</code>不会替换变量<code>$HOME</code>，如果要用shell处理，要么直接用shell形式<code>RUN &lt;command&gt;</code>，要么直接执行shell，如：<code>RUN [ &quot;sh&quot;, &quot;-c&quot;, &quot;echo $HOME&quot; ]</code>。<code>RUN</code>指令的缓存在下一个构建阶段不会失效，比如<code>RUN apt-get dist-upgrade -y</code>将会在下一个阶段重复使用，当然也可以使用<code>--no-cache</code>参数来让<code>RUN</code>指令失效，比如：<code>docker build --no-cache</code>，<code>RUN</code>指令的缓存也可以通过<code>ADD</code>指令失效。</p><h5 id="2-4-4-CMD"><a href="#2-4-4-CMD" class="headerlink" title="2.4.4 CMD"></a>2.4.4 <code>CMD</code></h5><p>&emsp;一个Dockerfile文件中仅可以只有一个<code>CMD</code>指令，如果有多个<code>CMD</code>，仅有最有一个<code>CMD</code>奏效，它的主要目的是为正在执行的容器提供默认值，这些默认值可以包括可执行文件，也可以省略可执行文件（但此时必须指定一个<code>ENTRYPOINT</code>指令），主要语法如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：exec形式，这是首选形式</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [<span class="string">"executable"</span>,<span class="string">"param1"</span>,<span class="string">"param2"</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 作为ENTRYPOINT默认参数</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [<span class="string">"param1"</span>,<span class="string">"param2"</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># shell形式</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> <span class="built_in">command</span> param1 param2</span></span><br></pre></td></tr></table></figure><p>如果<code>CMD</code>用于为<code>ENTRYPOINT</code>指令提供默认参数（即上述第2种形式），那<code>CMD</code>和<code>ENTRYPOINT</code>指令都应该用JSON格式指定（双引号<code>&quot;</code>）。当在shell或exec形式，<code>CMD</code>指令设置在运行镜像时要执行的命令，如果使用shell形式的<code>CMD</code>，<code>&lt;command&gt;</code>应该是<code>/bin/sh -c</code>：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> <span class="built_in">echo</span> <span class="string">"This is a test."</span> | wc -</span></span><br></pre></td></tr></table></figure><p>如果不想在shell中运行<code>&lt;command&gt;</code>，那就必须使用JSON数组的形式并使用可执行文件的全路径。数组形式是<code>CMD</code>的首选形式，任何额外的参数必须在数组中做独立表达，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [<span class="string">"/usr/bin/wc"</span>,<span class="string">"--help"</span>]</span></span><br></pre></td></tr></table></figure><p>如果想每次容器都执行相同的可执行文件，应该考虑使用<code>ENTRYPOINT</code>和<code>CMD</code>的配合使用。如果指定<code>docker run</code>的参数，那么它们将覆盖在<code>CMD</code>中指定的默认值。</p><p>注：<code>RUN</code>和<code>CMD</code>可能有些像，主要区别是：<code>RUN</code>是运行一个命令然后提交结果，而<code>CMD</code>在构建镜像的过程中不会干任何事，但指定镜像的预期命令。</p><h5 id="2-4-5-LABEL"><a href="#2-4-5-LABEL" class="headerlink" title="2.4.5 LABEL"></a>2.4.5 <code>LABEL</code></h5><p>&emsp;<code>LABEL</code>指令用于向镜像添加元数据，它是一个键值对，如果key值或value值中包含空格，需要使用双引号和反斜杠，一个镜像可以有多个标签，下面是示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 有空格用引号包裹</span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> <span class="string">"com.example.vendor"</span>=<span class="string">"ACME Incorporated"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># </span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> com.example.label-with-value=<span class="string">"foo"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># </span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> version=<span class="string">"1.0"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用行继续操作符跨行</span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> description=<span class="string">"This text illustrates \</span></span></span><br><span class="line"><span class="bash">that label-values can span multiple lines.<span class="string">"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 一个LABEL中设置多个标签</span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> multi.label1=<span class="string">"value1"</span> multi.label2=<span class="string">"value2"</span> other=<span class="string">"value3"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 一个LABEL中设置多个标签</span></span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> multi.label1=<span class="string">"value1"</span> \</span></span><br><span class="line"><span class="bash">      multi.label2=<span class="string">"value2"</span> \</span></span><br><span class="line"><span class="bash">      other=<span class="string">"value3"</span></span></span><br></pre></td></tr></table></figure><p><code>FROM</code>指令中指定的基本镜像中的标签会被当前构建的镜像继承过来，这些标签如果在当前构建的镜像指定相同的key值但不同的value值时是会被覆盖的，使用<code>docker inspect xxx</code>可以查看镜像xxx的标签，下面是openjdk部分标签：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">docker inspect 3675b9f543c5</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 结果</span></span><br><span class="line">[</span><br><span class="line">    &#123;</span><br><span class="line">        "Id": "sha256:3675b9f543c5db0d8d554f8e103a4cb98db26be5e5c88019cbe49dcd4fea4685",</span><br><span class="line">        "RepoTags": [</span><br><span class="line">            "openjdk:8-jdk-alpine"</span><br><span class="line">        ],</span><br><span class="line">        "RepoDigests": [</span><br><span class="line">            "openjdk@sha256:2a52fedf1d4ab53323e16a032cadca89aac47024a8228dea7f862dbccf169e1e"</span><br><span class="line">        ],</span><br><span class="line">        "Parent": "",</span><br><span class="line">        "Comment": "",</span><br><span class="line">        "Created": "2019-04-10T01:52:39.548813341Z",</span><br><span class="line">        "Container": "2d38510bb479e015e7990ac60a6af8855e1c9f7bf6b6a66bde2c8625355eccd9",</span><br><span class="line">        "ContainerConfig": &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;,</span><br><span class="line">        ...</span><br><span class="line">        "Metadata": &#123;</span><br><span class="line">            "LastTagTime": "0001-01-01T00:00:00Z"</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><h5 id="2-4-6-EXPOSE"><a href="#2-4-6-EXPOSE" class="headerlink" title="2.4.6 EXPOSE"></a>2.4.6 <code>EXPOSE</code></h5><p>&emsp;<code>EXPOSE</code>指令表示了docker容器运行时监听的特定网络端口，可以指定监听TCP（默认）或UDP，在实际运行容器<code>docker run</code>时可以使用<code>-p</code>标识去发布并映射到一个或多个端口。比如默认监听TCP，现在改成UDP：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">EXPOSE</span> <span class="number">80</span>/udp</span><br></pre></td></tr></table></figure><p>也可以同时监听TCP和UDP的80端口：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">EXPOSE</span> <span class="number">80</span>/tcp</span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">80</span>/udp</span><br></pre></td></tr></table></figure><p>如果在<code>docker run</code>时指定<code>-p</code>，那端口将为TCP和UDP各暴露一次。<code>-p</code>在主机上使用了一个短暂的高阶主机端口，因此TCP和UDP的端口将不相同（不懂）。当然就算在Dockerfile中配置过端口，在<code>docker run</code>时可以通过<code>-p</code>来覆盖这里定义的，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker <span class="keyword">run</span><span class="bash"> -p 80:80/tcp -p 80:80/udp ...</span></span><br></pre></td></tr></table></figure><p><code>docker network</code>可以在容器之间创建网络进行交流，不需要暴露或发布特定端口，因为连接到网络的容器可以通过任何端口相互通信。</p><h5 id="2-4-7-ENV"><a href="#2-4-7-ENV" class="headerlink" title="2.4.7 ENV"></a>2.4.7 <code>ENV</code></h5><p>&emsp;<code>ENV</code>指令用于设置环境变量，将环境变量设置<code>&lt;key&gt;</code>设置为<code>&lt;value&gt;</code>，这里设置的环境变量将会存在于所有后续指令的环境变量中，可以在命令行进行替换，可以使用<code>docker inspect xxx</code>查看环境变量的值。<code>ENV</code>有如下形式：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：设置单个变量值，在第一个空格后的字符串就是value（可能包含空格）</span></span><br><span class="line"><span class="keyword">ENV</span> &lt;key&gt; &lt;value&gt;</span><br><span class="line"><span class="comment"># 形式1示例，定义了3个环境变量</span></span><br><span class="line"><span class="keyword">ENV</span> myName John Doe</span><br><span class="line"><span class="keyword">ENV</span> myDog Rex The Dog</span><br><span class="line"><span class="keyword">ENV</span> myCat fluffy</span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2：允许同时设置多个环境变量，可以使用双引号 和行继续操作符 \</span></span><br><span class="line"><span class="keyword">ENV</span> &lt;key&gt;=&lt;value&gt; ...</span><br><span class="line"><span class="comment"># 形式2示例，定义了2个环境变量</span></span><br><span class="line"><span class="keyword">ENV</span> myName=<span class="string">"John Doe"</span> myDog=Rex\ The\ Dog \</span><br><span class="line">    myCat=fluffy</span><br></pre></td></tr></table></figure><p>使用<code>ENV</code>设置的环境变量将从生成镜像到镜像容器运行保持不变，使用<code>docker inspect</code>可以看到这些环境变量，在运行容器时可以使用<code>docker run --env &lt;key&gt;=&lt;value&gt;</code>来改变环境变量。如果为单个指令设置值的话，尽量使用<code>RUN &lt;key&gt;=&lt;value&gt; &lt;command&gt;</code>。</p><h5 id="2-4-8-ADD"><a href="#2-4-8-ADD" class="headerlink" title="2.4.8 ADD"></a>2.4.8 <code>ADD</code></h5><p>&emsp;<code>ADD</code>指令可以复制新的文件、目录或者远程URL中的<code>&lt;src&gt;</code>，将它们添加到镜像的文件系统中（具体是镜像的<code>&lt;dest&gt;</code>目录），具体语法有2种：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：将context中 src 复制到镜像中的 dest 目录</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2：和形式1一样，就多了双引号，主要用于src和dest路径中存在空格的情况</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] [<span class="string">"&lt;src&gt;"</span>,... <span class="string">"&lt;dest&gt;"</span>]</span></span><br></pre></td></tr></table></figure><p><code>&lt;src&gt;</code>是相对于context的相对路径，可以有多个，而且它可以包含通配符，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将所有以hom开头的为文件复制进去</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> hom* /mydir/</span></span><br><span class="line"><span class="comment"># ? 匹配单个字符，如"home.txt"</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> hom?.txt /mydir/</span></span><br></pre></td></tr></table></figure><p><code>&lt;dest&gt;</code>是镜像中的一个路径，可以是绝对路径，也可以是相对于<code>WORKDIR</code>的一个相对目录（以<code>/</code>开头就是绝对路径），如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将 test 复制到 `WORKDIR`/relativeDir/ 的相对路径</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> <span class="built_in">test</span> relativeDir/</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 将 test 复制到绝对路径 /absoluteDir/</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> <span class="built_in">test</span> /absoluteDir/</span></span><br></pre></td></tr></table></figure><p>当复制包含特殊字符名字（比如<code>[</code>和<code>]</code>）的文件或目录时，需要做一些处理防止被误认为通配符，比如复制<code>arr[0].txt</code>文件：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将 arr[0].txt文件复制到镜像中的 /mydir/ 目录</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> arr[[]0].txt /mydir/</span></span><br></pre></td></tr></table></figure><p>注：其中<code>--chown</code>是仅支持建立再Linux容器上的Dockerfile，win上无效。所有新文件或目录都是使用UID和GID为0开头（Linux中UID和GID即表示UserId和GroupId，0是超级用户，500起则是普通用户，表示的一种权限）被创建，除非使用<code>--chown</code>选项指定用户、用户组（或用UID和GID，2种方式可以混用），如果指定了用户却不指定用户组（或指定了UID却不指定GID），那将会使用和UID一样的GID。下面是示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ADD</span><span class="bash"> --chown=55:mygroup files* /somedir/</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> --chown=bin files* /somedir/</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> --chown=1 files* /somedir/</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> --chown=10:11 files* /somedir/</span></span><br></pre></td></tr></table></figure><p>&emsp;<code>ADD</code>遵循下面的规则：</p><ul><li><code>&lt;src&gt;</code>必须在context目录中，不能出现context之外的目录，CLI只将context发送给daemon，docker是无法读取context之外目录的文件、目录的；</li><li><code>&lt;src&gt;</code>如果是一个URL：<ul><li>如果<code>&lt;dest&gt;</code>不以斜杠<code>/</code>结尾，文件将会从URL下载并被复制到<code>&lt;dest&gt;</code>；</li><li>如果<code>&lt;dest&gt;</code>以斜杠<code>/</code>结尾，那将从URL推断文件名并将文件下载到<code>&lt;dest&gt;/&lt;filename&gt;</code>，如<code>ADD http://example.com/foobar /</code>将会创建<code>/foobar</code>文件；</li></ul></li><li>如果<code>&lt;src&gt;</code>是一个目录，那整个目录的内容（只是它内部的内容但不包含目录本身）将会被复制，包括文件系统的元信息；</li><li>如果<code>&lt;src&gt;</code>是本地context目录下的压缩包，那它将会被解压成一个目录（前提是可识别的压缩格式：gzip, bzip2 or xz），但仅限于context是本地目录的情况，如果<code>&lt;src&gt;</code>是URL资源就不会自动解压了。</li><li>如果<code>&lt;src&gt;</code>有多个，那<code>&lt;dest&gt;</code>必须只能是一个目录（即必须<code>/</code>结尾）；</li><li>如果<code>&lt;dest&gt;</code>不以<code>/</code>结尾，那将会被认为是一个文件（不是目录），那<code>&lt;src&gt;</code>的内容将会被写入到该文件中；</li><li>如果<code>&lt;dest&gt;</code>不存在，那将会创建（丢失的目录也会随之被创建）；</li></ul><h5 id="2-4-9-COPY"><a href="#2-4-9-COPY" class="headerlink" title="2.4.9 COPY"></a>2.4.9 <code>COPY</code></h5><p>&emsp;<code>COPY</code>和上述的<code>ADD</code>指令功能一样，但<code>COPY</code>的<code>&lt;src&gt;</code>不能是URL，也不能对压缩包进行解压，语法类似：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">COPY</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] &lt;src&gt;... &lt;dest&gt;</span></span><br><span class="line"><span class="keyword">COPY</span><span class="bash"> [--chown=&lt;user&gt;:&lt;group&gt;] [<span class="string">"&lt;src&gt;"</span>,... <span class="string">"&lt;dest&gt;"</span>]</span></span><br></pre></td></tr></table></figure><h5 id="2-4-10-ENTRYPOINT"><a href="#2-4-10-ENTRYPOINT" class="headerlink" title="2.4.10 ENTRYPOINT"></a>2.4.10 <code>ENTRYPOINT</code></h5><p>&emsp;<code>ENTRYPOINT</code>允许配置容器，语法为：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：exec形式，优先</span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2：shell形式</span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> <span class="built_in">command</span> param1 param2</span></span><br></pre></td></tr></table></figure><p>比如以默认内容启动nginx，监听80端口：<code>docker run -i -t --rm -p 80:80 nginx</code>。运行<code>docker run xxx</code>时指定命令行参数将会追加到exec形式的<code>ENTRYPOINT</code>指令中，且覆盖使用<code>CMD</code>指令指定的所有元素，允许将这些参数传给entry point，<code>-d</code>（即<code>docker run &lt;image&gt; -d</code>）即可将将这些参数传给entry point，甚至可以使用<code>docker run --entrypoint</code>选项覆盖Dockerfile中的<code>ENTRYPOINT</code>指令。shell形式禁止使用任何<code>CMD</code>或<code>run</code>命令行参数，缺点是<code>ENTRYPOINT</code>将作为<code>/bin/sh-c</code>的子命令启动，不会传递信号，即可执行文件经不会成为容器的<code>PID 1</code>且不会收到 Unix 信号，因此可执行文件将收不到<code>docker stop &lt;container&gt;</code>发的<code>SIGTERM</code>。Dockerfile中最后一条<code>ENTRYPOINT</code>指令才有效。</p><p>【exec形式的<code>ENTRYPOINT</code>】</p><p>&emsp;可以使用exec形式的<code>ENTRYPOINT</code>设置相当稳定的默认命令和参数，然后使用任意一种形式的<code>CMD</code>设置可能改变的额外默认值，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> [<span class="string">"top"</span>, <span class="string">"-b"</span>]</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [<span class="string">"-c"</span>]</span></span><br></pre></td></tr></table></figure><p>在运行该镜像容器时，<code>top</code>是唯一的进程。在命令行中可以使用<code>--entrypoint</code>选项来覆盖Dockerfile中<code>ENTRYPOINT</code>指令。</p><p>注：</p><ol><li>exec形式必须使用双引号<code>&quot;&quot;</code>（最终是被编译成JSON）；</li><li>exec形式不会对shell命令中的变量做替换（如<code>$HOME</code>不会被替换成用户根目录，比如<code>/root</code>），如有需要必须指定一个shell目录，如<code>ENTRYPOINT [ &quot;sh&quot;, &quot;-c&quot;, &quot;echo $HOME&quot; ]</code>；</li></ol><p>【shell形式的<code>ENTRYPOINT</code>】</p><p>&emsp;shell形式可以直接指定字符串，默认就会在<code>/bin/sh -c</code>中执行，理所当然的可以进行变量替换。</p><p>【总结】</p><p>&emsp;<code>CMD</code>和<code>ENTRYPOINT</code>指令定义了在运行容器时什么命令会执行，规则如下：</p><ol><li>Dockerfile应当指定至少一个<code>CMD</code>或<code>ENTRYPOINT</code>指令；</li><li>在将容器用作可执行文件时应当定义<code>ENTRYPOINT</code>；</li><li><code>CMD</code>指令应当作为<code>ENTRYPOINT</code>指令指定默认参数的一种方式，或者在容器中执行特殊命令；</li><li><code>CMD</code>指令可以在运行容器时指定被重写；</li><li>如果<code>CMD</code>在基础镜像上定义了，那在当前镜像中如果设置<code>ENTRYPOINT</code>将会重置上述的<code>CMD</code>的值（变为空值），此时必须在当前镜像中定义<code>CMD</code>的值；</li></ol><h5 id="2-4-11-VOLUME"><a href="#2-4-11-VOLUME" class="headerlink" title="2.4.11 VOLUME"></a>2.4.11 <code>VOLUME</code></h5><p>&emsp;<code>VOLUME</code>指令用于创建指定名字的挂载点，并将其标记为保存来自本地主机或其他容器的外部装入的卷。形式有JSON和纯字符串的：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1（JSON）</span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="bash"> [<span class="string">"/var/log/"</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2</span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="bash"> /var/<span class="built_in">log</span></span></span><br><span class="line"><span class="comment"># 可以指定多个</span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="bash"> /var/<span class="built_in">log</span> /var/db</span></span><br></pre></td></tr></table></figure><p>比如:</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> mkdir /myvol</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="string">"hello world"</span> &gt; /myvol/greeting</span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="bash"> /myvol</span></span><br></pre></td></tr></table></figure><p>将会创建一个新的挂载点<code>/myvol/greeting</code>并将<code>greeting</code>文件复制到上述新建的存储卷。使用<code>VOLUME</code>有如下的注意点：</p><ol><li>基于win平台存储卷的容器：容器内部存储卷的目的地必须为空目录或者不存在的目录（此时docker会自动创建），此外指定目录不能为C盘；</li><li>Dockerfile中更改存储卷：如果任何生成步骤在声明卷后然后又更改卷内的数据，则这些更改将被丢弃（即存储卷的使用周期只有当前容器）；</li><li>由于JSON，注意双引号；</li><li>在主机目录在容器运行时声明：主机目录（即挂载点）是天然依赖主机的，为了保持映像的可移植性，因为不能保证给定的主机目录在所有主机上都可用，因此不能在Dockerfile中挂载主机目录，<code>VOLUME</code>指令不支持指定<code>host-dir</code>参数，在创建或者运行容器时必须指定挂载点；</li></ol><h5 id="2-4-12-USER"><a href="#2-4-12-USER" class="headerlink" title="2.4.12 USER"></a>2.4.12 <code>USER</code></h5><p>&emsp;<code>USER</code>指令用于在运行镜像中的<code>RUN</code>、<code>CMD</code>和<code>ENTRYPOINT</code>指令时设置用户名（或<code>UID</code>）以及用户组（可选，或者<code>GID</code>），主要形式如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1</span></span><br><span class="line"><span class="keyword">USER</span> &lt;<span class="keyword">user</span>&gt;[:&lt;group&gt;]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2</span></span><br><span class="line"><span class="keyword">USER</span> &lt;UID&gt;[:&lt;GID&gt;]</span><br></pre></td></tr></table></figure><p>注：</p><ol><li>当用户没有用户组时，镜像将会运行在<code>root</code>用户组下；</li><li>win中，如果用户不是内置的账户，那必须先进行创建，可以使用<code>net user</code>来创建，下面是示例：</li></ol><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> microsoft/windowsservercore</span><br><span class="line"><span class="comment"># 在容器中先创建win用户</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> net user /add patrick</span></span><br><span class="line"><span class="comment"># Set it for subsequent commands</span></span><br><span class="line"><span class="keyword">USER</span> patrick</span><br></pre></td></tr></table></figure><h5 id="2-4-13-WORKDIR"><a href="#2-4-13-WORKDIR" class="headerlink" title="2.4.13 WORKDIR"></a>2.4.13 <code>WORKDIR</code></h5><p>&emsp;<code>WORKDIR</code>指令用于设置<code>RUN</code>、<code>CMD</code>、<code>ENTRYPOINT</code>、<code>COPY</code>、<code>ADD</code>指令的工作目录，如果<code>WORKDIR</code>指定的目录不存在，就会被创建（即使后续指令没有使用该目录），该目录可以在Dockerfile中使用多次，基本语法为：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> /path/to/workdir</span></span><br><span class="line">`</span><br></pre></td></tr></table></figure><p>如果提供了相对路径，那它是先前<code>WORKDIR</code>指定目录的相对路径，下面是示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 示例</span></span><br><span class="line"><span class="comment"># 绝对路径</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> /a</span></span><br><span class="line"><span class="comment"># 相对路径</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> b</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> c</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">pwd</span></span></span><br></pre></td></tr></table></figure><p>最终的路径为<code>/a/b/c</code>。此外<code>WORKDIR</code>指令可以解析之前使用<code>ENV</code>设置的环境变量，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ENV</span> DIRPATH /path</span><br><span class="line"><span class="keyword">WORKDIR</span><span class="bash"> <span class="variable">$DIRPATH</span>/<span class="variable">$DIRNAME</span></span></span><br><span class="line"><span class="comment"># 输出应为 /path/$DIRNAME</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">pwd</span></span></span><br></pre></td></tr></table></figure><h5 id="2-4-14-构建时参数ARG"><a href="#2-4-14-构建时参数ARG" class="headerlink" title="2.4.14 构建时参数ARG"></a>2.4.14 构建时参数<code>ARG</code></h5><p>&emsp;<code>ARG</code>指令用于用户在创建镜像时定义的一些变量，语法：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ARG</span> &lt;name&gt;[=&lt;default value&gt;]</span><br></pre></td></tr></table></figure><p>其中默认值是可选的，如果在构建时不指定构建参数将会使用默认值。可以在<code>docker build</code>时使用<code>--build-arg &lt;varname&gt;=&lt;value&gt;</code>指定这些变量的具体值，如果用户指定了Dockerfile中未定义的构建参数，构建时将会出现下面的日志：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[Warning] One or more build-args [foo] were not consumed.</span><br></pre></td></tr></table></figure><p>可以定义多个<code>ARG</code>指令，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 注：默认值是可省的</span></span><br><span class="line"><span class="keyword">FROM</span> busybox</span><br><span class="line"><span class="keyword">ARG</span> user1</span><br><span class="line"><span class="keyword">ARG</span> buildno</span><br></pre></td></tr></table></figure><p>注：不建议将密码这类的信息作为构建参数，因为任何用户都可以使用<code>docker history &lt;images&gt;</code>查看到构建参数。</p><p>【作用域】</p><p><code>ARG</code>变量定义从dockerfile中定义它的地方开始生效，而不是从参数在命令行或其他地方的使用开始生效，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> busybox</span><br><span class="line"><span class="keyword">USER</span> $&#123;<span class="keyword">user</span>:-some_user&#125;</span><br><span class="line"><span class="keyword">ARG</span> <span class="keyword">user</span></span><br><span class="line"><span class="keyword">USER</span> $<span class="keyword">user</span></span><br></pre></td></tr></table></figure><p>构建时使用<code>docker build --build-arg user=what_user .</code>，实际在Dockerfile中的第2行中定义了<code>USER</code>，并将变量<code>user</code>的值假设为<code>some_user</code>，第4行的<code>USER</code>指令定义了变量<code>user</code>是什么（将会从命令行中传过来）。在用<code>ARG</code>指令定义变量之前，任何变量的使用都是空字符串（那是，没定义就使用不报错就不错了）。<code>ARG</code>指令的作用域只在定义<code>ARG</code>的阶段生效，如果需要在多个阶段使用arg，每个阶段必须都包含<code>ARG</code>指令，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> busybox</span><br><span class="line"><span class="keyword">ARG</span> SETTINGS</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> ./run/setup <span class="variable">$SETTINGS</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> busybox</span><br><span class="line"><span class="keyword">ARG</span> SETTINGS</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> ./run/other <span class="variable">$SETTINGS</span></span></span><br></pre></td></tr></table></figure><p>【使用】</p><p>可以使用<code>ARG</code>和<code>ENV</code>指令指定<code>RUN</code>指令中指定的变量，使用<code>ENV</code>指令定义的环境变量会覆盖掉<code>ARG</code>指令中同名的变量，比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ARG</span> CONT_IMG_VER</span><br><span class="line"><span class="keyword">ENV</span> CONT_IMG_VER v1.<span class="number">0.0</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="variable">$CONT_IMG_VER</span></span></span><br></pre></td></tr></table></figure><p>构建镜像<code>docker build --build-arg CONT_IMG_VER=v2.0.1 .</code>最终的<code>CONT_IMG_VER</code>变量输出为<code>v1.0.0</code>。</p><p>【预定义的ARG】</p><p>Docker中有一些预定义好的参数<code>ARG</code>，这些参数可以直接在build时指定，而无需在Dockerfile中先进行定义，预定义的参数有：</p><ul><li><code>HTTP_PROXY</code></li><li><code>http_proxy</code></li><li><code>HTTPS_PROXY</code></li><li><code>https_proxy</code></li><li><code>FTP_PROXY</code></li><li><code>ftp_proxy</code></li><li><code>NO_PROXY</code></li><li><code>no_proxy</code></li></ul><p>使用时直接通过<code>--build-arg &lt;varname&gt;=&lt;value&gt;</code>传值进去即可。默认预定义的参数是无法使用<code>docker history</code>看到的，如果要覆盖默认行为（即预定义参数无法通过<code>docker history</code>看到），可以在Dockerfile中定义一个同名参数覆盖即可，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ARG</span> HTTP_PROXY</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="string">"Hello World"</span></span></span><br></pre></td></tr></table></figure><p>【构建缓存的影响】</p><p><code>ARG</code>变量不会像<code>ENV</code>变量一样将其持久化到镜像中，但它也以类似的方式影响着构建缓存。如果Dockerfile中定义了和之前构建定义的不同<code>ARG</code>变量值，在第一阶段就会出现“cache miss”，尤其是在所有的<code>RUN</code>指令都是用了之前<code>ARG</code>定义的变量就会造成上述的缓存未命中。所有预定义的<code>ARG</code>变量并不会被缓存除非在Dockerfile中覆盖过（换而言之，所有Dokcerfile中使用<code>ARG</code>定义的变量都会被缓存）。下面是示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Dockerfile1</span></span><br><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ARG</span> CONT_IMG_VER</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="variable">$CONT_IMG_VER</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Dockerfile2</span></span><br><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">ARG</span> CONT_IMG_VER</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> hello</span></span><br></pre></td></tr></table></figure><p>在构建时指定<code>--build-arg CONT_IMG_VER=&lt;value&gt;</code>，在上述2个Dockerfile的构建过程中，第3行会出现 cache miss 的提示（但我操作过程中进行的很顺利啊，并没有出现这个提示啊(￣ ‘i ￣;)，我是谁，我在哪……）。</p><h5 id="2-4-15-ONBUILD"><a href="#2-4-15-ONBUILD" class="headerlink" title="2.4.15 ONBUILD"></a>2.4.15 <code>ONBUILD</code></h5><p>&emsp;当一个镜像A被用于另一个镜像B的基本镜像时（即<code>FROM</code>），<code>ONBUILD</code>指令用于向一个镜像中添加一个 trigger，以便可以在一段时间后执行。触发器将在下游（即当前构建的镜像B）生成的context中执行，只要它是在下游Dockerfile中<code>FROM</code>指令之后立即插入的。流程如下：</p><ol><li>当看到<code>ONBUILD</code>时，构建器会向当前正在构建的镜像的 metadata 中添加 trigger，该指令不会影响当前构建；</li><li>在构建过程结束时，一系列的 trigger 将会存储到镜像的manifest中的<code>OnBuild</code>字段（一个数组对象）中，可以使用<code>docker inspect</code>查看这个字段；</li><li>然后这个镜像可能用作其他镜像构建的基础镜像，作为<code>FROM</code>指令的一部分，下游的构建器将会寻找<code>ONBUILD</code> trigger并按它们注册的顺序执行。如果其中某一个trigger执行失败了，<code>FROM</code>指令将被中止，从而导致构建失败；</li><li>触发器在执行后从最终镜像中清除，它们不是由“孙子”构建继承；</li></ol><p>下面是示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[...]</span><br><span class="line"><span class="keyword">ONBUILD</span> <span class="keyword">ADD</span><span class="bash"> . /app/src</span></span><br><span class="line"><span class="keyword">ONBUILD</span> <span class="keyword">RUN</span><span class="bash"> /usr/<span class="built_in">local</span>/bin/python-build --dir /app/src</span></span><br><span class="line">[...]</span><br></pre></td></tr></table></figure><p>注：</p><ul><li><code>ONBUILD</code>指令禁止嵌套使用，即类似于<code>ONBUILD ONBUILD</code>；</li><li><code>ONBUILD</code>指令可能不会触发<code>FROM</code>或<code>MAINTAINER</code>指令（即尽量不要在<code>ONBUILD</code>指令中注册<code>FROM</code>或<code>MAINTAINER</code>指令）；</li></ul><h5 id="2-4-16-STOPSIGNAL"><a href="#2-4-16-STOPSIGNAL" class="headerlink" title="2.4.16 STOPSIGNAL"></a>2.4.16 <code>STOPSIGNAL</code></h5><p>&emsp;<code>STOPSIGNAL</code>指令用于设置系统发送到容器中、目的用于退出的命令，这个信号可以是内核的 syscall 表中有效的无符号数字（比如<code>9</code>），或格式为<code>SIGNAME</code>的信号名字（比如<code>SIGKILL</code>）。基本语法如下：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">STOPSIGNAL</span> signal</span><br></pre></td></tr></table></figure><h5 id="2-4-17-HEALTHCHECK"><a href="#2-4-17-HEALTHCHECK" class="headerlink" title="2.4.17 HEALTHCHECK"></a>2.4.17 <code>HEALTHCHECK</code></h5><p>&emsp;<code>HEALTHCHECK</code>指令用于告诉Docker如何检测容器是否处于健康（工作）状态，有点类似于服务检测，这个功能可以检测到一些情况，如Web服务器卡在无限循环中，无法处理新连接，即使服务器进程仍在运行。当一个容器指定了一个健康检测，那该容器除了它本身的正常状态外，还会额外有一个“健康状态”<em>health status</em>（字段为<code>health_status</code>），健康状态初始值为<code>starting</code>，只有当健康检查（可以称作容器体检）通过了，它才会变成<code>healthy</code>，在连续次数（这个次数可以指定）的体检结果都不通过那 health status 将变成<code>unhealthy</code>。<code>HEALTHCHECK</code>的基本语法为：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 形式1：在容器内运行指定命令进行容器体检</span></span><br><span class="line"><span class="keyword">HEALTHCHECK</span><span class="bash"> [OPTIONS] CMD <span class="built_in">command</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 形式2：禁用从基映像继承的任何运行状况检查</span></span><br><span class="line"><span class="keyword">HEALTHCHECK</span><span class="bash"> NONE</span></span><br></pre></td></tr></table></figure><p>上述形式1中的<code>OPTIONS</code>可用选项有：</p><ul><li><code>--interval=DURATION</code>：表示容器体检的时间间隔，通常在容器首次进行体检（此时可以理解为delay）和体检失败后再次体检时用到，默认是30s；</li><li><code>--timeout=DURATION</code>：表示容器体检的超时时间，一次体检时长超过此时间则认为本次体检失败，置为默认为30s；</li><li><code>--start-period=DURATION</code>：表示容器的启动时长，在这个时间段内体检失败的结果不作数（即不算作<code>retries</code>），但是如果在这段时间内，容器体检成功了，从此刻（即使此刻还在<code>start-period</code>内）开始所有之后的检查就开始生效，<code>retries</code>开始统计，就认为容器已经启动成功，默认是0s；</li><li><code>--retries=N</code>：表示容器体检重试的最大次数，即第一次体检失败并不意味着容器不健康，只有失败时尝试指定次数都是失败，该容器此时的<code>health status</code>才会置为<code>unhealthy</code>，默认是3次；</li></ul><p>&emsp;形式1中<code>CMD</code>后可以是shell命令，如<code>HEALTHCHECK CMD /bin/check-running</code>，也可以是exec数组（和之前类似），这些命令的执行完成后的状态就可以推测出容器的health status，下面是多数命令的常用退出状态码：</p><ul><li><code>0</code>：success - 容器体检健康，可以正常使用；</li><li><code>1</code>：unhealthy - 容器体检不健康，不能进行正常工作；</li><li><code>2</code>：reserved - 不要使用此退出代码；</li></ul><p>比如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">HEALTHCHECK</span><span class="bash"> --interval=5m --timeout=3s \</span></span><br><span class="line"><span class="bash">  CMD curl -f http://localhost/ || <span class="built_in">exit</span> 1</span></span><br></pre></td></tr></table></figure><p>从前至后，参数分别为：检测间隔5分钟，检测超时时间为3s，检测行为指令<code>curl -f http://localhost/ || exit 1</code>。为了调式失败的探测<code>failing probes</code>，使用stdout和stderr命令输出的内容（只能存储最近的4096byte）都将会存储到健康状态中，这些输出都可以使用<code>docker inspect</code>命令查看到。</p><h5 id="2-4-18-SHELL"><a href="#2-4-18-SHELL" class="headerlink" title="2.4.18 SHELL"></a>2.4.18 <code>SHELL</code></h5><p>&emsp;<code>SHELL</code>指令允许在shell形式中使用的默认shell可以被重写，Linux中默认的Shell为<code>[&quot;/bin/sh&quot;, &quot;-c&quot;]</code>，win中默认的shell为<code>[&quot;cmd&quot;, &quot;/S&quot;, &quot;/C&quot;]</code>。Dockerfile中的<code>SHELL</code>指令必须使用JSON格式。<code>SHELL</code>指令在win上是非常有用的，因为win上有两种常用的shell：<code>cmd</code>和<code>powershell</code>，包含可选的shell：<code>sh</code>。</p><p>&emsp;<code>SHELL</code>指令可以出现多次，每个<code>SHELL</code>指令都重写之前的<code>SHELL</code>指令，并影响所有的后续指令，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> microsoft/windowsservercore</span><br><span class="line"></span><br><span class="line"><span class="comment"># Executed as cmd /S /C echo default</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> default</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Executed as cmd /S /C powershell -command Write-Host default</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> powershell -<span class="built_in">command</span> Write-Host default</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Executed as powershell -command Write-Host hello</span></span><br><span class="line"><span class="keyword">SHELL</span><span class="bash"> [<span class="string">"powershell"</span>, <span class="string">"-command"</span>]</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> Write-Host hello</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Executed as cmd /S /C echo hello</span></span><br><span class="line"><span class="keyword">SHELL</span><span class="bash"> [<span class="string">"cmd"</span>, <span class="string">"/S"</span>, <span class="string">"/C"</span>]</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> hello</span></span><br></pre></td></tr></table></figure><p>当在Dockerfile中的<code>RUN</code>、<code>CMD</code>、<code>ENTRYPOINT</code>指令中使用shell形式会被<code>SHELL</code>指令影响。下面的示例是win上的通用模式，它可以使用<code>SHELL</code>指令简化，如：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RUN</span><span class="bash"> powershell -<span class="built_in">command</span> Execute-MyCmdlet -param1 <span class="string">"c:\foo.txt"</span></span></span><br></pre></td></tr></table></figure><p>docker最终调用的命令为：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">cmd</span><span class="bash"> /S /C powershell -<span class="built_in">command</span> Execute-MyCmdlet -param1 <span class="string">"c:\foo.txt"</span></span></span><br></pre></td></tr></table></figure><p>但这样效率较低，因为存在一个不必要的命令处理器正在被调用（即shell），而且<code>RUN</code>指令以shell形式给出需要一个额外的<code>powershell -command</code>前缀。为了提高效率，有2种方式：</p><ol><li>直接使用<code>RUN</code>指令的JSON形式：<code>RUN [&quot;powershell&quot;, &quot;-command&quot;, &quot;Execute-MyCmdlet&quot;, &quot;-param1 \&quot;c:\\foo.txt\&quot;&quot;]</code>；</li><li>使用<code>SHELL</code>指令，配合使用shell形式，对于win用户是更加是更加自然的语法，特别是结合使用<code>escape</code>指定编译指令时，如：</li></ol><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># escape=`</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> microsoft/nanoserver</span><br><span class="line"><span class="keyword">SHELL</span><span class="bash"> [<span class="string">"powershell"</span>,<span class="string">"-command"</span>]</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> New-Item -ItemType Directory C:\Example</span></span><br><span class="line"><span class="keyword">ADD</span><span class="bash"> Execute-MyCmdlet.ps1 c:\example\</span></span><br><span class="line"><span class="bash">RUN c:\example\Execute-MyCmdlet -sample <span class="string">'hello world'</span></span></span><br></pre></td></tr></table></figure><p><code>SHELL</code>指令还可以用于修改shell的执行方式，如在win上使用<code>SHELL cmd /S /C /V:ON|OFF</code>，可以修改延迟的环境变量扩展语义。</p><p>附：</p><p>下面是整章Dockerfile最终的综合示例：</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 示例1</span></span><br><span class="line"><span class="comment"># Nginx</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># VERSION               0.0.1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span>      ubuntu</span><br><span class="line"><span class="keyword">LABEL</span><span class="bash"> Description=<span class="string">"This image is used to start the foobar executable"</span> Vendor=<span class="string">"ACME Products"</span> Version=<span class="string">"1.0"</span></span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> apt-get update &amp;&amp; apt-get install -y inotify-tools nginx apache2 openssh-server</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例2</span></span><br><span class="line"><span class="comment"># Firefox over VNC</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># VERSION               0.3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"></span><br><span class="line"><span class="comment"># Install vnc, xvfb in order to create a 'fake' display and firefox</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> apt-get update &amp;&amp; apt-get install -y x11vnc xvfb firefox</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> mkdir ~/.vnc</span></span><br><span class="line"><span class="comment"># Setup a password</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> x11vnc -storepasswd 1234 ~/.vnc/passwd</span></span><br><span class="line"><span class="comment"># Autostart firefox (might not be the best way, but it does the trick)</span></span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> bash -c <span class="string">'echo "firefox" &gt;&gt; /.bashrc'</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">5900</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash">    [<span class="string">"x11vnc"</span>, <span class="string">"-forever"</span>, <span class="string">"-usepw"</span>, <span class="string">"-create"</span>]</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例3</span></span><br><span class="line"><span class="comment"># Multiple images example</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># VERSION               0.1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> foo &gt; bar</span></span><br><span class="line"><span class="comment"># Will output something like ===&gt; 907ad6c2736f</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> ubuntu</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> moo &gt; oink</span></span><br><span class="line"><span class="comment"># Will output something like ===&gt; 695d7793cbe4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># You'll now have two images, 907ad6c2736f with /bar, and 695d7793cbe4 with</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-关于docker-build&quot;&gt;&lt;a href=&quot;#1-关于docker-build&quot; class=&quot;headerlink&quot; title=&quot;1. 关于docker build&quot;&gt;&lt;/a&gt;1. 关于&lt;code&gt;docker build&lt;/code&gt;&lt;/h2&gt;&lt;p&gt;&amp;emsp;&lt;code&gt;docker build&lt;/code&gt;可以基于Dockerfile和context打包出一个镜像，其中context是一系列在&lt;code&gt;PATH&lt;/code&gt;或&lt;code&gt;URL&lt;/code&gt;中指定的位置中的文件（context是递归的，包含子目录下的文件，build时会将context中的全部内容传递给docker daemon）。其中&lt;code&gt;PATH&lt;/code&gt;是指本地文件系统，&lt;code&gt;URL&lt;/code&gt;则是git仓库的地址。比如在当前目录下创建镜像：&lt;code&gt;docker build .&lt;/code&gt;，此时context就是当前目录&lt;code&gt;.&lt;/code&gt;。
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="docker" scheme="https://jacksonary.github.io/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>关于GraphEasy的简单捣腾</title>
    <link href="https://jacksonary.github.io/posts/cf04ba92.html"/>
    <id>https://jacksonary.github.io/posts/cf04ba92.html</id>
    <published>2019-05-18T07:22:00.000Z</published>
    <updated>2021-06-04T12:33:02.065Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-windows上安装GraphEasy"><a href="#1-windows上安装GraphEasy" class="headerlink" title="1. windows上安装GraphEasy"></a>1. windows上安装GraphEasy</h2><p>&emsp;从asciiflow了解到这个工具，因为之前一看windows上好像并没有什么好用的工具来画ascii流程图，偶然找到在线asciiflow，越用越费劲，一顿翻，找到了GraphEasy，<a id="more"></a>搞吧，发现好像安装没那么容易，各个教程都是千篇一律……关于windowns上的安装提及甚少！！对于一个敢于尝试（作死）的我，哼！妈的，就要弄你！不断摸索，终于搞定了U•ェ•*U，下面是教程（PS：快夸我）：</p><ol><li>安装<a href="https://www.perl.org/get.html" rel="external nofollow noopener noreferrer" target="_blank">perl5</a>；</li><li>下载工具包<a href="https://cpan.metacpan.org/authors/id/S/SH/SHLOMIF/Graph-Easy-0.76.tar.gz" rel="external nofollow noopener noreferrer" target="_blank">Graph-Easy-0.76.tar.gz</a>并解压；</li><li>进入上述解压目录，执行grap easy的编译：perl Makefile.PL（不要在gitBash中操作，会出现无法定位ExtUtils::MakeMaker的错误，务必在CMD中执行）；</li><li>启用<code>cpan</code>脚本：<code>perl -MCPAN -e shell</code>；</li><li>安装Graph::Easy：<code>install Graph::Easy</code>，若需要推出直接<code>exit</code>回车即可；</li><li>验证安装成功：<code>graph-easy -version</code>；</li></ol><h2 id="2-关于语法的简单摸索"><a href="#2-关于语法的简单摸索" class="headerlink" title="2. 关于语法的简单摸索"></a>2. 关于语法的简单摸索</h2><h3 id="2-1-创建文件"><a href="#2-1-创建文件" class="headerlink" title="2.1 创建文件"></a>2.1 创建文件</h3><p>&emsp;下面的语法都是基于 Graph::Easy::Parser 的（使用Perl驱动也是的，只是觉得Graph::Easy::Parser更加方便）。</p><p>【节点】</p><p>节点使用<code>[node-name]</code>方式表示，主要有2种：</p><p>1.<strong>单节点</strong>：即单个节点，用<code>[xx]</code>表示，比如<code>[a]</code>那出来的就一个节点，如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+</span><br><span class="line">| a |</span><br><span class="line">+---+</span><br></pre></td></tr></table></figure><p>2.<strong>复合节点</strong>：由多个节点组成的一个符合节点，用<code>[xx | xx | xx]</code>表示，节点之间使用<code>|</code>分隔，比如<code>[a | b | c | d]</code>，如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+---+---+---+</span><br><span class="line">| a | b | c | d |</span><br><span class="line">+---+---+---+---+</span><br></pre></td></tr></table></figure><p>【连接线】</p><p>节点之间使用连接线进行连接，连接线有多种，下面是一些常用的连接线：</p><p>1.<strong>单向箭头</strong>：使用<code>-&gt;</code>表示，比如<code>[a] -&gt; [b]</code>（如果有多个节点），输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | --&gt; | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>2.<strong>无方向连接线</strong>：使用<code>--</code>表示，比如<code>[a] -- [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | --- | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>3.<strong>双横线单向箭头</strong>：使用<code>==&gt;</code>表示，比如<code>[a] ==&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | ==&gt; | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>4.<strong>点横线单向箭头</strong>：使用<code>..&gt;</code>表示，比如<code>[a] ..&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | ..&gt; | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>5.<strong>波浪线单向箭头</strong>：使用<code>~~&gt;</code>表示，比如<code>[a] ~~&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | ~~&gt; | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>6.<strong>横线、点单向箭头</strong>：使用<code>.-&gt;</code>表示，比如<code>[a] .-&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+     +---+</span><br><span class="line">| a | .-&gt; | b |</span><br><span class="line">+---+     +---+</span><br></pre></td></tr></table></figure><p>7.<strong>双向箭头</strong>：使用<code>&lt;-&gt;</code>表示，比如<code>[a] &lt;-&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+      +---+</span><br><span class="line">| a | &lt;--&gt; | b |</span><br><span class="line">+---+      +---+</span><br></pre></td></tr></table></figure><p>8.<strong>双横线双向箭头</strong>：使用<code>&lt;=&gt;</code>表示，比如<code>[a] &lt;=&gt; [b]</code>，输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+      +---+</span><br><span class="line">| a | &lt;==&gt; | b |</span><br><span class="line">+---+      +---+</span><br></pre></td></tr></table></figure><p>【注解】</p><p>1.<strong>文档注释</strong>：在输入文件中同样支持注释（和普通配置文件的注释没啥区别），使用<code>#</code>开始，比如下面的3个注解：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># top</span><br><span class="line">[a] &lt;=&gt; [b] # mid</span><br><span class="line"># test</span><br></pre></td></tr></table></figure><p>2.<strong>连接线注解</strong>：通常在节点之间连线时会在上面加一些说明表示行为说明，可以在连接线标签后面追加<code>{label: &quot;说明&quot;}</code>（建议加上双引号，如果注解中有空格的话可能会造成阅读上的困难），比如<code>[a] -- {label: &quot;this is test line&quot;} [b]</code>，效果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">+---+  this is test line   +---+</span><br><span class="line">| a | -------------------- | b |</span><br><span class="line">+---+                      +---+</span><br></pre></td></tr></table></figure><p>【其他设置】</p><p>设置节点颜色，这里需要生成html格式才能看到效果，比如：</p><figure class="highlight perl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">node &#123; color: red; &#125;</span><br><span class="line"></span><br><span class="line">[ A ] &#123; class: foo; color: inherit; &#125;</span><br></pre></td></tr></table></figure><p>建议在不断的选区根节点操作，这样逻辑清晰一点，内部尽量减少中文的使用，最后一个综合示例（VOD的简单概念的组合）：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"># Template group</span><br><span class="line">[Template group] -&gt; [name]</span><br><span class="line">[Template group] -&gt; &#123;label: &quot;Single Template&quot;&#125; [Template]</span><br><span class="line"># Template</span><br><span class="line">[Template] -&gt; [video]</span><br><span class="line">[Template] -&gt; [audio]</span><br><span class="line">[Template] -&gt; [watermark]</span><br><span class="line"># video</span><br><span class="line">[video] -&gt; [definition]</span><br><span class="line">[video] -&gt; [bitRate]</span><br><span class="line">[video] -&gt; [encode]</span><br><span class="line"></span><br><span class="line"># 输出</span><br><span class="line"></span><br><span class="line">                                      +-----------+     +------------+</span><br><span class="line">                                      | watermark |     |   encode   |</span><br><span class="line">                                      +-----------+     +------------+</span><br><span class="line">                                        ^                 ^</span><br><span class="line">                                        |                 |</span><br><span class="line">                                        |                 |</span><br><span class="line">+----------------+  Single Template   +-----------+     +------------+     +---------+</span><br><span class="line">| Template group | -----------------&gt; | Template  | --&gt; |   video    | --&gt; | bitRate |</span><br><span class="line">+----------------+                    +-----------+     +------------+     +---------+</span><br><span class="line">  |                                     |                 |</span><br><span class="line">  |                                     |                 |</span><br><span class="line">  v                                     v                 v</span><br><span class="line">+----------------+                    +-----------+     +------------+</span><br><span class="line">|      name      |                    |   audio   |     | definition |</span><br><span class="line">+----------------+                    +-----------+     +------------+</span><br></pre></td></tr></table></figure><h3 id="2-2-生成输出文件"><a href="#2-2-生成输出文件" class="headerlink" title="2.2 生成输出文件"></a>2.2 生成输出文件</h3><p>&emsp;从文件生成对应的图，基本语法为<code>graph-easy [options] [inputfile [outputfile]]</code>，有如下功能：</p><p>【指定输入文件】</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 根据当前目录下的test.txt文件生成ASCII图</span><br><span class="line">graph-easy test.txt</span><br><span class="line"></span><br><span class="line"># 也可以通过--input指定</span><br><span class="line">graph-easy --input=test.txt</span><br></pre></td></tr></table></figure><p>上述的命令是等价的。</p><p>【指定输入文件的格式】</p><p>可以用<code>--form</code>指定输入文件的格式，如不指定则自动检测，合法格式有：</p><table><thead><tr><th>格式</th><th>说明</th></tr></thead><tbody><tr><td>graphviz</td><td>the DOT language</td></tr><tr><td>txt</td><td>Graph::Easy text</td></tr><tr><td>vcg</td><td>VCG text</td></tr><tr><td>gdl</td><td>GDL (Graph Description Language) text</td></tr></tbody></table><p>也可以使用<code>--from_格式名</code>的形式（如<code>--form_txt</code>）指定。</p><p>【指定输出文件】</p><p>输出文件是可以不指定的，会直接在控制台（DOS窗口）中展示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 根据当前目录下的test.txt文件生成ASCII图到test-out.txt中</span><br><span class="line">graph-easy test.txt test-out.txt</span><br><span class="line"></span><br><span class="line"># 也可以通过--output指定输出文件为test-out.txt文件</span><br><span class="line">graph-easy test.txt --output=test-out.txt</span><br></pre></td></tr></table></figure><p>【指定输出样式】</p><p>GraphEasy支持的样式主要有，通过<code>--as=xxx</code>指定或者直接通过<code>--样式</code>（如<code>--ascii</code>）或<code>--as_样式名</code>（如<code>as_ascii</code>）来指定：</p><table><thead><tr><th>格式</th><th>说明</th></tr></thead><tbody><tr><td>ascii</td><td>ASCII art rendering</td></tr><tr><td>boxart</td><td>Unicode Boxart rendering</td></tr><tr><td>html</td><td>HTML</td></tr><tr><td>svg</td><td>Scalable Vector Graphics</td></tr><tr><td>graphviz</td><td>the DOT language</td></tr><tr><td>dot</td><td>alias for “graphviz”</td></tr><tr><td>txt</td><td>Graph::Easy text</td></tr><tr><td>vcg</td><td>VCG (Visualizing Compiler Graphs - a subset of GDL) text</td></tr><tr><td>gdl</td><td>GDL (Graph Description Language) text</td></tr><tr><td>graphml</td><td>GraphML</td></tr></tbody></table><p>默认格式是由输出文件的后缀名决定的，如果没有后缀名默认就是<code>ascii</code>。</p><p>【指定渲染输出文件的程序】</p><p><code>--renderer</code>选项（默认是<code>dot</code>）可以指定渲染的程序，支持下面的：</p><table><thead><tr><th>格式</th><th>说明</th></tr></thead><tbody><tr><td>bmp</td><td>Windows bitmap</td></tr><tr><td>gif</td><td>GIF</td></tr><tr><td>hpgl</td><td>HP-GL/2 vector graphic</td></tr><tr><td>jpg</td><td>JPEG</td></tr><tr><td>pcl</td><td>PCL printer language</td></tr><tr><td>pdf</td><td>PDF</td></tr><tr><td>png</td><td>PNG</td></tr><tr><td>ps</td><td>Postscript</td></tr><tr><td>ps2</td><td>Postscript with PDF notations (see graphviz documentation)</td></tr><tr><td>tga</td><td>Targa bitmap</td></tr><tr><td>tif</td><td>TIFF bitmap</td></tr></tbody></table><p>【附】</p><p>其他的一些设置及使用参照<a href="https://metacpan.org/pod/Graph::Easy#NAME" rel="external nofollow noopener noreferrer" target="_blank">Graph-Easy</a>。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-windows上安装GraphEasy&quot;&gt;&lt;a href=&quot;#1-windows上安装GraphEasy&quot; class=&quot;headerlink&quot; title=&quot;1. windows上安装GraphEasy&quot;&gt;&lt;/a&gt;1. windows上安装GraphEasy&lt;/h2&gt;&lt;p&gt;&amp;emsp;从asciiflow了解到这个工具，因为之前一看windows上好像并没有什么好用的工具来画ascii流程图，偶然找到在线asciiflow，越用越费劲，一顿翻，找到了GraphEasy，
    
    </summary>
    
      <category term="学习" scheme="https://jacksonary.github.io/categories/%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="GraphEasy" scheme="https://jacksonary.github.io/tags/GraphEasy/"/>
    
      <category term="ASCII Flow" scheme="https://jacksonary.github.io/tags/ASCII-Flow/"/>
    
  </entry>
  
</feed>
